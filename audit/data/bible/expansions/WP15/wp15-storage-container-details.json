{
  "target": "docs/storage.html",
  "mode": "append",
  "note": "User-level save-code consequences (complete reads, exact wildcard semantics, disposal and FileShare) that the Storage guide states only in passing; appended after the code examples because the page's StorageContainer headings carry no ids to anchor to."
}
