{
  "target": "docs/spritebatch.html",
  "mode": "after_id",
  "id": "blendstate-presets",
  "note": "User-visible consequences of the shared SpriteBatch state contract (when Begin's states are applied, what End leaves on GraphicsDevice, Immediate exclusivity) that the SpriteBatch guide does not state; placed after the BlendState presets and before the code examples."
}
