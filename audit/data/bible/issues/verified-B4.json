{
 "pkg": "B4",
 "dispositions": [
  {
   "cand": "CNA-BUG-051",
   "source": "bible-cnabugs",
   "classification": "PROVEN FIXED",
   "published_as": null,
   "evidence": "The guarded include is gone: modules/content/src/Xna/ContentManager.cpp includes Microsoft/Xna/Framework/Media/Video/Video.hpp with no preprocessor guard and registers VideoTypeReader unconditionally (RegisterTypeReader<Media::Video>). The link side no longer excludes the video types anywhere: modules/media/CMakeLists.txt compiles every src/*.cpp, including Xna/Video/Video.cpp and Xna/Video/VideoPlayer.cpp, into cna_media in every configuration ('The stable XNA-facing Video and VideoPlayer implementations always live in cna_media') and only drops VideoDecoderUnavailable.cpp and AudioDurationProbeUnavailable.cpp when FFmpeg is available. The MINGW/WIN32/EMSCRIPTEN/ANDROID/iOS condition in modules/CMakeLists.txt now decides only whether the optional FFmpeg backend may be probed; without it VideoDecoderUnavailable.cpp supplies link-complete VideoDecoder symbols that throw System::NotSupportedException. Pinned by modules/media/tests/Microsoft/Xna/Framework/Media/Video/VideoBackendAvailabilityTests.cpp (DisabledBuildRetainsMetadataApiAndRejectsDecoding, compiled whenever CNA_VIDEO_AVAILABLE is undefined, which is every Windows build) and built with native MSVC by .github/workflows/d3d-windows-ci.yml (CNA_BUILD_TESTS=ON, full cmake --build); that workflow is configuration, not a record of a run.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "ContentManager.cpp, modules/media and modules/CMakeLists.txt are unchanged between the ledger pin and TARGET; the fix predates the pin, as the ledger's own TARGET note says."
  },
  {
   "cand": "CNA-BUG-052",
   "source": "bible-cnabugs",
   "classification": "STILL EXISTS",
   "published_as": "CNA-BUG-052",
   "evidence": "modules/content/src/Xna/ContentManager.cpp (SongTypeReader::GetExtensions still returns .mp3 .ogg .wav .flac .opus .aac .wma; SongTypeReader::Read constructs Media::Song, which checks existence only). cmake/ThirdPartySDL.cmake (cna_configure_vendored_sdl) still builds SDL3_mixer with SDLMIXER_OPUS=OFF, and the pinned SDL_mixer commit 3075d3ed has no AAC or WMA decoder at all (its src/ decoder list read with git from a clone that contains the commit). modules/audio/src/Backend/CnaMixer/CnaMixerDecoders.cpp (DecodeMixerAudio) decodes WAV, Ogg Vorbis, MP3 and FLAC only and refuses Ogg Opus by name. MediaPlayer::PlaySong returns silently when LoadMixerAudioFile fails.",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "cmake/ThirdPartySDL.cmake was restructured after the ledger pin (build manifest, patch series); the SDL3_mixer decoder switches are identical. The ledger's 'narrowed' TARGET note is not borne out: nothing about the claim narrowed. Merged with P2-126 (the library index's extension comment), same root."
  },
  {
   "cand": "P2-126",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "CNA-BUG-052",
   "evidence": "modules/media/include/CNA/Internal/Media/MediaLibraryIndex.hpp class comment lists .ogg/.oga/.mp3/.wav; modules/media/src/Internal/MediaLibraryIndex.cpp (HasSupportedAudioExtension) indexes .flac and .opus as well, and its own comment claims Opus is in the bundled decoder set, which the vendored SDL3_mixer build disables.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Folded into CNA-BUG-052: the advertised-format lists disagree with each other and with the decoders; the .opus indexing is the behavioural half."
  },
  {
   "cand": "P2-005",
   "source": "phase2",
   "classification": "NOT A BUG",
   "published_as": null,
   "evidence": "Documented design: modules/audio/include/CNA/Internal/Audio/AudioMixer.hpp states above DestroyMixer that it has no production caller, that native mixer state and the device are reclaimed by the OS at exit, and what a future caller must ensure. At exit the namespace-scope g_audioDevice (Backend/Sdl3Mixer/AudioMixer.cpp) is destroyed and Sdl3AudioDevice::Close (Platform/Sdl3/Sdl3AudioDevice.cpp) stops the stream behind its callback barrier (StopLocked) before SDL_DestroyAudioStream; CNA never calls SDL_Quit itself (Sdl3Platform.cpp leaves global SDL lifetime to the host), so that close does not race an SDL teardown. The MIX_Mixer left undestroyed is memory reclaimed at process exit. A tree search found DestroyMixer calls only in the mixer's own files, tests and the tools/audio harnesses. No failure mode follows.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Already stated on development/internals/audio/engine.html#sdl3-lifetime."
  },
  {
   "cand": "P2-006",
   "source": "phase2",
   "classification": "NOT A BUG",
   "published_as": null,
   "evidence": "Unreachable in production: SoundEffectInstance::Play (modules/audio/src/Xna/SoundEffectInstance.cpp) reaches CNA::Internal::Audio::EnsureMixer only with a non-null nativeAudioHandle_, which exists only after a mixer was created (SoundEffect's constructors call EnsureMixerOrThrowXna first; SDL3 audio loads call GetMixer through FinalizeAudioLoad; CnaMixer loads call EnsureMixer). The SDL3 mixer is destroyed only by DestroyMixer, which has no production caller, and the ALSA engine never destroys a created mixer before exit, so EnsureMixer cannot throw at that call in a shipped game; the std::runtime_error path needs a test or harness teardown first.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Latent inconsistency recorded on development/internals/audio/engine.html#failure-representation; becomes real only if a production DestroyMixer caller is added."
  },
  {
   "cand": "P2-007",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-02",
   "evidence": "cmake/UnitTests.cmake filters AudioMixerTests, CueTests, DynamicSoundEffectInstanceTests, OfflineAudioRendererTests, SoundBankTests, SoundEffectInstanceTests, SoundEffectTests and WaveBankTests out of CnaTests for every non-SDL3 audio selection (AudioCategoryTests is kept for ALSA); on ALSA the facade rests on CnaMixerXnaTests.cpp (7 cases), CnaMixerTests.cpp (24), AudioCategoryTests and the microphone suites.",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "cmake/UnitTests.cmake changed after the ledger pin in unrelated places (GL identity list, output gates); the audio filter is unchanged."
  },
  {
   "cand": "P2-008",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-01",
   "evidence": "All four comments confirmed at TARGET: SoundEffect.hpp (both Play overloads: 'false if the instance limit was reached'; SoundEffect::Play checks no limit); SoundEffect.cpp (FireAndForgetPanState comment and the 'Auto-destroy track (and free panState)' comment vs OnFireAndForgetStopped queuing to g_pendingPanStateCleanup); SoundEffectInstance.hpp is3D_ comment ('Never reset back to false', 'setPanProperty still updates Pan_') vs setPanProperty resetting is3D_ before playback and throwing after it; SoundEffectContentTypeReader.hpp (synthetic WAV + FromStream) vs SoundEffectContentTypeReader.cpp (ConvertXnbSoundToImportedSound, raw PCM16 constructor).",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-052",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-03",
   "evidence": "modules/input/src/Internal/SdlInputBridge.cpp: use_scancode_mode() is consulted only by SdlInputBridge::GetKeyFromScancode and PlatformInputBridge::ProcessEvent's KeyEvent branch (legacy InputManager set, text-input control keys); Keyboard::GetState (modules/input/src/Xna/Keyboard.cpp) copies IPlatformKeyboard's snapshot, and Sdl3Keyboard::Update (modules/platform/src/Sdl3/Sdl3InputServices.cpp) resolves held scancodes through SDL_GetKeyFromScancode with the current layout; no platform source reads FNA_KEYBOARD_USE_SCANCODES. docs/platform-input-notes.md still promises layout-independent bindings. The bridge test ScancodeMapUsedWhenScancodeModeForced shadows Keyboard::GetState with InputManager's accumulator.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-053",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-04",
   "evidence": "Comments above Sdl3Platform::EnsureControllerSubsystem (modules/platform/src/Sdl3/Sdl3Platform.cpp) and X11Platform::EnsureControllerSubsystem (modules/platform/src/X11/X11Platform.cpp) name Game::UpdateInput(); no such member exists (modules/runtime/src/Game.cpp): the gated gamepad/joystick pump is the tail of Game::PollEvents.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Merged with P2-057: both are pre-migration names for the input pump."
  },
  {
   "cand": "P2-057",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-04",
   "evidence": "docs/input-backend.md section 1 says SdlInputBridge 'retains a raw ProcessEvent overload' and cites src/Input/Internal/InputManager.cpp and src/Input/Internal/GestureDetector.cpp; SdlInputBridge.hpp declares no ProcessEvent (the only one is PlatformInputBridge::ProcessEvent) and the files are modules/input/src/Internal/*.cpp.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-054",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-05",
   "evidence": "modules/runtime/src/Game.cpp (Game::PollEvents, KeyEvent branch): a pressed, non-repeated KeyCode::F9 calls GraphicsDevice_.GetRenderer().DebugSimulateContextLoss() and F10 DebugRestoreContext(), with no build, option or opt-out guard; IGraphicsRenderer.hpp calls the channel test-only; overrides exist in the EasyGL, DirectX 9/11/12, Direct2D, WebGPU and GDI renderers.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Duplicates NEW-B2-01 (package B2, candidates P2-004 and P2-187): same code, same defect. Merge into NEW-B2-01 at orchestration."
  },
  {
   "cand": "P2-055",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-06",
   "evidence": "Sdl3InputServices.cpp (AccumulateWheel: static_cast<int>(notches) * 120 per event, pinned by Sdl3MouseTest.WheelEventsAccumulateInXnaUnitsAndTruncateBeforeScaling); Win32EventMapper.cpp (TranslateMouseWheel accumulates the raw WHEEL_DELTA units); WaylandMouse.cpp (AxisTo120 keeps value120, pinned by WaylandWheelMapping.Value120IsTakenAsItIs with a half notch = 60). XNA 4.0's MouseMessageHooker adds the raw WM_MOUSEWHEEL delta (decompiled reference).",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Published as a platform limitation, not a bug: each backend's choice is deliberate and tested; the backends disagree."
  },
  {
   "cand": "P2-056",
   "source": "phase2",
   "classification": "NOT A BUG",
   "published_as": null,
   "evidence": "InputManager::SetMouseRelativeMode has no caller outside tests, and neither has InputManager::GetMouseState, the only reader of the accumulated relative delta (search of modules excluding tests). Public Mouse::GetState and relative mode go through IPlatformMouse (Sdl3Mouse::ConsumeRelativeDelta). docs/input-backend.md documents InputManager as keeping only the accumulators for control-character/click synthesis and legacy raw-adapter tests, so the dormant path has no user-visible effect.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Test-compatibility surface; development/internals/input/events.html#mouse already says nothing outside tests enables it."
  },
  {
   "cand": "P2-116",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-07",
   "evidence": "modules/media/src/Xna/MediaPlayer.cpp (MediaPlayer::Play(Song*): queue_.Clear() then LoadSong(song) and PlaySong(song)); modules/media/src/Xna/MediaQueue.cpp (MediaQueue::Clear clears std::vector<std::unique_ptr<Song>>). A Song* from MediaQueue::getActiveSongProperty or operator[] is destroyed before it is read and, in SOUND_ENABLED builds, written. No test passes a queue-owned song.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-117",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-08",
   "evidence": "modules/media/src/Xna/MediaPlayer.cpp (Play(Song*)): 'if (previousSong != song)' compares a queue copy's address with the caller's pointer, so the flag is set on every call; FNA compares with Song's overloaded != (handle equality). CNA's Song::Equals / operator== compare handles but are not used here.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Merged with P2-118: both are the ActiveSongChanged flagging of the Play overloads."
  },
  {
   "cand": "P2-118",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-08",
   "evidence": "modules/media/src/Xna/MediaPlayer.cpp (Play(const SongCollection&, index)) never sets FrameworkDispatcher::ActiveSongChanged, while Play(Song*), NextSong and Update's end-of-queue path do; FNA's Play(SongCollection, int) has the same omission; the CNA header documents the event as 'Raised when the active song changes'.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-119",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-09",
   "evidence": "modules/media/src/Xna/MediaPlayer.cpp: Play(Song*) calls PlaySong(song) with the caller's Song, which (SOUND_ENABLED) sets its Duration from the mixer and increments its PlayCount; Stop() resets PlayCount only on queue copies; Play(SongCollection) and NextSong pass queue copies. modules/media/src/Xna/MediaLibrary.cpp (BuildFromRoots, songs block) says MediaPlayer::Play operates on a duplicate, never on the library's Song.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-120",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-10",
   "evidence": "modules/media/src/Xna/MediaPlayer.cpp (PlaySong): DestroyMusicTrack/DestroyMusicAudio run first, then a null LoadMixerAudioFile, null CreateMixerTrack, failed SetMixerTrackAudio or failed PlayMixerTrack returns before the timer and state are touched; g_songEnded was cleared, so a player that was Playing stays Playing with no track and Update never advances. XNA's MediaQueue.Play throws InvalidOperationException(SongPlaybackFailed) on a native failure (decompiled).",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Merged with P2-122 (another failure path of the same call)."
  },
  {
   "cand": "P2-122",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-10",
   "evidence": "modules/media/src/Xna/MediaPlayer.cpp (LoadSong: new Song(handle, name)) and modules/media/src/Xna/Song.cpp (Song::Song throws FileNotFoundException when the file does not exist): the copy is made after queue_.Clear(), so a vanished file throws out of Play with the queue already cleared (Play(Song*)) or partly refilled (collection), while the previous track keeps playing. FNA's LoadSong has the same shape.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-121",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-11",
   "evidence": "modules/media/include/Microsoft/Xna/Framework/Media/MediaPlayer.hpp documents ProgramExit as 'Called at application exit'; a search of modules and tools excluding tests finds only modules/c-api/src/CnaCApiMediaPlayer.cpp (cna_media_player_program_exit_ext) calling it; FNA registers ProgramExit on AppDomain.ProcessExit in MediaPlayer's static constructor.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-123",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-12",
   "evidence": "modules/media/src/Xna/MediaPlayer.cpp: LoadSong copies only handle and name (zero Duration); without SOUND_ENABLED PlaySong sets no duration and Update calls DetectSongEndedByElapsedTime(queue copy), which ignores zero durations, so no song ever ends in SDL2 or NULL audio builds (SOUND_ENABLED is defined only for SDL3 and ALSA in modules/CMakeLists.txt).",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-124",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-13",
   "evidence": "modules/media/src/Xna/Video/VideoPlayer.cpp (OpenDecoder sets video->parent_ = this; CloseDecoder writes video_->parent_ = nullptr and is run by Stop, Dispose, ~VideoPlayer and the next Play); Video has no destructor that detaches it and is implicitly copyable with its parent_ pointer (Video.hpp); Video::SetAudioTrackEXT/SetVideoTrackEXT call through parent_. Neither header states a lifetime rule.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-125",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-14",
   "evidence": "modules/media/src/Xna/Video/VideoPlayer.cpp (GetTexture: decoder_->NextFrame and DrainAndFlushAudioBuffer without a try); modules/video-ffmpeg/src/VideoDecoder.cpp (NextFrame, ProcessAudioPacket throw std::runtime_error for decode, send, receive, resample and I/O errors); VideoPlayer.hpp documents only ObjectDisposedException for GetTexture. OpenDecoder, by contrast, catches, closes the decoder and rethrows.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-127",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-15",
   "evidence": "modules/media/tests: the only natural song-end test (MediaPlayerTest.Mp3AndFlacSongsPlayThroughCnasOwnMixerToTheirEnd) is compiled only under CNA_AUDIO_PLATFORM_ALSA; MediaPlayerTests' own comment says the no-mixer/failed-install visualization branch is never exercised; MediaLibraryTests.DisposeMarksLibraryDisposed checks only the flag (MediaLibrary::Dispose sets isDisposed_ and nothing else); no test drives MediaLibrary::SavePicture's IOException path.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-151",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-16",
   "evidence": "modules/video-ffmpeg/src/VideoDecoder.cpp (SetVideoStream -> OpenVideoStreamByIndex): a new codec context is opened with no av_seek_frame/avformat_seek_file and no flush, and havePendingVideoPacket_ is not cleared; the comment in SetVideoStream (MEDIA-131) explains that a context created mid-stream fails on the next non-keyframe. VideoDecoderTests.SetVideoStreamReselectingTheSameStreamPreservesDimensions notes no two-video-stream fixture exists.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-152",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-17",
   "evidence": "modules/video-ffmpeg/src/VideoDecoder.cpp (yuv_planar8_to_rgba, yuv_planar16_to_rgba, generic_to_rgba NV12 branch, ConvertFrameToRGBA): one full-range BT.601 integer matrix, Y unscaled, for YUV and YUVJ formats alike; AVFrame color_range and colorspace are never read. VideoDecoderTests colour checks use +/-20 on synthetic bars.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-153",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B4-18",
   "evidence": "modules/CMakeLists.txt probes libavcodec, libavformat, libavutil and libswresample with pkg_check_modules and no version constraint (AUTO and ON); modules/video-ffmpeg/src/VideoDecoder.cpp uses AVCodecContext::ch_layout and swr_alloc_set_opts2 (FFmpeg 5.1 API) with no version guard; CI Linux lanes run ubuntu-24.04 and macOS lanes Homebrew ffmpeg; no CNA doc states a minimum.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-154",
   "source": "phase2",
   "classification": "NOT A BUG",
   "published_as": null,
   "evidence": "Third-party behaviour, characterised and pinned: libavformat's Matroska demuxer only logs 'File ended prematurely' for a truncated tail and returns AVERROR_EOF, so VideoDecoder::NextFrame (modules/video-ffmpeg/src/VideoDecoder.cpp) cannot tell it from a clean end. modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp pins it (TruncatedFileEndsAtCleanEOFRatherThanThrowing, whose comment records the investigation) and pins that genuine mid-stream corruption is surfaced (CorruptedMidStreamDataThrowsRatherThanSilentlyEndingCleanly). No CNA contract promises detection of a truncated container.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Stated on development/internals/modules/video-ffmpeg.html#failures."
  }
 ],
 "issues": [
  {
   "cand_ids": [
    "CNA-BUG-052",
    "P2-126"
   ],
   "id": "CNA-BUG-052",
   "class": "bug",
   "title": "SongTypeReader and MediaLibraryIndex offer song formats (.opus, .aac, .wma) that no CNA-configured mixer decodes",
   "summary": "Load<Song> resolves .opus, .aac and .wma files and the media library indexes .opus, but neither the vendored SDL3_mixer build nor CNA's ALSA mixer decodes them, so such songs load and then silently fail to play.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "ContentManager::Load<Media::Song> (loose-file tier, SongTypeReader::GetExtensions) and MediaLibrary::getSongsProperty (MediaLibraryIndex scan)",
   "expected": "<p>A loose-file extension list and a library scan should offer only formats the build can play. {{src:modules/media/src/Internal/MediaLibraryIndex.cpp|MediaLibraryIndex.cpp}} states that rule for itself (&#8220;Only formats this project's selected mixer decoder can genuinely PLAY are indexed&#8221;) and leaves out <code>.m4a</code>/<code>.aac</code> for exactly that reason. XNA's contract for <code>Load&lt;T&gt;</code> is a usable asset or a <code>ContentLoadException</code>, so a format the build cannot decode should either not be resolved or be refused at load time.</p>",
   "actual": "<p><code>SongTypeReader::GetExtensions</code> in {{src:modules/content/src/Xna/ContentManager.cpp|ContentManager.cpp}} returns <code>.mp3 .ogg .wav .flac .opus .aac .wma</code>, and <code>SongTypeReader::Read</code> constructs <code>Media::Song(path, stem)</code>, which only checks that the file exists. What can actually be decoded:</p><ul><li>SDL3 audio (the default): {{src:cmake/ThirdPartySDL.cmake|ThirdPartySDL.cmake}} builds the vendored SDL3_mixer (pinned at 3075d3ed, version 3.3.0) with <code>SDLMIXER_OPUS=OFF</code> and the mpg123, vorbisfile, tremor, WavPack, libFLAC, GME, libxmp and FluidSynth back ends off, which leaves dr_mp3, stb_vorbis, dr_flac, WAV/AIFF/VOC/AU and Timidity. SDL3_mixer has no AAC or WMA decoder in any configuration.</li><li>ALSA audio: <code>DecodeMixerAudio</code> in {{src:modules/audio/src/Backend/CnaMixer/CnaMixerDecoders.cpp|CnaMixerDecoders.cpp}} plays WAV, Ogg Vorbis, MP3 and FLAC and refuses Ogg Opus by name.</li></ul><p>So a song whose only file is <code>theme.opus</code>, <code>theme.aac</code> or <code>theme.wma</code> loads successfully; <code>MediaPlayer::Play</code> then reaches <code>PlaySong</code>, <code>LoadMixerAudioFile</code> returns nothing, and <code>PlaySong</code> returns without an error (the consequences are described in the entry on <code>MediaPlayer::Play</code>'s failure handling). The resolution order puts these three after <code>.mp3</code>, <code>.ogg</code>, <code>.wav</code> and <code>.flac</code>, so a playable sibling of the same name wins.</p><p>The media library repeats the mismatch: <code>HasSupportedAudioExtension</code> indexes <code>.opus</code> and names &#8220;decoder_opus (.opus)&#8221; as part of the bundled decoder set, which the vendored build disables, and the class comment in {{src:modules/media/include/CNA/Internal/Media/MediaLibraryIndex.hpp|MediaLibraryIndex.hpp}} still lists only <code>.ogg/.oga/.mp3/.wav</code> while the scan indexes six extensions. With <code>CNA_USE_SYSTEM_SDL=ON</code> the host's SDL3_mixer is linked instead and may include Opus; <code>.aac</code> and <code>.wma</code> stay undecodable.</p>",
   "sources": [
    {
     "path": "modules/content/src/Xna/ContentManager.cpp",
     "note": "SongTypeReader::GetExtensions and SongTypeReader::Read"
    },
    {
     "path": "cmake/ThirdPartySDL.cmake",
     "note": "cna_configure_vendored_sdl: the SDL3_mixer configure arguments (SDLMIXER_OPUS=OFF and the other decoder switches)"
    },
    {
     "path": "modules/audio/src/Backend/CnaMixer/CnaMixerDecoders.cpp",
     "note": "DecodeMixerAudio: WAV, Ogg Vorbis, MP3, FLAC; Ogg Opus refused"
    },
    {
     "path": "modules/audio/src/Backend/Sdl3Mixer/MixerEngine.cpp",
     "note": "LoadMixerAudioFile / FinalizeAudioLoad return an empty pointer on a decode failure"
    },
    {
     "path": "modules/media/src/Xna/MediaPlayer.cpp",
     "note": "MediaPlayer::PlaySong returns silently when LoadMixerAudioFile fails"
    },
    {
     "path": "modules/media/src/Internal/MediaLibraryIndex.cpp",
     "note": "HasSupportedAudioExtension: indexes .opus"
    },
    {
     "path": "modules/media/include/CNA/Internal/Media/MediaLibraryIndex.hpp",
     "note": "class comment listing four extensions"
    },
    {
     "path": "modules/audio/tests/Microsoft/Xna/Framework/Audio/CnaMixerXnaTests.cpp",
     "note": "WavOggMp3AndFlacFilesLoadAndOtherFormatsAreRefusedByName"
    },
    {
     "path": "modules/media/tests/Microsoft/Xna/Framework/Media/MediaLibraryTests.cpp",
     "note": "LibraryIndexesFlacAndOpusFilesWithTheirRealTags"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The decoder set of the pinned SDL_mixer commit (3075d3ed) was read with git from a clone that contains that commit (its <code>src/</code> decoder files and <code>CMakeLists.txt</code> options), because <code>third_party/</code> is a submodule. <code>ThirdPartySDL.cmake</code> changed after an earlier review (a build manifest and an SDL patch series), but the SDL3_mixer decoder switches are identical. The ALSA refusal of Opus is pinned by a test; the SDL3 refusal follows from the configure switch and was not run. Not checked: what a host SDL3_mixer decodes under <code>CNA_USE_SYSTEM_SDL=ON</code>.</p>",
   "reproduction": "<p>Illustrative; not compiled or run.</p><pre><code class=\"language-cpp\">// Content/theme.opus is the only file named \"theme\"; default SDL3 audio with the vendored SDL3_mixer\nMedia::Song song = content.Load&lt;Media::Song&gt;(\"theme\");   // succeeds\nMedia::MediaPlayer::Play(&amp;song);                           // no sound and no exception\n// MediaPlayer::getStateProperty() is still Stopped</code></pre>",
   "tests_current": "<p>{{src:modules/audio/tests/Microsoft/Xna/Framework/Audio/CnaMixerXnaTests.cpp|CnaMixerXnaTests.cpp}} pins the ALSA mixer's Opus refusal for a <code>SoundEffect</code>. {{src:modules/media/tests/Microsoft/Xna/Framework/Media/MediaLibraryTests.cpp|MediaLibraryTests.cpp}} pins that an <code>.opus</code> file is indexed with its tags, not that it plays. {{src:modules/content/tests/Microsoft/Xna/Framework/Content/ContentManagerSongXnbTests.cpp|ContentManagerSongXnbTests.cpp}} covers the <code>.xnb</code> tier. No test loads an <code>.opus</code>, <code>.aac</code> or <code>.wma</code> file through <code>Load&lt;Song&gt;</code> and plays it.</p>",
   "regression_test": "<p>A content test with a directory holding only <code>theme.aac</code> that requires <code>Load&lt;Song&gt;(\"theme\")</code> to throw <code>ContentLoadException</code> (or not to resolve it, if the list is trimmed); and a media test that the library index lists <code>.opus</code> only when the build has an Opus decoder, driven by a compile definition set from the same switch in <code>ThirdPartySDL.cmake</code>.</p>",
   "blast_radius": "<p>Loose-file songs in <code>.opus</code>, <code>.aac</code> or <code>.wma</code> with no playable sibling of the same name, and media libraries containing <code>.opus</code> files, on the SDL3 and ALSA audio selections. Compiled <code>.xnb</code> and <code>.cnb</code> songs, the four decodable formats, and <code>SoundEffect</code> loading are not affected.</p>",
   "workaround": "<p>Ship music as <code>.ogg</code>, <code>.mp3</code>, <code>.flac</code> or <code>.wav</code>.</p>",
   "related": {
    "deep": [
     [
      "deep-dives/services/media-contract.html#formats",
      "Songs, the media library and video: formats"
     ]
    ],
    "guide": [
     [
      "docs/audio.html#decoders",
      "Audio: decoders"
     ],
     [
      "docs/content-manager.html#built-in-content-type-readers",
      "ContentManager: built-in readers"
     ],
     [
      "docs/tutorials/122-media-library.html#formats",
      "Media library tutorial: formats"
     ]
    ],
    "internals": [
     [
      "development/internals/audio/engine.html#decoders",
      "Audio engine internals: decoders"
     ],
     [
      "development/internals/modules/media.html#scan",
      "Media module: the library scan"
     ]
    ]
   },
   "origin": "Bible ledger CNA-BUG-052 (re-verified at TARGET; not narrowed); Phase-2 candidate P2-126 (development/internals/modules/media.html)"
  },
  {
   "cand_ids": [
    "P2-007"
   ],
   "id": "NEW-B4-02",
   "class": "verification-gap",
   "title": "The XNA audio facade on CNA's own ALSA mixer is covered by seven facade tests; the SoundEffect, instance, dynamic-instance and XACT suites run only on SDL3",
   "summary": "Under CNA_AUDIO_PLATFORM=ALSA, eight SDL3_mixer-bound suites (about 440 test definitions) are filtered out, so SoundEffect, SoundEffectInstance, DynamicSoundEffectInstance, Cue, SoundBank and WaveBank behaviour on CNA's mixer rests on seven facade cases.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "n/a",
   "confidence": "verified-by-reading",
   "public_contract": "Microsoft::Xna::Framework::Audio (SoundEffect, SoundEffectInstance, DynamicSoundEffectInstance, AudioEngine, Cue, SoundBank, WaveBank) under CNA_AUDIO_PLATFORM=ALSA",
   "expected": "<p>Both implementations behind <code>MixerEngine.hpp</code> (SDL3_mixer for SDL3 audio, CNA's own mixer for ALSA) serve the same public audio contract, so the evidence for that contract should reach both. On SDL3 it is carried by the facade suites: by <code>TEST</code> macro count at TARGET, SoundEffectInstanceTests 133, CueTests 92, SoundEffectTests 61, DynamicSoundEffectInstanceTests 57, WaveBankTests 38, SoundBankTests 36, OfflineAudioRendererTests 17 and AudioMixerTests 8.</p>",
   "actual": "<p>{{src:cmake/UnitTests.cmake|UnitTests.cmake}} removes those eight suites from <code>CnaTests</code> for every non-SDL3 audio selection, because they read SDL3_mixer's own track handles. For ALSA it keeps AudioCategoryTests. What exercises the XNA facade on CNA's mixer is therefore {{src:modules/audio/tests/Microsoft/Xna/Framework/Audio/CnaMixerXnaTests.cpp|CnaMixerXnaTests.cpp}} (seven cases: a fire-and-forget play, an instance playing for its duration, pause/resume/stop, a looped instance, a dynamic instance consuming buffers, which file formats load, and one end-to-end recording through ALSA's <code>file</code> device), {{src:modules/audio/tests/CNA/Internal/Audio/CnaMixerTests.cpp|CnaMixerTests.cpp}} (24 mixer-level cases), AudioCategoryTests, the microphone suites and one ALSA-only MediaPlayer song-end case. Not exercised on ALSA: Apply3D and the pan crossfeed, pitch, authored loop regions, the 3D-versus-pan gate, instance state edge cases, Cue/SoundBank/WaveBank playback and the offline renderer.</p><p>The X11 + ALSA cell of {{src:.github/workflows/platform-ci.yml|platform-ci.yml}} runs a filtered <code>CnaAudioTests</code> selection on ALSA's silent <code>null</code> device; no lane plays to a physical sound card.</p>",
   "sources": [
    {
     "path": "cmake/UnitTests.cmake",
     "note": "the _cna_sdl3_mixer_tests filter for non-SDL3 audio selections"
    },
    {
     "path": "modules/audio/tests/Microsoft/Xna/Framework/Audio/CnaMixerXnaTests.cpp",
     "note": "the seven facade cases that run on ALSA"
    },
    {
     "path": "modules/audio/tests/CNA/Internal/Audio/CnaMixerTests.cpp",
     "note": "mixer-level cases for CNA's own mixer"
    },
    {
     "path": "modules/audio/src/Backend/CnaMixer/MixerEngine.cpp",
     "note": "the ALSA implementation of MixerEngine.hpp"
    },
    {
     "path": ".github/workflows/platform-ci.yml",
     "note": "X11 + ALSA cell: filtered CnaAudioTests on ALSA's null device"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; no test was run. Test counts are <code>TEST</code>/<code>TEST_F</code>/<code>TEST_P</code> definitions counted in the source files, not executed cases. Nothing is known to be wrong on the ALSA mixer; the gap is the missing evidence.</p>",
   "reproduction": null,
   "tests_current": "<p>As listed above. The SDL3 suites observe track state through SDL3_mixer handles (for example <code>SoundEffectInstanceTestAccess</code>), which is why they cannot compile against CNA's mixer.</p>",
   "regression_test": "<p>Move the facade assertions onto the backend-neutral <code>MixerEngine.hpp</code> queries (track playing, paused, gain, frequency ratio) so the same suites build for SDL3 and ALSA, and add them to the ALSA CI filter; or add an ALSA twin for each behaviour group listed above.</p>",
   "blast_radius": "<p>SDL-free X11 builds that select <code>CNA_AUDIO_PLATFORM=ALSA</code>. SDL3 audio builds are unaffected.</p>",
   "workaround": null,
   "related": {
    "deep": [
     [
      "deep-dives/services/sound-effects-and-streaming.html#evidence",
      "Sound effects, streaming and capture: the audio evidence"
     ]
    ],
    "internals": [
     [
      "development/internals/audio/engine.html#change",
      "Audio engine internals: tests and safe change route"
     ],
     [
      "development/internals/audio/engine.html#alsa-lifetime",
      "Audio engine internals: the ALSA mixer lifetime"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#platform",
      "Test architecture: platform tests"
     ]
    ],
    "guide": [
     [
      "docs/tutorials/137-alsa-audio.html#tests",
      "ALSA audio tutorial: tests"
     ],
     [
      "docs/audio.html#audio-tests",
      "Audio: tests"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-007 (development/internals/audio/engine.html)"
  },
  {
   "cand_ids": [
    "P2-008"
   ],
   "id": "NEW-B4-01",
   "class": "bug",
   "title": "Audio comments describe behaviour the code does not have: SoundEffect::Play's instance limit, FireAndForgetPanState freeing, the is3D_ latch and SoundEffectReader's WAV route",
   "summary": "SoundEffect.hpp promises that Play returns false at an instance limit CNA does not implement, and three internal comments in the audio and content modules contradict the code next to them.",
   "subsystem": "Documentation & release tooling",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "SoundEffect::Play() and SoundEffect::Play(float, float, float) documentation; maintainer comments in SoundEffect.cpp, SoundEffectInstance.hpp and SoundEffectContentTypeReader.hpp",
   "expected": "<p>Comments and Doxygen describe what the code does. For <code>SoundEffect::Play</code> the XNA meaning of a <code>false</code> return is specific: XNA 4.0's <code>SoundEffect.Play(float, float, float)</code> returns false when it catches <code>InstancePlayLimitException</code> (decompiled reference, <code>Microsoft.Xna.Framework.Audio/SoundEffect.cs</code>).</p>",
   "actual": "<ol><li>{{src:modules/audio/include/Microsoft/Xna/Framework/Audio/SoundEffect.hpp|SoundEffect.hpp}} documents both <code>Play</code> overloads as returning false &#8220;if the instance limit was reached&#8221;. <code>SoundEffect::Play(float, float, float)</code> in {{src:modules/audio/src/Xna/SoundEffect.cpp|SoundEffect.cpp}} checks no limit and CNA has none: it returns false for a disposed effect, a missing native audio handle, a track that cannot be created, bound or started, and always in a build without <code>SOUND_ENABLED</code>.</li><li>In SoundEffect.cpp the comment above <code>FireAndForgetPanState</code> says it is &#8220;freed in OnFireAndForgetStopped&#8221;, and the comment at <code>SetMixerTrackStoppedCallback</code> says the stopped callback frees it. <code>OnFireAndForgetStopped</code> only pushes it onto <code>g_pendingPanStateCleanup</code>; the next <code>Play</code> or process exit frees it. The deferral is deliberate (an AddressSanitizer heap-use-after-free, P11-PAN-002), so the stale comment invites reintroducing the bug.</li><li>The <code>is3D_</code> member comment in {{src:modules/audio/include/Microsoft/Xna/Framework/Audio/SoundEffectInstance.hpp|SoundEffectInstance.hpp}} says <code>setPanProperty()</code> keeps updating <code>Pan_</code> but stops writing the track, and that the flag is &#8220;never reset back to false once set&#8221;. <code>setPanProperty</code> in {{src:modules/audio/src/Xna/SoundEffectInstance.cpp|SoundEffectInstance.cpp}} clears <code>is3D_</code> before the first <code>Play</code> and throws <code>InvalidOperationException</code> for a 3D instance after it (CABI-25).</li><li>{{src:modules/content/include/CNA/Internal/Xnb/SoundEffectContentTypeReader.hpp|SoundEffectContentTypeReader.hpp}} says 8-bit PCM, float and ADPCM are wrapped in a synthetic WAV and decoded through <code>SoundEffect::FromStream</code>. <code>SoundEffectReader::Read</code> in {{src:modules/content/src/Xnb/SoundEffectContentTypeReader.cpp|SoundEffectContentTypeReader.cpp}} converts every format with <code>ConvertXnbSoundToImportedSound</code> and <code>ProcessImportedSoundEffect</code> and constructs through the raw 16-bit PCM buffer constructor.</li></ol>",
   "sources": [
    {
     "path": "modules/audio/include/Microsoft/Xna/Framework/Audio/SoundEffect.hpp",
     "note": "Play() and Play(float, float, float) return documentation"
    },
    {
     "path": "modules/audio/src/Xna/SoundEffect.cpp",
     "note": "SoundEffect::Play; FireAndForgetPanState comment; OnFireAndForgetStopped"
    },
    {
     "path": "modules/audio/include/Microsoft/Xna/Framework/Audio/SoundEffectInstance.hpp",
     "note": "is3D_ member comment"
    },
    {
     "path": "modules/audio/src/Xna/SoundEffectInstance.cpp",
     "note": "SoundEffectInstance::setPanProperty (CABI-25 gate)"
    },
    {
     "path": "modules/content/include/CNA/Internal/Xnb/SoundEffectContentTypeReader.hpp",
     "note": "SoundEffectReader class comment"
    },
    {
     "path": "modules/content/src/Xnb/SoundEffectContentTypeReader.cpp",
     "note": "SoundEffectReader::Read"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; nothing executed. The XNA meaning of the return value was read from the decompiled XNA 4.0 <code>SoundEffect</code>. The behaviours the comments misdescribe are pinned elsewhere: <code>SoundEffectInstanceTest.SetPanBeforePlaybackTakesTheInstanceOutOf3D</code> (whose own comment records that it replaces the older latch-for-life reading). The fire-and-forget pan path is exercised by <code>SoundEffectTest.PlayWithHardPanDoesNotCrash</code>, which does not observe when the pan state is freed.</p>",
   "reproduction": null,
   "tests_current": "<p>Comments are not tested. The code paths are covered by {{src:modules/audio/tests/Microsoft/Xna/Framework/Audio/SoundEffectTests.cpp|SoundEffectTests.cpp}} and {{src:modules/audio/tests/Microsoft/Xna/Framework/Audio/SoundEffectInstanceTests.cpp|SoundEffectInstanceTests.cpp}} (SDL3 audio only).</p>",
   "regression_test": "<p>No behavioural test applies: correct the four comments, and document the false-return cases <code>SoundEffect::Play</code> really has (or implement an instance limit and keep the XNA wording).</p>",
   "blast_radius": "<p>Readers of the public <code>SoundEffect</code> header, who may code against a limit that never triggers, and maintainers of the fire-and-forget, 3D-pan and XNB sound paths.</p>",
   "workaround": null,
   "related": {
    "deep": [
     [
      "deep-dives/services/sound-effects-and-streaming.html#soundeffect",
      "Sound effects, streaming and capture: SoundEffect"
     ]
    ],
    "internals": [
     [
      "development/internals/audio/engine.html#fire-and-forget",
      "Audio engine internals: fire-and-forget playback"
     ],
     [
      "development/internals/audio/engine.html#play",
      "Audio engine internals: one SoundEffectInstance playback"
     ]
    ],
    "guide": [
     [
      "docs/audio.html#tier-1-implemented-sdl3mixer",
      "Audio: the SDL3_mixer tier"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-008 (development/internals/audio/engine.html)"
  },
  {
   "cand_ids": [
    "P2-052"
   ],
   "id": "NEW-B4-03",
   "class": "bug",
   "title": "FNA_KEYBOARD_USE_SCANCODES does not make Keyboard::GetState layout-independent; only the input bridge and GetKeyFromScancodeEXT honour it",
   "summary": "CNA documents scancode mode as FNA's layout-independent key polling, but Keyboard::GetState reads the platform snapshot, which always resolves keys through the current layout, while GetKeyFromScancodeEXT switches to returning keys unchanged.",
   "subsystem": "Input",
   "status": "open",
   "severity": "medium",
   "confidence": "verified-by-reading",
   "public_contract": "Keyboard::GetState() and Keyboard::GetKeyFromScancodeEXT(Keys) under the FNA_KEYBOARD_USE_SCANCODES=1 environment switch",
   "expected": "<p>In FNA, <code>UseScancodes</code> is read once at start-up and <code>ToXNAKey</code> maps every key event through the scancode table when it is set, so <code>Keyboard.GetState()</code> reports the key at the US position whatever the layout, and <code>GetKeyFromScancodeEXT</code> returns its argument unchanged (FNA <code>SDL3_FNAPlatform.cs</code>, <code>ToXNAKey</code> and <code>GetKeyFromScancode</code>). CNA's own {{src:docs/platform-input-notes.md|platform-input-notes.md}} promises the same: &#8220;Set FNA_KEYBOARD_USE_SCANCODES=1 (read once at startup) for layout-independent physical-position key bindings&#8221; and &#8220;The physical position maps to the US-equivalent XNA Keys regardless of layout&#8221;.</p>",
   "actual": "<p><code>use_scancode_mode()</code> in {{src:modules/input/src/Internal/SdlInputBridge.cpp|SdlInputBridge.cpp}} is consulted in two places only: <code>SdlInputBridge::GetKeyFromScancode</code>, which then returns the key unchanged, and the <code>KeyEvent</code> branch of <code>PlatformInputBridge::ProcessEvent</code>, which maps the event through the scancode table for the legacy <code>InputManager</code> held-key set and the <code>TextInputEXT</code> control characters. <code>Keyboard::GetState</code> in {{src:modules/input/src/Xna/Keyboard.cpp|Keyboard.cpp}} never reads that set: it copies the <code>IPlatformKeyboard</code> snapshot, and <code>Sdl3Keyboard::Update</code> in {{src:modules/platform/src/Sdl3/Sdl3InputServices.cpp|Sdl3InputServices.cpp}} resolves each held scancode with <code>SDL_GetKeyFromScancode(scancode, SDL_KMOD_NONE, false)</code>, that is, through the current layout. No platform keyboard service reads the variable.</p><p>The two halves therefore disagree once the mode is on. On an AZERTY layout, holding the key at the US <code>W</code> position makes <code>GetState()</code> report <code>Keys::Z</code>, while <code>GetKeyFromScancodeEXT(Keys::W)</code> now returns <code>Keys::W</code>. The FNA idiom for positional bindings, <code>IsKeyDown(GetKeyFromScancodeEXT(Keys::W))</code>, works with the mode off and stops working when it is switched on.</p>",
   "sources": [
    {
     "path": "modules/input/src/Internal/SdlInputBridge.cpp",
     "note": "use_scancode_mode; SdlInputBridge::GetKeyFromScancode; PlatformInputBridge::ProcessEvent KeyEvent branch"
    },
    {
     "path": "modules/input/src/Xna/Keyboard.cpp",
     "note": "Keyboard::GetState and Keyboard::GetKeyFromScancodeEXT"
    },
    {
     "path": "modules/platform/src/Sdl3/Sdl3InputServices.cpp",
     "note": "Sdl3Keyboard::Update: SDL_GetKeyFromScancode with the current layout"
    },
    {
     "path": "docs/platform-input-notes.md",
     "note": "the scancode-mode promise (keyboard layouts, INPUT-KBD-014)"
    },
    {
     "path": "docs/input-fna-fidelity.md",
     "note": "scancode mode listed as layout-independent, matching FNA"
    },
    {
     "path": "modules/input/tests/CNA/Internal/Input/SdlInputBridgeKeyboardTests.cpp",
     "note": "ScancodeMapUsedWhenScancodeModeForced: a local Keyboard shim returns InputManager's state"
    },
    {
     "path": "modules/input/tests/Microsoft/Xna/Framework/Input/KeyboardInputTests.cpp",
     "note": "header comment: scancode mode not covered"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The FNA reference was read from the FNA-XNA/FNA sources at b3551247 (<code>SDL3_FNAPlatform.cs</code>). A search of <code>modules/platform/src</code> finds no reader of the variable; only the SDL3 keyboard service's translation was read in detail, the X11, Wayland and Win32 services were not examined for their layout behaviour.</p>",
   "reproduction": "<p>Illustrative; not compiled or run.</p><pre><code class=\"language-cpp\">// FNA_KEYBOARD_USE_SCANCODES=1, AZERTY layout, the key at the US \"W\" position held\nKeyboardState state = Keyboard::GetState();\nstate.IsKeyDown(Keys::Z);                                  // true: the layout key\nstate.IsKeyDown(Keys::W);                                  // false (FNA: true)\nstate.IsKeyDown(Keyboard::GetKeyFromScancodeEXT(Keys::W)); // false: the helper returns W unchanged</code></pre>",
   "tests_current": "<p>{{src:modules/input/tests/CNA/Internal/Input/SdlInputBridgeKeyboardTests.cpp|SdlInputBridgeKeyboardTests.cpp}} forces the mode with <code>SetScancodeModeForTests</code>, but its <code>Keyboard::GetState</code> is a local shim returning <code>InputManager::GetKeyboardState()</code>, so <code>ScancodeMapUsedWhenScancodeModeForced</code> asserts the legacy accumulator, not the public query. {{src:modules/input/tests/Microsoft/Xna/Framework/Input/KeyboardInputTests.cpp|KeyboardInputTests.cpp}} states that scancode mode is not covered because the flag is cached per process.</p>",
   "regression_test": "<p>With the test override on, a canned <code>IPlatformKeyboard</code> (or the SDL3 service fed a scancode whose layout key differs) must make the real <code>Microsoft::Xna::Framework::Input::Keyboard::GetState()</code> report the US-position key, and <code>IsKeyDown(GetKeyFromScancodeEXT(k))</code> must agree in both modes.</p>",
   "blast_radius": "<p>Games that set <code>FNA_KEYBOARD_USE_SCANCODES=1</code> and are played on a non-US layout. With the variable unset (the default) behaviour is consistent: layout keys from <code>GetState</code> and a layout translation from <code>GetKeyFromScancodeEXT</code>.</p>",
   "workaround": "<p>Leave the variable unset and bind by position with <code>IsKeyDown(Keyboard::GetKeyFromScancodeEXT(key))</code>.</p>",
   "related": {
    "internals": [
     [
      "development/internals/input/events.html#keyboard",
      "Input internals: keyboard"
     ]
    ],
    "deep": [
     [
      "deep-dives/services/input-model.html#layouts",
      "The input model: physical keys, layouts and the scancode helpers"
     ]
    ],
    "guide": [
     [
      "docs/input.html#keyboard",
      "Input: keyboard"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-052 (development/internals/input/events.html)"
  },
  {
   "cand_ids": [
    "P2-053",
    "P2-057"
   ],
   "id": "NEW-B4-04",
   "class": "bug",
   "title": "Input-pump documentation names code that does not exist: Game::UpdateInput(), an SdlInputBridge ProcessEvent overload and src/Input/Internal paths",
   "summary": "Comments in Sdl3Platform.cpp and X11Platform.cpp refer to Game::UpdateInput(), and docs/input-backend.md describes a raw SdlInputBridge::ProcessEvent overload and src/Input/... files; none exists at TARGET.",
   "subsystem": "Documentation & release tooling",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "Maintainer documentation of the input pump: the EnsureControllerSubsystem comments in the SDL3 and X11 platforms, and docs/input-backend.md",
   "expected": "<p>In-repo documentation names the functions and files a maintainer has to open. At TARGET the input pump is the tail of <code>Game::PollEvents</code> in {{src:modules/runtime/src/Game.cpp|Game.cpp}} (the only production caller of <code>PlatformInputBridge::ProcessEvent</code>, followed by the keyboard and mouse snapshot updates and the gamepad/joystick pump gated on <code>IsSubsystemInitialized</code>), and the input sources live under {{tree:modules/input/src|modules/input/src}}.</p>",
   "actual": "<ul><li>The comments above <code>Sdl3Platform::EnsureControllerSubsystem</code> ({{src:modules/platform/src/Sdl3/Sdl3Platform.cpp|Sdl3Platform.cpp}}) and <code>X11Platform::EnsureControllerSubsystem</code> ({{src:modules/platform/src/X11/X11Platform.cpp|X11Platform.cpp}}) explain the lazy controller start by what &#8220;Game::UpdateInput()&#8221; does; <code>Game</code> has no such member. The behaviour they describe is real but lives in <code>Game::PollEvents</code>.</li><li>{{src:docs/input-backend.md|input-backend.md}} section 1 says <code>SdlInputBridge</code> keeps &#8220;a raw ProcessEvent overload&#8221; for legacy SDL-shaped tests that delegates to the platform bridge. {{src:modules/input/include/CNA/Internal/Input/SdlInputBridge.hpp|SdlInputBridge.hpp}} declares no <code>ProcessEvent</code>; the only one is <code>PlatformInputBridge::ProcessEvent</code>.</li><li>The same document cites <code>src/Input/Internal/InputManager.cpp</code> and <code>src/Input/Internal/GestureDetector.cpp</code>, and its section 6 verifies thread-freedom &#8220;under src/Input/Internal/ or src/Input/Xna/&#8221;; the files are <code>modules/input/src/Internal/</code> and <code>modules/input/src/Xna/</code>.</li></ul>",
   "sources": [
    {
     "path": "modules/platform/src/Sdl3/Sdl3Platform.cpp",
     "note": "comment above Sdl3Platform::EnsureControllerSubsystem"
    },
    {
     "path": "modules/platform/src/X11/X11Platform.cpp",
     "note": "comment above X11Platform::EnsureControllerSubsystem"
    },
    {
     "path": "modules/runtime/src/Game.cpp",
     "note": "Game::PollEvents: the real pump and the IsSubsystemInitialized gate"
    },
    {
     "path": "docs/input-backend.md",
     "note": "sections 1 and 6"
    },
    {
     "path": "modules/input/include/CNA/Internal/Input/SdlInputBridge.hpp",
     "note": "no ProcessEvent declared"
    },
    {
     "path": "modules/input/include/CNA/Internal/Input/PlatformInputBridge.hpp",
     "note": "PlatformInputBridge::ProcessEvent, the single event funnel"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; a search of the TARGET tree for <code>UpdateInput</code> finds only these two comments, a design sketch under <code>misc/</code> and a plan entry. Not executed.</p>",
   "reproduction": null,
   "tests_current": "<p>Not applicable: documentation.</p>",
   "regression_test": "<p>No behavioural test: rename the references to <code>Game::PollEvents</code> and <code>PlatformInputBridge::ProcessEvent</code>, and correct the paths.</p>",
   "blast_radius": "<p>Maintainers following the comments or <code>docs/input-backend.md</code> to the pump; no runtime effect.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/input/events.html#frame",
      "Input internals: actual order in one Game frame"
     ],
     [
      "development/internals/runtime/frame.html#events",
      "One frame source trace: the event pump"
     ]
    ]
   },
   "origin": "Phase-2 candidates P2-053 and P2-057 (development/internals/input/events.html)"
  },
  {
   "cand_ids": [
    "P2-054"
   ],
   "id": "NEW-B4-05",
   "class": "bug",
   "title": "Game::PollEvents routes F9 and F10 to the renderer's test-only context-loss hooks in every build",
   "summary": "A non-repeated F9 or F10 press in any CNA game calls the renderer's DebugSimulateContextLoss() or DebugRestoreContext(), which CNA describes as a test seam, with no build guard or opt-out.",
   "subsystem": "Core & runtime",
   "status": "open",
   "severity": "medium",
   "confidence": "verified-by-reading",
   "public_contract": "Microsoft::Xna::Framework::Game::Run() and Tick() (through PollEvents); IGraphicsRenderer::DebugSimulateContextLoss() and DebugRestoreContext()",
   "expected": "<p>XNA's <code>Game</code> reserves no keys; a key press only reaches the game's input state. CNA itself describes the <code>DebugSimulateContextLoss()</code>/<code>DebugRestoreContext()</code> channel as test-only (the <code>RendererDeviceEvent</code> comment in {{src:modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp|IGraphicsRenderer.hpp}}).</p>",
   "actual": "<p>After handing each event to <code>PlatformInputBridge::ProcessEvent</code>, <code>Game::PollEvents</code> in {{src:modules/runtime/src/Game.cpp|Game.cpp}} calls <code>DebugSimulateContextLoss()</code> for a pressed, non-repeated <code>KeyCode::F9</code> and <code>DebugRestoreContext()</code> for F10, unconditionally. The game still receives the key. The base implementations are no-ops; the EasyGL, DirectX 9/11/12, Direct2D and WebGPU renderers override them with real device-loss simulation (context recreation, a lost-device state, a resource rebuild, or a <code>NotSupportedException</code> refusal), and GDI overrides them with empty bodies. The per-renderer consequences are detailed in the merged entry for this defect.</p>",
   "sources": [
    {
     "path": "modules/runtime/src/Game.cpp",
     "note": "Game::PollEvents, KeyEvent branch"
    },
    {
     "path": "modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp",
     "note": "DebugSimulateContextLoss / DebugRestoreContext defaults; the test-only channel comment"
    },
    {
     "path": "modules/renderers/easygl/src/EasyGLRenderer.cpp",
     "note": "EasyGLRenderer::DebugSimulateContextLoss / DebugRestoreContext"
    },
    {
     "path": "modules/renderers/webgpu/src/WebGPURenderer.cpp",
     "note": "WebGPURenderer::DebugSimulateContextLoss"
    }
   ],
   "evidence": "<p>Checked by reading <code>Game::PollEvents</code> at 009d40f5 and listing the renderer overrides; not executed. This is the same defect as the entry recorded from the runtime frame pages; it is listed here because the input internals page recorded it independently.</p>",
   "reproduction": null,
   "tests_current": "<p>{{src:modules/runtime/tests/Microsoft/Xna/Framework/GameEventSemanticsGoldenTests.cpp|GameEventSemanticsGoldenTests.cpp}} scripts an F9 press with and without repeat but records only public game state, so it cannot see the renderer call. No test asserts that an ordinary key press leaves the renderer alone.</p>",
   "regression_test": "<p>Feed a non-repeated F9 <code>KeyEvent</code> through <code>Game::PollEvents</code> with a renderer double that counts <code>DebugSimulateContextLoss()</code> calls and expect zero unless an explicit debug opt-in is enabled.</p>",
   "blast_radius": "<p>Every <code>Game</code> on the renderer families that override the hooks; games that bind F9 or F10 (quick save and quick load are common) trigger them on every press.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/input/events.html#frame",
      "Input internals: actual order in one Game frame"
     ],
     [
      "development/internals/runtime/frame.html#events",
      "One frame source trace: the event pump"
     ]
    ],
    "guide": [
     [
      "docs/input.html#keyboard",
      "Input: keyboard"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-054 (development/internals/input/events.html); duplicate of B2's NEW-B2-01 (P2-004, P2-187)"
  },
  {
   "cand_ids": [
    "P2-055"
   ],
   "id": "NEW-B4-06",
   "class": "platform-limitation",
   "title": "ScrollWheelValue granularity depends on the platform backend: SDL3 drops sub-notch wheel motion, Win32 and Wayland keep it",
   "summary": "The SDL3 mouse service truncates every wheel event to whole notches before scaling by 120, so high-resolution wheels and touchpads can scroll without changing ScrollWheelValue, while the Win32 and Wayland backends accumulate 1/120 units as XNA does.",
   "subsystem": "Input",
   "status": "open",
   "severity": "n/a",
   "confidence": "verified-by-reading",
   "public_contract": "MouseState::getScrollWheelValueProperty() and getHorizontalScrollWheelValueEXTProperty() as reported by Mouse::GetState()",
   "expected": "<p>XNA 4.0 on Windows accumulates the raw signed <code>WM_MOUSEWHEEL</code> delta into the wheel value (<code>MouseMessageHooker.WndProc</code>, decompiled reference), so a high-resolution wheel moves it in steps smaller than 120. FNA's SDL3 platform instead adds <code>(int) evt.wheel.y * 120</code>. A game should see the same wheel units whichever CNA backend it runs on.</p>",
   "actual": "<ul><li>SDL3: {{src:modules/platform/src/Sdl3/Sdl3EventMapper.cpp|Sdl3EventMapper.cpp}} passes SDL's float <code>wheel.x</code>/<code>wheel.y</code>, and <code>AccumulateWheel</code> in {{src:modules/platform/src/Sdl3/Sdl3InputServices.cpp|Sdl3InputServices.cpp}} converts each event to an integer number of notches before multiplying by 120. An event of 0.25 notch adds nothing, so a touchpad or smooth-scrolling wheel that reports fractions per event can leave <code>ScrollWheelValue</code> unchanged for a whole gesture.</li><li>Win32: <code>Win32EventMapper::TranslateMouseWheel</code> ({{src:modules/platform/src/Win32/Win32EventMapper.cpp|Win32EventMapper.cpp}}) accumulates the raw <code>WHEEL_DELTA</code> units, as XNA does.</li><li>Wayland: <code>AxisTo120</code> in {{src:modules/platform/src/Wayland/WaylandMouse.cpp|WaylandMouse.cpp}} keeps the compositor's <code>value120</code>, so half a notch is 60.</li><li>X11 core wheel buttons are whole notches by nature (<code>X11Mouse::AccumulateScroll</code>).</li></ul><p>{{src:docs/input-fna-fidelity.md|input-fna-fidelity.md}} records the SDL3 truncation as an intentional FNA match that keeps the value &#8220;a clean multiple of 120 exactly like XNA&#8221;; XNA's own hook does not truncate, and the Win32 and Wayland backends do not either.</p>",
   "sources": [
    {
     "path": "modules/platform/src/Sdl3/Sdl3InputServices.cpp",
     "note": "AccumulateWheel and Sdl3Mouse::ObserveEvent"
    },
    {
     "path": "modules/platform/src/Sdl3/Sdl3EventMapper.cpp",
     "note": "SDL_EVENT_MOUSE_WHEEL mapping"
    },
    {
     "path": "modules/platform/src/Win32/Win32EventMapper.cpp",
     "note": "Win32EventMapper::TranslateMouseWheel"
    },
    {
     "path": "modules/platform/src/Wayland/WaylandMouse.cpp",
     "note": "AxisTo120, WaylandMouse::EmitFrame and Update"
    },
    {
     "path": "modules/platform/src/X11/X11Mouse.cpp",
     "note": "X11Mouse::AccumulateScroll"
    },
    {
     "path": "docs/input-fna-fidelity.md",
     "note": "Mouse: the wheel truncation recorded as an intentional deviation"
    },
    {
     "path": "modules/platform/tests/CNA/Platform/Sdl3InputServicesTests.cpp",
     "note": "Sdl3MouseTest.WheelEventsAccumulateInXnaUnitsAndTruncateBeforeScaling"
    },
    {
     "path": "modules/platform/tests/CNA/Platform/WaylandMappingTests.cpp",
     "note": "WaylandWheelMapping.Value120IsTakenAsItIs"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The XNA behaviour was read from the decompiled XNA 4.0 <code>MouseMessageHooker</code>, FNA's from FNA-XNA/FNA b3551247. Each backend's rule is pinned by its own test (Sdl3MouseTest, Win32EventMapping.WheelTotalsAccumulateInXnaUnits, WaylandWheelMapping), so the difference is deliberate per backend, not accidental. Whether a given touchpad reports sub-notch events through SDL depends on the host and was not observed.</p>",
   "reproduction": null,
   "tests_current": "<p>{{src:modules/platform/tests/CNA/Platform/Sdl3InputServicesTests.cpp|Sdl3InputServicesTests.cpp}} pins the truncation (1.9 notches count as one), {{src:modules/platform/tests/CNA/Platform/Win32EventMapperTests.cpp|Win32EventMapperTests.cpp}} whole-notch accumulation and {{src:modules/platform/tests/CNA/Platform/WaylandMappingTests.cpp|WaylandMappingTests.cpp}} the half notch. No test states a cross-backend rule.</p>",
   "regression_test": "<p>If one rule is chosen, a backend-neutral conformance case: two half-notch wheel events must change <code>ScrollWheelValue</code> by the same amount (120, or 60 and 60) on every backend.</p>",
   "blast_radius": "<p>Games that read <code>ScrollWheelValue</code> for zoom or scrolling on the SDL3 backend with touchpads or free-spinning wheels, and games whose behaviour differs between the SDL3 and native Win32 or Wayland builds.</p>",
   "workaround": "<p>None inside the XNA surface; a game that needs smooth scrolling on SDL3 has to read wheel events itself.</p>",
   "related": {
    "internals": [
     [
      "development/internals/input/events.html#mouse",
      "Input internals: mouse state, coordinates and wheel ownership"
     ]
    ],
    "deep": [
     [
      "deep-dives/services/input-model.html#mousestate",
      "The input model: MouseState"
     ]
    ],
    "guide": [
     [
      "docs/input.html#mouse",
      "Input: mouse"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-055 (development/internals/input/events.html)"
  },
  {
   "cand_ids": [
    "P2-116"
   ],
   "id": "NEW-B4-07",
   "class": "bug",
   "title": "MediaPlayer::Play(Song*) reads and writes a destroyed Song when given a Song that the queue owns",
   "summary": "Play(Song*) clears the queue before it copies and plays the given song, so passing the queue's own song, such as getQueueProperty().getActiveSongProperty(), is a heap use-after-free.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "high",
   "confidence": "verified-by-reading",
   "public_contract": "MediaPlayer::Play(Song*) given a pointer obtained from MediaQueue::getActiveSongProperty() or MediaQueue::operator[]",
   "expected": "<p>In XNA, <code>MediaPlayer.Play(MediaPlayer.Queue.ActiveSong)</code> is a valid call (restart the current song): the queue's indexer returns a new managed <code>Song</code> wrapper (decompiled <code>MediaQueue</code>), and FNA's queue holds garbage-collected objects. CNA's <code>MediaQueue</code> header hands out raw pointers into the queue without a lifetime caveat, and <code>MediaPlayer::Play(Song*)</code> is documented only as &#8220;Clears the queue, enqueues the given song, and starts playback&#8221;.</p>",
   "actual": "<p><code>MediaPlayer::Play(Song*)</code> in {{src:modules/media/src/Xna/MediaPlayer.cpp|MediaPlayer.cpp}} saves <code>queue_[0]</code>, then calls <code>queue_.Clear()</code>. <code>MediaQueue::Clear</code> in {{src:modules/media/src/Xna/MediaQueue.cpp|MediaQueue.cpp}} clears a <code>std::vector&lt;std::unique_ptr&lt;Song&gt;&gt;</code>, destroying every queued <code>Song</code>. <code>Play</code> then calls <code>LoadSong(song)</code>, which reads <code>song-&gt;getHandle()</code> and <code>getNameProperty()</code> to build the new copy, and <code>PlaySong(song)</code>, which reads the handle again and, in a build with a mixer, writes <code>setDurationProperty</code> and <code>setPlayCountProperty</code> on it. When <code>song</code> is one of the queue's own Songs, all of these touch freed memory; the result can be a copy built from stale data, a crash, or heap corruption. <code>Play(const SongCollection&amp;, index)</code> has the same shape if the collection holds pointers taken from the queue.</p>",
   "sources": [
    {
     "path": "modules/media/src/Xna/MediaPlayer.cpp",
     "note": "MediaPlayer::Play(Song*), LoadSong, PlaySong"
    },
    {
     "path": "modules/media/src/Xna/MediaQueue.cpp",
     "note": "MediaQueue::Clear destroys the owned Songs"
    },
    {
     "path": "modules/media/include/Microsoft/Xna/Framework/Media/MediaQueue.hpp",
     "note": "getActiveSongProperty / operator[] return pointers into the queue; Add takes ownership"
    },
    {
     "path": "modules/media/tests/Microsoft/Xna/Framework/Media/MediaPlayerTests.cpp",
     "note": "PlayEnqueuesADuplicateNotTheOriginalInstance"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed (no sanitizer run). The ordering is unconditional: the destruction in <code>Clear</code> precedes the first read in <code>LoadSong</code> on every call, so the failure follows from the code, not from timing.</p>",
   "reproduction": "<p>Illustrative; not compiled or run.</p><pre><code class=\"language-cpp\">Song theme(\"Content/theme.ogg\", \"theme\");\nMediaPlayer::Play(&amp;theme);                                   // the queue now owns a copy\nMediaPlayer::Play(MediaPlayer::getQueueProperty().getActiveSongProperty());\n// Clear() destroys that copy before LoadSong and PlaySong use it: heap-use-after-free under ASan</code></pre>",
   "tests_current": "<p>{{src:modules/media/tests/Microsoft/Xna/Framework/Media/MediaPlayerTests.cpp|MediaPlayerTests.cpp}} pins that <code>Play</code> enqueues a copy (<code>PlayEnqueuesADuplicateNotTheOriginalInstance</code>); {{src:modules/media/tests/Microsoft/Xna/Framework/Media/MediaQueueTests.cpp|MediaQueueTests.cpp}} covers the queue alone. No test passes a queue-owned song to <code>Play</code>.</p>",
   "regression_test": "<p>A MediaPlayerTest run under AddressSanitizer: <code>Play(&amp;song)</code>, then <code>Play(getQueueProperty().getActiveSongProperty())</code>; expect no sanitizer report and a queue holding one Song whose handle equals the original. The fix is to copy handle and name (or build the new queue) before clearing the old one.</p>",
   "blast_radius": "<p>Every audio profile, for callers that replay or restart a song taken from <code>MediaPlayer::getQueueProperty()</code>. The reads happen in every build; the writes only where <code>SOUND_ENABLED</code> is defined (SDL3 and ALSA audio). Songs owned by the caller, by <code>ContentManager</code> results or by <code>MediaLibrary</code> are not affected.</p>",
   "workaround": "<p>Replay the <code>Song</code> object the game owns (the one originally passed to <code>Play</code>), never a pointer obtained from the queue.</p>",
   "related": {
    "deep": [
     [
      "deep-dives/services/media-contract.html#player",
      "Songs, the media library and video: MediaPlayer"
     ]
    ],
    "internals": [
     [
      "development/internals/modules/media.html#music-sharp-edges",
      "Media module: sharp edges recorded from the source"
     ],
     [
      "development/internals/modules/media.html#queue",
      "Media module: the queue"
     ]
    ],
    "guide": [
     [
      "docs/tutorials/122-media-library.html#playing",
      "Media library tutorial: playing songs"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-116 (development/internals/modules/media.html)"
  },
  {
   "cand_ids": [
    "P2-117",
    "P2-118"
   ],
   "id": "NEW-B4-08",
   "class": "bug",
   "title": "MediaPlayer flags ActiveSongChanged on every Play(Song*), even for the same song, and never for Play(const SongCollection&[, index])",
   "summary": "Play(Song*) compares the queue's copy with the caller's pointer, so the event fires on every call, while playing a collection never raises it even when the active song changes.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "MediaPlayer::ActiveSongChanged as raised by MediaPlayer::Play(Song*), Play(const SongCollection&) and Play(const SongCollection&, intcs)",
   "expected": "<p>{{src:modules/media/include/Microsoft/Xna/Framework/Media/MediaPlayer.hpp|MediaPlayer.hpp}} documents the event as &#8220;Raised when the active song changes&#8221;. XNA raises it from the native player whenever its active song changes (the <code>Media_ActiveSongChanged</code> managed call dispatched by <code>FrameworkDispatcher</code>, decompiled reference), which includes starting a collection; whether XNA raises it when the same song is replayed is decided in native code the managed IL does not show. FNA's <code>Play(Song)</code>, which CNA's code transcribes, raises it only when <code>previousSong != song</code> under <code>Song</code>'s overloaded operator, that is, when the file differs.</p>",
   "actual": "<ul><li><code>Play(Song*)</code> in {{src:modules/media/src/Xna/MediaPlayer.cpp|MediaPlayer.cpp}} keeps <code>Song* previousSong = queue_[0]</code> and flags the event when <code>previousSong != song</code>. The queue holds copies made by <code>LoadSong</code>, so a raw pointer comparison with the caller's Song is always unequal (except in the use-after-free case of the entry on queue-owned songs): replaying the same song raises <code>ActiveSongChanged</code> every time. CNA's <code>Song::Equals</code> compares handles as FNA's operator does, but is not used here.</li><li><code>Play(const SongCollection&amp;, index)</code> clears and refills the queue and calls <code>PlaySong</code> without setting <code>FrameworkDispatcher::ActiveSongChanged</code>. Only <code>MediaStateChanged</code> can fire, and only if the state changes, so switching from one playlist to another while music plays raises neither event. <code>NextSong</code> and the end-of-queue path in <code>Update</code> do set the flag. FNA's overload has the same omission.</li></ul>",
   "sources": [
    {
     "path": "modules/media/src/Xna/MediaPlayer.cpp",
     "note": "MediaPlayer::Play(Song*), Play(const SongCollection&, intcs), NextSong, Update"
    },
    {
     "path": "modules/media/include/Microsoft/Xna/Framework/Media/MediaPlayer.hpp",
     "note": "ActiveSongChanged documentation"
    },
    {
     "path": "modules/media/src/Xna/Song.cpp",
     "note": "Song::Equals and operator== compare handles"
    },
    {
     "path": "modules/media/tests/Microsoft/Xna/Framework/Media/MediaPlayerTests.cpp",
     "note": "ActiveSongChangedAndMediaStateChangedFireThroughFrameworkDispatcherUpdate"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. FNA was read at FNA-XNA/FNA b3551247 (<code>MediaPlayer.cs</code>, <code>Song.cs</code>); XNA's dispatch path from the decompiled <code>FrameworkDispatcher</code> and the native callback shim.</p>",
   "reproduction": "<p>Illustrative; not compiled or run.</p><pre><code class=\"language-cpp\">Song a(\"Content/a.ogg\", \"a\");\nMediaPlayer::Play(&amp;a);\nFrameworkDispatcher::Update();   // ActiveSongChanged (expected)\nMediaPlayer::Play(&amp;a);\nFrameworkDispatcher::Update();   // ActiveSongChanged again: same file (FNA: no event)\nMediaPlayer::Play(otherPlaylist);\nFrameworkDispatcher::Update();   // no ActiveSongChanged although the song changed</code></pre>",
   "tests_current": "<p><code>ActiveSongChangedAndMediaStateChangedFireThroughFrameworkDispatcherUpdate</code> in {{src:modules/media/tests/Microsoft/Xna/Framework/Media/MediaPlayerTests.cpp|MediaPlayerTests.cpp}} covers one <code>Play(Song*)</code> from an empty queue. No test replays a song or plays a collection and checks the event.</p>",
   "regression_test": "<p>Two dispatcher-driven cases: replaying the same Song raises no <code>ActiveSongChanged</code> (compare with <code>Equals</code>), and <code>Play(collection)</code> while another song is active raises it once.</p>",
   "blast_radius": "<p>Games that update a now-playing display or statistics from <code>ActiveSongChanged</code>. Playback itself is unaffected.</p>",
   "workaround": "<p>Track the active song in the game (compare <code>getActiveSongProperty()</code> handles each frame) instead of relying on the event.</p>",
   "related": {
    "deep": [
     [
      "deep-dives/services/media-contract.html#player",
      "Songs, the media library and video: MediaPlayer"
     ]
    ],
    "internals": [
     [
      "development/internals/modules/media.html#dispatcher",
      "Media module: the dispatcher flags"
     ],
     [
      "development/internals/modules/media.html#music-sharp-edges",
      "Media module: sharp edges recorded from the source"
     ]
    ]
   },
   "origin": "Phase-2 candidates P2-117 and P2-118 (development/internals/modules/media.html)"
  },
  {
   "cand_ids": [
    "P2-119"
   ],
   "id": "NEW-B4-09",
   "class": "bug",
   "title": "MediaPlayer::Play(Song*) records Duration and PlayCount on the caller's Song instead of the queued copy, and Stop never resets that PlayCount",
   "summary": "Play(Song*) hands the caller's Song to PlaySong, so the mixer duration and a PlayCount increment land on the library or content Song while the queue's active copy keeps zero, contrary to MediaLibrary.cpp's comment.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "Song::getDurationProperty() and Song::getPlayCountProperty() of the caller's Song and of MediaQueue::getActiveSongProperty() after MediaPlayer::Play(Song*)",
   "expected": "<p>CNA copies the Song into the queue on purpose (FNA's comment: XNA &#8220;duplicates the Song object and then assigns a bunch of stuff to it at Play time&#8221;), and {{src:modules/media/src/Xna/MediaLibrary.cpp|MediaLibrary.cpp}} (<code>BuildFromRoots</code>) relies on it: &#8220;MediaPlayer::Play() operates on a duplicate &#8230; not the library's original Song instance&#8221;. The active song in the queue should therefore carry what playback learns, the same way for every <code>Play</code> overload, and <code>Stop</code> should reset the counts it maintains.</p>",
   "actual": "<p>In {{src:modules/media/src/Xna/MediaPlayer.cpp|MediaPlayer.cpp}}, <code>Play(Song*)</code> calls <code>PlaySong(song)</code> with the caller's object, not the copy it just queued. In a build with a mixer <code>PlaySong</code> overwrites that Song's <code>Duration</code> with the mixer-reported length and increments its <code>PlayCount</code>. <code>Play(const SongCollection&amp;, index)</code> and <code>NextSong</code> pass the queue's copy instead. Consequences:</p><ul><li>after <code>Play(&amp;song)</code> the queue's active Song reports a zero Duration, after <code>Play(collection)</code> it reports the mixer value;</li><li>a library Song's PlayCount grows by one on every <code>Play(Song*)</code> and is never reset, because <code>Stop</code> resets PlayCount only on the queue's copies;</li><li>the <code>MediaLibrary.cpp</code> comment is wrong for this overload.</li></ul><p>FNA also passes the caller's Song to <code>PlaySong</code> (so its Duration lands there too) but never increments PlayCount; XNA's queue Song is a new wrapper around the same native media item, whose duration and play count are read from native metadata, so both objects agree.</p>",
   "sources": [
    {
     "path": "modules/media/src/Xna/MediaPlayer.cpp",
     "note": "MediaPlayer::Play(Song*), PlaySong, Stop"
    },
    {
     "path": "modules/media/src/Xna/MediaLibrary.cpp",
     "note": "MediaLibrary::BuildFromRoots: the songs-block comment on MediaPlayer::Play"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. FNA read at FNA-XNA/FNA b3551247 (<code>MediaPlayer.cs</code>).</p>",
   "reproduction": "<p>Illustrative; not compiled or run (SDL3 audio).</p><pre><code class=\"language-cpp\">Song s(\"Content/theme.ogg\", \"theme\");   // or a MediaLibrary song\nMediaPlayer::Play(&amp;s);\ns.getPlayCountProperty();                                                // 1, grows on every Play(&amp;s)\nMediaPlayer::getQueueProperty().getActiveSongProperty()-&gt;getDurationProperty(); // zero\nMediaPlayer::Stop();                                                     // s keeps its PlayCount</code></pre>",
   "tests_current": "<p><code>PlayEnqueuesADuplicateNotTheOriginalInstance</code> in {{src:modules/media/tests/Microsoft/Xna/Framework/Media/MediaPlayerTests.cpp|MediaPlayerTests.cpp}} checks that setting the original's PlayCount does not change the copy, the opposite direction; nothing checks which object <code>PlaySong</code> updates.</p>",
   "regression_test": "<p>With SDL3 audio and the dummy driver: after <code>Play(&amp;song)</code> the queue's active Song has a non-zero Duration and PlayCount 1 while the caller's Song is unchanged (or, if the caller's Song is meant to be updated, document it and reset it in <code>Stop</code>).</p>",
   "blast_radius": "<p>Games that display a song's length or play count from the queue's active Song, and MediaLibrary songs whose PlayCount is shown. Playback is unaffected.</p>",
   "workaround": "<p>Read Duration from the Song object the game passed to <code>Play(Song*)</code>, or play through a SongCollection.</p>",
   "related": {
    "deep": [
     [
      "deep-dives/services/media-contract.html#player",
      "Songs, the media library and video: MediaPlayer"
     ]
    ],
    "internals": [
     [
      "development/internals/modules/media.html#queue",
      "Media module: the queue"
     ],
     [
      "development/internals/modules/media.html#music-sharp-edges",
      "Media module: sharp edges recorded from the source"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-119 (development/internals/modules/media.html)"
  },
  {
   "cand_ids": [
    "P2-120",
    "P2-122"
   ],
   "id": "NEW-B4-10",
   "class": "bug",
   "title": "MediaPlayer::Play reports no error when a song cannot be played and can leave the player Playing with no track",
   "summary": "A mixer load, track or start failure makes PlaySong return silently after the previous track was destroyed, so the state may stay Playing indefinitely, and a vanished file or a missing audio device throws non-XNA exceptions after the queue was replaced.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "medium",
   "confidence": "verified-by-reading",
   "public_contract": "MediaPlayer::Play(Song*), Play(const SongCollection&), Play(const SongCollection&, intcs), MoveNext and MovePrevious: failure behaviour",
   "expected": "<p>XNA's <code>MediaQueue.Play(Song)</code> throws <code>InvalidOperationException</code> (<code>SongPlaybackFailed</code>) when the native player cannot play the song (decompiled reference). Whatever CNA chooses, a failed <code>Play</code> should leave a consistent player: either an exception and the previous state, or <code>Stopped</code>. <code>MediaPlayer.hpp</code> documents no failure mode.</p>",
   "actual": "<p>Three failure paths in {{src:modules/media/src/Xna/MediaPlayer.cpp|MediaPlayer.cpp}}:</p><ol><li><strong>Undecodable or unstartable song, silently.</strong> In a build with a mixer, <code>PlaySong</code> first destroys the current music track and audio and clears the song-ended flag, then returns without an error if <code>LoadMixerAudioFile</code> yields nothing (an unsupported format, a corrupt file), if no track can be created or bound, or if <code>PlayMixerTrack</code> fails. The timer and state are not touched: from Stopped the player stays Stopped with the song queued as active; from Playing it stays <em>Playing</em> with no track, <code>PlayPosition</code> keeps counting from the previous song, and <code>Update</code> never advances because the ended flag can no longer be set; from Paused, a later <code>Resume</code> reports Playing with nothing playing. Neither mixer's error text reaches the caller.</li><li><strong>No audio device: a raw std::runtime_error.</strong> The music load is the first mixer use in a game that plays music before any sound effect. On SDL3 audio <code>LoadMixerAudioFile</code> reaches <code>GetMixer</code>, on ALSA <code>EnsureMixer</code>, and either throws <code>std::runtime_error</code> out of <code>Play</code> after the queue was replaced. <code>SoundEffect</code>'s constructors convert the same failure to <code>NoAudioHardwareException</code>; MediaPlayer does not.</li><li><strong>Vanished file.</strong> <code>LoadSong</code> copies the song with <code>new Song(handle, name)</code>, whose constructor ({{src:modules/media/src/Xna/Song.cpp|Song.cpp}}) throws <code>FileNotFoundException</code> if the file no longer exists. The copy happens after <code>queue_.Clear()</code>, so the exception leaves an empty queue (or a partly refilled one for a collection) while the previous track keeps playing in the Playing state. FNA's <code>LoadSong</code> has the same shape.</li></ol>",
   "sources": [
    {
     "path": "modules/media/src/Xna/MediaPlayer.cpp",
     "note": "MediaPlayer::PlaySong failure returns; Play, LoadSong, Update"
    },
    {
     "path": "modules/audio/src/Backend/Sdl3Mixer/MixerEngine.cpp",
     "note": "LoadMixerAudioFile / FinalizeAudioLoad: empty result, or GetMixer's exception"
    },
    {
     "path": "modules/audio/src/Backend/CnaMixer/MixerEngine.cpp",
     "note": "LoadMixerAudioFile: EnsureMixer, then an empty result with the error kept in the engine"
    },
    {
     "path": "modules/audio/src/Backend/Sdl3Mixer/AudioMixer.cpp",
     "note": "GetMixer throws std::runtime_error when the device or mixer cannot be created"
    },
    {
     "path": "modules/media/src/Xna/Song.cpp",
     "note": "Song::Song(std::string, std::string): FileNotFoundException for a missing file"
    },
    {
     "path": "modules/audio/src/Xna/SoundEffect.cpp",
     "note": "EnsureMixerOrThrowXna: the NoAudioHardwareException conversion MediaPlayer lacks"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The XNA behaviour was read from the decompiled <code>MediaQueue.Play</code>; FNA's <code>LoadSong</code> from FNA-XNA/FNA b3551247. The ALSA mixer records a decode error string in its engine that nothing in the media module reads.</p>",
   "reproduction": "<p>Illustrative; not compiled or run (SDL3 audio, vendored SDL3_mixer).</p><pre><code class=\"language-cpp\">Song music(\"Content/music.ogg\", \"music\");\nSong voice(\"Content/voice.opus\", \"voice\");     // Opus is not decoded by this build\nMediaPlayer::Play(&amp;music);                     // Playing\nMediaPlayer::Play(&amp;voice);                     // music stops, nothing plays, no exception\nMediaPlayer::getStateProperty();                  // still MediaState::Playing, indefinitely</code></pre>",
   "tests_current": "<p>{{src:modules/media/tests/Microsoft/Xna/Framework/Media/MediaPlayerTests.cpp|MediaPlayerTests.cpp}} plays only decodable fixtures with <code>SDL_AUDIODRIVER=dummy</code>; its own comment on <code>VisualizationEnabledStateStaysConsistentWithGetVisualizationData</code> notes that mixer creation therefore never fails in the suite. No test plays an undecodable or deleted file or runs without an audio device.</p>",
   "regression_test": "<p>With a song playing, <code>Play</code> an undecodable file (a zero-byte <code>.ogg</code> is enough) and require a defined outcome: an <code>InvalidOperationException</code> with the previous song still active, or <code>Stopped</code>, never Playing without a track. Add the same for a file deleted between constructing the Song and calling <code>Play</code>.</p>",
   "blast_radius": "<p>Games playing unsupported formats (see CNA-BUG-052), corrupt or deleted files, and games started on machines without an audio output device (SDL3 and ALSA builds). Music loaded from decodable files on a working device is unaffected. In builds without a mixer (SDL2 or NULL audio) <code>PlaySong</code> has no load step, so only the vanished-file path applies there.</p>",
   "workaround": "<p>Use the four decodable formats, and check <code>MediaPlayer::getStateProperty()</code> together with a game-side timer rather than trusting Playing; catch <code>std::exception</code> around the first <code>Play</code>.</p>",
   "related": {
    "deep": [
     [
      "deep-dives/services/media-contract.html#player",
      "Songs, the media library and video: MediaPlayer"
     ]
    ],
    "internals": [
     [
      "development/internals/modules/media.html#state",
      "Media module: playback state"
     ],
     [
      "development/internals/modules/media.html#music-sharp-edges",
      "Media module: sharp edges recorded from the source"
     ],
     [
      "development/internals/audio/engine.html#failure-representation",
      "Audio engine internals: failure representation"
     ]
    ],
    "guide": [
     [
      "docs/audio.html#tier-1-implemented-sdl3mixer",
      "Audio: the SDL3_mixer tier"
     ]
    ]
   },
   "origin": "Phase-2 candidates P2-120 and P2-122 (development/internals/modules/media.html); the missing-device path was added at TARGET review"
  },
  {
   "cand_ids": [
    "P2-121"
   ],
   "id": "NEW-B4-11",
   "class": "bug",
   "title": "MediaPlayer::ProgramExit is documented as called at application exit, but nothing in CNA calls it",
   "summary": "Unlike FNA, which hooks ProcessExit, CNA never calls MediaPlayer::ProgramExit outside an explicit C API export, so the music track is not released by CNA at exit and the header's statement is false.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "MediaPlayer::ProgramExit() (CNAEXT) and the release of MediaPlayer's music track at exit",
   "expected": "<p>{{src:modules/media/include/Microsoft/Xna/Framework/Media/MediaPlayer.hpp|MediaPlayer.hpp}}: &#8220;Releases renderer media resources if initialized. Called at application exit.&#8221; FNA's <code>MediaPlayer</code> static constructor registers <code>ProgramExit</code> on <code>AppDomain.CurrentDomain.ProcessExit</code>.</p>",
   "actual": "<p>No <code>Game</code> shutdown path, static destructor or <code>atexit</code> handler calls it; within the modules the only caller is the C API export <code>cna_media_player_program_exit_ext</code> in {{src:modules/c-api/src/CnaCApiMediaPlayer.cpp|CnaCApiMediaPlayer.cpp}}. Unless the game stops the music itself, the music track (a raw pointer in {{src:modules/media/src/Xna/MediaPlayer.cpp|MediaPlayer.cpp}}) is therefore not destroyed at exit, and the namespace-scope music audio is released during static destruction in an order unspecified relative to the mixer's own statics. When a host does call <code>ProgramExit</code>, it destroys the track but leaves the state <code>Playing</code>. No crash was traced to this; the defect is the missing exit hook the header promises.</p>",
   "sources": [
    {
     "path": "modules/media/include/Microsoft/Xna/Framework/Media/MediaPlayer.hpp",
     "note": "ProgramExit documentation"
    },
    {
     "path": "modules/media/src/Xna/MediaPlayer.cpp",
     "note": "MediaPlayer::ProgramExit; the g_musicTrack and g_musicAudio statics"
    },
    {
     "path": "modules/c-api/src/CnaCApiMediaPlayer.cpp",
     "note": "cna_media_player_program_exit_ext, the only caller"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; caller search over <code>modules</code> and <code>tools</code> excluding tests. FNA read at FNA-XNA/FNA b3551247. Not executed; no exit-time failure was observed or reproduced.</p>",
   "reproduction": null,
   "tests_current": "<p>No test calls <code>ProgramExit</code> or checks exit-time release of the music track.</p>",
   "regression_test": "<p>Either register the call (for example from <code>Game</code> disposal or an <code>atexit</code> handler installed on first <code>Play</code>) and test that the track is released and the state is Stopped, or change the header to say the host must call it.</p>",
   "blast_radius": "<p>Games that exit while music is playing; the operating system reclaims the memory, so the practical effect is limited to exit-time ordering and leak reports. C API hosts that call the export are covered.</p>",
   "workaround": "<p>Call <code>MediaPlayer::Stop()</code> (or <code>ProgramExit()</code>) before leaving <code>Run()</code>.</p>",
   "related": {
    "internals": [
     [
      "development/internals/modules/media.html#state",
      "Media module: playback state"
     ]
    ],
    "maintainer": [
     [
      "development/handbook/debug-shutdown-and-lifetime.html#shapes",
      "Debug shutdown and lifetime: failure shapes"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-121 (development/internals/modules/media.html)"
  },
  {
   "cand_ids": [
    "P2-123"
   ],
   "id": "NEW-B4-12",
   "class": "bug",
   "title": "Without a mixer (SDL2 or NULL audio) songs never end: queued copies carry no Duration, so the elapsed-time fallback never fires",
   "summary": "LoadSong copies only a Song's file and name, so every queued Song has a zero Duration and, in builds without SOUND_ENABLED, DetectSongEndedByElapsedTime never reports an end: MediaPlayer stays Playing and the queue never advances.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "MediaPlayer::Update() (via FrameworkDispatcher::Update), MediaState, ActiveSongChanged and queue advance under CNA_AUDIO_PLATFORM=SDL2 or NULL",
   "expected": "<p><code>DetectSongEndedByElapsedTime</code> is documented in {{src:modules/media/include/Microsoft/Xna/Framework/Media/MediaPlayer.hpp|MediaPlayer.hpp}} as the song-end detector for builds without a native track-stopped signal, reporting an end whenever the duration is genuinely known. A Song loaded from <code>.xnb</code> carries its duration (<code>Song(path, name, durationMS)</code>), as do library songs whose duration was probed, so their end should be detected and the queue should advance as in XNA and FNA.</p>",
   "actual": "<p>{{src:modules/CMakeLists.txt|modules/CMakeLists.txt}} defines <code>SOUND_ENABLED</code> only for SDL3 and ALSA audio. In the other builds <code>Update</code> in {{src:modules/media/src/Xna/MediaPlayer.cpp|MediaPlayer.cpp}} asks <code>DetectSongEndedByElapsedTime(activeSong, TimerElapsed())</code>, where <code>activeSong</code> is the queue's copy. <code>LoadSong</code> builds that copy with <code>new Song(song-&gt;getHandle(), song-&gt;getNameProperty())</code>, dropping the duration, and <code>PlaySong</code> sets none without a mixer. The detector ignores a zero duration, so it never fires: the player stays <code>Playing</code> forever, <code>MediaStateChanged</code> and <code>ActiveSongChanged</code> are never raised for a natural end, and repeat, shuffle and queue advance never happen. The fallback is reachable only by calling the static function directly, which is what its tests do.</p>",
   "sources": [
    {
     "path": "modules/media/src/Xna/MediaPlayer.cpp",
     "note": "MediaPlayer::LoadSong, PlaySong (non-SOUND_ENABLED branch), Update, DetectSongEndedByElapsedTime"
    },
    {
     "path": "modules/media/include/Microsoft/Xna/Framework/Media/MediaPlayer.hpp",
     "note": "DetectSongEndedByElapsedTime documentation"
    },
    {
     "path": "modules/CMakeLists.txt",
     "note": "SOUND_ENABLED only for CNA_AUDIO_PLATFORM SDL3 and ALSA"
    },
    {
     "path": "modules/media/tests/Microsoft/Xna/Framework/Media/MediaPlayerTests.cpp",
     "note": "MediaPlayerNoSoundFallbackTest cases call the function directly"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The copy never receives a duration on this path in any configuration, so the conclusion does not depend on timing.</p>",
   "reproduction": "<p>Illustrative; not compiled or run (CNA_AUDIO_PLATFORM=NULL).</p><pre><code class=\"language-cpp\">Song s(\"Content/jingle.ogg\", \"jingle\", 500);   // known 0.5 s duration\nMediaPlayer::Play(&amp;s);\n// ... one second of frames calling FrameworkDispatcher::Update()\nMediaPlayer::getStateProperty();   // still MediaState::Playing</code></pre>",
   "tests_current": "<p>The four <code>MediaPlayerNoSoundFallbackTest</code> cases in {{src:modules/media/tests/Microsoft/Xna/Framework/Media/MediaPlayerTests.cpp|MediaPlayerTests.cpp}} call <code>DetectSongEndedByElapsedTime</code> with a hand-built Song that has a duration; none plays a Song through <code>Play</code> and <code>Update</code> in a no-mixer build.</p>",
   "regression_test": "<p>In a NULL-audio build: <code>Play</code> a Song constructed with a 100 ms duration, advance <code>Update</code> past 100 ms and require <code>Stopped</code> (or the next song). The fix is for <code>LoadSong</code> to copy the duration.</p>",
   "blast_radius": "<p>SDL2 and NULL audio selections only, where music is silent anyway: games whose logic waits for a song to end, playlists, repeat and shuffle. SDL3 and ALSA builds end songs through the mixer's stopped callback.</p>",
   "workaround": "<p>End or advance songs from game code with a timer in these profiles.</p>",
   "related": {
    "deep": [
     [
      "deep-dives/services/media-contract.html#player",
      "Songs, the media library and video: MediaPlayer"
     ]
    ],
    "internals": [
     [
      "development/internals/modules/media.html#sound-enabled",
      "Media module: the SOUND_ENABLED split"
     ],
     [
      "development/internals/audio/engine.html#no-mixer",
      "Audio engine internals: builds without a mixer"
     ]
    ],
    "guide": [
     [
      "docs/platforms.html#audio-table",
      "Platforms: audio selections"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-123 (development/internals/modules/media.html)"
  },
  {
   "cand_ids": [
    "P2-124"
   ],
   "id": "NEW-B4-13",
   "class": "bug",
   "title": "VideoPlayer and Video keep raw pointers to each other, so destroying a Video before its player is stopped writes to freed memory",
   "summary": "VideoPlayer stores a borrowed Video* and Video a borrowed VideoPlayer* parent; CloseDecoder, run by Stop, Dispose, the destructor and the next Play, writes through the stored Video*, and no header states the ordering rule.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "medium",
   "confidence": "verified-by-reading",
   "public_contract": "VideoPlayer::Play(Video*), Stop(), Dispose(), ~VideoPlayer(), getVideoProperty(); Video::SetAudioTrackEXT and SetVideoTrackEXT",
   "expected": "<p>In XNA and FNA, <code>Video</code> and <code>VideoPlayer</code> are garbage-collected references, so neither order of release matters. A C++ port must either own or share the relationship, or state the ordering rule in the headers; {{src:modules/media/include/Microsoft/Xna/Framework/Media/Video/VideoPlayer.hpp|VideoPlayer.hpp}} and {{src:modules/media/include/Microsoft/Xna/Framework/Media/Video/Video.hpp|Video.hpp}} state none.</p>",
   "actual": "<p><code>VideoPlayer::OpenDecoder</code> in {{src:modules/media/src/Xna/Video/VideoPlayer.cpp|VideoPlayer.cpp}} stores <code>video_ = video</code> and sets <code>video-&gt;parent_ = this</code>. <code>CloseDecoder</code> starts with <code>if (video_) video_-&gt;parent_ = nullptr;</code> and runs from <code>Stop</code>, <code>Dispose</code>, the destructor (through <code>Dispose</code>) and the next <code>Play</code>. <code>Video</code> declares no destructor, so a Video destroyed while a player still holds it leaves <code>video_</code> dangling, and the next of those calls writes into freed memory; <code>getVideoProperty()</code> returns the dangling pointer meanwhile. Ordinary member order is enough to trigger it: a class that declares its <code>VideoPlayer</code> before a <code>Video</code> member destroys the Video first.</p><p>The other direction exists too. <code>Video</code> is implicitly copyable, copies keep <code>parent_</code>, and <code>Video::SetAudioTrackEXT</code>/<code>SetVideoTrackEXT</code> call through it. The player clears <code>parent_</code> only on the Video it played, so a copy made during playback calls into a destroyed player later.</p>",
   "sources": [
    {
     "path": "modules/media/src/Xna/Video/VideoPlayer.cpp",
     "note": "VideoPlayer::OpenDecoder, CloseDecoder, Dispose, ~VideoPlayer, Play, Stop"
    },
    {
     "path": "modules/media/src/Xna/Video/Video.cpp",
     "note": "Video::SetAudioTrackEXT / SetVideoTrackEXT call through parent_"
    },
    {
     "path": "modules/media/include/Microsoft/Xna/Framework/Media/Video/Video.hpp",
     "note": "parent_ member; no destructor or copy control"
    },
    {
     "path": "modules/media/include/Microsoft/Xna/Framework/Media/Video/VideoPlayer.hpp",
     "note": "Play(Video*) documentation without a lifetime rule"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed (no sanitizer run). The write in <code>CloseDecoder</code> is unconditional whenever <code>video_</code> is set, so the failure follows from destruction order alone.</p>",
   "reproduction": "<p>Illustrative; not compiled or run (FFmpeg build).</p><pre><code class=\"language-cpp\">struct Cutscene {\n    VideoPlayer player;              // destroyed second\n    std::optional&lt;Video&gt; video;     // destroyed first\n};\n{\n    Cutscene c;\n    c.video.emplace(\"Content/intro.mkv\", &amp;graphicsDevice);\n    c.player.Play(&amp;*c.video);\n}   // ~Video, then ~VideoPlayer -&gt; Dispose -&gt; CloseDecoder writes into the destroyed Video</code></pre>",
   "tests_current": "<p>{{src:modules/media/tests/Microsoft/Xna/Framework/Media/Video/VideoPlayerTests.cpp|VideoPlayerTests.cpp}} covers playback, looping, track selection and disposal with the Video outliving the player; no test destroys or copies a Video while its player holds it.</p>",
   "regression_test": "<p>Under AddressSanitizer: play a Video, destroy it, then <code>Stop()</code> and destroy the player; and copy a playing Video, destroy the player, then call <code>SetAudioTrackEXT</code> on the copy. Both must be clean. A fix can detach in <code>~Video</code>, make the link a weak handle, or define copy semantics that drop <code>parent_</code>.</p>",
   "blast_radius": "<p>Every build with the video backend where a Video can die before its player: member order, containers that reallocate, cut-scene objects released in <code>UnloadContent</code>. Builds without FFmpeg cannot start playback, so the link is never set there.</p>",
   "workaround": "<p>Stop or dispose the player before the Video goes, declare the Video before the player, and do not copy a Video that is playing.</p>",
   "related": {
    "deep": [
     [
      "deep-dives/services/media-contract.html#video",
      "Songs, the media library and video: Video and VideoPlayer"
     ]
    ],
    "internals": [
     [
      "development/internals/modules/media.html#video-lifetime",
      "Media module: video ownership and lifetime"
     ]
    ],
    "guide": [
     [
      "docs/video-playback.html#videoplayer-class",
      "Video playback: VideoPlayer"
     ]
    ],
    "maintainer": [
     [
      "development/handbook/debug-shutdown-and-lifetime.html#borrowed",
      "Debug shutdown and lifetime: borrowed pointers"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-124 (development/internals/modules/media.html)"
  },
  {
   "cand_ids": [
    "P2-125"
   ],
   "id": "NEW-B4-14",
   "class": "bug",
   "title": "VideoPlayer::GetTexture lets the decoder's std::runtime_error escape, undocumented, and leaves the player Playing",
   "summary": "A decode, packet, resample or I/O error mid-stream is thrown by VideoDecoder::NextFrame as std::runtime_error through GetTexture, whose header documents only ObjectDisposedException, and the player stays Playing so the next call tries again.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "VideoPlayer::GetTexture() exceptions and the player state after a mid-stream decode failure",
   "expected": "<p>{{src:modules/media/include/Microsoft/Xna/Framework/Media/Video/VideoPlayer.hpp|VideoPlayer.hpp}} documents <code>GetTexture</code> as throwing <code>ObjectDisposedException</code> only. <code>Play</code> treats a decoder failure transactionally: <code>OpenDecoder</code> catches, closes the decoder, resets the state to Stopped and rethrows. The frame path should either be documented as throwing, with a defined state afterwards, or report the failure through the state.</p>",
   "actual": "<p><code>GetTexture</code> in {{src:modules/media/src/Xna/Video/VideoPlayer.cpp|VideoPlayer.cpp}} loops over <code>decoder_-&gt;NextFrame</code> and <code>DrainAndFlushAudioBuffer</code> with no <code>try</code>. <code>NextFrame</code> and <code>ProcessAudioPacket</code> in {{src:modules/video-ffmpeg/src/VideoDecoder.cpp|VideoDecoder.cpp}} throw <code>std::runtime_error</code> for a video decode error, a rejected or failed packet send, a failed flush, a demux I/O error and audio decode or resample failures (a deliberate change so that corruption is no longer mistaken for end of stream). The exception reaches the caller, usually inside <code>Draw</code>. It is not a <code>System::Exception</code>, so a handler written for that type misses it. The player stays <code>Playing</code> with the decoder open, and every later <code>GetTexture</code> re-enters the decoder.</p>",
   "sources": [
    {
     "path": "modules/media/src/Xna/Video/VideoPlayer.cpp",
     "note": "VideoPlayer::GetTexture (no try) versus OpenDecoder (catch, CloseDecoder, rethrow)"
    },
    {
     "path": "modules/video-ffmpeg/src/VideoDecoder.cpp",
     "note": "VideoDecoder::NextFrame and ProcessAudioPacket throw sites"
    },
    {
     "path": "modules/media/include/Microsoft/Xna/Framework/Media/Video/VideoPlayer.hpp",
     "note": "GetTexture documentation"
    },
    {
     "path": "modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp",
     "note": "CorruptedMidStreamDataThrowsRatherThanSilentlyEndingCleanly"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The decoder-level throw is pinned by <code>VideoDecoderTest.CorruptedMidStreamDataThrowsRatherThanSilentlyEndingCleanly</code>; what the player does with it follows from <code>GetTexture</code>'s code.</p>",
   "reproduction": null,
   "tests_current": "<p>{{src:modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp|VideoDecoderTests.cpp}} pins the throw at decoder level with <code>corrupt_test_h264.mp4</code>. {{src:modules/media/tests/Microsoft/Xna/Framework/Media/Video/VideoPlayerTests.cpp|VideoPlayerTests.cpp}} has no case that plays a corrupt file through <code>GetTexture</code>.</p>",
   "regression_test": "<p>Play <code>corrupt_test_h264.mp4</code> through a <code>VideoPlayer</code> and call <code>GetTexture</code> until the corruption is reached; require the documented outcome (for example an exception of a documented type followed by <code>Stopped</code>) and no second throw from the next call.</p>",
   "blast_radius": "<p>Builds with the FFmpeg backend playing corrupt or truncated-in-the-middle files. Clean files and trailing truncation of Matroska files (reported as end of stream) are unaffected.</p>",
   "workaround": "<p>Catch <code>std::exception</code> around <code>GetTexture</code> and call <code>Stop()</code> on failure.</p>",
   "related": {
    "deep": [
     [
      "deep-dives/services/media-contract.html#video",
      "Songs, the media library and video: Video and VideoPlayer"
     ]
    ],
    "internals": [
     [
      "development/internals/modules/video-ffmpeg.html#failures",
      "FFmpeg video: what throws, what returns false"
     ],
     [
      "development/internals/modules/media.html#video-play",
      "Media module: video playback"
     ]
    ],
    "guide": [
     [
      "docs/video-playback.html#rendering-pattern",
      "Video playback: the rendering pattern"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-125 (development/internals/modules/media.html)"
  },
  {
   "cand_ids": [
    "P2-127"
   ],
   "id": "NEW-B4-15",
   "class": "verification-gap",
   "title": "Media paths without tests: the natural song end on SDL3_mixer, the failed visualization install, MediaLibrary::Dispose and SavePicture's IOException",
   "summary": "The song-ended callback path on the default SDL3 audio, the visualization branch where no tap can be installed, MediaLibrary disposal semantics and SavePicture's write-failure exception are implemented but untested.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "n/a",
   "confidence": "verified-by-reading",
   "public_contract": "MediaPlayer::Update end-of-song handling, MediaPlayer::setIsVisualizationEnabledProperty, MediaLibrary::Dispose and MediaLibrary::SavePicture",
   "expected": "<p>Each behaviour a game relies on has at least one test in the default configuration (SDL3 audio).</p>",
   "actual": "<ul><li><strong>Natural song end on SDL3_mixer.</strong> <code>OnMusicTrackStopped</code> sets the ended flag from the audio thread and <code>Update</code> advances, stops or repeats. The only test that lets a song finish, <code>MediaPlayerTest.Mp3AndFlacSongsPlayThroughCnasOwnMixerToTheirEnd</code>, is compiled only for ALSA audio.</li><li><strong>Visualization without a tap.</strong> The comment on <code>VisualizationEnabledStateStaysConsistentWithGetVisualizationData</code> states that the suite always has a dummy SDL device, so the no-mixer and failed-install branches of <code>setIsVisualizationEnabledProperty</code> are correct by construction only.</li><li><strong>MediaLibrary::Dispose.</strong> It only sets <code>isDisposed_</code>; the collection accessors do not consult it. <code>MediaLibraryTestFixture.DisposeMarksLibraryDisposed</code> checks the flag and nothing else.</li><li><strong>SavePicture failure.</strong> <code>MediaLibrary::SavePicture</code> throws <code>System::IO::IOException</code> when the store returns no path. The store's name-sanitising and empty-root cases are tested at store level (<code>SavedPictureStoreTests.cpp</code>), but no test drives the public method into the exception.</li></ul>",
   "sources": [
    {
     "path": "modules/media/src/Xna/MediaPlayer.cpp",
     "note": "OnMusicTrackStopped, Update, setIsVisualizationEnabledProperty"
    },
    {
     "path": "modules/media/tests/Microsoft/Xna/Framework/Media/MediaPlayerTests.cpp",
     "note": "Mp3AndFlacSongsPlayThroughCnasOwnMixerToTheirEnd (ALSA only); the visualization comments"
    },
    {
     "path": "modules/media/src/Xna/MediaLibrary.cpp",
     "note": "MediaLibrary::Dispose; MediaLibrary::SavePicture"
    },
    {
     "path": "modules/media/tests/Microsoft/Xna/Framework/Media/MediaLibraryTests.cpp",
     "note": "DisposeMarksLibraryDisposed; the SavePicture cases"
    },
    {
     "path": "modules/media/tests/CNA/Internal/Media/SavedPictureStoreTests.cpp",
     "note": "store-level sanitising and empty-root cases"
    }
   ],
   "evidence": "<p>Checked by reading the test sources at 009d40f5; no test was run. Nothing in these paths is known to be wrong; the gap is evidence. <code>MediaLibrary::Dispose</code> not guarding its accessors is recorded as observed, not judged against XNA here.</p>",
   "reproduction": null,
   "tests_current": "<p>As listed above: the ALSA-only song-end case, the visualization round-trip cases with a working device, the dispose-flag case and the successful and null-stream SavePicture cases.</p>",
   "regression_test": "<p>A short-fixture song played with <code>SDL_AUDIODRIVER=dummy</code> until <code>Update</code> reports Stopped (and the next song with repeat on); a visualization test with a forced mixer-creation failure (the mixer's test override hooks allow it); a MediaLibrary test that pins what accessors do after Dispose; a SavePicture test against an unwritable pictures root expecting <code>IOException</code>.</p>",
   "blast_radius": "<p>Games that rely on songs ending (playlists, repeat) on the default SDL3 audio, on visualization data on headless or audio-less machines, on MediaLibrary disposal, or on SavePicture error handling.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/modules/media.html#testing",
      "Media module: human change and validation map"
     ],
     [
      "development/internals/modules/media.html#visualization",
      "Media module: visualization"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#recipes",
      "Test architecture: change recipes"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-127 (development/internals/modules/media.html)"
  },
  {
   "cand_ids": [
    "P2-151"
   ],
   "id": "NEW-B4-16",
   "class": "bug",
   "title": "VideoDecoder::SetVideoStream switches to another video stream mid-playback without seeking, so decoding resumes at a non-keyframe position",
   "summary": "A genuine SetVideoTrackEXT switch during playback opens a fresh codec context at the demuxer's current position without a seek or flush; CNA's own comment explains that such a context fails on the next non-keyframe.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "low",
   "confidence": "strong",
   "public_contract": "VideoPlayer::SetVideoTrackEXT(intcs) and Video::SetVideoTrackEXT(intcs) during playback",
   "expected": "<p>After a switch to another video stream, <code>GetTexture</code> continues with frames of the new stream at the current play position (decoding from the preceding keyframe), or the method is documented as effective only before <code>Play</code>.</p>",
   "actual": "<p><code>VideoPlayer::SetVideoTrackEXT</code> ({{src:modules/media/src/Xna/Video/VideoPlayer.cpp|VideoPlayer.cpp}}) calls <code>VideoDecoder::SetVideoStream</code> during playback and, on a real switch, only recreates the frame texture. <code>OpenVideoStreamByIndex</code> in {{src:modules/video-ffmpeg/src/VideoDecoder.cpp|VideoDecoder.cpp}} frees the old codec context and opens a new one with no demuxer seek and no flush, so the new stream's decoding starts from whatever packet comes next. The comment in <code>SetVideoStream</code> describes the consequence for the same-stream case it now avoids: a context recreated mid-stream &#8220;has no reference frame to decode the very next (non-keyframe) packet against&#8221; and the send fails. <code>havePendingVideoPacket_</code> is not cleared either, so a packet of the old stream retained after an <code>EAGAIN</code> would be sent to the new decoder. The resulting exception surfaces through <code>GetTexture</code> (see the entry on its exceptions). A preference set before <code>Play</code> is applied right after <code>Open</code>, at the start of the file, and is not affected.</p>",
   "sources": [
    {
     "path": "modules/video-ffmpeg/src/VideoDecoder.cpp",
     "note": "VideoDecoder::SetVideoStream, OpenVideoStreamByIndex, NextFrame (havePendingVideoPacket_)"
    },
    {
     "path": "modules/media/src/Xna/Video/VideoPlayer.cpp",
     "note": "VideoPlayer::SetVideoTrackEXT, OpenDecoder"
    },
    {
     "path": "modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp",
     "note": "SetVideoStreamReselectingTheSameStreamPreservesDimensions"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. That the new context starts mid-stream is mechanical; whether the next packet is rejected or decoded into artefacts depends on the codec and on the keyframe spacing, which is why the confidence is <em>strong</em> rather than verified. No fixture has two video streams.</p>",
   "reproduction": null,
   "tests_current": "<p>{{src:modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp|VideoDecoderTests.cpp}} tests only re-selecting the single video stream of <code>multi_track_audio.mkv</code>, and its comment records that no second-video-stream fixture exists. Audio stream switching is tested.</p>",
   "regression_test": "<p>A fixture with two video streams of different sizes and a keyframe interval shorter than the file: play, advance past the first keyframe, switch to stream 1, and require frames of the new size from <code>GetTexture</code> without an exception.</p>",
   "blast_radius": "<p>Games that switch video tracks while a multi-video-stream file is playing, with the FFmpeg backend. Track selection before playback and audio track switching are unaffected.</p>",
   "workaround": "<p>Select the video track before <code>Play</code>, or <code>Stop</code>, select, and <code>Play</code> again.</p>",
   "related": {
    "deep": [
     [
      "deep-dives/services/media-contract.html#video",
      "Songs, the media library and video: Video and VideoPlayer"
     ]
    ],
    "internals": [
     [
      "development/internals/modules/video-ffmpeg.html#seek-tracks",
      "FFmpeg video: SetAudioStream and SetVideoStream"
     ]
    ],
    "guide": [
     [
      "docs/video-playback.html#videoplayer-class",
      "Video playback: VideoPlayer"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-151 (development/internals/modules/video-ffmpeg.html)"
  },
  {
   "cand_ids": [
    "P2-152"
   ],
   "id": "NEW-B4-17",
   "class": "bug",
   "title": "VideoDecoder converts every YUV frame with one full-range BT.601 matrix: limited-range and BT.709 video decode with wrong levels and colours",
   "summary": "The planar and NV12 converters ignore the frame's colour range and matrix, so ordinary limited-range video keeps black at 16 and white at 235, and BT.709 content is decoded with BT.601 coefficients.",
   "subsystem": "Audio & media",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "VideoPlayer::GetTexture() frame contents (CNA::Internal::Media::VideoDecoder::NextFrame RGBA output)",
   "expected": "<p>YUV-to-RGB conversion honours the frame's declared range and matrix: limited (&#8220;tv&#8221;) range expands luma 16&#8211;235 and chroma 16&#8211;240 to full scale, and BT.709 content, the norm for HD video, uses BT.709 coefficients. FFmpeg reports both on every frame (<code>AVFrame::color_range</code>, <code>AVFrame::colorspace</code>).</p>",
   "actual": "<p><code>yuv_planar8_to_rgba</code> and <code>yuv_planar16_to_rgba</code> in {{src:modules/video-ffmpeg/src/VideoDecoder.cpp|VideoDecoder.cpp}} compute <code>R = Y + 359&#183;V/256</code>, <code>G = Y &#8722; (88&#183;U + 183&#183;V)/256</code>, <code>B = Y + 454&#183;U/256</code> with U and V centred on 128 and Y used as is: the full-range BT.601 matrix (the code comment says so). <code>ConvertFrameToRGBA</code> routes YUV420P/422P/444P and their YUVJ variants, and the 10- and 12-bit formats after a shift to 8 bits, to these functions; the NV12 branch of <code>generic_to_rgba</code> uses the same coefficients. Neither range nor matrix is read. For a limited-range frame, black (Y=16) decodes to RGB 16,16,16 and white (Y=235) to 235,235,235, so contrast drops by about 14% and blacks are grey; BT.709 content additionally gets BT.601 hue and saturation. Only full-range BT.601 video (the YUVJ formats) comes out right.</p>",
   "sources": [
    {
     "path": "modules/video-ffmpeg/src/VideoDecoder.cpp",
     "note": "yuv_planar8_to_rgba, yuv_planar16_to_rgba, generic_to_rgba (NV12), ConvertFrameToRGBA"
    },
    {
     "path": "modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp",
     "note": "Decodes420/422/444ChromaToCorrectColors, Decodes10/12BitToCorrectColors: +/-20 tolerance"
    },
    {
     "path": "tests/assets/media/video/manifest.json",
     "note": "reference colours sampled at authoring time; recommends +/-10 on 8-bit"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; the black and white values above are the formula evaluated by hand at U=V=128, not a run. The fixtures are ffmpeg <code>smptebars</code> patterns whose references (about 190 for a 75% bar) were sampled after a converting decode; with the missing expansion a limited-range 75% white would come out near 180, inside the tests' &#177;20 window but outside the &#177;10 the manifest recommends. Whether the fixtures are limited-range was not checked.</p>",
   "reproduction": null,
   "tests_current": "<p>{{src:modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp|VideoDecoderTests.cpp}} samples one frame of colour bars per pixel format with a &#177;20 tolerance; there is no full-black or full-white patch, no explicitly tagged range, and no BT.709 fixture.</p>",
   "regression_test": "<p>Fixtures encoded with explicit <code>-color_range tv</code>/<code>pc</code> and <code>-colorspace bt601</code>/<code>bt709</code>, each with 0% and 100% patches and primaries; assert within &#177;3 of the expected RGB.</p>",
   "blast_radius": "<p>Every FFmpeg-backed video whose frames are limited range (most H.264, VP9 and AV1 files) or BT.709. The texture pipeline after decoding is unaffected.</p>",
   "workaround": "<p>Encode game videos as full-range BT.601 (for example <code>yuvj420p</code>), which the converter handles correctly.</p>",
   "related": {
    "internals": [
     [
      "development/internals/modules/video-ffmpeg.html#decoder-convert",
      "FFmpeg video: conversion to RGBA"
     ]
    ],
    "guide": [
     [
      "docs/video-playback.html#ffmpeg-setup",
      "Video playback: FFmpeg setup"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-152 (development/internals/modules/video-ffmpeg.html)"
  },
  {
   "cand_ids": [
    "P2-153"
   ],
   "id": "NEW-B4-18",
   "class": "bug",
   "title": "CNA_ENABLE_VIDEO=AUTO accepts any pkg-config FFmpeg, but VideoDecoder.cpp needs the FFmpeg 5.1 channel-layout API",
   "summary": "The FFmpeg probe states no minimum version while VideoDecoder.cpp uses AVCodecContext::ch_layout and swr_alloc_set_opts2, so a host with older FFmpeg development packages enables video at configure time and then fails to compile.",
   "subsystem": "Build & CI",
   "status": "open",
   "severity": "low",
   "confidence": "strong",
   "public_contract": "CMake option CNA_ENABLE_VIDEO (AUTO, the default, and ON) and the cna_video_ffmpeg target",
   "expected": "<p><code>modules/CMakeLists.txt</code> documents AUTO as &#8220;use FFmpeg only when all required pkg-config modules are present&#8221; and ON as reporting an unsupported configuration immediately. Both rely on the probe telling a usable FFmpeg from an unusable one, so an FFmpeg too old for the sources should make AUTO fall back and ON fail at configure time with a clear message.</p>",
   "actual": "<p>{{src:modules/CMakeLists.txt|modules/CMakeLists.txt}} calls <code>pkg_check_modules</code> for the four libraries without a version constraint. {{src:modules/video-ffmpeg/src/VideoDecoder.cpp|VideoDecoder.cpp}} reads <code>AVCodecContext::ch_layout</code> and builds its resampler with <code>swr_alloc_set_opts2</code>, both part of the channel-layout API introduced with FFmpeg 5.1, with no <code>LIBAVCODEC_VERSION_INT</code> fallback. On a host whose pkg-config finds FFmpeg 4.x development packages (still the packaged version on older LTS distributions), AUTO sets <code>CNA_FFMPEG_AVAILABLE</code> and the build then fails compiling <code>cna_video_ffmpeg</code> instead of falling back to the no-video backend. No CNA document names a minimum FFmpeg version, and every CI lane that installs FFmpeg uses a recent one (ubuntu-24.04 runners, Homebrew on macOS).</p>",
   "sources": [
    {
     "path": "modules/CMakeLists.txt",
     "note": "the CNA_ENABLE_VIDEO probe: pkg_check_modules without versions"
    },
    {
     "path": "modules/video-ffmpeg/src/VideoDecoder.cpp",
     "note": "VideoDecoder::Open and OpenAudioStreamByIndex (ch_layout); the resampler setup (swr_alloc_set_opts2)"
    },
    {
     "path": "modules/video-ffmpeg/CMakeLists.txt",
     "note": "cna_video_ffmpeg links the PkgConfig imported targets"
    },
    {
     "path": ".github/workflows/general-tests-ci.yml",
     "note": "ubuntu-24.04 runner with the distribution's FFmpeg packages"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not tried against an old FFmpeg. The API ages are FFmpeg's (the <code>AVChannelLayout</code> fields and <code>swr_alloc_set_opts2</code> arrived in 5.1); a compile failure with FFmpeg 4.x follows from the missing declarations but was not observed.</p>",
   "reproduction": "<p>Not run. On a host with FFmpeg 4.x development packages: <code>cmake -S . -B build</code> reports &#8220;video enabled (AUTO; FFmpeg backend cna_video_ffmpeg)&#8221;, and <code>cmake --build build</code> fails in <code>VideoDecoder.cpp</code> on <code>ch_layout</code> and <code>swr_alloc_set_opts2</code>.</p>",
   "tests_current": "<p>No configure test covers the FFmpeg probe's version handling; the video tests run only where a recent FFmpeg is installed.</p>",
   "regression_test": "<p>Add version floors to the probe (for example <code>libswresample&gt;=4.5</code> and the matching libavcodec release) or a <code>check_symbol_exists(swr_alloc_set_opts2 ...)</code>, and a script-mode CMake test that feeds a fake old <code>.pc</code> file and expects AUTO to fall back and ON to fail.</p>",
   "blast_radius": "<p>Default (AUTO) builds on hosts with FFmpeg older than 5.1 installed; they fail instead of building without video. Hosts without FFmpeg, and Windows, Emscripten, Android and iOS (where the probe is skipped), are unaffected.</p>",
   "workaround": "<p>Configure with <code>-DCNA_ENABLE_VIDEO=OFF</code>, or install FFmpeg 5.1 or newer.</p>",
   "related": {
    "internals": [
     [
      "development/internals/modules/video-ffmpeg.html#selection",
      "FFmpeg video: configure-time selection"
     ]
    ],
    "guide": [
     [
      "docs/video-playback.html#ffmpeg-setup",
      "Video playback: FFmpeg setup"
     ],
     [
      "docs/building.html#cmake-options",
      "Building: CMake options"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-153 (development/internals/modules/video-ffmpeg.html)"
  }
 ]
}
