{
 "qa": "Q3",
 "verdicts": [
  {
   "id": "CNA-BUG-035",
   "verdict": "CONFIRMED",
   "evidence": "Re-read .github/workflows/devices-tests.yml at 009d40f5 and matched both filter strings mechanically against every TEST/TEST_F/TEST_P definition. modules/devices/tests/Microsoft/Devices: 474 definitions in 28 suites; the 21-suite DEVICES_GTEST_FILTER selects 445; the seven unmatched suites and counts are exactly as published (DevicesShutdownCoordinatorTest 4, DevicesShutdownOrderingTest 3 counting the #else GTEST_SKIP stub, EnvironmentTests 3, IndependentReferenceCrossCheckTests 2, NativeDiagnosticSinkTest 9, PlatformSensorMigrationTests 4, PlatformVibrateMigrationTests 4). modules/devices-ext/tests/CNA/Devices: 56 definitions; FileDialogTests.* and MessageBoxTests.* match no suite (FileDialogTest, FileDialogWithoutAServiceTest, MessageBoxTest, MessageBoxWithoutAServiceTest: 12 cases, inside #ifdef CNA_DEVICES; renamed in 234b6d3e3, PLAT-90a). CNA_DEVICES is option(... OFF) in CMakeLists.txt and becomes a compile definition only through cna_build_config (modules/CMakeLists.txt); only the devices-asan/-tsan/-ubsan presets set it ON and devices-tests.yml (devices-ubsan) is the only workflow using any of them. The header still names SetBackendForTesting(). No claim falsified; severity medium stands.",
   "corrections": {},
   "note": "Two refinements, not corrections: (1) the same workflow pins sharp-runtime bc8dbf41 and therefore stops at configure at TARGET (CNA-BUG-199), so these filter gaps are latent until that pin moves; cross-linking the two entries would help a reader. (2) cmake/UnitTests.cmake already builds directory-derived focused executables CnaDevicesTests and CnaDevicesExtTests (EXCLUDE_FROM_ALL, not CTests); running them from the workflow is the smallest fix and could be named in regression_test."
  },
  {
   "id": "CNA-BUG-062",
   "verdict": "CONFIRMED",
   "evidence": "Re-read MODULE_SCOPE, publishing_modules, validate_module_scope, build_inventory and main in tools/c-api/generate_coverage_inventory.py; git ls-tree at TARGET confirms modules/design/include, modules/diagnostics/include and modules/inspector/include exist (added by c9c0774a9, b7e856221, dfc7b0aea). Re-executed read-only against the extracted TARGET tree (python3 -B, no bytecode written, TMPDIR in the session scratchpad): validate_module_scope raises naming exactly those three modules; generate_coverage_inventory.py --check exits 2; generate_limitations.py --check exits 2; test_coverage_scope.py runs 29 tests with 3 errors (test_the_repository_classifies_every_publishing_module, test_discovery_admits_no_out_of_scope_header, test_the_repository_admits_no_lowercase_detail_header); check_release_gate.check_coverage_has_no_planned_rows and check_limitations_current both return 'not met' while tools/c-api/release_gate.json records limitations-matrix as met. The four CTests are in cmake/Tests/ModuleProbes.cmake under CNA_BUILD_TESTS (not Emscripten/Android) and Python3_Interpreter_FOUND, outside CNA_BUILD_C_API; general-tests-ci.yml's KNOWN_FAILURES names only EasyGL_GraphicsDevice_ReferenceStencil. COVERAGE.md snapshot numbers match the entry. Nothing falsified.",
   "corrections": {},
   "note": ""
  },
  {
   "id": "CNA-BUG-199",
   "verdict": "CORRECTED",
   "evidence": "Re-read cmake/SharpRuntimeConsumption.cmake (Resources in CNA_SHARP_RUNTIME_DEFAULT_COMPONENTS; 8c1c8b412, 2026-09-20, an ancestor of TARGET) and the root CMakeLists.txt, which FORCE-merges every default component back into SHARP_RUNTIME_COMPONENTS before add_subdirectory(${CNA_SHARP_RUNTIME_ROOT}). Confirmed the six pins (htmldom-ci, devices-tests, emscripten-multi-renderer-ci: bc8dbf41; apple-ci both jobs and metal-macos-ci: f23ded28; content-pipeline-windows-ci: df1b42ab) and, in the local sharp-runtime object store, that none of the three revisions has modules/resources or registers Resources or Xml.Serialization, while cmake/SharpRuntimeComponents.cmake there raises FATAL_ERROR 'Unknown Sharp Runtime component' for an unregistered name. The defect stands. Two statements are inaccurate: 'The remaining workflows clone a branch head through clone_siblings.sh' is false for d3d-windows-ci.yml and gdi-windows-ci.yml, which check out sharp-runtime's default branch with actions/checkout and no ref; and the blast radius does not say that content-pipeline-windows-ci.yml runs only on pushes to content-pipeline-final or by manual dispatch. 'No workflow overrides SHARP_RUNTIME_COMPONENTS' is true but moot: the root merges the defaults back in.",
   "corrections": {
    "actual": "<p>{{src:cmake/SharpRuntimeConsumption.cmake|SharpRuntimeConsumption.cmake}} lists <code>Resources</code> in <code>CNA_SHARP_RUNTIME_DEFAULT_COMPONENTS</code> (added on 2026-09-20 for ResourceContentManager) and, off Windows, <code>Xml.Serialization</code>. The root {{src:CMakeLists.txt|CMakeLists.txt}} passes that list as <code>SHARP_RUNTIME_COMPONENTS</code> before <code>add_subdirectory</code> of the sharp-runtime checkout, and merges every default component back into a cached or user-supplied <code>SHARP_RUNTIME_COMPONENTS</code> (<code>FORCE</code>), so no override can drop <code>Resources</code>. {{src:.github/workflows/htmldom-ci.yml|htmldom-ci.yml}}, {{src:.github/workflows/devices-tests.yml|devices-tests.yml}} and {{src:.github/workflows/emscripten-multi-renderer-ci.yml|emscripten-multi-renderer-ci.yml}} check out sharp-runtime bc8dbf41 (2026-08-15); {{src:.github/workflows/apple-ci.yml|apple-ci.yml}} (both jobs) and {{src:.github/workflows/metal-macos-ci.yml|metal-macos-ci.yml}} f23ded28 (2026-08-15); {{src:.github/workflows/content-pipeline-windows-ci.yml|content-pipeline-windows-ci.yml}} df1b42ab (2026-08-28). None of the three revisions has <code>modules/resources</code> or <code>modules/xml-serialization</code>, and their <code>cmake/SharpRuntimeComponents.cmake</code> stops with <code>FATAL_ERROR</code> &#8220;Unknown Sharp Runtime component&#8221; for an unregistered name. Most other workflows clone a branch head through {{src:scripts/ci/clone_siblings.sh|clone_siblings.sh}} (the pushed or pull-request branch when sharp-runtime has it, otherwise <code>next</code>); {{src:.github/workflows/d3d-windows-ci.yml|d3d-windows-ci.yml}} and {{src:.github/workflows/gdi-windows-ci.yml|gdi-windows-ci.yml}} (manual dispatch only) check out sharp-runtime's default branch with <code>actions/checkout</code> and no <code>ref</code>. Apart from the six pins, the snapshot records no sharp-runtime revision.</p>",
    "blast_radius": "<p>At this snapshot the HTML_DOM browser suite, the Microsoft::Devices suites, the Emscripten multi-renderer bundle, the macOS and iOS lanes and the Metal lane cannot configure; neither can the Windows content-pipeline lane, which runs only on pushes to <code>content-pipeline-final</code> or by manual dispatch. Lanes that clone a sharp-runtime branch carrying the Resources component (normally <code>next</code>, through <code>clone_siblings.sh</code>) are unaffected; the two manual-dispatch Windows lanes that check out sharp-runtime's default branch depend on that branch's contents, which the snapshot does not record.</p>"
   },
   "note": "Only the last sentence of actual and the blast radius change; title, summary, severity and the six-workflow finding are correct."
  },
  {
   "id": "CNA-BUG-200",
   "verdict": "CONFIRMED",
   "evidence": "Re-executed python3 -B scripts/check_no_posix_setenv.py against the read-only TARGET tree (text scan): exactly the twelve lines (WaylandPlatform.cpp unsetenv XDG_ACTIVATION_TOKEN; WaylandDesktopPortalTests.cpp x2, WaylandProtocolTests.cpp x5 incl. one line holding setenv and unsetenv, WaylandTestCompositor.cpp x1, WaylandTestEnvironment.cpp x3), exit 1. Sceptical check of reachability: the add_test in modules/graphics-ext/examples/CMakeLists.txt is inside if(CNA_BUILD_TESTS) and that block runs its own find_package(Python3 ... QUIET) before if(Python3_Interpreter_FOUND), so the test does not depend on a root-scope Python lookup; modules/CMakeLists.txt adds graphics-ext unconditionally and graphics-ext adds examples unconditionally, so the gate is registered in every test-enabled tree with Python. The Wayland lines arrived in 678c6a51b/227157d2c (2026-09-15, after the gate); WaylandPlatform.cpp is compiled only for the Wayland selection and cmake/UnitTests.cmake filters the Wayland tests otherwise, so the MinGW hazard is indeed absent. general-tests-ci.yml KNOWN_FAILURES names only EasyGL_GraphicsDevice_ReferenceStencil; docs/opengl4-renderer.md records the failure. Nothing falsified.",
   "corrections": {},
   "note": ""
  },
  {
   "id": "CNA-BUG-202",
   "verdict": "CONFIRMED",
   "evidence": "Re-read modules/renderers/CMakeLists.txt (the family loop re-points CNA_GRAPHICS_RENDERER and CNA_RENDERER_TARGET_DEFINE per family; cna_renderer_common_setup makes the family macro PRIVATE to the renderer target, so an example executable does not see it), the example guards (sdl-gpu, software, stub: CNA_BUILD_TESTS AND CNA_GRAPHICS_RENDERER STREQUAL identity; vulkan additionally CNA_BUILD_EXAMPLES and not Emscripten/Win32; headless and easygl additionally require _cna_default_renderer_identity), cna_register_renderer_test in cmake/TestHelpers.cmake (adds no CNA_GRAPHICS_RENDERER environment; its only failure regexes are the Vulkan-validation and OpenGL4 GL-error gates), docs/runtime-renderer-selection.md precedence (SetPreferred, then the environment variable, then the compile-time default), CMakePresets.json multi-renderer (HEADLESS;SOFTWARE;STUB, default HEADLESS; CNA_BUILD_TESTS defaults ON in CMakeLists.txt) and stub_smoke_test.cpp (static_cast<StubRenderer&>(dev.GetRenderer()) in every Draw; the reference is never used afterwards, so under HEADLESS the cast is undefined behaviour and no check is STUB-specific). multi-renderer-ci.yml runs only CnaRendererDefaultSelection_* and filtered CnaTests. GL4-0035 skip confirmed in opengl4_smoke_test.cpp. Not executed. Nothing falsified.",
   "corrections": {},
   "note": ""
  },
  {
   "id": "CNA-BUG-206",
   "verdict": "CONFIRMED",
   "evidence": "Re-read scripts/run-oracle-corpus-diff-software.sh: it exits 1 only when RENDER_FAILED (missing reference or failed render) is non-zero; any non-zero xna-diff.py exit, including a crash, increments DIFFERENT and the script exits 0. Its own header calls it 'a measurement, not an exact-image gate', which contradicts the SOFTWARE-344 comment above the Software_XnaLineCoverage registration in modules/renderers/software/examples/CMakeLists.txt that promotes the two line scenes to an exact regression. cna_register_renderer_test adds only the Vulkan-validation/OpenGL4 GL-error FAIL_REGULAR_EXPRESSION gates, neither of which matches DIFF. scripts/run-oracle-corpus-diff-easygl.sh exits 1 on any FAIL. No workflow references the test or the script (gltf-renderer-stride-ci runs only -L gltf-conformance; platform-ci only named platform tests). Not executed. Nothing falsified.",
   "corrections": {},
   "note": "Severity medium is at the upper edge (the guard runs only locally), but a regression test that cannot fail on the regression it is named for justifies it."
  }
 ]
}
