{
 "pkg": "B5",
 "dispositions": [
  {
   "cand": "CNA-BUG-006",
   "source": "bible-cnabugs",
   "classification": "STILL EXISTS",
   "published_as": "CNA-BUG-006",
   "evidence": "modules/graphics/src/Xna/SpriteEffect.cpp (SpriteEffect::CacheEffectParameters looks up \"MatrixTransform\"; SpriteEffect::OnApply returns when matrixParam_ is null); modules/graphics/src/Xna/Effect.cpp (Effect::Effect(GraphicsDevice&) adds only the Default technique); EffectParameterCollection::operator[] returns nullptr for an unknown name; SpriteEffectTests.cpp tests Clone and Apply-does-not-throw only",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "No renderer or SpriteBatch uses the SpriteEffect class; latent for ordinary sprite drawing."
  },
  {
   "cand": "CNA-BUG-011",
   "source": "bible-cnabugs",
   "classification": "PROVEN FIXED",
   "published_as": null,
   "evidence": "WebGPU half: modules/renderers/webgpu/src/WebGPURenderer.cpp WebGPURenderer::CreateEffectRenderer returns a WebGPUEffectRenderer that compiles the WGSL source pair; WebGPUSpriteBatchRenderer::SetCustomEffect routes a custom-WGSL ShaderEffect through it (WEBGPU-142) and throws NotSupportedException only for effects it cannot run; SupportsCapability(CustomEffects) falls through to the base true, now backed. Pinned by WebGPU_SpriteBatch_ShaderEffect (webgpu_spritebatch_shadereffect_test.cpp: red and blue uTint prove the custom WGSL runs on the sprite path), WebGPU_ShaderEffect3D and GraphicsDeviceCapabilityTest.SupportsCustomEffects. The other renderer named in the claim is a retired renderer family: the identity is listed in CNA_RENDERER_RETIRED_IDENTITIES of cmake/RendererIdentities.cmake, not in CNA_RENDERER_PUBLIC_IDENTITIES (25 public identities), cna_require_public_renderer_identity() refuses it by name at configure time, and modules/renderers has no directory for its family at TARGET. Every current renderer without a source-shader route answers CustomEffects false explicitly (SOFTWARE, FNA3D, PORTABLEGL, METAL, STUB, the 2D renderers, SDL_GPU without CNA_SDL_GPU_SHADER_EFFECTS).",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Duplicate subject of CNA-BUG-053."
  },
  {
   "cand": "CNA-BUG-012",
   "source": "bible-cnabugs",
   "classification": "PARTIALLY FIXED",
   "published_as": "CNA-BUG-012",
   "evidence": "Fixed: WebGPURenderer::CreateOcclusionQuery returns a real WebGPUOcclusionQueryRenderer (WEBGPU-84; WebGpuMrtOcclusionContract.OcclusionQueryCapabilityIsTrue, WebGPU_OcclusionQuery); SdlGpuRenderer::SupportsCapability answers OcclusionQuery false and SdlGpuRenderer::CreateOcclusionQuery throws NotSupportedException (SDLGPU-80); OcclusionQuery::OcclusionQuery refuses on a false capability (SOFTWARE-199). Survives: GraphicsDeviceCapabilityTests.cpp ExpectedCapabilities() default arm still expects occlusion (and custom effects) true for SDL_GPU, METAL and STUB; sdlgpu_occlusionquery_limitation_test.cpp expects the renderer's exact message, which the public constructor's earlier generic refusal makes unreachable; CNA's own record (plans/plan_street_sdlgpu.md STREETS-0008, plans/plan_pre_sdlgpu_closeout.md PSG-0009) lists SdlGpu_OcclusionQuery_Limitation among the failing classic SDL_GPU CTests.",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "The SDL_GPU limitation-test mismatch is a new finding folded into this entry (same subject: tests encoding the wrong SDL_GPU occlusion expectation)."
  },
  {
   "cand": "CNA-BUG-013",
   "source": "bible-cnabugs",
   "classification": "OBSOLETE",
   "published_as": null,
   "evidence": "Concerns three retired renderer families: for each, the identity is listed in CNA_RENDERER_RETIRED_IDENTITIES of cmake/RendererIdentities.cmake, not in CNA_RENDERER_PUBLIC_IDENTITIES (25 public identities), cna_require_public_renderer_identity() refuses it by name at configure time, and modules/renderers has no directory for its family at TARGET. The generic lesson is pinned for current renderers: IGraphicsRenderer::SupportsCapability delegates StencilBuffer to SupportsStencilBuffer(), and GraphicsDeviceCapabilityTest.SupportsStencilBuffer asserts both answers agree for the active renderer.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "CNA-BUG-014",
   "source": "bible-cnabugs",
   "classification": "OBSOLETE",
   "published_as": null,
   "evidence": "Concerns a retired renderer family: the identity is listed in CNA_RENDERER_RETIRED_IDENTITIES of cmake/RendererIdentities.cmake, not in CNA_RENDERER_PUBLIC_IDENTITIES (25 public identities), cna_require_public_renderer_identity() refuses it by name at configure time, and modules/renderers has no directory for its family at TARGET. The ledger itself recorded the item as fixed in alpha.1.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "CNA-BUG-015",
   "source": "bible-cnabugs",
   "classification": "OBSOLETE",
   "published_as": null,
   "evidence": "Concerns a retired renderer family: the identity is listed in CNA_RENDERER_RETIRED_IDENTITIES of cmake/RendererIdentities.cmake, not in CNA_RENDERER_PUBLIC_IDENTITIES (25 public identities), cna_require_public_renderer_identity() refuses it by name at configure time, and modules/renderers has no directory for its family at TARGET. At TARGET the Texture3D capability is answered truthfully by the current renderers that lack storage (HeadlessRenderer::SupportsCapability returns false for Texture3D).",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "CNA-BUG-037",
   "source": "bible-cnabugs",
   "classification": "OBSOLETE",
   "published_as": null,
   "evidence": "Concerns a retired renderer family and its runtime shader-compiler loading: the identity is listed in CNA_RENDERER_RETIRED_IDENTITIES of cmake/RendererIdentities.cmake, not in CNA_RENDERER_PUBLIC_IDENTITIES (25 public identities), cna_require_public_renderer_identity() refuses it by name at configure time, and modules/renderers has no directory for its family at TARGET.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "CNA-BUG-038",
   "source": "bible-cnabugs",
   "classification": "OBSOLETE",
   "published_as": null,
   "evidence": "Concerns a retired renderer family's shader generator and backends: the identity is listed in CNA_RENDERER_RETIRED_IDENTITIES of cmake/RendererIdentities.cmake, not in CNA_RENDERER_PUBLIC_IDENTITIES (25 public identities), cna_require_public_renderer_identity() refuses it by name at configure time, and modules/renderers has no directory for its family at TARGET.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "CNA-BUG-053",
   "source": "bible-cnabugs",
   "classification": "PROVEN FIXED",
   "published_as": null,
   "evidence": "Same subject as CNA-BUG-011. WebGPU: WebGPURenderer::CreateEffectRenderer exists and compiles WGSL; WebGPUSpriteBatchRenderer::SetCustomEffect runs a custom-WGSL ShaderEffect (WEBGPU-142); pinned by WebGPU_SpriteBatch_ShaderEffect, WebGPU_ShaderEffect3D and GraphicsDeviceCapabilityTest.SupportsCustomEffects. The second renderer is a retired renderer family: the identity is listed in CNA_RENDERER_RETIRED_IDENTITIES of cmake/RendererIdentities.cmake, not in CNA_RENDERER_PUBLIC_IDENTITIES (25 public identities), cna_require_public_renderer_identity() refuses it by name at configure time, and modules/renderers has no directory for its family at TARGET. The base default of IGraphicsRenderer::SupportsCapability is now false for MultiStreamVertexInput, CompiledEffects and the float render-target entries, and GraphicsDevice derives ComputeShaders/IndirectDraw/HalfFloatTextureLinearFiltering from default-false virtuals.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Duplicate of CNA-BUG-011."
  },
  {
   "cand": "CNA-BUG-054",
   "source": "bible-cnabugs",
   "classification": "PARTIALLY FIXED",
   "published_as": "CNA-BUG-054",
   "evidence": "Fixed: HeadlessRenderer::SetRenderTargets binds a whole set (boundRenderTargets_); SoftwareRenderer::SupportsCapability answers MRT and occlusion explicitly with real CPU implementations (SOFTWARE-120 Software_MrtStockEffectContract, SOFTWARE-122 Software_OcclusionQuery_ExactContract); WebGPU binds 2 to 4 targets (WEBGPU-85/86/87, WebGPU_MRT, WebGpuMrtOcclusionContract.TwoTargetBindSucceedsAndBothTargetsReceiveOwnContent); the shared GraphicsDeviceCapabilityTest.TheMultipleRenderTargetCapabilityMatchesWhatBindingActuallyDoes now binds two targets. Survives: HEADLESS still reports OcclusionQuery true (default arm of HeadlessRenderer::SupportsCapability) while HeadlessOcclusionQueryRenderer::PixelCount is the constant 1 and PixelCountIsPreciseEXT keeps the default true; OcclusionQueryPixelCountPrecisionTests.cpp skips HEADLESS.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "The precise-count claim on HEADLESS is part of the surviving defect."
  },
  {
   "cand": "P2-032",
   "source": "phase2",
   "classification": "NOT A BUG",
   "published_as": null,
   "evidence": "cmake/RendererCombinations.cmake deliberately permits OPENGL4 beside an EasyGL identity (only PORTABLEGL is refused beside real GL). The premise that the pair is unevidenced is contradicted by CNA's own record: plans/plan_opengl4_modern_graphics.md (GL4-0006 and the Workstream B baselines) configured CNA_GRAPHICS_RENDERERS=\"OPENGL4;OPENGLES3;OPENGL33\" in one tree (Wayland, SDL-free) and ran the classic corpus and CnaGraphicsExtTests per runtime renderer on one Radeon 780M. Its absence from CI is CI scope (no workflow under .github/workflows builds OPENGL4 at all), not a defect of the combination.",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "development/internals/graphics/backends.html#opengl4-and-easygl overstates this as 'permitted by configure rules rather than evidenced'."
  },
  {
   "cand": "P2-033",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-01",
   "evidence": "GraphicsDevice.hpp documents DeviceLost as 'never raised on desktop' and the deviceStatus_ comment says only D3D9 calls the callback; GraphicsDevice.cpp repeats it in createRenderer and getGraphicsDeviceStatusProperty. RendererDeviceEvent::Lost is raised by DirectX9Renderer, DirectX11Renderer::CheckDeviceRemoved, DirectX12Renderer::CheckDeviceRemovedEXT, Direct2DRenderer::RecreateDeviceResourcesForRecovery, VulkanRenderer::CheckDeviceLostEXT and WebGPURenderer::DebugSimulateContextLoss.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-034",
   "source": "phase2",
   "classification": "NOT A BUG",
   "published_as": null,
   "evidence": "Game-thread affinity is CNA's threading model for GraphicsDevice: no CNA header promises instance thread safety, and the only cross-thread facility is IRendererThreadContextLease (GraphicsDevice::AcquireRendererThreadContextLease), returned by the EasyGL and OpenGL4 families for background content loading. Unlocked mutation therefore violates no stated contract; development/internals/graphics/device.html#thread already states the limit.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Design limitation, already documented on the site; not a defect."
  },
  {
   "cand": "P2-035",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-02",
   "evidence": "modules/renderers/easygl/src/EasyGLRendererDescriptor.cpp file header still calls a runtime GL profile phase P11 future work while the file declares CreateGraphicsRendererForProfile (P11); cmake/RendererCombinations.cmake's comment on CNA_RENDERER_SHARED_EASYGL says lifting the ODR rule is P11 although the same file records that P11 removed it.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "The RendererCombinations.cmake comment is an additional stale site found while verifying."
  },
  {
   "cand": "P2-036",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-03",
   "evidence": "HeadlessRenderer.hpp documents GetLastFrameStatistics as the frame ending at the most recent Present(); HeadlessRenderer::GetLastFrameStatistics returns stats - statsAtLastPresent (work since that Present); headless_coverage_gaps_test.cpp checks B and C pin the code behaviour.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-037",
   "source": "phase2",
   "classification": "NOT A BUG",
   "published_as": null,
   "evidence": "The SDL_VIDEODRIVER=x11/DISPLAY environment on Headless_XnaPixelCenter (modules/renderers/headless/examples/CMakeLists.txt) contradicts the file header's policy but is inert: HeadlessRendererDescriptor.cpp declares windowKind None, needsWindow false and needsVideoSubsystem false, and the fixture's HEADLESS arm (xna_pixel_center_contract_test.cpp, RunNonRasterizing) touches no SDL. No behaviour depends on it.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Registration tidy-up only."
  },
  {
   "cand": "P2-038",
   "source": "phase2",
   "classification": "NOT A BUG",
   "published_as": null,
   "evidence": "HeadlessTextureRenderer(state, width, height, typeNameOverride) in HeadlessRenderer.cpp has no caller at TARGET (the only construction site, HeadlessRenderer::CreateTexture, uses the ImageData constructor; no class derives from it). Dead code has no observable behaviour; its missing texturesCreated increment and trace would matter only if it gained a caller.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Code hygiene, not a defect."
  },
  {
   "cand": "P2-039",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-04",
   "evidence": "SdlGpuRenderer.cpp: QueueCustomEffect3DDrawEXT derives attributes from the first vertex buffer's declaration with buffer_slot 0, IssueCustomEffect3DDrawEXT binds one vertex buffer; DrawInstancedPrimitivesEx sends instanced ShaderEffect draws there (SMG-0022); GetAdditionalLimitationsTextEXT still says ShaderEffect instancing is not implemented.",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "Published as a functional gap with the misleading limitation text."
  },
  {
   "cand": "P2-040",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-05",
   "evidence": "SdlGpuRenderer::SupportsComputeShadersEXT returns device_ != nullptr and SupportsShaderLanguageEXT accepts SpirV whenever a device exists; SdlGpuComputeShaderRenderer::CompileProgram (SdlGpuModern.cpp) and CreateShaderFromRemappedSpirvEXT pass SDL_GPU_SHADERFORMAT_SPIRV directly, unlike CreateSdlGpuShaderEXT's shadercross route; CNA's own comments (cmake/RendererSelection.cmake, modules/renderers/sdl-gpu/CMakeLists.txt) state D3D12 and Metal need translation.",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "No D3D12 or Metal run."
  },
  {
   "cand": "P2-041",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-06",
   "evidence": "modules/renderers/sdl-gpu/examples/CMakeLists.txt guards only on CNA_GRAPHICS_RENDERER STREQUAL SDL_GPU, which modules/renderers/CMakeLists.txt re-points per family; cna_register_renderer_test (cmake/TestHelpers.cmake) sets no renderer selection; contrast the _cna_default_renderer_identity guard in the HEADLESS and EasyGL blocks.",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "Consolidated with P2-046, P2-047 and P2-050 (one mechanism)."
  },
  {
   "cand": "P2-042",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-07",
   "evidence": "On non-Windows hosts CNA_SDLGPU_HEADLESS_TEST_ENVIRONMENT equals CNA_SDLGPU_TEST_ENVIRONMENT (SDL_VIDEODRIVER=x11 by default); SdlGpu_2D_HeadlessDriver adds CNA_SDLGPU_TEST_FORCE_HEADLESS=1, which SdlGpuRenderer::SdlGpuRenderer refuses unless IsIsolatedVideoDriver (dummy/offscreen). CNA's own record lists the test failing (PSG-0009; still among the 26 failures after STREETS-0008).",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "Failure recorded by CNA; cause attributed by reading."
  },
  {
   "cand": "P2-043",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-08",
   "evidence": "modules/core/include/CNA/GraphicsRendererSelection.hpp documents IsLatched as true when the first GraphicsDevice begins construction; GraphicsDevice::resolveRenderer calls GraphicsRendererSelectionAccessEXT::Latch only after a candidate was created (RTR-P5-18); GraphicsRendererFallbackTest.AFailedResolutionDoesNotLatch pins the code.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-044",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-09",
   "evidence": "GraphicsDevice constructor try block: resolveRenderer() latches, then UpdateViewportFromWindow and three state pushes; the catch (...) calls destroyNativeResources and setVideoSubsystemAcquired(false) and rethrows without unlatching (only GraphicsRendererSelection::ResetForTestingEXT clears the latch). No test injects a failure after resolution.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Reachability of a throw in those four steps not established."
  },
  {
   "cand": "P2-045",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-10",
   "evidence": "modules/renderers/CMakeLists.txt enters common/d3d only when the default is DIRECTX11/DIRECTX12, before the per-family loop; directx11/directx12 CMakeLists link cna_renderer_d3dcommon PUBLIC. The same pre-loop pattern gates the GDI configuration of modules/renderers/software (CNA_GDI_SOFTWARE_SOURCES, cna_renderer_software_headers) on GDI being the default.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "GDI half is an additional finding with the same mechanism; Windows-only, not configured."
  },
  {
   "cand": "P2-046",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-06",
   "evidence": "modules/renderers/software/examples/CMakeLists.txt guards on CNA_GRAPHICS_RENDERER STREQUAL SOFTWARE and even links cna_renderer_software for the non-default case, but registers no renderer selection; in the multi-renderer preset (CMakePresets.json, HEADLESS;SOFTWARE;STUB, default HEADLESS) the Software_* executables run under HEADLESS.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Consolidated into NEW-B5-06."
  },
  {
   "cand": "P2-047",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-06",
   "evidence": "modules/renderers/stub/examples/CMakeLists.txt guards on CNA_GRAPHICS_RENDERER STREQUAL STUB only; stub_smoke_test.cpp static_casts the device renderer to StubRenderer& (undefined behaviour under another renderer, the shape GL4-0035 found crashing for OpenGL4) and its checks also hold under HEADLESS.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Consolidated into NEW-B5-06."
  },
  {
   "cand": "P2-048",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-11",
   "evidence": "modules/renderers/stub/examples/CMakeLists.txt cna_stub_test links SDL3::SDL3 unconditionally; stub_smoke_test.cpp includes the SDL3 header and calls SDL_WasInit; HEADLESS (if(TARGET SDL3::SDL3)) and SOFTWARE (early return) examples guard the same case.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Not configured."
  },
  {
   "cand": "P2-049",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-12",
   "evidence": "StubRenderer.hpp comment on StubTextureRenderer: SetData()/GetData() accepted as no-op defaults and discarded; ITextureRenderer::GetData(level, x, y, w, h, data, length) defaults to false and Texture2D converts false into NotSupportedException.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-050",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-06",
   "evidence": "modules/renderers/vulkan/examples/CMakeLists.txt guards on CNA_BUILD_EXAMPLES, CNA_BUILD_TESTS and CNA_GRAPHICS_RENDERER STREQUAL VULKAN only; no renderer selection in the registrations; sets such as OPENGLES3;VULKAN;SOFTWARE;HEADLESS;STUB (docs/runtime-renderer-selection.md) enter the block with OPENGLES3 as default.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Consolidated into NEW-B5-06."
  },
  {
   "cand": "P2-051",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-13",
   "evidence": "VulkanRenderer::VulkanRenderer creates raw handles (instance_, debugMessenger_, device_, swapchain_, ...) in sequence with no try/catch or scope guard; PickPhysicalDevice throws after CreateInstance/CreateSurface; the class destructor, which handles partial state, never runs for a constructor that threw.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Previously an open question; settled by reading."
  },
  {
   "cand": "P2-095",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-14",
   "evidence": "RenderPipeline::releaseDeviceResourcesEXT resets sceneTarget_ and the chain pool only; BloomPass, SsaoPass and VolumetricFogPass own private RenderTargetPool members; getGpuMemoryEstimateBytes counts chain pool and scene target only; RenderPipeline.hpp documents 'Drops every target the pipeline owns'.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-096",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-15",
   "evidence": "RenderPipeline::begin sets frameOpen_ then runs ShadowMap::begin, drawCasters_, ShadowMap::end unguarded; RenderPipeline::end clears frameOpen_ then runs drawTransparentPhase before SetRenderTarget(nullptr); PipelineExceptionSafetyTest covers pass exceptions only.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": ""
  },
  {
   "cand": "P2-097",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-16",
   "evidence": "cmake/Tests/ModuleProbes.cmake registers 18 *ShaderPackageReproducibility CTests, none for modules/graphics-ext/src/shaders/shadow_caster or tests/CNA/Graphics/shaders/modern_resource_interop; the generator skips (77) without libshaderc.so.1 and no workflow installs shaderc.",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "Merged with P2-098 (both are shader-package drift guards)."
  },
  {
   "cand": "P2-098",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-16",
   "evidence": "ClusteredForwardEffectTests.cpp PackagedEsSourceRetainsEveryPublicGlslFragment checks kForwardEsFragmentSource only; kForwardDesktopFragmentSource (forward.desktop.frag.glsl, added for OPENGL33/OPENGL4 in GL4-0029) is unpinned; at TARGET it differs from the ES file only in its header lines.",
   "changed_since_bible_pin": true,
   "executed": "none",
   "note": "Merged into NEW-B5-16."
  },
  {
   "cand": "P2-099",
   "source": "phase2",
   "classification": "STILL EXISTS",
   "published_as": "NEW-B5-17",
   "evidence": "RenderPipelineTests.cpp TheFixedPassOrderIsSsaoThenBloomThenTonemapThenFxaa, SsrSitsBetweenSsaoAndBloom and DepthOfFieldSitsBeforeBloomBecauseItBelongsToTheLens assert getLastFramePassCount() only; no test observes the order of RenderPipeline::end()'s chain_.addPass calls.",
   "changed_since_bible_pin": false,
   "executed": "none",
   "note": "Wider than the candidate stated: no ordering relation is asserted at all. development/internals/modules/graphics-ext.html#frame-chain says these tests 'assert parts of this order'."
  }
 ],
 "issues": [
  {
   "cand_ids": [
    "CNA-BUG-006"
   ],
   "id": "CNA-BUG-006",
   "class": "bug",
   "title": "SpriteEffect caches a MatrixTransform parameter that never exists, so OnApply() never sets the sprite projection",
   "summary": "SpriteEffect looks up a MatrixTransform parameter its base Effect never creates, so Parameters[\"MatrixTransform\"] is null and OnApply() returns before computing the orthographic projection and half-pixel offset.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>Microsoft::Xna::Framework::Graphics::SpriteEffect</code>: its <code>MatrixTransform</code> parameter and <code>OnApply()</code>",
   "expected": "<p>XNA 4.0's <code>SpriteEffect</code> carries a <code>MatrixTransform</code> matrix parameter, and applying the effect sets it to a half-pixel offset times an orthographic projection over the current viewport. CNA's own <code>SpriteEffect::OnApply()</code> body computes exactly that (<code>Matrix::CreateOrthographicOffCenter</code> over the viewport, multiplied after <code>Matrix::CreateTranslation(-0.5f, -0.5f, 0.0f)</code>), and the class comment says the effect \"applies a single world-view-projection matrix transform to a textured quad\".</p>",
   "actual": "<p>Both constructors call <code>SpriteEffect::CacheEffectParameters()</code>, which stores <code>getParametersProperty()[\"MatrixTransform\"]</code>. Nothing ever adds a parameter of that name: <code>Effect::Effect(GraphicsDevice&amp;)</code> adds only a technique named <code>Default</code>, and <code>SpriteEffect</code> adds no parameter. <code>EffectParameterCollection::operator[](const std::string&amp;)</code> returns <code>nullptr</code> for an absent name, so <code>matrixParam_</code> is always null and <code>OnApply()</code> returns at its first statement. Code that reads <code>effect.getParametersProperty()[\"MatrixTransform\"]</code>, as XNA code does, receives a null pointer, and <code>Apply()</code> never computes the projection.</p>",
   "sources": [
    {
     "path": "modules/graphics/src/Xna/SpriteEffect.cpp",
     "note": "SpriteEffect::CacheEffectParameters (lookup of MatrixTransform) and SpriteEffect::OnApply (early return on a null parameter)"
    },
    {
     "path": "modules/graphics/include/Microsoft/Xna/Framework/Graphics/SpriteEffect.hpp",
     "note": "class SpriteEffect: matrixParam_ and the class comment"
    },
    {
     "path": "modules/graphics/src/Xna/Effect.cpp",
     "note": "Effect::Effect(GraphicsDevice&) adds only the Default technique"
    },
    {
     "path": "modules/graphics/src/Xna/EffectParameterCollection.cpp",
     "note": "EffectParameterCollection::operator[](const std::string&) returns nullptr for an unknown name"
    },
    {
     "path": "modules/graphics/tests/Microsoft/Xna/Framework/Graphics/SpriteEffectTests.cpp",
     "note": "only Clone() and Apply()-does-not-throw are tested"
    },
    {
     "path": "modules/c-api/src/CnaCApiEffects.cpp",
     "note": "cna_sprite_effect_create, the only in-tree creator outside tests"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. No renderer consumes the <code>SpriteEffect</code> class: the renderers that run XNA's sprite shader (for example <code>D3D9SpriteBatch.cpp</code>, <code>D3D12SpriteBatch.cpp</code> and <code>SoftwareSpriteBatch.cpp</code>) load their own embedded compiled <code>SpriteEffect.fxb</code> and look up <code>MatrixTransform</code> in that blob, and <code>SpriteBatch.cpp</code> never instantiates the class. Its only in-tree creator outside tests is the C API's <code>cna_sprite_effect_create</code>. The defect is therefore latent for ordinary <code>SpriteBatch</code> drawing and visible to code that instantiates <code>SpriteEffect</code> directly.</p>",
   "reproduction": "<pre><code class=\"language-cpp\">// Illustrative; not compiled or run for this entry.\nusing namespace Microsoft::Xna::Framework::Graphics;\nGraphicsDevice device;\nSpriteEffect effect(device);\nEffectParameter* transform = effect.getParametersProperty()[\"MatrixTransform\"];\n// XNA 4.0: a Matrix parameter that Apply() fills with halfPixelOffset * projection.\n// CNA at 009d40f5: transform == nullptr, and effect.Apply() computes nothing.\neffect.Apply();</code></pre>",
   "tests_current": "<p><code>SpriteEffectTests.cpp</code> has <code>CloneReturnsIndependentSpriteEffect</code> and <code>CloneAndOriginalApplyIndependently</code> (<code>Apply()</code> does not throw). Neither asserts that the parameter exists or that <code>Apply()</code> writes a value.</p>",
   "regression_test": "<p>A <code>SpriteEffectTests.cpp</code> case asserting that <code>getParametersProperty()[\"MatrixTransform\"]</code> is non-null and, after <code>Apply()</code> on a device with a known viewport, holds <code>CreateTranslation(-0.5, -0.5, 0) * CreateOrthographicOffCenter(0, width, height, 0, 0, 1)</code>.</p>",
   "blast_radius": "<p>Code that constructs <code>SpriteEffect</code> directly (C++ or <code>cna_sprite_effect_create</code>) and reads, sets or relies on <code>MatrixTransform</code>, for example XNA code ported unchanged. Ordinary <code>SpriteBatch</code> drawing is unaffected because each renderer supplies the sprite transform itself; the C API's <code>cna_effect_is_exact_stock_sprite_effect</code> identity query is unaffected.</p>",
   "workaround": "<p>Do not rely on <code>SpriteEffect</code>'s <code>MatrixTransform</code>; ordinary <code>SpriteBatch</code> drawing needs no explicit <code>SpriteEffect</code>.</p>",
   "related": {
    "guide": [
     [
      "docs/effects.html#spriteeffect",
      "Effects: SpriteEffect"
     ]
    ],
    "internals": [
     [
      "development/internals/graphics/draw-call.html#public",
      "Draw-call path: the public entry points"
     ]
    ],
    "maintainer": [
     [
      "development/handbook/change-public-xna-behavior.html#decide",
      "Change public XNA behaviour"
     ]
    ]
   },
   "origin": "Bible ledger CNA-BUG-006"
  },
  {
   "cand_ids": [
    "CNA-BUG-012"
   ],
   "id": "CNA-BUG-012",
   "class": "bug",
   "title": "Two tests still encode the wrong occlusion-query expectation for SDL_GPU after the renderer was made truthful",
   "summary": "SDL_GPU now reports OcclusionQuery false and refuses queries, but the shared capability test still expects true and SdlGpu_OcclusionQuery_Limitation expects a refusal message the public constructor never produces.",
   "subsystem": "Testing & evidence",
   "status": "narrowed",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>GraphicsDeviceCapabilityTest.SupportsOcclusionQuery</code> / <code>SupportsCustomEffects</code> (<code>ExpectedCapabilities()</code>) and the <code>SdlGpu_OcclusionQuery_Limitation</code> CTest, against <code>OcclusionQuery::OcclusionQuery(GraphicsDevice&amp;)</code>",
   "expected": "<p>A renderer that truthfully reports <code>GraphicsCapability::OcclusionQuery</code> as false and refuses <code>OcclusionQuery</code> construction should pass both the shared capability suite and its own limitation test. <code>ExpectedCapabilities()</code> in <code>GraphicsDeviceCapabilityTests.cpp</code> exists to state each renderer's accepted boundary (\"Per-renderer arms below preserve each accepted capability boundary\"), and SDLGPU-80 fixed SDL_GPU's: capability false, detailed profile <code>Unsupported</code>, and a <code>System::NotSupportedException</code> naming the SDL_gpu reason.</p>",
   "actual": "<p>The renderer half of the original defect is fixed (see Evidence). Two tests still disagree with it:</p><ul><li><code>ExpectedCapabilities()</code> has explicit arms only for OPENGLES2/WEBGL1, PORTABLEGL, FNA3D and SOFTWARE; every other renderer takes <code>default: return {true, true, true}</code>. <code>SupportsOcclusionQuery</code> therefore expects <code>true</code> on SDL_GPU, whose <code>SdlGpuRenderer::SupportsCapability</code> answers <code>false</code>, and <code>SupportsCustomEffects</code> expects <code>true</code> on any SDL_GPU build without <code>CNA_SDL_GPU_SHADER_EFFECTS</code> (every Windows, Apple and Emscripten build, and Linux without libshaderc), where the answer is <code>false</code>. The same catch-all mis-describes METAL (no MRT, occlusion queries or custom effects by policy) and STUB (false for everything).</li><li><code>sdlgpu_occlusionquery_limitation_test.cpp</code> requires <code>error.what()</code> to equal the message of <code>SdlGpuRenderer::CreateOcclusionQuery</code>. The public constructor now refuses first, whenever the profile is Reach or <code>SupportsCapability(OcclusionQuery)</code> is false, with \"OcclusionQuery is not supported by the active graphics profile and renderer.\" On SDL_GPU the capability is false, so the renderer's message is unreachable through the public constructor and the check \"public OcclusionQuery construction throws the exact SDL_gpu limitation\" fails.</li></ul>",
   "sources": [
    {
     "path": "modules/graphics/tests/Microsoft/Xna/Framework/Graphics/GraphicsDeviceCapabilityTests.cpp",
     "note": "ExpectedCapabilities() default arm; SupportsOcclusionQuery and SupportsCustomEffects"
    },
    {
     "path": "modules/renderers/sdl-gpu/examples/sdlgpu_occlusionquery_limitation_test.cpp",
     "note": "exact-message check on public OcclusionQuery construction"
    },
    {
     "path": "modules/graphics/src/Xna/OcclusionQuery.cpp",
     "note": "OcclusionQuery::OcclusionQuery refuses on Reach or a false capability before calling CreateOcclusionQuery"
    },
    {
     "path": "modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp",
     "note": "SdlGpuRenderer::SupportsCapability (OcclusionQuery false) and SdlGpuRenderer::CreateOcclusionQuery (throws)"
    },
    {
     "path": "modules/renderers/sdl-gpu/CMakeLists.txt",
     "note": "CNA_SDL_GPU_SHADER_EFFECTS only with a target-native libshaderc, never on Windows, Apple or Emscripten"
    },
    {
     "path": "modules/renderers/webgpu/src/WebGPURenderer.cpp",
     "note": "WebGPURenderer::CreateOcclusionQuery, the fixed WebGPU half"
    },
    {
     "path": "plans/plan_pre_sdlgpu_closeout.md",
     "note": "PSG-0009: SdlGpu_OcclusionQuery_Limitation in the list of failing classic SDL_GPU CTests"
    },
    {
     "path": "plans/plan_street_sdlgpu.md",
     "note": "STREETS-0008: the same set, 26 failures, still failing in CNA's latest recorded run"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. What is fixed: <code>WebGPURenderer::CreateOcclusionQuery</code> returns a real <code>WebGPUOcclusionQueryRenderer</code> (WEBGPU-84), pinned by <code>WebGpuMrtOcclusionContract.OcclusionQueryCapabilityIsTrue</code> and the <code>WebGPU_OcclusionQuery</code> pixel test; SDL_GPU answers the capability false and <code>CreateOcclusionQuery</code> throws (SDLGPU-80); and the public constructor refuses whenever the capability is false (SOFTWARE-199), so a null factory can no longer hand out a silent query. What survives is on the test side. CNA's own records corroborate the second item: <code>plans/plan_sdlgpu.md</code> records <code>SdlGpu_OcclusionQuery_Limitation</code> passing 5/5 when SDLGPU-80 landed on a line of history whose constructor still called the renderer factory directly; after both changes met, the test is in PSG-0009's list of classic SDL_GPU failures and remains among the 26 failures of the latest recorded run (STREETS-0008; one Radeon 780M under RADV, private compositor). The first item is by reading: no CNA record of <code>GraphicsDeviceCapabilityTest.*</code> under SDL_GPU after SDLGPU-80 was found, while CNA's records do show the same catch-all failing five cases under STUB (<code>integration/BATCH_1_STABILIZATION.md</code>).</p>",
   "reproduction": "<pre><code class=\"language-bash\"># Not executed for this entry. An SDL_GPU build with tests, on a host with a Vulkan GPU and a display:\nctest --test-dir &lt;sdl-gpu build&gt; -R 'GraphicsDeviceCapabilityTest\\.SupportsOcclusionQuery$|^SdlGpu_OcclusionQuery_Limitation$' --output-on-failure</code></pre>",
   "tests_current": "<p><code>GraphicsDeviceCapabilityTests.cpp</code> (per-renderer expectations; its MRT case already compares the capability with a real two-target bind), <code>sdlgpu_occlusionquery_limitation_test.cpp</code>, <code>OcclusionQueryPixelCountPrecisionTests.cpp</code> (skips when the capability is false) and, for the fixed WebGPU half, <code>WebGpuMrtOcclusionContractTests.cpp</code> and <code>webgpu_occlusion_query_test.cpp</code>. Nothing checks that <code>ExpectedCapabilities()</code> has an arm for every renderer whose answers differ from the catch-all.</p>",
   "regression_test": "<p>Give <code>ExpectedCapabilities()</code> explicit arms for SDL_GPU (occlusion false; custom effects equal to whether <code>CNA_SDL_GPU_SHADER_EFFECTS</code> is defined), METAL and STUB, and make the limitation test assert the public constructor's refusal (or have the constructor forward the renderer's reason). A structural guard in the style of <code>TheMultipleRenderTargetCapabilityMatchesWhatBindingActuallyDoes</code>: on a HiDef device, assert that <code>SupportsCapability(OcclusionQuery)</code> equals whether <code>OcclusionQuery</code> construction succeeds.</p>",
   "blast_radius": "<p>Test evidence only; renderer behaviour is correct. SDL_GPU runs of the shared capability suite and of the classic <code>^SdlGpu</code> CTests report failures that are not renderer defects, which inflates the recorded SDL_GPU failure count and can hide real regressions among them. Renderers with explicit arms are unaffected.</p>",
   "workaround": "<p>When triaging an SDL_GPU run, treat these two failures as test defects.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/sdl-gpu.html#tests",
      "SDL_gpu renderer internals: tests"
     ],
     [
      "development/internals/graphics/device.html#capabilities",
      "GraphicsDevice internals: capability answers"
     ]
    ],
    "guide": [
     [
      "docs/rendering-backends.html#capability-reporting",
      "Graphics renderers: capability reporting"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#gpu",
      "Test architecture: GPU tests"
     ]
    ],
    "maintainer": [
     [
      "development/handbook/fix-a-renderer-bug.html#prove",
      "Fix a renderer bug: prove it"
     ]
    ]
   },
   "origin": "Bible ledger CNA-BUG-012 (narrowed); the SDL_GPU limitation-test mismatch found during re-verification"
  },
  {
   "cand_ids": [
    "CNA-BUG-054"
   ],
   "id": "CNA-BUG-054",
   "class": "bug",
   "title": "HEADLESS reports occlusion-query support and a precise pixel count while its query always answers 1",
   "summary": "HEADLESS inherits OcclusionQuery = true and the default isPixelCountPreciseEXT() = true, but HeadlessOcclusionQueryRenderer completes at once and returns PixelCount() == 1 whatever was drawn.",
   "subsystem": "Graphics & renderers",
   "status": "narrowed",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>CNA::GraphicsCapability::OcclusionQuery</code> and <code>OcclusionQuery::isPixelCountPreciseEXT()</code> on the HEADLESS renderer",
   "expected": "<p><code>GraphicsCapability::OcclusionQuery</code> is documented as \"Real raster-sample occlusion queries (OcclusionQuery.Begin/End/PixelCount)\", and the comment on <code>IGraphicsRenderer::SupportsCapability</code> requires every renderer with a narrower contract, \"including no-renderer\" ones, to override the applicable entries truthfully. <code>OcclusionQuery::isPixelCountPreciseEXT()</code> (CNAEXT) is true only \"when the pixel count is a genuine per-fragment tally\".</p>",
   "actual": "<p><code>HeadlessRenderer::SupportsCapability</code> overrides only <code>Texture3D</code>, <code>AdditiveBlending</code> and <code>MultiStreamVertexInput</code>; <code>OcclusionQuery</code> reaches <code>default: return true</code>. <code>HeadlessRenderer::CreateOcclusionQuery</code> returns a <code>HeadlessOcclusionQueryRenderer</code> whose <code>End()</code> marks it complete and whose <code>PixelCount()</code> is the constant 1 (its comment calls this the conservative \"everything visible\" answer). It does not override <code>PixelCountIsPreciseEXT()</code>, so the interface default <code>true</code> is reported. On a HiDef HEADLESS device a game sees occlusion queries advertised, a result available at once, <code>PixelCount == 1</code> for any geometry (including none), and a claim that 1 is an exact fragment tally.</p>",
   "sources": [
    {
     "path": "modules/renderers/headless/src/HeadlessRenderer.cpp",
     "note": "HeadlessRenderer::SupportsCapability (default arm) and HeadlessRenderer::CreateOcclusionQuery"
    },
    {
     "path": "modules/renderers/headless/include/CNA/Internal/Renderers/Headless/HeadlessRenderer.hpp",
     "note": "HeadlessOcclusionQueryRenderer: End, IsComplete, PixelCount returning 1"
    },
    {
     "path": "modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp",
     "note": "IOcclusionQueryRenderer::PixelCountIsPreciseEXT default true; IGraphicsRenderer::SupportsCapability contract comment"
    },
    {
     "path": "modules/graphics/include/CNA/GraphicsCapability.hpp",
     "note": "GraphicsCapability::OcclusionQuery documentation"
    },
    {
     "path": "modules/graphics/include/Microsoft/Xna/Framework/Graphics/OcclusionQuery.hpp",
     "note": "isPixelCountPreciseEXT documentation"
    },
    {
     "path": "modules/graphics/tests/Microsoft/Xna/Framework/Graphics/OcclusionQueryPixelCountPrecisionTests.cpp",
     "note": "PixelCountMatchesWhatTheQuerySaysItIs skips HEADLESS"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The rest of the original finding is fixed: <code>HeadlessRenderer::SetRenderTargets</code> binds a whole set into <code>boundRenderTargets_</code> instead of refusing counts above one; SOFTWARE answers MRT and occlusion explicitly and backs them with four CPU colour attachments (SOFTWARE-120, <code>Software_MrtStockEffectContract</code>) and an exact CPU query (SOFTWARE-122, <code>Software_OcclusionQuery_ExactContract</code>); WebGPU binds two to four targets (WEBGPU-85/86/87, <code>WebGPU_MRT</code>, <code>WebGpuMrtOcclusionContract.TwoTargetBindSucceedsAndBothTargetsReceiveOwnContent</code>); and the shared <code>GraphicsDeviceCapabilityTest.TheMultipleRenderTargetCapabilityMatchesWhatBindingActuallyDoes</code> now attempts a two-target bind and compares it with the capability. The surviving HEADLESS half is visible in CNA's own test: <code>OcclusionQueryPixelCountPrecisionTest.PixelCountMatchesWhatTheQuerySaysItIs</code> skips HEADLESS (\"validates query lifetime but has no raster samples to count\") instead of comparing the capability and the precision claim with the result. Whether HEADLESS should answer false or keep a documented bookkeeping query is a maintainer decision; what is recorded here is the contradiction with the two documented contracts.</p>",
   "reproduction": "<pre><code class=\"language-cpp\">// Illustrative; not compiled or run for this entry. Build with CNA_GRAPHICS_RENDERER=HEADLESS.\nusing namespace Microsoft::Xna::Framework::Graphics;\nGraphicsDevice device(GraphicsAdapter::getDefaultAdapterProperty(), GraphicsProfile::HiDef,\n                      PresentationParameters());\nbool advertised = device.SupportsCapability(CNA::GraphicsCapability::OcclusionQuery); // true\nOcclusionQuery query(device);\nquery.Begin();\nquery.End();                                    // nothing was drawn\nbool complete = query.getIsCompleteProperty();  // true at once\nint pixels = query.getPixelCountProperty();     // 1\nbool precise = query.isPixelCountPreciseEXT();  // true</code></pre>",
   "tests_current": "<p><code>OcclusionQueryPixelCountPrecisionTests.cpp</code>: <code>XnaLifecycleRejectsUnavailableAndInvalidSequences</code> runs on HEADLESS (lifecycle only); <code>PixelCountMatchesWhatTheQuerySaysItIs</code> skips it. <code>GraphicsDeviceCapabilityTest.SupportsOcclusionQuery</code> expects true for HEADLESS through its catch-all arm, so it pins the current answer. HEADLESS statistics count query creation (<code>occlusionQueriesCreated</code>), not results.</p>",
   "regression_test": "<p>Either make HEADLESS answer <code>OcclusionQuery</code> false (updating the capability test's expectation), or keep the bookkeeping query, override <code>PixelCountIsPreciseEXT()</code> to false and let <code>PixelCountMatchesWhatTheQuerySaysItIs</code> run on HEADLESS instead of skipping: with precision false it asserts a count of at most 1, which the constant satisfies.</p>",
   "blast_radius": "<p>HEADLESS only. Games and test suites run under HEADLESS that branch on <code>PixelCount</code>, or compute coverage ratios such as a lens-flare fade, always take the \"visible\" path and get 1/area; capability-driven feature selection believes real queries exist. Renderers with real queries and renderers that answer false are unaffected.</p>",
   "workaround": "<p>Under HEADLESS, do not treat <code>PixelCount</code> as coverage; check the active renderer before relying on occlusion results.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/headless.html#resources",
      "HEADLESS renderer internals: resources"
     ]
    ],
    "guide": [
     [
      "docs/rendering-backends.html#capability-matrix",
      "Graphics renderers: capability matrix"
     ]
    ],
    "architecture": [
     [
      "development/architecture/graphics.html#families",
      "Graphics architecture: renderer families"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#gpu",
      "Test architecture: GPU tests"
     ]
    ]
   },
   "origin": "Bible ledger CNA-BUG-054 (narrowed to the HEADLESS occlusion half)"
  },
  {
   "cand_ids": [
    "P2-033"
   ],
   "id": "NEW-B5-01",
   "class": "bug",
   "title": "GraphicsDevice's DeviceLost documentation and device-status comments still say only Direct3D 9 reports device loss",
   "summary": "The DeviceLost Doxygen says the event is never raised on desktop and three comments say only Direct3D 9 calls the device-event callback, while six renderer families raise it at TARGET.",
   "subsystem": "Documentation & release tooling",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>GraphicsDevice::DeviceLost</code> and <code>GraphicsDevice::getGraphicsDeviceStatusProperty()</code> documentation",
   "expected": "<p>The public header says when <code>DeviceLost</code> is raised and what <code>getGraphicsDeviceStatusProperty()</code> can return, so a game can decide whether it must handle device loss on its target renderer.</p>",
   "actual": "<p><code>GraphicsDevice.hpp</code> documents <code>DeviceLost</code> as \"Raised when the device is lost (XNA compliance; never raised on desktop)\", and the comment on <code>deviceStatus_</code> says \"Every renderer except D3D9 never calls that callback\". <code>GraphicsDevice.cpp</code> repeats it in <code>createRenderer</code> (\"Nine of the ten renderers never call this\") and in <code>getGraphicsDeviceStatusProperty</code>. At TARGET <code>RendererDeviceEvent::Lost</code> is raised by six families: DirectX 9, DirectX 11 (<code>DirectX11Renderer::CheckDeviceRemoved</code>), DirectX 12 (<code>DirectX12Renderer::CheckDeviceRemovedEXT</code>), Direct2D (<code>Direct2DRenderer::RecreateDeviceResourcesForRecovery</code>), Vulkan (<code>VulkanRenderer::CheckDeviceLostEXT</code> on <code>VK_ERROR_DEVICE_LOST</code>, a desktop path) and WebGPU; all but Vulkan also raise <code>Resetting</code>/<code>Reset</code> after recovery. <code>DeviceLost</code> is therefore raised on desktop hosts, and <code>GraphicsDeviceStatus</code> can become <code>Lost</code> or <code>NotReset</code> on more renderers than Direct3D 9.</p>",
   "sources": [
    {
     "path": "modules/graphics/include/Microsoft/Xna/Framework/Graphics/GraphicsDevice.hpp",
     "note": "DeviceLost documentation and the deviceStatus_ member comment"
    },
    {
     "path": "modules/graphics/src/Xna/GraphicsDevice.cpp",
     "note": "createRenderer (deviceEventCallback comment) and getGraphicsDeviceStatusProperty"
    },
    {
     "path": "modules/renderers/vulkan/src/VulkanRenderer.cpp",
     "note": "VulkanRenderer::CheckDeviceLostEXT raises Lost on VK_ERROR_DEVICE_LOST"
    },
    {
     "path": "modules/renderers/directx11/src/DirectX11Renderer.cpp",
     "note": "DirectX11Renderer::CheckDeviceRemoved raises Lost"
    },
    {
     "path": "modules/renderers/directx12/src/DirectX12Renderer.cpp",
     "note": "DirectX12Renderer::CheckDeviceRemovedEXT raises Lost"
    },
    {
     "path": "modules/renderers/direct2d/src/Direct2DRenderer.cpp",
     "note": "Direct2DRenderer::RecreateDeviceResourcesForRecovery raises Lost and Reset"
    },
    {
     "path": "modules/renderers/webgpu/src/WebGPURenderer.cpp",
     "note": "WebGPURenderer::DebugSimulateContextLoss and DebugRestoreContext; OnDeviceLost only logs"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The six raise sites are the renderers' own code; for WebGPU the event comes from the <code>DebugSimulateContextLoss</code>/<code>DebugRestoreContext</code> hooks, while the native <code>OnDeviceLost</code> callback only logs (its comment notes that the pinned wgpu-native delivers no callback for an application-initiated destroy). Behaviour is not affected; the public documentation and the maintainer comments are.</p>",
   "reproduction": null,
   "tests_current": "<p><code>vulkan_device_lost_contract_test.cpp</code> and the renderers' own recovery examples exercise the events; nothing checks the header text.</p>",
   "regression_test": "<p>Documentation fix: state which families raise <code>DeviceLost</code>/<code>DeviceReset</code> (and that Vulkan reports loss without recovery), in <code>GraphicsDevice.hpp</code> and the two <code>GraphicsDevice.cpp</code> comments.</p>",
   "blast_radius": "<p>Readers of the public header and generated API reference, and maintainers of the device-loss path. A game that trusts \"never raised on desktop\" skips its <code>DeviceLost</code> handling on Vulkan or Direct3D hosts.</p>",
   "workaround": "<p>Handle <code>DeviceLost</code>/<code>DeviceReset</code> on every renderer; see the device-loss section of the Development graphics architecture.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/device.html#capabilities",
      "GraphicsDevice internals"
     ]
    ],
    "architecture": [
     [
      "development/architecture/graphics.html#loss",
      "Graphics architecture: device loss"
     ]
    ],
    "guide": [
     [
      "docs/rendering-backends.html#verification",
      "Graphics renderers: verification"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-033 (development/internals/graphics/device.html)"
  },
  {
   "cand_ids": [
    "P2-035"
   ],
   "id": "NEW-B5-02",
   "class": "bug",
   "title": "Two EasyGL comments still describe runtime GL profiles (phase P11) as future work",
   "summary": "EasyGLRendererDescriptor.cpp's file header and the CNA_RENDERER_SHARED_EASYGL comment in RendererCombinations.cmake say a runtime GL profile is still to come, although both files implement or record it.",
   "subsystem": "Documentation & release tooling",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "Maintainer comments in <code>EasyGLRendererDescriptor.cpp</code> and <code>cmake/RendererCombinations.cmake</code> (EasyGL identities sharing one binary)",
   "expected": "<p>In-source comments describe the code beside them; they are what a maintainer reads before changing the EasyGL descriptor or the combination rules.</p>",
   "actual": "<p>The file header of <code>EasyGLRendererDescriptor.cpp</code> says the five identities are \"told apart by the CNA_GL_PROFILE_* compile definition\" and that \"Making the profile itself a RUNTIME choice -- which is what would let two GL profiles coexist in one binary -- is plans/plan_runtimerenderer.md phase P11, not this task.\" The same file declares <code>CreateGraphicsRendererForProfile(args, GlProfile)</code> (\"plans/plan_runtimerenderer.md P11\"), and every descriptor passes its own identity's profile at run time. In <code>cmake/RendererCombinations.cmake</code> the comment above <code>CNA_RENDERER_SHARED_EASYGL</code> says two EasyGL identities in one binary would be \"an ODR violation\" and that \"Lifting this is plans/plan_runtimerenderer.md phase P11\", while <code>cna_validate_renderer_combination</code> records that P11 \"REMOVED the rule that used to sit here\"; the list is still defined and validated by <code>scripts/check_renderer_combinations.py</code>, but no rule reads it.</p>",
   "sources": [
    {
     "path": "modules/renderers/easygl/src/EasyGLRendererDescriptor.cpp",
     "note": "file header versus CreateGraphicsRendererForProfile and the per-identity descriptors"
    },
    {
     "path": "cmake/RendererCombinations.cmake",
     "note": "comment on CNA_RENDERER_SHARED_EASYGL versus the P11 note in cna_validate_renderer_combination"
    },
    {
     "path": "scripts/check_renderer_combinations.py",
     "note": "still validates CNA_RENDERER_SHARED_EASYGL as a rule list"
    },
    {
     "path": "docs/runtime-renderer-selection.md",
     "note": "CNA's own verified OPENGLES3;OPENGLES2;OPENGL33 combination"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. CNA's own <code>docs/runtime-renderer-selection.md</code> lists <code>OPENGLES3;OPENGLES2;OPENGL33;SOFTWARE;HEADLESS</code> as a verified combination (\"Three EasyGL GL profiles in one binary\") and says that after phase P11 the profile branches are compiled either way. Only the comments are wrong.</p>",
   "reproduction": null,
   "tests_current": "<p><code>scripts/check_renderer_combinations.py</code> keeps the rule lists and the documentation in step but checks identity names, not comments.</p>",
   "regression_test": "<p>No test is needed; rewrite both comments, and either use or drop <code>CNA_RENDERER_SHARED_EASYGL</code> (updating the checker's list with it).</p>",
   "blast_radius": "<p>Maintainers only. A reader who trusts either comment would believe two EasyGL profiles cannot share a binary, or that the profile is a compile-time define, and could reintroduce a rule or a build-time branch that P11 removed. No runtime behaviour is affected.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/graphics/easygl.html#identity",
      "EasyGL renderer internals: identity"
     ],
     [
      "development/internals/graphics/selection.html#configure",
      "Renderer selection internals: configure"
     ]
    ],
    "guide": [
     [
      "docs/rendering-backends.html#combination-rules",
      "Graphics renderers: combination rules"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-035 (development/internals/graphics/easygl.html); the RendererCombinations.cmake comment found during re-verification"
  },
  {
   "cand_ids": [
    "P2-036"
   ],
   "id": "NEW-B5-03",
   "class": "bug",
   "title": "HeadlessRenderer::GetLastFrameStatistics() reports the frame in progress, not the finished frame its documentation names",
   "summary": "GetLastFrameStatistics() is documented as the statistics of the frame ending at the most recent Present(), but it subtracts the snapshot taken at that Present from the running totals, and a test pins that behaviour.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>CNA::Internal::Renderers::Headless::HeadlessRenderer::GetLastFrameStatistics()</code> (HEADLESS debugging API)",
   "expected": "<p>The declaration's comment: \"Statistics for the frame ending at the most recent Present() call (diffed against the cumulative totals as of the Present() before that).\" The name says the same, and CNA's <code>docs/headless-renderer.md</code> calls the pair \"cumulative and per-frame\" counters.</p>",
   "actual": "<p><code>HeadlessRenderer::Present</code> copies the running counters into <code>statsAtLastPresent</code>, and <code>GetLastFrameStatistics()</code> returns the running counters minus that snapshot. Called right after a <code>Present()</code> it returns zeros; called during a frame it returns that frame's work so far. No snapshot of the previous Present is kept, so the documented \"last finished frame\" figure cannot be produced. <code>Headless_CoverageGaps</code> checks B and C assert the current-frame behaviour (zero at the top of a frame, exactly two draw calls after two draws), so the code is the intended behaviour and the comment and name are what is wrong.</p>",
   "sources": [
    {
     "path": "modules/renderers/headless/include/CNA/Internal/Renderers/Headless/HeadlessRenderer.hpp",
     "note": "GetLastFrameStatistics declaration and comment"
    },
    {
     "path": "modules/renderers/headless/src/HeadlessRenderer.cpp",
     "note": "HeadlessRenderer::GetLastFrameStatistics and HeadlessRenderer::Present (statsAtLastPresent)"
    },
    {
     "path": "modules/renderers/headless/examples/headless_coverage_gaps_test.cpp",
     "note": "checks B and C pin the current-frame semantics"
    },
    {
     "path": "docs/headless-renderer.md",
     "note": "describes the pair as cumulative and per-frame counters"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The test file states its intent: the diff \"genuinely isolates the current frame\". A caller that reads the statistics from a <code>Game</code> after <code>EndDraw()</code> (which presents) always sees an empty frame.</p>",
   "reproduction": "<pre><code class=\"language-cpp\">// Illustrative; not compiled or run for this entry. HEADLESS renderer.\nauto&amp; headless = dynamic_cast&lt;CNA::Internal::Renderers::Headless::HeadlessRenderer&amp;&gt;(device.GetRenderer());\n// ... one DrawPrimitives call in this frame ...\ndevice.Present();\nauto last = headless.GetLastFrameStatistics();\n// Documented: last.drawCallCount == 1 (the frame that just ended). Actual: 0.</code></pre>",
   "tests_current": "<p><code>Headless_CoverageGaps</code> (<code>headless_coverage_gaps_test.cpp</code>) checks B and C pin the current-frame semantics; nothing reads the statistics after a <code>Present</code> expecting the previous frame.</p>",
   "regression_test": "<p>Correct the comment (and preferably the name, or add a current-frame alias) to say \"since the most recent Present()\"; if a finished-frame figure is wanted, keep a second snapshot at each <code>Present</code> and add a check that reads it immediately after <code>Present</code>.</p>",
   "blast_radius": "<p>Tests and tools that use the HEADLESS statistics API and read it after <code>Present()</code> get zeros. <code>GetStatistics()</code> (cumulative) is unaffected; no other renderer has this API.</p>",
   "workaround": "<p>Read <code>GetLastFrameStatistics()</code> before the frame's <code>Present()</code>, or diff two <code>GetStatistics()</code> snapshots.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/headless.html#testing",
      "HEADLESS renderer internals: testing"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#groups",
      "Test architecture: groups"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-036 (development/internals/graphics/headless.html)"
  },
  {
   "cand_ids": [
    "P2-039"
   ],
   "id": "NEW-B5-04",
   "class": "functional-gap",
   "title": "SDL_GPU ShaderEffect draws bind only the first vertex stream, and the limitation report misstates ShaderEffect instancing",
   "summary": "SDL_GPU's custom-effect draw takes its attributes from the first vertex buffer and binds slot 0 only, so other streams are never consumed, while its limitation text calls ShaderEffect instancing unimplemented although instance-indexed draws work.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "n/a",
   "confidence": "verified-by-reading",
   "public_contract": "<code>GraphicsDevice::DrawInstancedPrimitives</code> / <code>DrawIndexedPrimitives</code> with a CNAEXT <code>ShaderEffect</code> applied, on SDL_GPU; <code>GraphicsDevice::GetRendererCapabilityReportEXT()</code>",
   "expected": "<p>XNA-style hardware instancing binds a per-vertex stream and a per-instance stream (a <code>VertexBufferBinding</code> with an instance frequency) and draws with an effect that reads both. SDL_GPU reports <code>MultiStreamVertexInput</code> true, and its stock routes build one vertex-buffer layout per bound stream. The renderer's English limitation text (<code>GetAdditionalLimitationsTextEXT</code>, part of <code>GetRendererCapabilityReportEXT()</code>) is meant to state what an application cannot branch on.</p>",
   "actual": "<p>With a ShaderEffect applied, <code>DispatchStockDrawEXT</code> and <code>DrawInstancedPrimitivesEx</code> send the draw to <code>QueueCustomEffect3DDrawEXT</code>, which derives every attribute from the first buffer's <code>VertexDeclaration</code> (location = element index, <code>buffer_slot = 0</code>); <code>IssueCustomEffect3DDrawEXT</code> binds exactly one vertex buffer at slot 0. Per-instance streams and additional per-vertex streams in <code>GpuDrawParams::vertexStreams</code> are ignored, and a shader that consumes a location beyond the first declaration is refused with <code>System::NotSupportedException</code>. Instanced ShaderEffect draws do work when the shader reads per-instance data from a storage buffer by <code>gl_InstanceIndex</code> (SMG-0022, the engine layer's particle route). The limitation text nevertheless says \"CNAEXT ShaderEffect instancing is not implemented\", which is wrong for that route and silent about the stream restriction.</p>",
   "sources": [
    {
     "path": "modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp",
     "note": "SdlGpuRenderer::QueueCustomEffect3DDrawEXT (attributes from the first declaration, buffer_slot 0)"
    },
    {
     "path": "modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp",
     "note": "SdlGpuRenderer::IssueCustomEffect3DDrawEXT binds one vertex buffer; DrawInstancedPrimitivesEx SMG-0022 branch"
    },
    {
     "path": "modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp",
     "note": "SdlGpuRenderer::GetAdditionalLimitationsTextEXT"
    },
    {
     "path": "modules/renderers/webgpu/src/WebGPURenderer.cpp",
     "note": "WebGPURenderer::BuildCustomEffectVertexLayoutsEXT, the WMG-0021 comparison that binds per-instance streams"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The SMG-0019 to SMG-0022 custom-effect path was added shortly before TARGET. For comparison, WebGPU's <code>BuildCustomEffectVertexLayoutsEXT</code> (WMG-0021) gives a custom-effect draw one layout for the per-vertex stream at slot 0 and one for each per-instance stream. On SDL_GPU the location check prevents silently wrong output when the shader reads a missing location; a stream the shader never reads is simply unused.</p>",
   "reproduction": null,
   "tests_current": "<p>The storage-buffer instancing route is exercised through the particle system in <code>CnaGraphicsExtTests</code> (CNA records SDL_GPU at 931/0/31 of 962, SMG-0042). No SDL_GPU test draws a ShaderEffect with a per-instance <code>VertexBufferBinding</code> or with two per-vertex streams, and no test reads the instancing sentence of the limitation text (<code>sdlgpu_smoke_test.cpp</code> and <code>sdlgpu_occlusionquery_limitation_test.cpp</code> check other sentences).</p>",
   "regression_test": "<p>An SDL_GPU example that draws a ShaderEffect with a per-vertex quad stream plus a per-instance offset stream and reads back two distinct instance positions (it fails today with the location refusal), and an assertion on the limitation text once it states the stream restriction precisely.</p>",
   "blast_radius": "<p>SDL_GPU only, and only draws shaded by a CNAEXT ShaderEffect. Stock-effect draws and storage-buffer instancing are unaffected; compiled XNA effects use a separate route not assessed here. Code ported from XNA that feeds instance data through a second vertex stream to a custom shader is refused on this renderer.</p>",
   "workaround": "<p>On SDL_GPU, give a ShaderEffect its per-instance data through a storage buffer indexed by <code>gl_InstanceIndex</code>, or interleave all per-vertex attributes into one stream.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/sdl-gpu.html#modern",
      "SDL_gpu renderer internals: modern features"
     ]
    ],
    "guide": [
     [
      "docs/shader-effects.html#vertex-shader-inputs-and-vertexdeclaration",
      "Shader effects: vertex inputs"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-039 (development/internals/graphics/sdl-gpu.html)"
  },
  {
   "cand_ids": [
    "P2-040"
   ],
   "id": "NEW-B5-05",
   "class": "bug",
   "title": "SDL_GPU claims compute and SPIR-V shader intake on every device, including Direct3D 12 and Metal drivers that cannot take raw SPIR-V",
   "summary": "SupportsComputeShadersEXT() and SupportsShaderLanguageEXT(SpirV) answer true whenever a device exists, but compute pipelines and ShaderEffect SPIR-V payloads reach SDL_gpu as raw SPIR-V, which its Direct3D 12 and Metal drivers reject.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "low",
   "confidence": "strong",
   "public_contract": "<code>GraphicsDevice::SupportsCapability(GraphicsCapability::ComputeShaders)</code>, <code>GraphicsDevice::SupportsShaderLanguageEXT</code> and <code>CNA::Graphics::ComputeShader</code> on SDL_GPU",
   "expected": "<p><code>IGraphicsRenderer::SupportsComputeShadersEXT()</code> answers \"whether this renderer really implements compute\", and <code>SupportsShaderLanguageEXT</code> is true \"only when the implemented shader intake consumes that exact pair\". <code>GraphicsDevice::SupportsCapability(ComputeShaders)</code> is derived from the former precisely so that compute is never claimed by default.</p>",
   "actual": "<p><code>SdlGpuRenderer::SupportsComputeShadersEXT()</code> returns <code>device_ != nullptr</code>, and <code>SdlGpuRenderer::SupportsShaderLanguageEXT</code> accepts <code>SpirV</code> for vertex and fragment whenever a device exists (compute follows <code>SupportsComputeShadersEXT</code>). Stock shaders reach a non-SPIR-V driver through <code>CreateSdlGpuShaderEXT</code>, which chooses SDL_shadercross when the device does not accept SPIR-V. The other two intakes do not: <code>SdlGpuComputeShaderRenderer::CompileProgram</code> creates the compute pipeline with <code>SDL_GPU_SHADERFORMAT_SPIRV</code>, and the ShaderEffect SPIR-V route (<code>SdlGpuEffectRenderer::CompileProgram</code> to <code>CreateShaderFromRemappedSpirvEXT</code>) does the same. With <code>CNA_SDL_GPU_SHADERCROSS=ON</code>, the default on Windows and macOS, the device may be created on SDL_gpu's Direct3D 12 or Metal driver; there the capability is true and creation fails, so <code>CNA::Graphics::ComputeShader</code> throws <code>ShaderCompilationExceptionEXT</code> carrying SDL's error instead of the <code>NotSupportedException</code> a false capability would give, and a shader package that prefers its SPIR-V variant selects a payload the device cannot load.</p>",
   "sources": [
    {
     "path": "modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp",
     "note": "SdlGpuRenderer::SupportsComputeShadersEXT and SdlGpuRenderer::SupportsShaderLanguageEXT"
    },
    {
     "path": "modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp",
     "note": "CreateSdlGpuShaderEXT and SelectSdlGpuShaderCreationRouteEXT (the stock-shader shadercross route)"
    },
    {
     "path": "modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp",
     "note": "CreateShaderFromRemappedSpirvEXT and SdlGpuEffectRenderer::CompileProgram (raw SPIR-V ShaderEffect intake)"
    },
    {
     "path": "modules/renderers/sdl-gpu/src/SdlGpuModern.cpp",
     "note": "SdlGpuComputeShaderRenderer::CompileProgram passes SDL_GPU_SHADERFORMAT_SPIRV"
    },
    {
     "path": "cmake/RendererSelection.cmake",
     "note": "CNA_SDL_GPU_SHADERCROSS defaults ON on WIN32 and APPLE; D3D12 and Metal need translated shaders"
    },
    {
     "path": "modules/renderers/sdl-gpu/CMakeLists.txt",
     "note": "comment: D3D12 and Metal cannot consume ShaderEffect's raw SPIR-V path"
    },
    {
     "path": "modules/graphics-ext/src/ComputeShader.cpp",
     "note": "ComputeShader::compile: capability check, then ShaderCompilationExceptionEXT on an invalid program"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed; no Direct3D 12 or Metal run exists for this entry. That SDL_gpu's Direct3D 12 and Metal drivers need translated shaders is CNA's own statement in <code>cmake/RendererSelection.cmake</code>, in <code>modules/renderers/sdl-gpu/CMakeLists.txt</code> and in the constructor's refusal when no driver accepts SPIR-V. On Linux SDL_gpu offers only its Vulkan driver, where both answers are correct and <code>sdlgpu_smoke_test.cpp</code> asserts them. Which driver SDL picks on a Windows host that has both Vulkan and Direct3D 12 was not established; on macOS the device is Metal. Engine-layer consumers mostly survive the claim: <code>ClusteredLightCompute</code> catches the failure and uses its CPU path, and the passes that also require <code>CustomEffects</code> are off because that capability is false on Windows and Apple builds.</p>",
   "reproduction": "<pre><code class=\"language-cpp\">// Illustrative; not compiled or run for this entry. SDL_GPU build with CNA_SDL_GPU_SHADERCROSS=ON,\n// on macOS (SDL_gpu's Metal driver) or on Windows with SDL_GPU_DRIVER=direct3d12.\nusing namespace Microsoft::Xna::Framework::Graphics;\nGraphicsDevice device;\nbool claimed = device.SupportsCapability(CNA::GraphicsCapability::ComputeShaders); // true\nCNA::Graphics::ComputeShader shader(device, spirvComputeSource);\n// Expected with a truthful capability: NotSupportedException before any creation.\n// Actual: ShaderCompilationExceptionEXT carrying \"SDL_CreateGPUComputePipeline failed: ...\".</code></pre>",
   "tests_current": "<p><code>sdlgpu_smoke_test.cpp</code> asserts that the device-level ComputeShaders and IndirectDraw answers equal the renderer's and are true, on Vulkan. <code>SdlGpu_HeadlessGraphicsDevice</code> and <code>SdlGpu_BackbufferFormat</code> are portable to a headless Direct3D 12 run through CNA's isolated Wine/vkd3d launcher, but neither creates a compute pipeline or a SPIR-V ShaderEffect. No test runs either intake on a non-SPIR-V driver.</p>",
   "regression_test": "<p>Make both answers depend on <code>SDL_GetGPUShaderFormats(device_)</code> including <code>SDL_GPU_SHADERFORMAT_SPIRV</code> (or route both intakes through SDL_shadercross), and extend the Direct3D 12 probe run to assert that <code>SupportsCapability(ComputeShaders)</code> equals whether a trivial compute pipeline can be created.</p>",
   "blast_radius": "<p>SDL_GPU on a Direct3D 12 or Metal driver only (a shadercross build, the default on Windows and macOS): CNAEXT <code>ComputeShader</code>, storage-buffer compute work and shader packages that select a SPIR-V variant. Vulkan-driver SDL_GPU (every Linux build) and stock drawing are unaffected.</p>",
   "workaround": "<p>On Windows, select SDL_gpu's Vulkan driver (<code>SDL_GPU_DRIVER=vulkan</code>) where one is installed; on macOS, use another renderer for CNAEXT compute.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/sdl-gpu.html#shaders",
      "SDL_gpu renderer internals: shaders"
     ]
    ],
    "guide": [
     [
      "docs/rendering-backends.html#modern-features",
      "Graphics renderers: modern features"
     ],
     [
      "docs/cnaext-engine.html#renderers",
      "CNAEXT engine layer: renderers"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-040 (development/internals/graphics/sdl-gpu.html)"
  },
  {
   "cand_ids": [
    "P2-041",
    "P2-046",
    "P2-047",
    "P2-050"
   ],
   "id": "NEW-B5-06",
   "class": "bug",
   "title": "In a multi-renderer build, non-default renderers' example CTests are registered but run under the default renderer",
   "summary": "The SDL_GPU, SOFTWARE, STUB and VULKAN example blocks are entered for a non-default member of CNA_GRAPHICS_RENDERERS, yet their registrations select no renderer, so each executable runs under the build default.",
   "subsystem": "Testing & evidence",
   "status": "open",
   "severity": "medium",
   "confidence": "verified-by-reading",
   "public_contract": "Renderer example CTests (<code>SdlGpu_*</code>, <code>Software_*</code>, <code>Stub_Smoke</code>, <code>Vulkan_*</code>) in a <code>CNA_GRAPHICS_RENDERERS</code> build, including CNA's <code>multi-renderer</code> preset",
   "expected": "<p>A CTest named for a renderer tests that renderer. CNA already enforces this for three families: the HEADLESS and EasyGL example blocks are entered only when that family is the build default (<code>_cna_default_renderer_identity</code>, VKPAR-0003), and GL4-0035 made the OpenGL4-only examples skip unless the device actually runs OPENGL4, after a <code>static_cast</code> to the wrong renderer crashed in a multi-renderer tree.</p>",
   "actual": "<p><code>modules/renderers/CMakeLists.txt</code> re-points <code>CNA_GRAPHICS_RENDERER</code> to each family's identity while entering it, and the SDL_GPU, SOFTWARE, STUB and VULKAN example files guard only on <code>CNA_GRAPHICS_RENDERER STREQUAL \"&lt;identity&gt;\"</code> (VULKAN also on <code>CNA_BUILD_EXAMPLES</code>), so their blocks are entered for a non-default member. <code>cna_register_renderer_test</code> adds no <code>CNA_GRAPHICS_RENDERER</code> environment and the executables call no <code>SetPreferred</code>, so each runs under the build default. In CNA's <code>multi-renderer</code> preset (<code>HEADLESS;SOFTWARE;STUB</code>, default HEADLESS, tests on by default) <code>Stub_Smoke</code> runs on HEADLESS, <code>static_cast</code>s the HEADLESS renderer to <code>StubRenderer&amp;</code> (undefined behaviour) and, because HEADLESS also opens no window and initialises no SDL video, can pass while testing nothing of STUB; the <code>Software_*</code> suite runs its CPU-rasterizer assertions against HEADLESS. The SOFTWARE block even links <code>cna_renderer_software</code> for exactly this non-default case without selecting the renderer. Most other families' example files use the same identity-only guard.</p>",
   "sources": [
    {
     "path": "modules/renderers/CMakeLists.txt",
     "note": "per-family loop re-points CNA_GRAPHICS_RENDERER while entering each family"
    },
    {
     "path": "modules/renderers/sdl-gpu/examples/CMakeLists.txt",
     "note": "block guard on CNA_GRAPHICS_RENDERER STREQUAL SDL_GPU only"
    },
    {
     "path": "modules/renderers/software/examples/CMakeLists.txt",
     "note": "block guard and the non-default include/link fix in cna_software_test"
    },
    {
     "path": "modules/renderers/stub/examples/CMakeLists.txt",
     "note": "block guard on CNA_GRAPHICS_RENDERER STREQUAL STUB only"
    },
    {
     "path": "modules/renderers/stub/examples/stub_smoke_test.cpp",
     "note": "Draw: static_cast of the device renderer to StubRenderer&"
    },
    {
     "path": "modules/renderers/vulkan/examples/CMakeLists.txt",
     "note": "block guard on CNA_BUILD_EXAMPLES, CNA_BUILD_TESTS and the identity"
    },
    {
     "path": "modules/renderers/headless/examples/CMakeLists.txt",
     "note": "the contrasting _cna_default_renderer_identity guard (VKPAR-0003)"
    },
    {
     "path": "cmake/TestHelpers.cmake",
     "note": "cna_register_renderer_test sets no renderer selection"
    },
    {
     "path": "CMakePresets.json",
     "note": "multi-renderer preset: HEADLESS;SOFTWARE;STUB with HEADLESS as default"
    },
    {
     "path": "modules/renderers/opengl4/examples/opengl4_smoke_test.cpp",
     "note": "GL4-0035 run-time skip under another renderer"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; no multi-renderer build was configured or run for this entry. CNA's CI multi-renderer jobs build named targets (<code>CnaTests</code>, the selection demo) and run <code>CnaTests</code> and <code>CnaRendererDefaultSelection_*</code> only, so the misattributed example CTests do not run there; they are registered in any local multi-renderer tree and run by a plain <code>ctest</code>. <code>scripts/run-all-renderer-smoke-tests.sh --multi</code> sets <code>CNA_GRAPHICS_RENDERER</code> per renderer itself, which is CNA's documented way to smoke-test several renderers from one build (<code>docs/runtime-renderer-selection.md</code>). The GL4-0035 change records the analogous OpenGL4 failure: an unconditional cast to <code>OpenGL4Renderer</code> was undefined behaviour when a multi-renderer tree selected EasyGL at run time, and crashed.</p>",
   "reproduction": "<pre><code class=\"language-bash\"># Not executed for this entry.\ncmake --preset multi-renderer\ncmake --build cmake-build-multi --target cna_test_stub_smoke\nctest --test-dir cmake-build-multi -R '^Stub_Smoke$' --output-on-failure   # runs under HEADLESS</code></pre>",
   "tests_current": "<p><code>CnaRendererDefaultSelection_*</code> checks that the default is a member of the set, and CI proves each compiled-in renderer is reachable through <code>cna_demo_renderer_selection</code>; nothing checks that a renderer-named example ran on that renderer.</p>",
   "regression_test": "<p>Either guard each family's example block on <code>_cna_default_renderer_identity</code> as HEADLESS and EasyGL do, or have <code>cna_register_renderer_test</code> add <code>CNA_GRAPHICS_RENDERER=&lt;identity&gt;</code> to each registration's environment (the smoke script's mechanism), plus a configure-time check that in the <code>multi-renderer</code> preset every registered <code>Stub_*</code> and <code>Software_*</code> test carries its renderer selection.</p>",
   "blast_radius": "<p>Evidence produced by local multi-renderer trees, including CNA's own <code>multi-renderer</code> preset and sets such as <code>OPENGLES3;VULKAN;SOFTWARE;HEADLESS;STUB</code>: renderer-named example results describe the default renderer (false passes such as <code>Stub_Smoke</code>, false failures in suites that read pixels or cast the renderer). Single-renderer builds, <code>CnaTests</code> and the HEADLESS and EasyGL example blocks are unaffected.</p>",
   "workaround": "<p>Run such examples with <code>CNA_GRAPHICS_RENDERER=&lt;identity&gt;</code> in the environment, as <code>scripts/run-all-renderer-smoke-tests.sh --multi</code> does, or collect renderer evidence from a single-renderer build.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/selection.html#runtime",
      "Renderer selection internals: runtime policy"
     ],
     [
      "development/internals/graphics/software.html#tests",
      "SOFTWARE renderer internals: tests"
     ],
     [
      "development/internals/graphics/stub.html#tests",
      "STUB renderer internals: tests"
     ]
    ],
    "guide": [
     [
      "docs/runtime-renderer-selection.html#modes",
      "Runtime renderer selection: build modes"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#gpu",
      "Test architecture: GPU tests"
     ]
    ],
    "maintainer": [
     [
      "development/handbook/change-build-configuration.html#renderer-rules",
      "Change the build configuration: renderer rules"
     ]
    ]
   },
   "origin": "Phase-2 candidates P2-041, P2-046, P2-047 and P2-050 (sdl-gpu, software, stub and vulkan Development pages)"
  },
  {
   "cand_ids": [
    "P2-042"
   ],
   "id": "NEW-B5-07",
   "class": "bug",
   "title": "SdlGpu_2D_HeadlessDriver forces headless mode under the x11 video driver that the SDL_GPU constructor refuses for it",
   "summary": "On non-Windows hosts the SDL_GPU headless registrations reuse the ordinary test environment (SDL_VIDEODRIVER=x11 by default), but SdlGpu_2D_HeadlessDriver also sets CNA_SDLGPU_TEST_FORCE_HEADLESS=1, which the constructor accepts only under dummy or offscreen.",
   "subsystem": "Testing & evidence",
   "status": "open",
   "severity": "low",
   "confidence": "reproduced",
   "public_contract": "The <code>SdlGpu_2D_HeadlessDriver</code> CTest (SDLGPU-102) and the <code>CNA_SDLGPU_TEST_VIDEO_DRIVER</code> cache option",
   "expected": "<p>SDLGPU-102's registration comment: run \"the canonical, unchanged Game-based Texture2D/SpriteBatch scene with a normal platform window ... while replacing only SDL_gpu presentation with the renderer-owned virtual backbuffer\". A routinely registered CTest should pass in the default configuration of the host it is registered for.</p>",
   "actual": "<p>In <code>modules/renderers/sdl-gpu/examples/CMakeLists.txt</code>, <code>CNA_SDLGPU_TEST_VIDEO_DRIVER</code> defaults to <code>x11</code> on Linux-like hosts, making <code>CNA_SDLGPU_TEST_ENVIRONMENT</code> <code>SDL_VIDEODRIVER=x11;DISPLAY=...;WAYLAND_DISPLAY=</code>; on non-Windows hosts <code>CNA_SDLGPU_HEADLESS_TEST_ENVIRONMENT</code> is the same string. <code>SdlGpu_2D_HeadlessDriver</code> appends <code>CNA_SDLGPU_TEST_FORCE_HEADLESS=1</code>. <code>SdlGpuRenderer::SdlGpuRenderer</code> throws \"CNA_SDLGPU_TEST_FORCE_HEADLESS=1 requires the SDL dummy or offscreen video driver\" whenever the switch is set, a window exists (the 2D test is a <code>Game</code>) and <code>SDL_GetCurrentVideoDriver()</code> is not <code>dummy</code> or <code>offscreen</code>. With a display present the test fails at device creation; without one, <code>ProbeGpuDisplayAvailable</code> skips it. Only a configuration with <code>CNA_SDLGPU_TEST_VIDEO_DRIVER=dummy</code> or <code>offscreen</code> can pass it.</p>",
   "sources": [
    {
     "path": "modules/renderers/sdl-gpu/examples/CMakeLists.txt",
     "note": "CNA_SDLGPU_TEST_VIDEO_DRIVER default, CNA_SDLGPU_HEADLESS_TEST_ENVIRONMENT and the SdlGpu_2D_HeadlessDriver registration"
    },
    {
     "path": "modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp",
     "note": "SdlGpuRenderer::SdlGpuRenderer: the forced-headless refusal; IsIsolatedVideoDriver"
    },
    {
     "path": "modules/renderers/sdl-gpu/examples/sdlgpu_2d_test.cpp",
     "note": "main: ProbeGpuDisplayAvailable, then Game::Run"
    },
    {
     "path": "plans/plan_pre_sdlgpu_closeout.md",
     "note": "PSG-0009: the 27 classic SDL_GPU failures, this test among them"
    },
    {
     "path": "plans/plan_street_sdlgpu.md",
     "note": "STREETS-0008: 26 failures remain, the old 27 minus SdlGpu_Smoke"
    }
   ],
   "evidence": "<p>CNA's own record executed it: <code>SdlGpu_2D_HeadlessDriver</code> is in PSG-0009's list of classic SDL_GPU failures (private compositor, one Radeon 780M under RADV) and remains among the 26 failures of the latest recorded run (STREETS-0008). That the forced-headless refusal is the cause is by reading at 009d40f5; the failing run's log is not in the tree. The same record names 24 further failing classic SDL_GPU tests that are not analysed here: SdlGpu_BackbufferFormat, SdlGpu_ConstructorExceptionSafety, SdlGpu_Deferred_Scissor, SdlGpu_DepthBias, SdlGpu_DepthlessCube_SpriteBatchCompatibility, SdlGpu_DrawLineTopology, SdlGpu_EasyGLOracle_depthstencilstate_stencil_twosided, SdlGpu_EasyGLOracle_draw_range_validation, SdlGpu_EasyGLOracle_surface_format_throws, SdlGpu_Parity_compressed_cube, SdlGpu_Parity_compressed_cube_fallback, SdlGpu_Parity_dual_texture_terms, SdlGpu_RenderState, SdlGpu_RenderTargetCube_Formats, SdlGpu_RenderTargetCube_MsaaMip, SdlGpu_RenderTarget_SetData, SdlGpu_SwapchainRecovery, SdlGpu_Texture2D_FormatMatrix, SdlGpu_Texture2D_FormatMatrixFallback, SdlGpu_TextureCube_FormatMatrix, SdlGpu_TextureCube_FormatMatrixFallback, SdlGpu_TextureFilterMipContract, SdlGpuIndexedDrawRangeTest.MutatingSourceBuffersAfterQueuingDoesNotChangeQueuedDraws and SdlGpuIndexedDrawRangeTest.RejectsIndexedRangesOutsideTheBoundBuffers. The 26th, <code>SdlGpu_OcclusionQuery_Limitation</code>, is analysed under CNA-BUG-012.</p>",
   "reproduction": "<pre><code class=\"language-bash\"># Not executed for this entry. A Linux SDL_GPU build with tests and the default CNA_SDLGPU_TEST_VIDEO_DRIVER (x11), on a host with a display:\nctest --test-dir &lt;sdl-gpu build&gt; -R '^SdlGpu_2D_HeadlessDriver$' --output-on-failure\n# A build configured with -DCNA_SDLGPU_TEST_VIDEO_DRIVER=offscreen is expected to pass it.</code></pre>",
   "tests_current": "<p><code>SdlGpu_HeadlessGraphicsDevice</code> uses the same environment but creates its device without a window, so the refusal does not apply; <code>SdlGpu_2D</code> runs the same scene without the switch. Nothing checks that a registration setting <code>CNA_SDLGPU_TEST_FORCE_HEADLESS=1</code> also selects an isolated video driver.</p>",
   "regression_test": "<p>Give the forced-headless registrations an isolated driver on non-Windows hosts (for example <code>SDL_VIDEODRIVER=offscreen;DISPLAY=;WAYLAND_DISPLAY=</code> in <code>CNA_SDLGPU_HEADLESS_TEST_ENVIRONMENT</code>) and re-run the classic <code>^SdlGpu</code> set to confirm the test leaves the failure list.</p>",
   "blast_radius": "<p>Test evidence for SDL_GPU on Linux-like hosts: one routinely registered CTest fails for a configuration reason and inflates the recorded failure count, and the forced-headless presentation path itself goes unexercised there. On Windows the headless registrations select SDL's dummy driver and are unaffected.</p>",
   "workaround": "<p>Configure with <code>-DCNA_SDLGPU_TEST_VIDEO_DRIVER=offscreen</code> (or <code>dummy</code>) when running the headless SDL_GPU tests.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/sdl-gpu.html#tests",
      "SDL_gpu renderer internals: tests"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#skip-convention",
      "Test architecture: skip convention"
     ]
    ],
    "guide": [
     [
      "docs/verification.html#renderers",
      "Verification: renderers"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-042 (development/internals/graphics/sdl-gpu.html)"
  },
  {
   "cand_ids": [
    "P2-043"
   ],
   "id": "NEW-B5-08",
   "class": "bug",
   "title": "GraphicsRendererSelection::IsLatched() is documented to latch when the first GraphicsDevice begins construction; it latches on successful resolution",
   "summary": "The public comment on IsLatched() says the selection latches when the first GraphicsDevice begins construction, but GraphicsDevice::resolveRenderer latches only after a candidate renderer was created, and a failed resolution deliberately leaves it open.",
   "subsystem": "Documentation & release tooling",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>CNA::GraphicsRendererSelection::IsLatched()</code> (CNAEXT) documentation",
   "expected": "<p>The comment on <code>IsLatched()</code> tells callers from when <code>SetPreferred</code>, <code>SetFallbackChain</code> and <code>EnableAutomaticFallback</code> are refused.</p>",
   "actual": "<p><code>GraphicsRendererSelection.hpp</code>: \"Becomes true when the first GraphicsDevice begins construction.\" <code>GraphicsDevice::resolveRenderer</code> calls <code>GraphicsRendererSelectionAccessEXT::Latch(candidate-&gt;type)</code> at its end, after a candidate was created; its comment (plan RTR-P5-18) explains that latching at the start was \"the first cut, and it was wrong\". While the fallback chain is being walked <code>IsLatched()</code> is false, and after a resolution that created nothing it stays false (<code>GraphicsRendererFallbackTest.AFailedResolutionDoesNotLatch</code>).</p>",
   "sources": [
    {
     "path": "modules/core/include/CNA/GraphicsRendererSelection.hpp",
     "note": "IsLatched documentation comment"
    },
    {
     "path": "modules/graphics/src/Xna/GraphicsDevice.cpp",
     "note": "GraphicsDevice::resolveRenderer: the Latch call and its RTR-P5-18 comment"
    },
    {
     "path": "modules/core/src/GraphicsRendererSelection.cpp",
     "note": "GraphicsRendererSelectionAccessEXT::Latch; SetPreferred refuses when latched"
    },
    {
     "path": "modules/graphics/tests/CNA/GraphicsRendererFallbackTests.cpp",
     "note": "AFailedResolutionDoesNotLatch and SubstitutionStillLatchesTheSelection"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The tests pin the code, not the comment.</p>",
   "reproduction": null,
   "tests_current": "<p><code>GraphicsRendererSelectionTests.cpp</code>, <code>GraphicsRendererFallbackTests.cpp</code> (<code>SubstitutionStillLatchesTheSelection</code>, <code>AFailedResolutionDoesNotLatch</code>, <code>RecoveringAfterAFailedResolutionActuallyWorks</code>) and <code>MultiRendererFallbackTests.cpp</code>.</p>",
   "regression_test": "<p>Documentation fix (\"becomes true once the first GraphicsDevice has created its renderer\"); the fallback tests already pin the behaviour.</p>",
   "blast_radius": "<p>Readers of the public header and generated API reference, and code that polls <code>IsLatched()</code> to decide whether it may still call <code>SetPreferred</code>. Behaviour is unaffected.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/graphics/selection.html#latch",
      "Renderer selection internals: runtime policy and latch"
     ],
     [
      "development/internals/modules/core.html#selection",
      "Core module internals: renderer selection"
     ]
    ],
    "guide": [
     [
      "docs/runtime-renderer-selection.html#api",
      "Runtime renderer selection: API"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-043 (development/internals/graphics/selection.html)"
  },
  {
   "cand_ids": [
    "P2-044"
   ],
   "id": "NEW-B5-09",
   "class": "bug",
   "title": "A GraphicsDevice constructor that fails after renderer resolution leaves the selection latched to the renderer it destroyed",
   "summary": "The GraphicsDevice constructor latches the selection inside resolveRenderer(), then updates the viewport and pushes default states; if one of those throws, the catch block destroys the renderer without unlatching, so SetPreferred is refused.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>GraphicsDevice</code> construction failure path and <code>CNA::GraphicsRendererSelection::SetPreferred</code> / <code>IsLatched</code> / <code>GetActive</code>",
   "expected": "<p>The design stated in <code>GraphicsDevice::resolveRenderer</code> (plan RTR-P5-18): a construction that throws must not freeze the selection, otherwise \"a game that caught the error could not then try a different configuration -- and GetActive() would report a renderer that had never been created\". <code>GraphicsRendererFallbackTest.AFailedResolutionDoesNotLatch</code> and <code>RecoveringAfterAFailedResolutionActuallyWorks</code> pin this for a resolution that created nothing.</p>",
   "actual": "<p>The constructor's <code>try</code> block calls <code>resolveRenderer()</code>, which ends with <code>GraphicsRendererSelectionAccessEXT::Latch</code>, and then <code>UpdateViewportFromWindow()</code>, <code>setBlendStateProperty(BlendState::Opaque)</code>, <code>setDepthStencilStateProperty(DepthStencilState::Default)</code> (when the renderer has depth/stencil) and <code>setRasterizerStateProperty(RasterizerState::CullCounterClockwise)</code>. Its <code>catch (...)</code> calls <code>destroyNativeResources()</code>, releases the video subsystem and rethrows; nothing resets the latch (only the test helper <code>ResetForTestingEXT</code> does). If any of those four steps throws, the renderer is gone, <code>IsLatched()</code> stays true, <code>GetActive()</code> names the destroyed renderer, and <code>SetPreferred</code> or <code>SetFallbackChain</code> throw <code>InvalidOperationException</code>, so the game cannot retry with another renderer in the same process.</p>",
   "sources": [
    {
     "path": "modules/graphics/src/Xna/GraphicsDevice.cpp",
     "note": "GraphicsDevice constructor try/catch (destroyNativeResources, no unlatch) and GraphicsDevice::resolveRenderer (Latch at the end)"
    },
    {
     "path": "modules/core/src/GraphicsRendererSelection.cpp",
     "note": "GraphicsRendererSelectionAccessEXT::Latch; ResetForTestingEXT is the only unlatch"
    },
    {
     "path": "modules/graphics/tests/CNA/GraphicsRendererFallbackTests.cpp",
     "note": "AFailedResolutionDoesNotLatch covers only failures before a renderer exists"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The missing unlatch follows mechanically from the code; whether any current renderer can throw from those state pushes or the viewport update on a freshly created device was not established, so the window may be narrow. No test injects a failure after resolution.</p>",
   "reproduction": null,
   "tests_current": "<p><code>GraphicsRendererFallbackTests.cpp</code> and <code>MultiRendererFallbackTests.cpp</code> cover failures during resolution (for example through <code>CNA_DEBUG_FAIL_RENDERER_INIT</code>), not after it.</p>",
   "regression_test": "<p>A fallback test with a test renderer whose state application throws after creation, asserting afterwards that <code>IsLatched()</code> is false and <code>SetPreferred</code> is accepted; the fix is to unlatch (and clear the active renderer) in the constructor's catch block when this construction performed the latch.</p>",
   "blast_radius": "<p>Games that catch a failed <code>GraphicsDevice</code> construction and retry with another renderer, and multi-renderer test fixtures that construct devices repeatedly in one process. Successful constructions and failures during resolution are unaffected.</p>",
   "workaround": "<p>Choose the renderer before the first device and treat a failed construction as fatal for the process.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/selection.html#resolve",
      "Renderer selection internals: resolution"
     ],
     [
      "development/internals/graphics/device.html#construct",
      "GraphicsDevice internals: construction"
     ]
    ],
    "guide": [
     [
      "docs/runtime-renderer-selection.html#fallback",
      "Runtime renderer selection: fallback"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-044 (development/internals/graphics/selection.html)"
  },
  {
   "cand_ids": [
    "P2-045"
   ],
   "id": "NEW-B5-10",
   "class": "bug",
   "title": "modules/renderers/CMakeLists.txt enters the shared D3D and GDI helper directories only when DIRECTX11/12 or GDI is the default",
   "summary": "Before its per-family loop the file adds common/d3d, and the GDI configuration of software, only when the default renderer matches, so a Windows multi-renderer build with DIRECTX11, DIRECTX12 or GDI as a non-default member lacks the targets it links.",
   "subsystem": "Build & CI",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>CNA_GRAPHICS_RENDERERS</code> multi-renderer builds on Windows that <code>cmake/RendererCombinations.cmake</code> accepts",
   "expected": "<p>A set accepted by <code>cna_validate_renderer_combination</code> builds. The loop's own comment says each family must be entered with its own identity re-established regardless of which member is the default, and <code>docs/runtime-renderer-selection.md</code> lists the only refused sets (PORTABLEGL beside real GL, GDI beside SOFTWARE, cross-platform pairs).</p>",
   "actual": "<p>Two helper entries sit before the loop and read <code>CNA_GRAPHICS_RENDERER</code>, which at that point names the default: <code>add_subdirectory(common/d3d)</code> only if it is DIRECTX11 or DIRECTX12, and <code>add_subdirectory(software)</code> (the GDI configuration that publishes <code>CNA_GDI_SOFTWARE_SOURCES</code> and defines <code>cna_renderer_software_headers</code>) only if it is GDI. The loop then enters <code>directx11</code> and <code>directx12</code>, which link <code>cna_renderer_d3dcommon</code> PUBLIC and whose public headers include D3DCommon headers, and <code>gdi</code>, which calls <code>cna_add_renderer(${CNA_GDI_SOFTWARE_SOURCES})</code> and links <code>cna_renderer_software_headers</code>. With <code>DIRECTX9;DIRECTX11</code> (default DIRECTX9) or <code>DIRECTX11;GDI</code> (default DIRECTX11), the non-default family refers to a target that was never created and compiles without the headers it includes; neither set is refused by the combination rules.</p>",
   "sources": [
    {
     "path": "modules/renderers/CMakeLists.txt",
     "note": "pre-loop guards for common/d3d and the GDI software configuration; the per-family loop"
    },
    {
     "path": "modules/renderers/directx11/CMakeLists.txt",
     "note": "links cna_renderer_d3dcommon PUBLIC"
    },
    {
     "path": "modules/renderers/directx12/CMakeLists.txt",
     "note": "links cna_renderer_d3dcommon PUBLIC"
    },
    {
     "path": "modules/renderers/common/d3d/CMakeLists.txt",
     "note": "defines cna_renderer_d3dcommon"
    },
    {
     "path": "modules/renderers/gdi/CMakeLists.txt",
     "note": "cna_add_renderer(CNA_GDI_SOFTWARE_SOURCES) and cna_renderer_software_headers"
    },
    {
     "path": "modules/renderers/software/CMakeLists.txt",
     "note": "GDI branch publishing CNA_GDI_SOFTWARE_SOURCES and cna_renderer_software_headers"
    },
    {
     "path": "cmake/RendererCombinations.cmake",
     "note": "cna_validate_renderer_combination accepts these sets"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; no Windows multi-renderer build was configured. CMake treats a <code>target_link_libraries</code> item that names no target as a plain library name, so the D3D case would surface as a compile error (missing D3DCommon include root) or a link error rather than as a clear configure message; the GDI case loses the shared CPU-2D sources and the Software include root. No CI job or CNA record builds a Windows multi-renderer set; the verified sets in <code>docs/runtime-renderer-selection.md</code> are all non-Windows.</p>",
   "reproduction": "<pre><code class=\"language-bash\"># Not executed for this entry (needs a Windows toolchain).\ncmake -S . -B build -DCNA_GRAPHICS_RENDERER=DIRECTX9 -DCNA_GRAPHICS_RENDERERS=\"DIRECTX9;DIRECTX11\"\ncmake --build build</code></pre>",
   "tests_current": "<p><code>CnaRendererDefaultSelection_*</code> and <code>scripts/check_renderer_combinations.py</code> check membership and the rule lists, not that every accepted set configures. CI's multi-renderer sets are <code>HEADLESS;SOFTWARE;STUB</code>, <code>OPENGL33;VULKAN;SOFTWARE;HEADLESS</code> and the Emscripten browser set.</p>",
   "regression_test": "<p>Move both helper entries into the loop (enter <code>common/d3d</code> when any member is DIRECTX11 or DIRECTX12, the GDI software configuration when GDI is a member), and configure <code>DIRECTX9;DIRECTX11</code> and <code>DIRECTX11;GDI</code> once on the Windows cross-compile lane.</p>",
   "blast_radius": "<p>Windows multi-renderer builds whose default is not the DIRECTX11, DIRECTX12 or GDI member. Single-renderer builds, and sets in which that member is the default, are unaffected; no non-Windows family uses these helpers.</p>",
   "workaround": "<p>Make the DIRECTX11, DIRECTX12 or GDI member the default of the set, or build those renderers separately.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/selection.html#configure",
      "Renderer selection internals: configure-time sequence"
     ]
    ],
    "architecture": [
     [
      "development/build/architecture.html#axis-renderer",
      "Build architecture: the renderer axis"
     ]
    ],
    "maintainer": [
     [
      "development/handbook/change-build-configuration.html#renderer-rules",
      "Change the build configuration: renderer rules"
     ]
    ],
    "guide": [
     [
      "docs/rendering-backends.html#combination-rules",
      "Graphics renderers: combination rules"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-045 (development/internals/graphics/selection.html); the GDI half found during re-verification"
  },
  {
   "cand_ids": [
    "P2-048"
   ],
   "id": "NEW-B5-11",
   "class": "bug",
   "title": "Stub_Smoke needs SDL3 although the STUB renderer does not, so a STUB test build without SDL cannot configure",
   "summary": "stub_smoke_test.cpp includes the SDL3 header unconditionally and the cna_stub_test macro links SDL3::SDL3 with no if(TARGET) guard, unlike the HEADLESS and SOFTWARE example files, so STUB with CNA_ENABLE_SDL=OFF and tests on fails at generate time.",
   "subsystem": "Build & CI",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "Building the STUB renderer with <code>CNA_BUILD_TESTS=ON</code> (the default) and <code>CNA_ENABLE_SDL=OFF</code>",
   "expected": "<p>STUB \"touches no SDL window/video subsystem/GPU library\" (<code>modules/renderers/stub/CMakeLists.txt</code>), and the other SDL-free-capable renderers' example files cope with a configuration without SDL: HEADLESS links SDL3 only <code>if(TARGET SDL3::SDL3)</code> (X11-0090) and SOFTWARE returns early with a status message (NPV-0106).</p>",
   "actual": "<p>The <code>cna_stub_test</code> macro in <code>modules/renderers/stub/examples/CMakeLists.txt</code> links <code>SDL3::SDL3</code> in both of its branches, and <code>stub_smoke_test.cpp</code> includes the SDL3 header, calls <code>SDL_WasInit</code> (check A) and casts the window handle to <code>SDL_Window*</code> (check B). In a <code>CNA_ENABLE_SDL=OFF</code> configuration with STUB selected (or in <code>CNA_GRAPHICS_RENDERERS</code>) and tests on, CMake stops at generate time because a <code>::</code>-qualified link item names no target.</p>",
   "sources": [
    {
     "path": "modules/renderers/stub/examples/CMakeLists.txt",
     "note": "cna_stub_test macro links SDL3::SDL3 unconditionally"
    },
    {
     "path": "modules/renderers/stub/examples/stub_smoke_test.cpp",
     "note": "SDL3 include; checks A and B use SDL"
    },
    {
     "path": "modules/renderers/headless/examples/CMakeLists.txt",
     "note": "cna_headless_test: if(TARGET SDL3::SDL3) guard"
    },
    {
     "path": "modules/renderers/software/examples/CMakeLists.txt",
     "note": "early return when SDL3::SDL3 is absent"
    },
    {
     "path": "modules/renderers/stub/CMakeLists.txt",
     "note": "the renderer itself needs no SDL"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; such a configuration was not configured. CMake policy CMP0028 (NEW since CMake 3.0; the project requires 3.20) makes a <code>::</code> link item that is not a target a generate-time error. Checks A and B are the smoke test's only SDL uses.</p>",
   "reproduction": "<pre><code class=\"language-bash\"># Not executed for this entry.\ncmake -S . -B build -DCNA_ENABLE_SDL=OFF -DCNA_PLATFORM=X11 -DCNA_GRAPHICS_RENDERER=STUB -DCNA_BUILD_TESTS=ON</code></pre>",
   "tests_current": "<p><code>Stub_Smoke</code> is the only family-local STUB test. The SDL-free CI lanes in <code>platform-ci.yml</code> build <code>OPENGL33;VULKAN;SOFTWARE;HEADLESS</code>, never STUB, so the configuration is not exercised.</p>",
   "regression_test": "<p>Guard the SDL3 link with <code>if(TARGET SDL3::SDL3)</code> and the SDL checks with a define (as HEADLESS does with <code>CNA_HEADLESS_TEST_HAS_SDL</code>), and add STUB to one SDL-free configure lane.</p>",
   "blast_radius": "<p>Only configurations that build STUB with tests and without SDL. Builds with SDL, STUB builds with <code>CNA_BUILD_TESTS=OFF</code>, and the renderer itself are unaffected.</p>",
   "workaround": "<p>Configure with <code>CNA_BUILD_TESTS=OFF</code>, or keep SDL enabled, when STUB is selected.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/stub.html#tests",
      "STUB renderer internals: tests"
     ]
    ],
    "architecture": [
     [
      "development/build/architecture.html#axis-sdl",
      "Build architecture: the SDL axis"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#platform",
      "Test architecture: platform tests"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-048 (development/internals/graphics/stub.html)"
  },
  {
   "cand_ids": [
    "P2-049"
   ],
   "id": "NEW-B5-12",
   "class": "bug",
   "title": "StubTextureRenderer's comment says GetData() is accepted and discarded, but the inherited default refuses the read",
   "summary": "The class comment on StubTextureRenderer says SetData()/GetData() are accepted no-op defaults, while the ITextureRenderer::GetData default it inherits returns false, which the shared Texture2D layer turns into NotSupportedException.",
   "subsystem": "Documentation & release tooling",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "Maintainer comment on <code>CNA::Internal::Renderers::Stub::StubTextureRenderer</code>",
   "expected": "<p>The class comment describes what the STUB texture does with uploads and reads.</p>",
   "actual": "<p><code>StubRenderer.hpp</code>: \"Stores only width/height -- no pixel data of any kind; SetData()/GetData() are accepted (inherited no-op defaults) and simply discarded.\" Uploads are indeed discarded, but <code>ITextureRenderer::GetData(level, x, y, w, h, data, dataLength)</code> defaults to <code>false</code> (\"this renderer read nothing back\"), and <code>Texture2D</code> raises <code>System::NotSupportedException</code> on <code>false</code> wherever it needs the renderer's copy (render-target content, partial updates, block-compressed data). Reads are refused, not accepted.</p>",
   "sources": [
    {
     "path": "modules/renderers/stub/include/CNA/Internal/Renderers/Stub/StubRenderer.hpp",
     "note": "StubTextureRenderer class comment"
    },
    {
     "path": "modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp",
     "note": "ITextureRenderer::GetData default returns false, with its rationale"
    },
    {
     "path": "modules/graphics/src/Xna/Texture2D.cpp",
     "note": "Texture2D raises NotSupportedException when the renderer read nothing back"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The interface comment explains why the default became a refusal: a silent no-op default \"fabricated a complete, uniformly transparent-black frame\". Only the STUB comment is stale; behaviour follows the interface.</p>",
   "reproduction": null,
   "tests_current": "<p><code>Stub_Smoke</code> uploads a texture but never reads one back; CNA's Stub lane records classify the shared suites' cube-texture refusals as the expected STUB taxonomy.</p>",
   "regression_test": "<p>No test is needed; correct the comment to say reads are refused through the interface default.</p>",
   "blast_radius": "<p>Maintainers reading the STUB renderer. No behaviour is affected.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/graphics/stub.html#behavior",
      "STUB renderer internals: behaviour"
     ],
     [
      "development/internals/graphics/resources.html#failure",
      "Textures and render targets: failure paths"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-049 (development/internals/graphics/stub.html)"
  },
  {
   "cand_ids": [
    "P2-051"
   ],
   "id": "NEW-B5-13",
   "class": "bug",
   "title": "VulkanRenderer's constructor leaks the Vulkan handles it created when a later construction step throws",
   "summary": "VulkanRenderer::VulkanRenderer creates the instance, debug messenger, device, swapchain and other raw handles in sequence with no cleanup path, so a throw from a later step such as PickPhysicalDevice leaves them undestroyed.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>GraphicsDevice</code> construction on VULKAN, including runtime fallback from VULKAN to another renderer",
   "expected": "<p>A failed renderer construction releases what it created, so that a fallback chain can move to the next renderer without leaking, and so that Vulkan object tracking and LeakSanitizer stay clean. <code>VulkanRenderer::~VulkanRenderer</code> is written for partial states (it destroys only the instance when <code>device_</code> is null), but a destructor never runs for an object whose constructor threw.</p>",
   "actual": "<p>The constructor body calls <code>CreateInstance</code>, <code>SetupDebugMessenger</code> (validation builds), <code>CreateSurface</code>, <code>PickPhysicalDevice</code>, <code>CreateLogicalDevice</code>, <code>CreateSwapchain</code> and then image views, depth resources, render passes, framebuffers, command pool and buffers, sync objects, sampler, descriptor layout and pool and sprite buffers, with no <code>try</code>/<code>catch</code> and no scope guard. The handles are plain members (<code>VkInstance instance_</code>, <code>VkDebugUtilsMessengerEXT debugMessenger_</code>, <code>VkDevice device_</code>, <code>VkSwapchainKHR swapchain_</code> and more), so only RAII members such as <code>platformSurface_</code> are cleaned up while the exception unwinds. <code>PickPhysicalDevice</code> throws \"Vulkan: no GPU found\" or \"Vulkan: no suitable GPU\" after the instance and surface exist, so every failed attempt on a host without a usable Vulkan device leaks the instance (and the debug messenger in validation builds); a throw after <code>CreateLogicalDevice</code> also leaks the device and everything created on it.</p>",
   "sources": [
    {
     "path": "modules/renderers/vulkan/src/VulkanRenderer.cpp",
     "note": "VulkanRenderer::VulkanRenderer (no cleanup path), VulkanRenderer::PickPhysicalDevice and VulkanRenderer::~VulkanRenderer"
    },
    {
     "path": "modules/renderers/vulkan/include/CNA/Internal/Renderers/Vulkan/VulkanRenderer.hpp",
     "note": "raw handle members instance_, debugMessenger_, device_, swapchain_"
    },
    {
     "path": "modules/renderers/vulkan/examples/vulkan_unavailable_device_skip.cpp",
     "note": "VULKAN-013: the two routine construction failures"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. In C++ only fully constructed members are destroyed when a constructor throws; the class destructor does not run. VULKAN-013's terminate handler exists because <code>PickPhysicalDevice</code>'s two refusals are routine on machines without a usable device. SDL_GPU has a comparable guard and test (<code>SdlGpu_ConstructorExceptionSafety</code>); VULKAN has neither.</p>",
   "reproduction": null,
   "tests_current": "<p><code>vulkan_unavailable_device_skip.cpp</code> turns the two expected failures into a CTest skip; the VULKAN-408 validation-output gate reports leaks only for a device that is destroyed. No test constructs the renderer with a failure injected after <code>CreateInstance</code>.</p>",
   "regression_test": "<p>Give the constructor a cleanup path (a function-try-block or a scope guard calling a teardown that tolerates partial state) and add a failure-injection seam after each creation step, asserting under the validation layer's object tracking that no handle survives a failed construction.</p>",
   "blast_radius": "<p>VULKAN only, on hosts where construction fails part-way (no suitable GPU, surface or swapchain failure, out of memory). A fallback chain that continues to another renderer keeps the leaked handles for the life of the process, and sanitizer or validation-layer runs on such hosts report them. Successful constructions are unaffected.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/graphics/vulkan.html#construct",
      "Vulkan renderer internals: construction"
     ]
    ],
    "maintainer": [
     [
      "development/handbook/debug-shutdown-and-lifetime.html#order",
      "Debug shutdown and lifetime"
     ]
    ],
    "guide": [
     [
      "docs/runtime-renderer-selection.html#fallback",
      "Runtime renderer selection: fallback"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-051 (development/internals/graphics/vulkan.html)"
  },
  {
   "cand_ids": [
    "P2-095"
   ],
   "id": "NEW-B5-14",
   "class": "bug",
   "title": "RenderPipeline::releaseDeviceResourcesEXT() keeps the Bloom, SSAO and volumetric-fog target pools, and the memory estimate never counts them",
   "summary": "The DeviceReset handler documented to drop every target the pipeline owns resets only the scene target and the chain's pool; the private pools of BloomPass, SsaoPass and VolumetricFogPass survive, and getGpuMemoryEstimateBytes() excludes them.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>CNA::Graphics::RenderPipeline::releaseDeviceResourcesEXT()</code> and <code>getGpuMemoryEstimateBytes()</code> / <code>FrameStatistics::gpuMemoryEstimateBytes</code> (CNAEXT)",
   "expected": "<p><code>RenderPipeline.hpp</code>: <code>releaseDeviceResourcesEXT()</code> \"Drops every target the pipeline owns, so the next frame allocates fresh ones\" and is called automatically on <code>DeviceReset</code>, because \"after a context loss every GPU object the pipeline held names storage the driver has already destroyed\"; <code>getGpuMemoryEstimateBytes()</code> \"Returns an estimate of the GPU memory the pipeline's own targets occupy\".</p>",
   "actual": "<p><code>releaseDeviceResourcesEXT()</code> resets <code>sceneTarget_</code> and <code>chain_.getTargetPool()</code> and zeroes the statistics. <code>BloomPass</code>, <code>SsaoPass</code> and <code>VolumetricFogPass</code>, all owned by the pipeline, allocate from private <code>RenderTargetPool pool_</code> members that the reset path does not touch (<code>resize()</code> resets the Bloom and SSAO pools only; the fog volume has a fixed size). <code>getGpuMemoryEstimateBytes()</code> sums the chain pool and the scene target only, so a frame with bloom, SSAO or volumetric fog under-reports, and after a reset the estimate reads 0 while those targets are still allocated.</p>",
   "sources": [
    {
     "path": "modules/graphics-ext/src/RenderPipeline.cpp",
     "note": "RenderPipeline::releaseDeviceResourcesEXT, getGpuMemoryEstimateBytes, resize and the DeviceReset subscription"
    },
    {
     "path": "modules/graphics-ext/include/CNA/Graphics/RenderPipeline.hpp",
     "note": "releaseDeviceResourcesEXT and getGpuMemoryEstimateBytes documentation"
    },
    {
     "path": "modules/graphics-ext/src/BloomPass.cpp",
     "note": "BloomPass::resetTargets and pool_ allocations"
    },
    {
     "path": "modules/graphics-ext/src/SsaoPass.cpp",
     "note": "SsaoPass::resetTargets and pool_ allocations"
    },
    {
     "path": "modules/graphics-ext/include/CNA/Graphics/VolumetricFogPass.hpp",
     "note": "private RenderTargetPool pool_ with no reset entry point"
    },
    {
     "path": "modules/graphics-ext/tests/CNA/Graphics/DeviceLifecycleTests.cpp",
     "note": "DeviceLossTest and MultiDeviceTest assert an estimate of 0 after a reset"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. <code>DeviceLossTest.EverySubsystemSurvivesAResetAndRendersAgain</code> and <code>MultiDeviceTest.AResetOnOneDeviceDoesNotDisturbTheOther</code> assert <code>getGpuMemoryEstimateBytes() == 0</code> after a reset; they pass because the estimate omits the pass pools, not because those pools were released. What a kept pooled target does after a real loss depends on the renderer and was not established: WebGPU's <code>DebugRestoreContext</code> rebuilds every registered live resource (<code>RecreateAfterDeviceLossEXT</code>), which would keep such targets usable there, whereas the pipeline's documentation assumes they are dead.</p>",
   "reproduction": "<pre><code class=\"language-cpp\">// Illustrative; not compiled or run for this entry. HiDef device on a renderer that supports the passes.\nCNA::Graphics::RenderPipeline pipeline(device);\npipeline.resize(256, 256);\npipeline.getSettings().setBloomEnabled(true);\npipeline.begin(Color::Black);\npipeline.end();\nstd::size_t before = pipeline.getGpuMemoryEstimateBytes(); // excludes BloomPass's own targets\npipeline.releaseDeviceResourcesEXT();\nstd::size_t after = pipeline.getGpuMemoryEstimateBytes();  // 0, while BloomPass still holds them</code></pre>",
   "tests_current": "<p><code>DeviceLifecycleTests.cpp</code> (<code>DeviceLossTest.*</code>, <code>MultiDeviceTest.*</code>), <code>PipelineDiagnosticsTests.cpp</code> (<code>PipelineDeviceResetTest.*</code>) and the passes' own suites (<code>BloomPassTests.cpp</code>, <code>SsaoPassTests.cpp</code>, <code>VolumetricFogPassTests.cpp</code>). None observes the pass pools through the pipeline.</p>",
   "regression_test": "<p>Have <code>releaseDeviceResourcesEXT()</code> reset the Bloom, SSAO and volumetric-fog pools (adding a reset to <code>VolumetricFogPass</code>), include those pools in <code>getGpuMemoryEstimateBytes()</code>, and extend <code>DeviceLossTest</code> to enable bloom and fog and assert that the estimate before a reset exceeds the chain's alone and is 0 afterwards.</p>",
   "blast_radius": "<p>CNAEXT <code>RenderPipeline</code> users with bloom, SSAO or volumetric fog enabled: memory statistics read low, and after <code>DeviceReset</code> those three passes keep their previous targets. The reset half is reachable only on renderers that raise <code>DeviceReset</code> and can run these passes. XNA-only builds are unaffected.</p>",
   "workaround": "<p>After a device reset, dispose and recreate the <code>RenderPipeline</code> rather than relying on its reset handler.</p>",
   "related": {
    "internals": [
     [
      "development/internals/modules/graphics-ext.html#targets",
      "Graphics extension internals: render targets"
     ]
    ],
    "guide": [
     [
      "docs/cnaext-engine.html#limits",
      "CNAEXT engine layer: limits"
     ]
    ],
    "architecture": [
     [
      "development/architecture/graphics.html#loss",
      "Graphics architecture: device loss"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-095 (development/internals/modules/graphics-ext.html)"
  },
  {
   "cand_ids": [
    "P2-096"
   ],
   "id": "NEW-B5-15",
   "class": "bug",
   "title": "A throwing shadow-caster or transparent-phase callback leaves RenderPipeline and its ShadowMap in an unrecoverable state",
   "summary": "RenderPipeline::begin() raises frameOpen_ before running the shadow-caster callback between ShadowMap::begin and end, and end() lowers it before the transparent callback; an exception from either skips the cleanup that pass exceptions get.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "low",
   "confidence": "verified-by-reading",
   "public_contract": "<code>CNA::Graphics::RenderPipeline::begin()</code>/<code>end()</code> with callbacks registered through <code>setShadowScene</code> and <code>setTransparentScene</code> (CNAEXT)",
   "expected": "<p>MOD-713 made the pipeline recover from an exception inside a post-process pass: <code>PipelineExceptionSafetyTest.AThrowingPassLeavesNoTargetBoundAndTheNextFrameRendersNormally</code> requires that no target stays bound and that \"a throw is not a one-way door into a broken object\", and <code>AFailedFrameDoesNotLeaveTheFrameOpen</code> notes that <code>CubeShadowMap</code> and <code>ShadowMap</code> once had the same trap at their own <code>begin()</code>. The two caller callbacks are the same kind of caller code running inside a frame.</p>",
   "actual": "<p>In <code>RenderPipeline::begin</code>, <code>frameOpen_</code> is raised first; with shadows enabled, <code>shadowMap_-&gt;begin(...)</code>, the caster callback and <code>shadowMap_-&gt;end()</code> then run unguarded. If the callback throws, the shadow map's target stays bound, the <code>ShadowMap</code> stays open (its next <code>begin</code> throws \"a shadow pass is already open\") and the pipeline's frame stays open (its next <code>begin</code> throws \"a frame is already open\"); calling <code>end()</code> to recover runs the transparent phase and the chain for a frame whose scene target was never bound. In <code>RenderPipeline::end</code>, <code>frameOpen_</code> is lowered first and <code>drawTransparentPhase()</code> runs before <code>SetRenderTarget(nullptr)</code>: a throwing transparent callback leaves the scene target bound and the <code>DepthRead</code>/<code>NonPremultiplied</code> state (or the order-independent accumulation targets) in place, and the next <code>Present</code> refuses while a render target is bound.</p>",
   "sources": [
    {
     "path": "modules/graphics-ext/src/RenderPipeline.cpp",
     "note": "RenderPipeline::begin (shadow pass), RenderPipeline::end and RenderPipeline::drawTransparentPhase"
    },
    {
     "path": "modules/graphics-ext/src/ShadowMap.cpp",
     "note": "ShadowMap::begin refuses a second open pass; ShadowMap::end restores the back buffer"
    },
    {
     "path": "modules/graphics-ext/include/CNA/Graphics/RenderPipeline.hpp",
     "note": "setShadowScene and setTransparentScene"
    },
    {
     "path": "modules/graphics-ext/tests/CNA/Graphics/PipelineDiagnosticsTests.cpp",
     "note": "PipelineExceptionSafetyTest covers pass exceptions only"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. Both existing exception-safety tests throw from a user post-process pass, which runs after the unbind in <code>end()</code>; no test throws from either callback.</p>",
   "reproduction": "<pre><code class=\"language-cpp\">// Illustrative; not compiled or run for this entry.\npipeline.getSettings().setShadowsEnabled(true);\npipeline.setShadowScene(&amp;shadowMap, light, bounds, [] { throw std::runtime_error(\"caster\"); });\ntry { pipeline.begin(Color::Black); } catch (const std::runtime_error&amp;) {}\npipeline.begin(Color::Black);   // throws std::logic_error: a frame is already open</code></pre>",
   "tests_current": "<p><code>PipelineDiagnosticsTests.cpp</code> (<code>PipelineExceptionSafetyTest.*</code>, pass exceptions), <code>RenderPipelineTests.cpp</code> (<code>TheShadowPassRunsBeforeTheSceneTargetIsBound</code>, <code>EachMissingIngredientLeavesTheShadowPassUnrun</code>) and <code>TransparentPhaseTests.cpp</code>; none throws from a callback.</p>",
   "regression_test": "<p>Two cases beside <code>PipelineExceptionSafetyTest</code>: a caster callback that throws once, followed by a normal <code>begin()</code>/<code>end()</code> that must succeed with no target bound and a reusable <code>ShadowMap</code>; and a transparent callback that throws once, followed by assertions that no target is bound, default depth and blend state are back, and the next frame renders. The fix is a scope guard in each routine.</p>",
   "blast_radius": "<p>CNAEXT <code>RenderPipeline</code> users whose shadow-caster or transparent callbacks can throw (content loading, validation exceptions from draw calls). The shadow path leaves the pipeline and <code>ShadowMap</code> unusable until they are recreated; the transparent path leaves the frame bound to the scene target. Pass exceptions, and games without these callbacks, are unaffected.</p>",
   "workaround": "<p>Catch exceptions inside both callbacks; after a callback failure, recreate the <code>RenderPipeline</code> and the <code>ShadowMap</code>.</p>",
   "related": {
    "internals": [
     [
      "development/internals/modules/graphics-ext.html#frame-begin",
      "Graphics extension internals: begin"
     ],
     [
      "development/internals/modules/graphics-ext.html#frame-end",
      "Graphics extension internals: end"
     ]
    ],
    "guide": [
     [
      "docs/cnaext-engine.html#limits",
      "CNAEXT engine layer: limits"
     ]
    ],
    "maintainer": [
     [
      "development/handbook/debug-shutdown-and-lifetime.html#shapes",
      "Debug shutdown and lifetime: failure shapes"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-096 (development/internals/modules/graphics-ext.html)"
  },
  {
   "cand_ids": [
    "P2-097",
    "P2-098"
   ],
   "id": "NEW-B5-16",
   "class": "verification-gap",
   "title": "graphics-ext shader-package drift guards have holes: no reproducibility check for shadow_caster, none run in CI, desktop clustered-forward fragment unpinned",
   "summary": "Two checked-in packages (shadow_caster and the test package modern_resource_interop) have no ShaderPackageReproducibility CTest, CI installs no shaderc so every such CTest skips, and only the ES clustered-forward fragment is pinned to the public GLSL helpers.",
   "subsystem": "Testing & evidence",
   "status": "open",
   "severity": "n/a",
   "confidence": "verified-by-reading",
   "public_contract": "Checked-in shader packages of <code>modules/graphics-ext</code> (<code>package.json</code> plus generated header) used by <code>ShadowMap</code>, <code>SpotShadowMap</code> and <code>ClusteredForwardEffect</code>",
   "expected": "<p>Plan MOD-2216, as recorded in <code>cmake/Tests/ModuleProbes.cmake</code>: \"checked-in packages must remain derivable from their declared source files\", enforced by one reproducibility CTest per package running <code>tools/shader_package/generate_shader_package.py --check</code>. The clustered-forward helper pin exists \"rather than allowing a second copy of an algorithm to drift unnoticed\".</p>",
   "actual": "<p><code>ModuleProbes.cmake</code> registers 18 reproducibility CTests; two checked-in packages have none: <code>modules/graphics-ext/src/shaders/shadow_caster</code> (the directional and spot casters used by <code>ShadowMap</code> and <code>SpotShadowMap</code>) and the test package <code>tests/CNA/Graphics/shaders/modern_resource_interop</code>. The generator exits 77 (skip) when <code>libshaderc.so.1</code> is absent and no workflow under <code>.github/workflows</code> installs shaderc, so every registered reproducibility CTest skips in CI; the gate runs only on a developer machine with shaderc. Separately, <code>ClusteredForwardEffectTest.PackagedEsSourceRetainsEveryPublicGlslFragment</code> checks that <code>kForwardEsFragmentSource</code> contains the five public helper texts (<code>ClusteredLightBuffer::getLightLookupGlsl()</code> and four others); the hand-maintained desktop source <code>forward.desktop.frag.glsl</code> (<code>kForwardDesktopFragmentSource</code>, consumed by OPENGL33 and OPENGL4) is not checked, so a helper change copied into the ES file only passes every test.</p>",
   "sources": [
    {
     "path": "cmake/Tests/ModuleProbes.cmake",
     "note": "the 18 *ShaderPackageReproducibility registrations; none for shadow_caster or modern_resource_interop"
    },
    {
     "path": "modules/graphics-ext/src/shaders/shadow_caster/package.json",
     "note": "package without a reproducibility check"
    },
    {
     "path": "modules/graphics-ext/src/ShadowCasterShaderPackages.cpp",
     "note": "the caster packages used by ShadowMap and SpotShadowMap"
    },
    {
     "path": "modules/graphics-ext/tests/CNA/Graphics/shaders/modern_resource_interop/package.json",
     "note": "test package without a reproducibility check"
    },
    {
     "path": "tools/shader_package/generate_shader_package.py",
     "note": "find_shaderc: exit code 77 (SKIP) without libshaderc.so.1"
    },
    {
     "path": "modules/graphics-ext/tests/CNA/Graphics/ClusteredForwardEffectTests.cpp",
     "note": "PackagedEsSourceRetainsEveryPublicGlslFragment checks the ES fragment only"
    },
    {
     "path": "modules/graphics-ext/src/shaders/clustered_forward/forward.desktop.frag.glsl",
     "note": "the unpinned desktop fragment"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. At TARGET the desktop fragment differs from the ES one only in its header lines (<code>#version 330 core</code> instead of <code>#version 300 es</code> and the precision statement), so there is no drift today, and the clustered-forward GPU tests exercise the desktop variant behaviourally on OPENGL4 (CNA's record in <code>plans/plan_opengl4_modern_graphics.md</code>, GL4-0029, and <code>CnaGraphicsExtTests</code> 955/0/7). What is missing is a guard against the next edit.</p>",
   "reproduction": null,
   "tests_current": "<p>The 18 reproducibility CTests (label <code>graphics;shader;generator</code>, <code>SKIP_RETURN_CODE 77</code>), <code>ClusteredForwardEffectTests.cpp</code> (the ES pin and GPU tests that run whichever variant the renderer selects), and the shadow-map suites, which test casters behaviourally.</p>",
   "regression_test": "<p>Register <code>ShadowCasterShaderPackageReproducibility</code> and <code>ModernResourceInteropShaderPackageReproducibility</code> like the other 18; install libshaderc in one CI job so the generator checks run instead of skipping; extend <code>PackagedEsSourceRetainsEveryPublicGlslFragment</code> to <code>kForwardDesktopFragmentSource</code> (or generate the desktop file from the ES one).</p>",
   "blast_radius": "<p>Engine-layer maintainers: a shader edit without regeneration, or a helper change applied to one variant, ships unnoticed until a renderer-specific visual test catches it. Runtime behaviour at TARGET is unaffected.</p>",
   "workaround": "<p>Run the generator with <code>--check</code> locally for every package touched (it needs <code>libshaderc.so.1</code>) and diff the ES and desktop clustered-forward sources.</p>",
   "related": {
    "internals": [
     [
      "development/internals/modules/graphics-ext.html#shaders-tests",
      "Graphics extension internals: shader tests"
     ],
     [
      "development/internals/modules/graphics-ext.html#shaders-clustered",
      "Graphics extension internals: clustered forward"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#ci",
      "Test architecture: CI"
     ]
    ],
    "guide": [
     [
      "docs/cnaext-engine.html#evidence",
      "CNAEXT engine layer: evidence"
     ]
    ]
   },
   "origin": "Phase-2 candidates P2-097 and P2-098 (development/internals/modules/graphics-ext.html); modern_resource_interop found during re-verification"
  },
  {
   "cand_ids": [
    "P2-099"
   ],
   "id": "NEW-B5-17",
   "class": "verification-gap",
   "title": "No test asserts RenderPipeline's fixed post-process order; the order-named tests only count passes",
   "summary": "RenderPipeline::end() adds up to fourteen built-in passes and the user passes in a deliberate order, but the three tests named after that order assert only getLastFramePassCount(), so any permutation passes.",
   "subsystem": "Testing & evidence",
   "status": "open",
   "severity": "n/a",
   "confidence": "verified-by-reading",
   "public_contract": "Built-in pass order of <code>CNA::Graphics::RenderPipeline::end()</code> (CNAEXT engine layer)",
   "expected": "<p>The order is a stated contract in <code>RenderPipeline::end()</code> (\"Fixed order, and the order is the point\"): SSAO, SSR, volumetric fog, light shafts, height fog, motion blur, depth of field, lens flare, bloom, tonemap, colour grade, chromatic aberration, FXAA, film grain, then user passes, each position justified in a comment.</p>",
   "actual": "<p><code>TheFixedPassOrderIsSsaoThenBloomThenTonemapThenFxaa</code>, <code>SsrSitsBetweenSsaoAndBloom</code> and <code>DepthOfFieldSitsBeforeBloomBecauseItBelongsToTheLens</code> enable a set of passes, run one frame and assert <code>getLastFramePassCount()</code> (5, 5 and 3), plus, in the first, that a user pass ran once; they pass with the passes in any order. <code>UserPassesRunAfterTheBuiltInOnes</code> also only counts. The one ordered assertion on pass names, <code>PassTimingTest.EachPassReportsItsOwnNameAndItsOwnTime</code>, builds its own two-pass <code>PostProcessChain</code> (Tonemap, FXAA) instead of asking the pipeline. Nothing observes the positions of volumetric fog, light shafts, height fog, motion blur, lens flare, colour grade, chromatic aberration or film grain.</p>",
   "sources": [
    {
     "path": "modules/graphics-ext/src/RenderPipeline.cpp",
     "note": "RenderPipeline::end: the chain_.addPass sequence"
    },
    {
     "path": "modules/graphics-ext/tests/CNA/Graphics/RenderPipelineTests.cpp",
     "note": "the three order-named tests and UserPassesRunAfterTheBuiltInOnes assert counts"
    },
    {
     "path": "modules/graphics-ext/tests/CNA/Graphics/PassTimingTests.cpp",
     "note": "EachPassReportsItsOwnNameAndItsOwnTime (own chain) and ThePipelineSurfacesTheChainsTimings"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. The pipeline already exposes what an ordering test needs on renderers with a GPU timer: with timing on, <code>getPassTimingsEXT()</code> reports one named entry per pass in chain order (<code>ThePipelineSurfacesTheChainsTimings</code> prints the names but asserts only that samples exist).</p>",
   "reproduction": null,
   "tests_current": "<p><code>RenderPipelineTests.cpp</code>, <code>PassTimingTests.cpp</code> and each pass's own suite (<code>LensPassesTests.cpp</code>, <code>VolumetricFogPassTests.cpp</code>, <code>ColorGradePassTests.cpp</code> and others), which test a pass in isolation.</p>",
   "regression_test": "<p>One <code>RenderPipelineTests</code> case that enables every built-in pass, turns GPU timing on, runs a few presented frames and compares the names from <code>getPassTimingsEXT()</code> with the documented sequence (skipping where no GPU timer exists), or a test-only accessor for the chain's pass list after <code>end()</code>.</p>",
   "blast_radius": "<p>Engine-layer maintainers: a reordering of the <code>addPass</code> calls in <code>RenderPipeline::end()</code>, which changes the image (for example tonemapping before bloom), passes the suite. Runtime behaviour at TARGET is unaffected.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/modules/graphics-ext.html#frame-chain",
      "Graphics extension internals: the pass chain"
     ]
    ],
    "guide": [
     [
      "docs/cnaext-engine.html#contents",
      "CNAEXT engine layer: contents"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#recipes",
      "Test architecture: recipes"
     ]
    ]
   },
   "origin": "Phase-2 candidate P2-099 (development/internals/modules/graphics-ext.html)"
  },
  {
   "cand_ids": [],
   "id": "NEW-B5-18",
   "class": "bug",
   "title": "24 classic SDL_GPU CTests fail in CNA's own latest recorded run, with no cause recorded",
   "summary": "CNA's SDL_GPU records list 26 classic ^SdlGpu CTests still failing just before TARGET; two are analysed in their own entries, and the other 24, covering formats, render targets, render state and recovery, have no recorded cause.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "medium",
   "confidence": "reproduced",
   "public_contract": "SDL_GPU renderer behaviour pinned by the classic <code>^SdlGpu</code> CTest set (renderer examples, shared parity fixtures, EasyGL oracle cases and <code>SdlGpuIndexedDrawRangeTest</code>)",
   "expected": "<p>Each registered <code>^SdlGpu</code> CTest states part of the renderer's contract; the parity fixtures and EasyGL oracle cases are the same checks other renderers are held to. SDL_GPU is declared <code>Supported</code> in <code>GraphicsBackendMaturity.hpp</code>, so a routine run of its own suite is expected to pass.</p>",
   "actual": "<p>PSG-0009 (<code>plans/plan_pre_sdlgpu_closeout.md</code>) recorded 202 passing and 27 failing of 229 classic SDL_GPU CTests. The SDL_GPU modern-graphics and street workstreams re-ran the set after their changes and report \"the same 27 by name\" (SMG-0042: 204 / 27 of 231 with two new tests) until STREETS-0007 repaired <code>SdlGpu_Smoke</code>'s stale checks; the last record before TARGET (STREETS-0008, <code>plans/plan_street_sdlgpu.md</code>) is 26 failures, \"none new\". Two of the 26 are analysed elsewhere (<code>SdlGpu_OcclusionQuery_Limitation</code> under CNA-BUG-012, <code>SdlGpu_2D_HeadlessDriver</code> under its own entry). The other 24, grouped here by name only:</p><ul><li>Formats and targets: <code>SdlGpu_Texture2D_FormatMatrix</code>, <code>SdlGpu_Texture2D_FormatMatrixFallback</code>, <code>SdlGpu_TextureCube_FormatMatrix</code>, <code>SdlGpu_TextureCube_FormatMatrixFallback</code>, <code>SdlGpu_Parity_compressed_cube</code>, <code>SdlGpu_Parity_compressed_cube_fallback</code>, <code>SdlGpu_RenderTargetCube_Formats</code>, <code>SdlGpu_RenderTargetCube_MsaaMip</code>, <code>SdlGpu_RenderTarget_SetData</code>, <code>SdlGpu_TextureFilterMipContract</code>, <code>SdlGpu_EasyGLOracle_surface_format_throws</code>.</li><li>State and draws: <code>SdlGpu_RenderState</code>, <code>SdlGpu_DepthBias</code>, <code>SdlGpu_Deferred_Scissor</code>, <code>SdlGpu_DrawLineTopology</code>, <code>SdlGpu_EasyGLOracle_depthstencilstate_stencil_twosided</code>, <code>SdlGpu_EasyGLOracle_draw_range_validation</code>, <code>SdlGpu_Parity_dual_texture_terms</code>, <code>SdlGpuIndexedDrawRangeTest.MutatingSourceBuffersAfterQueuingDoesNotChangeQueuedDraws</code>, <code>SdlGpuIndexedDrawRangeTest.RejectsIndexedRangesOutsideTheBoundBuffers</code>.</li><li>Device, presentation and lifetime: <code>SdlGpu_BackbufferFormat</code>, <code>SdlGpu_ConstructorExceptionSafety</code>, <code>SdlGpu_SwapchainRecovery</code>, <code>SdlGpu_DepthlessCube_SpriteBatchCompatibility</code>.</li></ul>",
   "sources": [
    {
     "path": "plans/plan_pre_sdlgpu_closeout.md",
     "note": "PSG-0009: the 27 failing classic SDL_GPU CTests by name; PSG-0010: the configuration they belong to"
    },
    {
     "path": "plans/plan_street_sdlgpu.md",
     "note": "STREETS-0007 and STREETS-0008: the same set re-run, 26 failures remain"
    },
    {
     "path": "plans/plan_sdlgpu_modern_graphics.md",
     "note": "SMG-0042 and earlier re-runs: the 27 failures identical by name"
    },
    {
     "path": "plans/plan_sdlgpu.md",
     "note": "SDLGPU-11: SdlGpu_SwapchainRecovery recorded passing when it was added"
    },
    {
     "path": "modules/renderers/sdl-gpu/examples/CMakeLists.txt",
     "note": "registrations of the SdlGpu_* example, parity and EasyGL-oracle CTests"
    },
    {
     "path": "modules/graphics/tests/Microsoft/Xna/Framework/Graphics/SdlGpuIndexedDrawRangeTests.cpp",
     "note": "the two failing SdlGpuIndexedDrawRangeTest cases"
    },
    {
     "path": "modules/core/include/CNA/GraphicsBackendMaturity.hpp",
     "note": "SDL_GPU declared Supported"
    }
   ],
   "evidence": "<p>CNA's own record executed the set, on one machine: <code>tools/platform/run_gpu_tests_private.sh</code> (private Weston and rootful Xwayland), an AMD Radeon 780M under RADV Mesa 25.0.7, SDL_gpu on its Vulkan driver, a Release build with <code>CNA_SDL_GPU_SHADERCROSS=OFF</code> and libshaderc present (PSG-0010). The record calls them \"Real renderer results, none of them a dead test\", and STREETS-0008 re-ran all 27 on a stashed baseline to show they predate its change. No per-test output or cause is kept in the tree, except PSG-0009's remark that <code>SdlGpu_Parity_dual_texture_terms</code> may be the XNA opaque-black null-texture rule that WebGPU's twin failed until WMG-0025. <code>SdlGpu_SwapchainRecovery</code> passed 5/5 when SDLGPU-11 added it, so at least that one regressed later. Nothing was re-run for this entry: whether each failure is a renderer defect or a stale expectation is not established, which is why this entry is one cluster rather than 24 diagnoses. No CI workflow runs any SDL_GPU test.</p>",
   "reproduction": "<pre><code class=\"language-bash\"># The command CNA's own record uses; not executed for this entry.\ntools/platform/run_gpu_tests_private.sh cmake-build-sdlgpu -R '^SdlGpu' -j4</code></pre>",
   "tests_current": "<p>The failing CTests themselves, registered in <code>modules/renderers/sdl-gpu/examples/CMakeLists.txt</code> and <code>SdlGpuIndexedDrawRangeTests.cpp</code>. They run only where someone runs the classic set on a GPU host; no workflow names SDL_GPU.</p>",
   "regression_test": "<p>The tests exist; what is missing is triage. Record each failure's cause in the SDL_GPU plan, fix the renderer or the stale expectation so the CTest turns green, and put the classic <code>^SdlGpu</code> set behind a recorded gate (a GPU-capable CI lane or a release checklist) so the count cannot silently stay at 26.</p>",
   "blast_radius": "<p>SDL_GPU only. The areas these tests pin (texture and cube-map formats and their fallbacks, compressed cubes, cube render targets with MSAA and mips, <code>RenderTarget2D</code> <code>SetData</code>, filtering and mips, render state, depth bias, deferred scissor, line topology, two-sided stencil, draw-range validation, dual-texture terms, indexed draw ranges, back-buffer formats, constructor exception safety and swapchain recovery) may diverge from the tested contract. Which of them affect a real game is unknown until each is triaged. Other renderers are unaffected.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/graphics/sdl-gpu.html#tests",
      "SDL_gpu renderer internals: tests"
     ]
    ],
    "guide": [
     [
      "docs/verification.html#renderers",
      "Verification: renderers"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#gpu",
      "Test architecture: GPU tests"
     ]
    ],
    "maintainer": [
     [
      "development/handbook/fix-a-renderer-bug.html#triage",
      "Fix a renderer bug: triage"
     ]
    ]
   },
   "origin": "new finding at TARGET review: CNA's SDL_GPU run records (PSG-0009, SMG-0042, STREETS-0008) read while verifying P2-042 and CNA-BUG-012"
  },
  {
   "cand_ids": [],
   "id": "NEW-B5-19",
   "class": "bug",
   "title": "WebGPU ignores a real device loss: OnDeviceLost only logs, so DeviceLost, the device status and the draw gate react only to the debug hook",
   "summary": "WebGPURenderer sets its lost flag, closes CanBeginDrawEXT() and raises DeviceLost only inside DebugSimulateContextLoss; the callback wired for a real loss only writes to stderr, so a genuine loss is neither reported nor gated.",
   "subsystem": "Graphics & renderers",
   "status": "open",
   "severity": "medium",
   "confidence": "verified-by-reading",
   "public_contract": "<code>GraphicsDevice::DeviceLost</code> / <code>DeviceReset</code>, <code>getGraphicsDeviceStatusProperty()</code> and the <code>CanBeginDrawEXT()</code> gate behind <code>GraphicsDeviceManager::BeginDraw</code>, on the WEBGPU renderer",
   "expected": "<p>XNA raises <code>DeviceLost</code> and reports <code>GraphicsDeviceStatus::Lost</code> when the device is lost. CNA's <code>docs/webgpu-renderer.md</code> adds a WebGPU-specific rule: a lost device \"must be gated BEFORE the acquire, never after\", because <code>wgpuSurfaceGetCurrentTexture</code> on a surface whose device is lost panics inside wgpu-native and aborts the process, and <code>CanBeginDrawEXT()</code> returning false \"is what stands between a lost device and that abort\". The same document states that on the web target the device-lost callback does fire (emdawnwebgpu bridges <code>GPUDevice.lost</code>).</p>",
   "actual": "<p><code>RequestDeviceOnlyEXT</code> (also reached through <code>RequestAdapterAndDevice</code>) registers the free function <code>OnDeviceLost</code> as <code>deviceLostCallbackInfo.callback</code> with no user data. <code>OnDeviceLost</code> prints \"CNA WebGPU device lost (...)\" to stderr and does nothing else; without a renderer pointer it cannot set <code>deviceLost_</code>, raise <code>RendererDeviceEvent::Lost</code> or close <code>CanBeginDrawEXT()</code> (which returns <code>!deviceLost_</code>). The only code that does those things is <code>WebGPURenderer::DebugSimulateContextLoss</code>, and the only recovery is <code>DebugRestoreContext</code>; both are debug hooks the renderer never calls itself. When a real loss reaches the callback, the game receives no <code>DeviceLost</code>, <code>GraphicsDeviceStatus</code> stays <code>Normal</code>, <code>BeginDraw</code> keeps returning true and the renderer continues its frame loop against the lost device; nothing rebuilds it.</p>",
   "sources": [
    {
     "path": "modules/renderers/webgpu/src/WebGPURenderer.cpp",
     "note": "OnDeviceLost (logs only) and its registration in WebGPURenderer::RequestDeviceOnlyEXT"
    },
    {
     "path": "modules/renderers/webgpu/src/WebGPURenderer.cpp",
     "note": "WebGPURenderer::DebugSimulateContextLoss and DebugRestoreContext, the only places deviceLost_ changes and Lost/Reset are raised"
    },
    {
     "path": "modules/renderers/webgpu/include/CNA/Internal/Renderers/WebGPU/WebGPURenderer.hpp",
     "note": "CanBeginDrawEXT returns !deviceLost_"
    },
    {
     "path": "docs/webgpu-renderer.md",
     "note": "Device loss: what the pin actually does (WEBGPU-180) and the device-loss gate (WEBGPU-181)"
    },
    {
     "path": "modules/renderers/webgpu/examples/webgpu_context_recovery_test.cpp",
     "note": "recovery evidence drives the debug hooks only"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5; not executed. That the callback propagates nothing is certain from the code. How often a real loss reaches it is not: natively, CNA measured that the pinned wgpu-native v29.0.1.1 delivers no device-lost callback for an application-initiated destroy (WEBGPU-180), and whether a driver-reported loss delivers one was not measured; in a browser, CNA states that the callback fires but derived that from the emdawnwebgpu port's JavaScript without a browser confirmation. What the browser does with the renderer's continued calls on a lost device was not established here. Natively, if a later pin does deliver the callback, the open gate leaves exactly the acquire path CNA measured to abort the process.</p>",
   "reproduction": "<pre><code class=\"language-cpp\">// Illustrative; not compiled or run for this entry. Web target (emdawnwebgpu), where CNA states the\n// device-lost callback fires. Destroy the device from outside the renderer's debug hook:\nwgpuDeviceDestroy(webGpuRenderer.Device());\n// Expected: DeviceLost raised, GraphicsDeviceStatus == Lost, CanBeginDrawEXT() == false.\n// Actual per the code: one \"CNA WebGPU device lost (...)\" line on stderr; status stays Normal and the\n// next BeginDraw proceeds.</code></pre>",
   "tests_current": "<p><code>webgpu_context_recovery_test.cpp</code> verifies the debug-hook path end to end (the gate goes false and back, each event fires once, resources survive). No test delivers a device-lost callback or asserts that one reaches <code>GraphicsDevice</code>.</p>",
   "regression_test": "<p>Pass the renderer as the callback's <code>userdata1</code> and have it set <code>deviceLost_</code> and raise <code>RendererDeviceEvent::Lost</code> (ignoring the loss CNA itself causes in <code>DebugSimulateContextLoss</code>); then a browser test in the WEBGPU-196 style that destroys the device from outside the renderer and asserts that <code>DeviceLost</code> fired and <code>CanBeginDrawEXT()</code> is false.</p>",
   "blast_radius": "<p>WEBGPU only, most visibly on the web target. Games that rely on <code>DeviceLost</code>, <code>DeviceReset</code> or <code>GraphicsDeviceStatus</code> to pause, save or rebuild never hear of a real loss, and the renderer keeps drawing against a dead device. DirectX 9, 11 and 12, Direct2D and Vulkan raise <code>Lost</code> from their own real error paths and are unaffected.</p>",
   "workaround": null,
   "related": {
    "internals": [
     [
      "development/internals/graphics/device.html#resources",
      "GraphicsDevice internals: device loss"
     ]
    ],
    "architecture": [
     [
      "development/architecture/graphics.html#loss",
      "Graphics architecture: device loss"
     ]
    ],
    "guide": [
     [
      "docs/rendering-backends.html#modern-gpu",
      "Graphics renderers: modern GPU renderers"
     ]
    ]
   },
   "origin": "new finding at TARGET review: tracing every RendererDeviceEvent::Lost raise site while verifying P2-033"
  },
  {
   "cand_ids": [],
   "id": "NEW-B5-20",
   "class": "verification-gap",
   "title": "No CI workflow builds or tests the OPENGL4 renderer",
   "summary": "OPENGL4, declared Supported and merged at this snapshot with several hundred family CTests, is named by none of the 20 workflows, so its evidence is CNA's recorded runs on one Radeon 780M under Mesa, and Windows and macOS are unvalidated.",
   "subsystem": "Build & CI",
   "status": "open",
   "severity": "n/a",
   "confidence": "verified-by-reading",
   "public_contract": "The OPENGL4 renderer identity (<code>GraphicsBackendMaturity::Supported</code>) and its CTest suites",
   "expected": "<p>A renderer declared <code>Supported</code> has its build and tests exercised automatically, so that a regression is caught when it is introduced. CNA's workflows do this for OPENGLES3, OPENGL33, VULKAN, SDL_RENDERER, HEADLESS, SOFTWARE, STUB, the Windows Direct3D/Direct2D/GDI families, METAL and the browser renderers.</p>",
   "actual": "<p>No file under <code>.github/workflows</code> and no preset in <code>CMakePresets.json</code> names OPENGL4. The only evidence is CNA's own record in <code>docs/opengl4-renderer.md</code>: <code>ctest -R '^OpenGL4_'</code> 407/407 on Wayland (EGL) and 406/406 on X11 (GLX), <code>CnaGraphicsTests</code> 2,833/0/57, <code>CnaRendererTests</code> 338/0/10, <code>CnaGraphicsExtTests</code> 960/0/7 and a sanitizer run, all measured on one AMD Radeon 780M (Mesa radeonsi) through <code>tools/platform/run_gpu_tests_private.sh</code>, with \"Windows and macOS remain unvalidated\". The renderer's GL-error output gate (<code>cmake/TestHelpers.cmake</code>), its forced GL 4.1 floor test, the 346 EasyGL example sources rebuilt against it and the <code>OPENGL4;OPENGLES3;OPENGL33</code> same-binary tree (GL4-0006) all run only where someone runs them by hand.</p>",
   "sources": [
    {
     "path": "docs/opengl4-renderer.md",
     "note": "Validation section: the recorded runs and the unvalidated platforms"
    },
    {
     "path": "modules/core/include/CNA/GraphicsBackendMaturity.hpp",
     "note": "OpenGL4 declared Supported"
    },
    {
     "path": "modules/renderers/opengl4/examples/CMakeLists.txt",
     "note": "the OpenGL4_* CTest registrations"
    },
    {
     "path": "cmake/TestHelpers.cmake",
     "note": "cna_append_opengl4_gl_error_gate_pattern, the GL-error output gate"
    },
    {
     "path": "CMakePresets.json",
     "note": "no preset selects OPENGL4"
    },
    {
     "path": "plans/plan_opengl4_modern_graphics.md",
     "note": "GL4-0006: the local OPENGL4;OPENGLES3;OPENGL33 tree and its baselines"
    }
   ],
   "evidence": "<p>Checked by reading at 009d40f5 (every workflow file and <code>CMakePresets.json</code> searched for each of the 25 public identities); nothing executed. OPENGL4 is not alone: no workflow names WEBGPU, SDL_GPU, FNA3D, PORTABLEGL, FREEDIRECT or DIRECTX9 either, as the verification page states. This entry is scoped to OPENGL4, which the TARGET commit itself merged (\"integrate opengl4-modern-graphics\") and whose GL corpus and presentation code it shares with EasyGL through headers such as <code>GlStockShaderSources.hpp</code> and <code>GlPresentationSurfaceState.hpp</code>, so an EasyGL change that CI does check can still break OPENGL4 unobserved. Whether OPENGL4's suites pass on a CI host's Mesa software driver was not established.</p>",
   "reproduction": null,
   "tests_current": "<p><code>OpenGL4_*</code> CTests, the EasyGL parity corpus rebuilt against OpenGL4 (<code>OpenGL4_EasyGLParity_*</code>) and the shared suites under <code>CNA_GRAPHICS_RENDERER=OPENGL4</code>; all run only locally.</p>",
   "regression_test": "<p>A workflow job that configures <code>CNA_GRAPHICS_RENDERER=OPENGL4</code> (for example on the SDL-free X11 lane under Xvfb and Mesa, like <code>platform-ci.yml</code>'s OPENGL33 job) and runs <code>ctest -R '^OpenGL4_'</code> plus <code>CnaGraphicsTests</code> with the GL-error gate active.</p>",
   "blast_radius": "<p>OPENGL4 users and maintainers. Regressions in the renderer, in the GL headers it shares with EasyGL, or in shared graphics code surface only when someone runs its suites by hand on a GPU host; Windows and macOS builds have never been validated. Runtime behaviour at TARGET is unaffected.</p>",
   "workaround": "<p>Before relying on OPENGL4, run its suites locally (<code>tools/platform/run_gpu_tests_private.sh</code> with <code>-R '^OpenGL4_'</code>) on the target driver.</p>",
   "related": {
    "internals": [
     [
      "development/internals/graphics/opengl4.html#tests",
      "OpenGL4 renderer internals: tests"
     ],
     [
      "development/internals/graphics/opengl4.html#gaps",
      "OpenGL4 renderer internals: gaps"
     ]
    ],
    "guide": [
     [
      "docs/verification.html#ci",
      "Verification: CI"
     ]
    ],
    "tests": [
     [
      "development/testing/architecture.html#ci",
      "Test architecture: CI"
     ]
    ]
   },
   "origin": "new finding at TARGET review: searching .github/workflows and CMakePresets.json for every public identity while verifying P2-032"
  }
 ]
}
