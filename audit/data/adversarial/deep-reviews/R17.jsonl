{"id": "CNA-BUG-137", "outcome": "HOLDS WITH NOTES", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug137.cpp: g++ -std=c++23 -O0 -g -fsanitize=address, compiling TARGET modules/media/src/Xna/MediaPlayer.cpp, MediaQueue.cpp, Song.cpp, SongCollection.cpp, src/Internal/VisualizationCapture.cpp and VisualizationFFT.cpp, modules/math/src/MathHelper.cpp plus a probe stub defining FrameworkDispatcher::ActiveSongChanged and MediaStateChanged; linked with prebuilt sharp-runtime archives (sibling checkout, branch next, NOT pinned by TARGET); no SOUND_ENABLED. Main: Song theme(existing file); Play(&theme); Play(getQueueProperty().getActiveSongProperty()).", "result": "AddressSanitizer: heap-use-after-free, READ of size 8 inside std::string copy in MediaPlayer::LoadSong, called from MediaPlayer::Play (the second call); the 152-byte Song was freed by std::vector::clear inside MediaQueue::Clear called from the same Play, and allocated by LoadSong in the first Play. Confirms the ordering claim; the read happens even without a mixer."}], "refutation_attempt": "Looked for a lifetime caveat in the Play doc comment (none), a test passing a queue-owned song (none), a compensating copy before Clear (previousSong is only a pointer comparison) and a C ABI route (its queue getters copy entries out). XNA IL: the queue indexer returns a new Song wrapper, so the call is valid in XNA.", "notes": "Evidence basis can move from source-verified to reproduced: an ASan probe built from the TARGET MediaPlayer, MediaQueue and Song sources (no mixer) reports the heap-use-after-free in LoadSong called from Play. Text otherwise accurate, including that Play(const SongCollection&, index) has the same shape (SongCollection holds non-owning raw pointers). Optionally add that the C ABI is not affected because its queue getters copy the entry out.", "correction": {"evidence_note": "Reproduced by the audit at 009d40f5, not by CNA's own tests. A probe built from the TARGET sources of MediaPlayer, MediaQueue, Song and SongCollection with AddressSanitizer (no SOUND_ENABLED, so no mixer) called Play(&theme) and then Play(getQueueProperty().getActiveSongProperty()); ASan reported a heap-use-after-free read in MediaPlayer::LoadSong called from Play, on a Song freed by MediaQueue::Clear called from the same Play. The sharp-runtime System types came from a sibling checkout that TARGET does not pin. The writes in PlaySong (duration, play count) were not exercised because they exist only in SOUND_ENABLED builds. The C ABI cannot reach this path: cna_media_queue_get_at and cna_media_queue_get_active_song copy the queue entry out."}}
{"id": "CNA-BUG-001", "outcome": "NEEDS CORRECTION", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-plane.cpp and r17-plane2.cpp: g++ -std=c++23 -O0 -g, all 17 TARGET modules/math/src/*.cpp compiled next to the probe and linked with prebuilt sharp-runtime archives (sibling checkout, branch next, NOT pinned by TARGET). Called Plane::Transform and, for comparison, a local copy of the XNA IL formula (rows of the inverse read directly).", "result": "Translation(1,0,0) on plane (0,1,0,D=-1): CNA Normal(1,1,0) D=-1, XNA formula Normal(0,1,0) D=-1. Plane y=2 (0,1,0,D=-2) by CreateRotationZ(pi/2): CNA Normal(1,0,0) D=-2, XNA formula Normal(-1,0,0) D=-2. CreateRotationY(0.5)*CreateTranslation(3,4,5) on (0.6,0.8,0,-1): CNA Normal(0.7622,4.8,5.5385), XNA (0.5265,0.8,-0.2877). Identity and Scale(2,3,4) agree with XNA. The C ABI smoke case (plane y=0, translation (0,2,0)) gives the correct (0,1,0,-2) even with the defect. Matrix::Transpose(m,m) on CreateTranslation(1,2,3) leaves M14=1 and M41=1 (distinct-object transpose gives M41=0)."}], "refutation_attempt": "Read the decompiled XNA Plane.Transform and Matrix.Transpose, FNA Plane.cs and Matrix.cs at /rv/tmp/FNA, all callers of Plane::Transform and every Transpose call site (Plane.cpp is the only aliased one), and the tests. Ran the numeric claims. Everything reproduced; the only wrong detail is the FNA description.", "notes": "Central claim, both worked numbers, blast radius and test observations all reproduce. One published detail is wrong: the Expected text says FNA's implementation inverts and transposes into a fresh temporary. FNA's Plane.Transform makes the very same aliased call, Matrix.Transpose(ref transformedMatrix, out transformedMatrix); what makes it safe is that FNA's Matrix.Transpose builds the result in a local and assigns at the end (the XNA IL loads all sixteen fields into locals first). CNA copied FNA's call but not the alias-safe Transpose, so the root cause is Matrix::Transpose.", "correction": {"expected": "A plane is transformed by the inverse-transpose of the matrix: the plane (Normal, D) taken as a row vector is multiplied by transpose(inverse(M)). The genuine XNA 4.0 implementation inverts the matrix and then reads rows of the inverse directly, so it never transposes (read from the IL of the genuine XNA 4.0 Microsoft.Xna.Framework assembly; not executed). FNA's Plane.Transform (read at FNA commit 1358793, 2026-08-01) makes the same call CNA makes, Matrix.Transpose(ref transformedMatrix, out transformedMatrix), and is correct because FNA's Matrix.Transpose builds the transposed value in a local and assigns it at the end; the XNA IL of Matrix.Transpose likewise loads all sixteen fields into locals before writing. For M = Matrix::CreateTranslation(1, 0, 0) and the plane y = 1 (Normal (0, 1, 0), D = -1), a translation along X leaves the plane unchanged.", "evidence_note": "Reproduced by the audit at 009d40f5, not by CNA's own tests. The TARGET math sources were compiled with g++ -O0 into a probe that calls Plane::Transform and compares it with the XNA formula. Translation (1,0,0) on the plane (0,1,0, D=-1) gave Normal (1,1,0), D=-1 against XNA's (0,1,0), D=-1. The plane y=2 rotated by CreateRotationZ(PiOver2) gave Normal (1,0,0) against XNA's (-1,0,0). Identity and pure scale matched XNA, and the C ABI smoke case (plane y=0, translation along Y) also matched, which confirms it hides the defect. The sharp-runtime types came from a sibling checkout that TARGET does not pin. The search of Transpose call sites, and the reading of XNA IL and FNA, were done by reading."}}
{"id": "CNA-BUG-008", "outcome": "HOLDS WITH NOTES", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug008.cpp: real TARGET modules/content/src/Xna/ContentManager.cpp, Cnb/CnbLoaderRegistry.cpp, Cnb/CnbDocument.cpp, media/src/Xna/Song.cpp, core/src/Logger.cpp and TargetPlatform.cpp, platform headless sources and all math sources compiled g++ -O0 with -DCNA_RENDERER_HEADLESS, linked non-PIE with --unresolved-symbols=ignore-all plus 10 fake typeinfo stand-ins (graphics types never touched) and prebuilt sharp-runtime archives (sibling checkout, branch next, NOT pinned by TARGET). A directory holding an empty theme.ogg and a theme.cnj containing {\"cna\":\"1\",\"type\":\"r17data\"}; ContentManager cm(nullptr, dir); cm.Load<Song>(\"theme\") and cm.Load<Song>(\"theme.ogg\").", "result": "Load<Song>(\"theme\") succeeded and returned handle 'r17-content/theme.cnj' (name 'theme'); Load<Song>(\"theme.ogg\") returned handle 'r17-content/theme.ogg'. So the .cnj is resolved ahead of the media file, no exception, and the explicit-extension workaround works. The Load<Video> half was not run (needs a GraphicsDevice and FFmpeg)."}], "refutation_attempt": "Searched misc/cnj.md and the Cnj*Tests for any Song or Video .cnj support or rejection (none: the sourceFile capability matrix lists Texture2D, SoundEffect, TextureCube, SpriteFont, Effect, Model only). Looked for an earlier .cnj guard in Load<T> or the two readers (none). Checked Song::Song and Video::Video for a content sniff (existence only).", "notes": "Song half reproduced by execution; Video half stays source-verified. Two wording fixes. The Focused reproduction line `Media::Song song = content.Load(\"theme\");` cannot compile: Load is a template, it must read content.Load<Media::Song>(\"theme\"). The contract-bearing text should be cited: misc/cnj.md says the resolver tries .cnj for every registered type and lists the per-reader sourceFile matrix, which has no Song or Video row, so nothing enforces or rejects a .cnj for them.", "correction": {"evidence_note": "Reproduced for Song by the audit at 009d40f5; the Video half was checked by reading only. The TARGET ContentManager and Song sources were compiled with g++ -O0 (headless renderer macro, headless platform installed) into a probe with Content/theme.ogg (empty) and Content/theme.cnj (a small JSON envelope) side by side: Load<Song>(\"theme\") returned a Song whose handle ended in theme.cnj without throwing, while Load<Song>(\"theme.ogg\") returned the .ogg. The sharp-runtime types came from a sibling checkout that TARGET does not pin. No Song or Video .cnj envelope type exists in the content module, and misc/cnj.md, which states that the resolver tries .cnj for every registered type, lists no Song or Video row in its per-reader sourceFile matrix. What MediaPlayer::Play reports for a JSON file depends on the audio backend's decoder and was not checked."}}
{"id": "CNA-BUG-041", "outcome": "HOLDS WITH NOTES", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug041.cpp: real TARGET ContentManager.cpp, ContentLoadException.cpp, Song.cpp, runtime/src/TitleContainer.cpp and TitleLocation.cpp, headless platform sources (same link recipe as the BUG-008 probe; sharp-runtime from the unpinned sibling checkout). HeadlessPlatform installed, TitleLocation::setPathProperty(<dir holding r17-content/theme.ogg>), current_path changed to /, then TitleContainer::OpenStream(\"r17-content/theme.ogg\") and ContentManager(nullptr, \"r17-content\").Load<Song>(\"theme.ogg\").", "result": "TitleContainer resolved the name against TitleLocation and opened the file. ContentManager with the relative root threw ContentLoadException: \"could not load asset 'theme.ogg' ---> Could not find file 'r17-content/theme.ogg'\", i.e. it resolved against the working directory. Not run: the SDL3 default (SDL_GetBasePath equals the executable directory, so no manual TitleLocation would be needed) and everything Android."}], "refutation_attempt": "Looked for any ContentManager, Game or runtime path that reads TitleLocation, GetBasePath or changes the working directory (none; only CnjSourceFile.hpp and StorageDevice use current_path). Read the decompiled XNA ContentManager.RootDirectory and OpenStream, which combine a relative root with TitleLocation.Path. Checked every platform's GetBasePath and the Android branches.", "notes": "Claim holds, including the Android halves (only TryReadAssetBytes has the platform route; the .cnb tier and ResolveAssetPath gate on std::filesystem::exists). The XNA premise is confirmed by the decompiled ContentManager, which builds fullRootDirectory from TitleLocation.Path. Only the evidence text should change: the working-directory versus TitleLocation divergence has now been run (with TitleLocation set by hand), while the SDL3 default and the Android tiers remain source-verified.", "correction": {"evidence_note": "Checked by reading at 009d40f5, plus one audit run for the central claim. A probe built from the TARGET ContentManager, TitleContainer and TitleLocation sources (headless platform, TitleLocation set by hand to the directory holding Content, working directory changed to /) showed TitleContainer::OpenStream finding the asset while ContentManager with the relative root \"Content\" threw ContentLoadException, so ContentManager resolves against the working directory. The SDL3 default, where SDL_GetBasePath supplies the executable directory, and the Android and Apple legs were not run. Whether SDL_LoadFile (reached by TryReadAssetBytes' platform fallback) resolves a relative name inside an Apple app bundle depends on SDL's own I/O layer and was not checked, so the Apple leg of the .xnb tier may work; the .cnb and loose tiers never reach that fallback. The site's ContentManager guide documents the working-directory rule, and the content-runtime internals page hedges the Android .cnb question; this entry supplies the reading for both. The disagreement with TitleContainer exists only on the SDL3 platform: the SDL2, X11, Wayland, native Win32, headless and terminal platforms answer GetBasePath() through StandardFileSystem::GetBasePath (StandardFileSystem.cpp), which returns the working directory, so on those platforms TitleLocation and ContentManager agree with each other and both differ from XNA's executable-directory rule."}}
{"id": "CNA-BUG-146", "outcome": "HOLDS WITH NOTES", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug146.cpp linked with the real TARGET modules/video-ffmpeg/src/VideoDecoder.cpp (g++ -O0, system FFmpeg 7.1 headers and libs; no CNA build). Clips made with the ffmpeg CLI from a raw 5-bar yuv420p file (limited range, BT.709-encoded black, white, red, green, blue, tagged tv/bt709) and the TARGET fixture tests/assets/media/video/chroma_420.mkv. Decoded the first frame with VideoDecoder::NextFrame and read the bar centres.", "result": "Synthetic clip, correct values black (0,0,0), white (255,255,255), red (255,0,0), green (0,255,0), blue (0,0,255); CNA gave black (16,16,16), white (233,236,235), red (199,0,17), green (44,255,25), blue (18,1,218). The TARGET SMPTE fixture (ffprobe: yuv420p, tv range, bt470bg) decoded to 75% white (180,180,180) and blacks at 12-15, against 190 and 0 from ffmpeg's own rgb24 conversion, so it sits inside the tests' +/-20 window but outside the +/-10 the manifest recommends."}], "refutation_attempt": "Looked for a swscale or colour-range call anywhere in VideoDecoder.cpp (none; the module deliberately links no libswscale), a documented limitation in plan_media.md and the header (only 'BT.601' comments), and for compensation in the texture path. Checked FNA's YUVToRGBA.fx as a baseline: it always assumes limited range, so CNA's full-range choice is not FNA behaviour either.", "notes": "Claim reproduces exactly as described. The Evidence sentence 'Whether the fixtures are limited-range was not checked' can be replaced: the fixtures are tagged tv range and bt470bg, and the 75% white bar decodes to 180. The entry could add that FNA's YUVToRGBA effect (the design CNA replaced) subtracts 16/256 and applies the 1.164 factor, so FNA expands limited range, using fixed BT.709 coefficients, whereas CNA does neither.", "correction": {"evidence_note": "Reproduced by the audit at 009d40f5. The TARGET VideoDecoder.cpp was built with g++ -O0 against system FFmpeg 7.1 and run on a synthetic limited-range BT.709 clip: black decoded to (16,16,16), white to about (233,236,235), full-scale red to (199,0,17), where a range- and matrix-aware conversion gives 0, 255 and (255,0,0). The TARGET fixture chroma_420.mkv is tagged tv range and bt470bg; its 75% white bar decodes to (180,180,180) and its black bars to 12-15, against about 190 and 0 from ffmpeg's own conversion, which is inside the tests' +/-20 window but outside the +/-10 the manifest recommends. The formula constants are the full-range BT.601 ones. FNA's replaced YUVToRGBA effect subtracts 16/256 and applies the 1.164 factor with fixed BT.709 coefficients, so it expands limited range where CNA does not."}}
{"id": "CNA-BUG-143", "outcome": "NEEDS CORRECTION", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug143.cpp: TARGET modules/media/src/Xna/Video/VideoPlayer.cpp and Video.cpp compiled with g++ -O0 -fsanitize=address (no SOUND_ENABLED, headless renderer macro, Video built with a null GraphicsDevice), linked with the real TARGET video-ffmpeg VideoDecoder.cpp and system FFmpeg 7.1, non-PIE, unresolved symbols ignored (only graphics types, never touched); TARGET fixture chroma_420.mkv. Three modes, each a separate process.", "result": "Mode a: Video* v=new Video; VideoPlayer p; p.Play(v); delete v; p.Stop() gave ASan heap-use-after-free WRITE of size 8 in VideoPlayer::CloseDecoder called from Stop, inside the freed Video (offset of parent_). Mode b: copy of a playing Video, delete the player, copy->SetAudioTrackEXT(0) gave heap-use-after-free READ in VideoPlayer::SetAudioTrackEXT called from Video::SetAudioTrackEXT. Mode c (no copy): p.Play(v1); p.Play(v2); p.Stop(); delete p; v1->SetAudioTrackEXT(0) gave the same heap-use-after-free READ."}], "refutation_attempt": "Tried to find a guard: Video has no destructor or copy control, VideoPlayer never clears the old Video, nothing detaches in ~Video. Checked the ordering claim in Play (video_ assigned before OpenDecoder runs CloseDecoder) and found it also means the previously played Video is never detached, which the entry does not say.", "notes": "Both published directions reproduce. One statement is incomplete and misleading: the entry says the player clears parent_ only on the Video it played, so only a copy is dangerous. In fact Play assigns video_ = video before OpenDecoder calls CloseDecoder, so CloseDecoder clears parent_ on the NEW video and the previously played Video keeps parent_ pointing at the player. No copy is needed: Play(a), Play(b), destroy the player, then a.SetAudioTrackEXT is a use-after-free (mode c). The regression test and workaround should cover that.", "correction": {"actual": "VideoPlayer::OpenDecoder in VideoPlayer.cpp stores video_ = video and sets video->parent_ = this. CloseDecoder starts with if (video_) video_->parent_ = nullptr; and runs from Stop, Dispose and the destructor (through Dispose). Video declares no destructor, so a Video destroyed while a player still holds it leaves video_ dangling, and the next of those calls writes into freed memory; getVideoProperty() returns the dangling pointer meanwhile, and a SetVideoTrackEXT call on the player during playback reads through it (ReconfigureVideoOutputForCurrentTrack). A later Play(other) is not affected in that direction: it assigns video_ = video before OpenDecoder runs CloseDecoder. Ordinary member order is enough to trigger it: a class that declares its VideoPlayer before a Video member destroys the Video first.\nThe other direction exists too. Video::SetAudioTrackEXT and SetVideoTrackEXT call through parent_ without checking that the player is alive. Video is implicitly copyable and copies keep parent_, so a copy made during playback calls into a destroyed player later. The same happens without any copy: because Play assigns video_ = video before OpenDecoder calls CloseDecoder, CloseDecoder clears parent_ on the new Video, so the Video played earlier keeps a parent_ that points at the player. Play(a), Play(b), destroy the player, then a.SetAudioTrackEXT(0) reads freed memory.", "evidence_note": "Reproduced by the audit at 009d40f5, not by CNA's own tests. The TARGET VideoPlayer.cpp and Video.cpp were compiled with AddressSanitizer against the real FFmpeg VideoDecoder.cpp and a TARGET fixture clip (Video built with no GraphicsDevice, no SOUND_ENABLED). Deleting a playing Video and then calling Stop() gave a heap-use-after-free WRITE in VideoPlayer::CloseDecoder. Copying a playing Video, destroying the player and calling SetAudioTrackEXT on the copy gave a heap-use-after-free READ in VideoPlayer::SetAudioTrackEXT, and so did Play(a), Play(b), destroying the player and calling a.SetAudioTrackEXT with no copy at all. The sharp-runtime types came from a sibling checkout that TARGET does not pin."}}
{"id": "CNA-BUG-116", "outcome": "HOLDS", "basis_after": "source-verified", "executed": [], "refutation_attempt": "Searched for any other StretchRect or resolve site (only D3D9RenderTargets ResolveForTransitionEXT and UnbindAsRenderTarget, which GraphicsDevice never calls), for GetData or sampling resolving lazily (GetData reads the resolved colorSurface_ without resolving), for GraphicsDevice rejecting MSAA in MRT sets, and for a documented refusal. Needs Direct3D 9 on Windows, so not runnable here.", "notes": "Holds. Only wording: DirectX9Renderer.hpp still carries a stale comment on currentCustomRT_ saying MRT is not tracked yet and inherits the base default, contradicting the implemented MRT branch. The entry could cite it as the reason the gap was never noticed, and it supports keeping the class as bug since the call succeeds and silently leaves stale textures instead of refusing.", "correction": null}
{"id": "CNA-BUG-045", "outcome": "HOLDS", "basis_after": "reproduced", "executed": [{"probe": "The entry's own reproduction against the TARGET tree: grep -n -i -E 'cgltf|stb_image|enet|dr_libs|dr_flac|dr_mp3' THIRD_PARTY_NOTICES.md, and the same pattern (plus stb) on NOTICE.md; then grep of the section headings.", "result": "No matches in THIRD_PARTY_NOTICES.md (exit 1) and none in NOTICE.md (a five-line file about Ms-PL and FNA). The file's sections are DirectXMesh, Draco, avatar content, glTF assets, XNA effects, FNA3D, wgpu-native, FreeType, stb_vorbis and Liberation Mono, so the omission is real. The cgltf and ENet LICENSE files are MIT with the copyright lines the entry quotes, and the dr_flac and stb headers carry the public-domain or MIT-0 alternatives."}], "refutation_attempt": "Looked for the notices elsewhere (NOTICE.md, README, tools/provenance): NOTICE.md lists nothing, and the provenance gate only checks vendor/ (GoogleTest), not third_party/. Checked that each component is really compiled in (CGLTF_IMPLEMENTATION, STB_IMAGE_IMPLEMENTATION, DR_FLAC_IMPLEMENTATION, ThirdPartyENet.cmake under CNA_ENABLE_NET default ON).", "notes": "Holds. Optional precision: stb_image and stb_image_write are also implemented, with STB_IMAGE_STATIC, in modules/content/src/GltfImport/GltfImportCore.cpp, not only in ImageLoader.cpp. The claim that no gate compares third_party/ with the notices file is right: tools/provenance/provenance_gate.py checks only vendor/ against third-party.json. A grep is a reading, so the basis is reproduced only in the sense of the entry's own focused reproduction.", "correction": null}
{"id": "CNA-BUG-061", "outcome": "HOLDS", "basis_after": "source-verified", "executed": [{"probe": "grep -n -E '3,?467|3,?627|160 remaining|160 gaps' on README.md, docs/xna-4-api-coverage.md, docs/coverage.md and docs/xna-4-runtime-member-coverage.md at TARGET; python3 read of docs/xna-4-runtime-member-coverage.json (member_coverage.represented).", "result": "README.md prints 3,467 of 3,627 (95.59%) and 'lists all 160 remaining gaps'; docs/xna-4-api-coverage.md prints 3,467/3,627 and 'all 160 gaps'; docs/coverage.md opening paragraph prints 3,467/3,627 as what the audit 'now reports'; the generated report prints 'Strict documented members: 3627 / 3627', MISSING 0, NEEDS_REVIEW 0, and the JSON member_coverage.represented is 3627. plans/plan_xna_runtime_surface.md also says 'the current report is 3,467/3,627'."}], "refutation_attempt": "Checked whether the prose figures are labelled historical (docs/coverage.md is a historical snapshot but its opening paragraph makes a present-tense claim about the current audit), whether the report itself carries 3467 anywhere (it shows a 3463 pre-fix baseline and 164 newly represented), and git history: the report was regenerated at 3627/3627 on 2026-09-21 without touching the README.", "notes": "Holds; class and severity are fine for a documentation drift in the CNA repo. Optional precision: the report's own pre-fix baseline is 3463 of 3627 with 164 missing, so the stale 3,467 and 160 are an intermediate figure, not the baseline.", "correction": null}
{"id": "CNA-BUG-028", "outcome": "NEEDS CORRECTION", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug028.cpp linked with all TARGET modules/math/src objects (g++ -O0; sharp-runtime types from the unpinned sibling checkout). Compared Curve::ComputeTangent(Smooth) with a float32 model of the decompiled XNA 4.0 IL (|v1-v0| < 1.1920929E-07 test, (dv * |p0-p|) / (p1-p0)): the entry's reproduction, a narrow-span case, and 1,000,000 random sorted triples.", "result": "Entry reproduction (keys (0,0), (1,0), (2,1e-8), key 1): CNA in=5e-09 out=5e-09, XNA model 0 and 0. Narrow span (positions 0, 1e-9, 2e-9, values 0, 3, 6): CNA in=0 out=3, XNA model 3 and 3, so the two tangents of one key follow different rules. Ordinary inputs: CNA differs from the strict-float32 XNA model in 34.4% of in tangents and 34.3% of out tangents, by at most 2 ulp (caveat: real XNA on x87 may keep wider intermediates)."}], "refutation_attempt": "Re-read the decompiled XNA IL and FNA Curve.cs and MathHelper.cs (CNA equals FNA), CNA's WithinEpsilon and machine epsilon, and every test touching Smooth tangents. Ran the entry's two claims and compared against a model of the IL. The defect holds; the published Current tests text contradicts the entry's own re-verification paragraph.", "notes": "Central claim, thresholds and reproduction values are right. Two published statements need fixing. The Current tests paragraph says the C ABI curve smoke test exercises Linear tangents only, but CurveSmoke.c calls cna_curve_compute_tangents_in_out(FLAT, SMOOTH) and expects 2.0 on evenly spaced keys, as the Evidence paragraph already says; the Evidence text also repeats its first three sentences. And 'not measured' for last-bit differences can now be answered: about a third of random inputs differ by up to 2 ulp from a float32 model. Corrected wording: the smoke test runs one Smooth out tangent on evenly spaced keys, which cannot tell the rules apart.", "correction": {"evidence_note": "Reproduced by the audit at 009d40f5. The TARGET Curve.cpp was built with g++ -O0 and compared with a float32 model of the decompiled XNA 4.0 IL. The entry's reproduction gave CNA tangents of 5e-09 against XNA's 0. Keys at 0, 1e-9 and 2e-9 with values 0, 3 and 6 gave CNA in=0 and out=3 against XNA's 3 and 3. On 1,000,000 random sorted key triples CNA's tangents differed from the model's in about 34% of cases, by at most 2 ulp, because the operations are ordered differently (real XNA on an x87 JIT may keep wider intermediates, so the last-bit figure is against a strict float32 model). The XNA algorithm was read from its IL; an earlier reading that treated the out-tangent test as the correct one compared against FNA only, and against XNA both branches deviate. The C ABI curve smoke test does run a Smooth out tangent (compute_tangents_in_out with Flat in and Smooth out over keys at 0, 1, 2 with values 0, 2, 4 expects 2.0), but that input has evenly spaced keys on which CNA and XNA agree, so it cannot tell the rules apart."}}
{"id": "CNA-BUG-232", "outcome": "HOLDS", "basis_after": "source-verified", "executed": [{"probe": "grep -n -i platform, grep -n '^|' and grep for 'exactly one' on docs/physical-modules.md; grep for cna_platform, cna_diagnostics and Globalization in modules/{runtime,input,audio,devices-ext}/CMakeLists.txt; count of _cna_framework_modules in modules/CMakeLists.txt; grep for xvfb_screenshot_demo in CMake files.", "result": "The word 'platform' does not occur in the document; its table has 19 rows and no platform, content-pipeline or c-api row (the declared set has 22 modules). The runtime row lists graphics, input, content, audio, media, core, math with Core.Base and IO, while the CMake file also links cna_diagnostics, cna_platform and Globalization; input, audio and devices-ext omit cna_platform (audio also cna_diagnostics). Line 126 says top-level examples/ holds exactly one thing, examples/golden/, but examples/xvfb_screenshot_demo.cpp exists and is compiled by modules/renderers/easygl/examples and sdl-renderer/examples CMake files."}], "refutation_attempt": "Looked for platform mentioned under another name (cna_platform, native platform), for the missing rows described in a separate document that this one defers to, for the demo file being untracked or unused, and checked each cited CMake link line and the 22-module count.", "notes": "Holds. Greps are reading, so the basis stays source-verified; no runnable reproduction exists for a documentation drift.", "correction": null}
{"id": "CNA-BUG-104", "outcome": "HOLDS", "basis_after": "source-verified", "executed": [], "refutation_attempt": "Searched the DirectX9 module for any SupportsCapability override or GraphicsCapability mention (none), checked that GraphicsDevice::SupportsCapability forwards these two capabilities to the renderer, read the base default and its header contract (renderers with device-dependent gaps 'must override the applicable entries truthfully'), and both anisotropy code paths. Direct3D 9 is Windows-only, so not runnable here.", "notes": "Holds. The contract-bearing text is the IGraphicsRenderer::SupportsCapability comment in IGraphicsRenderer.hpp saying a renderer with device-dependent capability gaps must override the applicable entries truthfully; citing it would justify class bug rather than gap. Both quoted code paths are exactly as described.", "correction": null}
{"id": "CNA-BUG-069", "outcome": "HOLDS WITH NOTES", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug069.cpp linked with the TARGET modules/math/src objects (g++ -O0; sharp-runtime types from the unpinned sibling checkout). Called Ray::Intersects(BoundingBox) and Ray::Intersects(BoundingSphere) and compared with float32 models of the decompiled XNA 4.0 BoundingBox.Intersects(ref Ray) and Ray.Intersects(BoundingSphere), for the entry's cases, near-parallel rays and 2,000,000 random rays.", "result": "Zero-direction ray at the centre of a unit box: CNA nullopt, XNA 0. Ray on the surface of a unit sphere pointing outward: CNA nullopt, XNA 0. Threshold: origin (-5,0,0), direction (5e-7,0,0) toward a unit box: CNA hits at t=8e6, XNA (parallel below 1e-6, origin outside the X slab) misses; direction 1e-7 gives CNA 4e7 versus XNA miss. Random rays: no hit/miss disagreement, but CNA differs from the model in 2.1% of box results (1 ulp) and 1.0% of sphere results (up to 0.18% relative, near-tangent rays)."}], "refutation_attempt": "Compared against the decompiled XNA IL rather than FNA (the entry's premise: initial tMin 0, 1E-06 parallel threshold, <= inside test) and confirmed each on the model; checked RayTests.cpp for any zero-direction, near-parallel or on-surface case (none). Every claim reproduced.", "notes": "All three claims reproduce. The Blast radius sentence 'normal rays are unaffected' is slightly too strong: the operation order differs from XNA (XNA multiplies by 1/dir and computes the sphere root as r*r - (|d|^2 - t*t)), so a small share of ordinary rays differ by 1 ulp for boxes and, for near-tangent sphere rays, by up to about 0.2% (measured against a strict float32 model; real XNA on x87 may keep wider intermediates). Suggest 'normal rays are unaffected in hit/miss outcome'.", "correction": {"evidence_note": "Reproduced by the audit at 009d40f5. The TARGET math sources were compiled with g++ -O0 and compared with float32 models of the decompiled XNA 4.0 IL. A zero-direction ray at a box centre returned nullopt (XNA 0); a ray starting on a unit sphere and pointing outward returned nullopt (XNA 0); a ray from (-5,0,0) with direction (5e-7,0,0) toward a unit box returned t=8e6 (XNA: no hit, because XNA treats a component below 1e-6 as parallel). On 2,000,000 random rays hit and miss outcomes never disagreed, but about 2% of box results differed by 1 ulp and about 1% of sphere results differed, by up to 0.2% for near-tangent rays, because the operations are ordered differently. The sharp-runtime types came from a sibling checkout that TARGET does not pin."}}
{"id": "CNA-BUG-210", "outcome": "NEEDS CORRECTION", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug210.cpp with TARGET modules/diagnostics/src/Diagnostics.cpp and modules/inspector/src/Protocol.cpp compiled unchanged (g++ -std=c++23 -O0 -DCNA_DIAGNOSTICS_LEVEL=2, standard library only). Registered counters of 4096, 4097 and 5000 bytes, a 5000-byte resource label, a 5000-byte event name (NameHandle plus MarkEvent, resolved through ReadEvents and ResolveName), then ProtocolCodec::Encode of the snapshot and events responses; also 1 MiB and 1 MiB+1 counter names.", "result": "4096-byte name: id 1, Encode succeeded. 4097 and 5000 bytes: ids non-zero, Encode(metrics snapshot) threw std::length_error 'Inspector string exceeds its protocol bound'. 5000-byte resource label: registered, Encode(resources snapshot) threw. 5000-byte event name: registered, three events read, Encode(events) threw. Registration accepts exactly 1 MiB (id 4) and refuses 1 MiB+1 (id 0). Agent.cpp maps std::length_error to ErrorCode::LimitExceeded by reading; the agent round trip was not run."}], "refutation_attempt": "Looked for a documented bound or refusal policy: docs/inspector.md does not state the 4,096-byte string bound, and its Limit error bullet continues 'Oversized complete snapshots are refused rather than partially mislabeled', which documents refusal (not truncation) as the design. Confirmed no unregister for metrics and that Agent.cpp catches length_error.", "notes": "Defect reproduces exactly. Two published statements need fixing. Expected offers truncation with a marker as an acceptable outcome, but docs/inspector.md says oversized snapshots are refused rather than partially mislabeled, so the documented design is refusal. And the entry quotes only the first half of that bullet ('reduce event count or preview bounds') and says no remedy is offered, omitting the refusal sentence. The surviving defect: the 4,096-byte bound is neither documented nor enforced at registration, so one provider-valid name disables whole views.", "correction": {"expected": "The Inspector is a consumer of the unchanged provider v1 contract, and docs/inspector.md says oversized complete snapshots are refused rather than partially mislabeled. That refusal policy is therefore intended, but it should be predictable: either the two modules agree on one string bound that registration enforces and the documentation states, or the refusal is limited to the offending item instead of every snapshot and event batch that contains it.", "evidence_note": "Reproduced by the audit at 009d40f5. TARGET's Diagnostics.cpp and Protocol.cpp were compiled unchanged (g++ -std=c++23 -DCNA_DIAGNOSTICS_LEVEL=2) and driven directly: a 4,096-byte counter name encoded, while 4,097-byte and 5,000-byte counter names, a 5,000-byte resource label and a 5,000-byte resolved event name were all registered (non-zero id) and made ProtocolCodec::Encode throw length_error; registration accepts exactly 1 MiB and refuses 1 MiB plus one byte. The agent round trip and the browser view were not run; the mapping to LimitExceeded is by reading Agent.cpp. docs/inspector.md documents the refusal of oversized complete snapshots but neither the 4,096-byte string bound nor its remedy."}}
{"id": "CNA-BUG-016", "outcome": "HOLDS WITH NOTES", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug016.cpp including only TARGET's header-only modules/content/include/CNA/Internal/Json.hpp, g++ -std=c++23 -O0, run on Linux x86-64 with the default 8 MiB stack: ParseJson(std::string(d,'[') + std::string(d,']')) for several d, each in its own process, then a bisection of the crash depth.", "result": "d=1,000 and 10,000 parse; d=20,000, 30,000, 50,000 and 100,000 end in SIGSEGV. Bisection at -O0: 13,281 levels (26,562 bytes) parse, 13,437 levels (about 26.9 KB) crash, roughly 620 bytes of stack per level. No JsonParseException is ever thrown. (The -O2 comparison in the entry was not repeated: probes are -O0 only.)"}], "refutation_attempt": "Read Json.hpp for any depth counter or limit (none), checked whether the class comment 'correct nesting/depth tracking' really promises a limit (it describes grammar correctness), and confirmed the LocalGamerServicesStore header promise of never throwing for a corrupt file and the XNB-side maxObjectNestingDepth guard. Tried the crash at several depths.", "notes": "Reproduces. Two wording fixes. The summary says 'a 100,000-byte document', but the evidence and reproduction use 100,000 levels, which is a 200,000-byte document; at -O0 a document of about 27 KB is enough. And the Expected text leans on 'correct nesting/depth tracking' in the Json.hpp class comment, which describes grammar correctness, not a depth limit; the firmer contract is the JsonParseException promise, the LocalGamerServicesStore never-throws-for-a-corrupt-file comment and the XNB-side depth limit.", "correction": {"summary": "JsonParser recurses once per nesting level with no ceiling; a document of 100,000 nested arrays (200,000 bytes) crashed a probe with SIGSEGV instead of throwing JsonParseException, and with the default 8 MiB stack at -O0 about 13,400 levels (27 KB) are already enough.", "evidence_note": "Reproduced by the audit at 009d40f5. A probe that includes only TARGET's header-only Json.hpp, compiled with g++ -std=c++23 at -O0 and run on Linux x86-64 with the default 8 MiB stack, parsed 10,000 and 13,281 nested-array levels and crashed with SIGSEGV from 13,437 levels (about 27 KB) up, with 20,000, 50,000 and 100,000 levels all crashing; no JsonParseException was thrown. The entry's earlier run also crashed at 100,000 levels at -O2 and parsed 10,000. The full ContentManager path was not executed; that every .cnj load reaches ParseJson is by reading ParseCnjEnvelope. There is no JSON or .cnj fuzz target."}}
{"id": "CNA-BUG-062", "outcome": "HOLDS", "basis_after": "reproduced", "executed": [{"probe": "In the read-only TARGET tree with PYTHONDONTWRITEBYTECODE=1: python3 tools/c-api/generate_coverage_inventory.py --check; python3 tools/c-api/generate_limitations.py --check; python3 -m unittest test_coverage_scope (from tools/c-api); and an in-process call of check_release_gate.check_coverage_has_no_planned_rows and check_limitations_current. check_release_gate.py --check as a whole was not run (compiles probes).", "result": "Both --check commands print 'The runtime C API scope model does not describe this tree' naming modules/design/include, modules/diagnostics/include and modules/inspector/include and exit 2; test_coverage_scope ran 29 tests with 3 errors (RuntimeError from validate_module_scope through discover_headers); both release-gate measurement functions return 'not met'. Also read: the four add_test calls in cmake/Tests/ModuleProbes.cmake sit inside CNA_BUILD_TESTS and Python but outside CNA_BUILD_C_API, KNOWN_FAILURES in general-tests-ci.yml names only EasyGL_GraphicsDevice_ReferenceStencil, and release_gate.json records limitations-matrix as met."}], "refutation_attempt": "Checked that MODULE_SCOPE really lacks the three modules (it lists 14 runtime and 4 out-of-scope modules), that nothing else classifies them, that the CTests are registered in ordinary builds, and that the CI known-failure list does not absorb them. Ran every failing command rather than trusting the entry. All reproduce.", "notes": "Holds; class, severity and numbers are right. The CI red-status claim remains inferred from configuration (CI results are not visible), as the entry already says.", "correction": null}
{"id": "CNA-BUG-190", "outcome": "HOLDS", "basis_after": "inferred-strong", "executed": [], "refutation_attempt": "Read modules/renderers/CMakeLists.txt end to end, the DIRECTX11, DIRECTX12, GDI, software and metal family CMake files, every other definition of cna_renderer_d3dcommon (only common/d3d), the combination validator (HEADLESS;DIRECTX11 and DIRECTX9;DIRECTX11 pass), the docs table and presets. No guard or fallback found. Not executed: the rules forbid running CMake.", "notes": "Holds as described, including that a DIRECTX11 or DIRECTX12 default enters common/d3d and is not hit. Basis stays inferred-strong: whether the first failure is at generate, compile or link time is inferred. gdi/CMakeLists.txt only prints a status line for an empty CNA_GDI_SOFTWARE_SOURCES, it does not stop the configure, which matches the entry's description of an archive without its CPU-2D sources.", "correction": null}
{"id": "CNA-BUG-186", "outcome": "HOLDS", "basis_after": "inferred-strong", "executed": [], "refutation_attempt": "Read the probe in modules/CMakeLists.txt (four unversioned pkg_check_modules calls in the ON and AUTO branches), grepped VideoDecoder.cpp for a version guard (none) and the docs and CMake files for a stated minimum (none). Only FFmpeg 7.1 headers exist on this machine, so the compile failure with 4.x could not be run.", "notes": "Holds. Only a compile against FFmpeg 4.x headers would upgrade the basis, and none is installed here. The entry's introduction version (5.1) matches FFmpeg's channel-layout rework. Optional: plans/plan_native_platform_validation.md (NPV-0128) already records that CI runners have FFmpeg 6 and that another CNA decoder needed FFmpeg 7, which supports the point that no lane covers an old FFmpeg.", "correction": null}
{"id": "CNA-BUG-113", "outcome": "NEEDS CORRECTION", "basis_after": "recorded-by-cna", "executed": [], "refutation_attempt": "Re-read all four renderer paths (Vulkan computeScissor, SDL_GPU ApplyScissorForRef, WebGPU ApplyDrawScissor, Metal ShouldSkipDraw), the decompiled XNA ScissorRectangle setter (zero extents are accepted) and the shared fixture's contract table and E1 check. Behaviour confirmed by reading. GPU, display and Windows-free execution are not possible here, so CNA's own recorded fixture stays the evidence.", "notes": "Defect holds. The Current tests paragraph is wrong and contradicts the entry's own re-verification: it says the Vulkan and SDL_GPU scissor fixtures use non-empty rectangles, but modules/graphics/examples/deferred_scissor_capture_test.cpp check E1 sets zero-width, zero-height and 0x0 scissors on the 3D render-target path and, through the per-renderer contract (emptyScissorDrawsNothing=false for VULKAN and SDL_GPU), asserts the unclipped result. Replace it with that. The regression test is then a flip of those two declarations once fixed, not a new fixture.", "correction": {"evidence_note": "Checked by reading the named sources at 009d40f5; nothing was built or executed for this entry. The METAL behaviour was read in MetalRasterState (NativeScissor, ShouldSkipDraw) and metal-renderer.md. CNA's own records agree: deferred_scissor_capture_test.cpp, registered as Vulkan_Deferred_Scissor and SdlGpu_Deferred_Scissor, sets zero-width, zero-height and 0x0 scissors in check E1 (3D draws into a render target) and declares through emptyScissorDrawsNothing=false that VULKAN and SDL_GPU do not clip them, while the other families in that fixture (WEBGPU, EASYGL, OPENGL4, SOFTWARE, HEADLESS, DIRECTX9, DIRECTX11, DIRECTX12) declare that a degenerate rectangle rasterizes nothing; remediation/REMEDIATION_PROGRESS.md records the same divergence as an independent finding that was not fixed. So the existing tests do use empty rectangles (on the 3D render-target path; the SpriteBatch path with an empty rectangle is not exercised) but assert the divergent result. Current tests, corrected: Vulkan_Deferred_Scissor and SdlGpu_Deferred_Scissor exercise empty rectangles in E1 and pin the unclipped output for these two renderers; fixing the renderers means flipping those declarations."}}
{"id": "CNA-GAP-026", "outcome": "HOLDS", "basis_after": "recorded-by-cna", "executed": [], "refutation_attempt": "Grepped the SDL_RENDERER family for a SetSamplerAddressMode override (none; the base default is a silent no-op), read the test header's empirical finding and the completeness doc's blocked-decisions section (options a, b, c, none picked), and looked for a capability flag claiming address-mode support (none, which would make it a bug). Needs an SDL renderer and a display.", "notes": "Holds; a documented, deliberately undecided limitation, so functional-gap is right. CNA's own test header says the equivalence to Clamp was 'confirmed empirically' and that wiring SDL_SetRenderTextureAddressMode changed nothing, which is the recorded run the basis rests on.", "correction": null}
{"id": "CNA-BUG-115", "outcome": "HOLDS", "basis_after": "inferred-probable", "executed": [], "refutation_attempt": "Looked for a clamp or caps check between the public PresentationInterval and the D3D9 parameters (toSwapInterval in GraphicsDevice.cpp maps Two to 2 unchanged; BuildPresentParameters has no caps test), for a bounded retry in EnsureDeviceSize (none: presentationDirty_ stays set), and read the XNA PresentInterval documentation. Direct3D 9 is Windows-only, so the Reset failure could not be run.", "notes": "Holds; basis inferred-probable is right because Reset's behaviour with Two on a windowed device was never observed. The XNA spec (xna4-spec PresentInterval.xml) supports the expected text: One and Immediate are 'always available for both windowed and full-screen swap chains', while Two says to check the device's PresentInterval capability. The retry loop itself is a deliberate resilience design (comment in EnsureDeviceSize); the defect is that a persistent failure is treated like a transient one.", "correction": null}
{"id": "CNA-GAP-038", "outcome": "NEEDS CORRECTION", "basis_after": "source-verified", "executed": [], "refutation_attempt": "Tried to show the flag is reachable or the winding is handled: read the ContentManager warning and AppendGltfInstanceReportEXT, MeshInstanceOut::mirroredEXT, Model.hpp and the ModelMesh headers, and traced the import report to the Model. The gap is real and documented in code as 'a documented limit'; but the report does expose the mirrored placements to an application, contrary to the published sentence.", "notes": "The gap holds. One published sentence is wrong: 'an application has to recompute the determinant from the bone transforms to act on it'. Model::getGltfImportReportEXTProperty() (set at the end of ReadGltfModel) carries one diagnostic with Code mirrored-winding-unapplied and Subject = the node name for each mirrored placement, and its message tells the application to reverse culling, so the placements can be found by name without recomputing anything; what is missing is a per-ModelMesh flag. The Workaround should mention the report first.", "correction": {"actual": "ContentManager.cpp logs, per mirrored placement, that CNA \"does not reverse the triangle winding for it, so this placement's front faces are back-facing under the default cull mode\", and GltfImportCore.cpp (AppendGltfInstanceReportEXT) records mirrored-winding-unapplied: \"the application must reverse culling for this draw\". The reason is architectural: the index and vertex buffers are shared with unmirrored placements of the same mesh, and cull state is per-draw device state. No public Model or ModelMesh property carries a per-mesh mirroring flag. The only public route is the Model's import report (Model::getGltfImportReportEXTProperty), which lists each mirrored placement by node name in a diagnostic whose Code is mirrored-winding-unapplied, so an application has to map that node name to the ModelMesh it draws, or recompute the determinant from the bone transforms."}}
{"id": "CNA-GAP-009", "outcome": "HOLDS", "basis_after": "source-verified", "executed": [{"probe": "A Python scan over every .hpp and .cpp under TARGET modules/ (excluding tests and examples) for a Draw overload taking a texture renderer followed by four floats, plus greps for the test registrations.", "result": "Exactly one default (IGraphicsRenderer.hpp) and three overriding implementations: EasyGLRenderer, SoftwareSpriteBatch (SOFTWARE) and OpenGL4SpriteBatchRenderer; no other renderer, PortableGL included (own sprite batch class, no float overload). SpriteBatch::flushBatch passes s.destX/destY/destWidth/destHeight to renderer_->Draw. spritebatch_subpixel_contract_test.cpp is registered in easygl and software example CMake files and in opengl4's EasyGLParityCorpus.cmake only."}], "refutation_attempt": "Tried to find a renderer overriding the float overload under other parameter names (the scan is name-independent), a sub-pixel path bypassing it (SpriteBatch always forwards floats), and a documented contract making truncation a bug: the interface comment calls it the default ('one that does not is unchanged'), so functional-gap is right.", "notes": "Holds. The scan is a reading tool, so the basis stays source-verified; it supports the list of three overriding renderers exactly.", "correction": null}
{"id": "CNA-GAP-008", "outcome": "HOLDS WITH NOTES", "basis_after": "source-verified", "executed": [], "refutation_attempt": "Re-read QueueCustomEffect3DDrawEXT (attributes from the first declaration, buffer_slot 0), IssueCustomEffect3DDrawEXT, the SMG-0022 branch in DrawInstancedPrimitivesEx, SupportsCapability (MultiStreamVertexInput true) and the limitation text. Searched tests and docs for anyone reading the instancing sentence (none) and plans for the sentence's origin: plan_sdlgpu_modern_graphics.md shows it predates SMG-0022. Needs an SDL_GPU device, so not run.", "notes": "Facts hold. Class deserves a second look: GraphicsCapability.hpp says a renderer that would 'silently render from a subset of the bound streams' must report MultiStreamVertexInput false, and that the classic one-per-vertex-plus-one-per-instance shape needs no capability and is unaffected, yet SDL_GPU reports true while its ShaderEffect route ignores extra streams. The limitation text also claims all sixteen stream bindings and says ShaderEffect instancing is not implemented, which SMG-0022 made stale. The misstatement half reads as a low bug in the report text; consider splitting or reclassifying, and cite that header comment.", "correction": null}
{"id": "CNA-PLAT-005", "outcome": "HOLDS", "basis_after": "source-verified", "executed": [{"probe": "grep -c -i 'occlusion|SDL_GPUQuery|querypool|query_pool' and a grep for 'query' on an SDL 3.5.0 SDL_gpu.h found on this machine (living-room-simulator's prebuilt SDL source, commit 590c2508, version 3.5.0). This is NOT CNA's gitlinked SDL commit (cbe3fbe9), which is not available in the read-only TARGET tree.", "result": "0 occurrences of occlusion or query-pool names; the only query API in the header is SDL_QueryGPUFence (a fence completion query). Consistent with the entry's claim for a 3.5.0 SDL_gpu; not proof for CNA's exact submodule commit."}], "refutation_attempt": "Checked that CreateOcclusionQuery throws NotSupportedException with the quoted text, that SupportsCapability(OcclusionQuery) returns false and the limitation text repeats it, that the test files exist (sdlgpu_occlusionquery_limitation_test.cpp), and looked for a fence, timestamp or software fallback in the renderer (none). Class check: the cause is a missing capability in a third-party library, so platform-limitation fits.", "notes": "Holds. The dependency claim rests on the header of a 3.5.0 SDL copy other than CNA's pinned submodule commit, so it stays source-verified rather than reproduced.", "correction": null}
{"id": "CNA-PLAT-011", "outcome": "HOLDS WITH NOTES", "basis_after": "inferred-strong", "executed": [{"probe": "grep -rIn -i 'idbfs|syncfs|FS\\.mount|lidbfs|emscripten_idb|IndexedDB|localStorage|FS.persist' over TARGET code, CMake, scripts and JSON (excluding docs, plans, audit); grep of docs and plans for idbfs or syncfs; grep for Emscripten in modules/storage/src.", "result": "No hit in code or build files (the only FS mentions are -sEXPORTED_RUNTIME_METHODS lists that export FS, in c-api, webgpu and html-dom CMake files); nothing in docs or plans; modules/storage/src has no Emscripten branch, so StorageDevice resolves XDG_DATA_HOME, LOCALAPPDATA or HOME and creates the directory in Emscripten's default in-memory file system. docs/c-api/WASM_ARTIFACT.md says FS is exported so a consumer can populate MEMFS."}], "refutation_attempt": "Looked for any persistence mount in build flags, the platform layer, SDL glue and the C ABI wasm artifact (none), for a documented statement that saves are expected to persist (none) and for a test (none). Not run: a browser is needed to observe the loss on reload.", "notes": "The absence claim reproduces by grep, and the inference (writes go to MEMFS) is standard Emscripten behaviour. Only a class question remains: the browser can persist through IDBFS, CNA merely does not mount or sync it, and the entry's own workaround is to do that in the embedding page, so it reads as much like a functional-gap (no web persistence feature) as a platform-limitation. Either is defensible; the wording could say which one the project means.", "correction": null}
{"id": "CNA-VGAP-046", "outcome": "HOLDS WITH NOTES", "basis_after": "source-verified", "executed": [{"probe": "python3 scripts/compare-fna-reference.py <fna.json> <cna.json> --category math on two two-line JSON files in build-probe: one where the CNA side has an extra key, one where the CNA side lacks a key the FNA side has. Plus greps of every ctest invocation in .github/workflows and of the test registrations and labels.", "result": "Extra CNA-only key: 'PASS: all compared values match', exit 0. Key missing on the CNA side: FAIL, exit 1, named. So the comparison really is one-directional, as the entry says (and as its docstring says on purpose). Workflow reading: no ctest filter or label selects D3D9_XNA_Diff (no DIRECTX9 job), Fna3d_XNA_Oracle (no FNA3D job) or Software_XnaLineCoverage (label Software; filters are gltf-conformance, input, GDI, matrix renderer, and -R name patterns); only the unfiltered general-tests-ci run reaches EasyGL_XnaLineCoverage."}], "refutation_attempt": "Read every ctest and test-binary invocation in all 20 workflows, the D3D matrix (DIRECTX11, DIRECTX12, DIRECT2D only), the registrations and labels of the three tests, the FNA harness CMake (executable without add_test) and xna-diff.py's Pillow import. Found gltf-renderer-stride-ci.yml installs python3-pil, but only for the viewer L7 job, which runs none of these tests.", "notes": "Holds. The title and summary say the reachable image check 'runs where Pillow is not installed', but the body says only that the workflow installs no Pillow and that whether the runner image provides it is not recorded. xna-diff.py itself says it relies on Pillow being 'already-installed on this machine'. Soften the title to 'the workflow that reaches it installs no Pillow'. Optionally note that the glTF L7 job does install python3-pil, so the project knows the package is not implicit.", "correction": {"title": "No workflow runs the XNA reference-image corpus comparisons or the FNA value-differential harness, and the one reachable image check runs in a workflow that installs no Pillow", "summary": "D3D9_XNA_Diff, Fna3d_XNA_Oracle and Software_XnaLineCoverage are in no workflow's test selection, the one reachable image-oracle CTest runs in a workflow that installs no Pillow, and the FNA value-differential harness is manual because it needs mono and a built FNA.dll."}}
{"id": "CNA-VGAP-028", "outcome": "HOLDS", "basis_after": "source-verified", "executed": [{"probe": "ls and Python reads of the fixture directories and manifests; grep -rn 'xna40/differential' over every .cpp, .hpp, CMake and Python file at TARGET (excluding docs, plans, audit).", "result": "tests/reference/xna40/differential holds 130 .xnb files plus differential-oracle.json; the fixtures.json of tests/assets/xnb/cna says 'Nothing here has been validated against a real XNA 4.0 runtime'; tests/assets/xnb/xna40 holds two .xnb files (Hud.xnb, ContentManifestListStrings.xnb); every test reference to the differential corpus sits in modules/content-pipeline/tests (XnaXmlSourceRouteTests, XnaErrorParityTests, XnaDifferentialBuildTests, XmaEncoderServiceTests) or is a comment in DxtUtilTests.cpp; nothing under modules/content/tests loads it."}], "refutation_attempt": "Looked for a runtime Load<T> or reader test that reads the differential corpus (none), for external fixtures of Curve, Texture3D, Video and the four stock effects among the MonoGame files (none in the listing), and checked that XmaEncoderServiceTests, the only test reading SoundEffect differential files, concerns a type outside the entry's list.", "notes": "Holds; greps and listings only. Checking that each named reader lacks an externally produced runtime test rests on the absence of any load of the differential files and on the MonoGame fixture list.", "correction": null}
{"id": "CNA-VGAP-052", "outcome": "HOLDS", "basis_after": "source-verified", "executed": [{"probe": "grep of InspectorTests.cpp for TEST macros, includes, Stop and WebBridge; grep of modules/inspector/tests and the module CMake for WebBridge; greps of WebBridge.cpp for each named check; grep of workflows for CNA_BUILD_INSPECTOR; docs/inspector.md for the smoke test.", "result": "InspectorTests.cpp has 7 InspectorProtocolTests, 1 InspectorFrontendTests and 19 InspectorAgentTests; it includes Agent, Client, Protocol, InternalSocket and WebAssets only, never WebBridge; no Stop() call anywhere. WebBridge.cpp contains the 127.0.0.1:<port> Host check, x-cna-inspector-ui-token check, MaximumConcurrentConnections = 32 with a 503, HttpHeaderTimeoutMilliseconds = 1000 and the CSP, nosniff and no-store headers. Agent.hpp says Stop() is 'safe to call more than once'. No workflow sets CNA_BUILD_INSPECTOR. docs/inspector.md records a live HTTP smoke test, not an automated one."}], "refutation_attempt": "Looked for any other test file, script or CTest that drives the bridge or Stop (none under modules/inspector/tests, none in the module CMake), for a Stop call hidden in a fixture destructor helper (agents are destroyed via unique_ptr, as the entry says), and for CI coverage (none builds the inspector).", "notes": "Holds; counts and named checks are exact.", "correction": null}
{"id": "CNA-BUG-270", "outcome": "HOLDS WITH NOTES", "basis_after": "source-verified", "executed": [{"probe": "grep -rn 'invalid on return' modules/c-api/include; reading of the two header blocks, of both implementations in CnaCApiEngineLayer.cpp, of docs/c-api/OWNERSHIP.md and of EngineLayerSmoke.c around the owned-pass and owned-environment calls.", "result": "Only two lines say 'invalid on return whether or not the call succeeded' (engine_layer.h pass and environment @param). The same header's descriptions say the handle is released on success, and OWNERSHIP.md says the transferred category means consumed on success and unchanged on failure. In both implementations every refusal (invalid chain, invalid or lending pass, invalid skybox, non-cube handle) returns before GetRuntimeHandles().Release, and the skybox comment calls the release the last step that can fail. The refusal cases cannot be run without an engine-layer build."}], "refutation_attempt": "Looked for a true reading of the @param line (a conservative 'treat as dead' rule) and for an implementation that consumes on refusal (none: even the release-failure branch restores the chain and returns an error). Checked that the phrase appears only in these two places.", "notes": "Holds. One wording point: Current tests says 'None found' although tests_present is true. EngineLayerSmoke.c does exercise both routes: it pins consumption on success (a second release must fail) and the CNA_INVALID_HANDLE refusal, so cite it and say what it lacks, namely a refusal with a valid handle (a lending pass, a non-cube handle) checked for a still-valid handle.", "correction": {"evidence_note": "Read at 009d40f5; nothing was built or run. Found by the C13 conservation reviewer (native C API appendix) and re-verified against the header text and the two implementations. Current tests, corrected: modules/c-api/tests/pure_c/EngineLayerSmoke.c calls both routes. It checks that a successful hand-over consumes the handle (a second release fails) and that CNA_INVALID_HANDLE is refused, but no test hands over a valid handle that a route refuses (a pass that is lending its effect, a handle that is not a cube map) and then checks that the handle is still valid."}}
{"id": "CNA-GAP-063", "outcome": "NEEDS CORRECTION", "basis_after": "source-verified", "executed": [{"probe": "A pure-Python model, written from the TARGET Game::Tick source and the decompiled XNA 4.0 Game.Tick and GameClock, run on a virtual clock for 10 seconds at several targets (a model of the code, not the code itself; the real Game::Tick needs the whole runtime and was not built).", "result": "16.7, 400 and 500 ms: CNA and XNA both update every interval. 505 and 507 ms: XNA model 20 updates, CNA 0 updates (and about 190 to 135 draws per second). 508.5, 520 and 1000 ms: XNA model 0 updates and 0 draws, CNA 0 updates with draws at about 1/(target - 500 ms): 19 draws in 10 s at 1 s. So the ceiling (about 508 ms in XNA, 500 ms in CNA) and the CNA-keeps-drawing difference reproduce in the model."}], "refutation_attempt": "Re-read Game::Tick (wait loops, clamp to MaxElapsedTime, then the fixed update loop), the setter, the decompiled XNA Game.Tick and GameClock (elapsedAdjustedTime measured from the last advanced frame, clamped to 500 ms, num == 0 returns before AdvanceFrameTime), and the published reproduction. The gap holds; two published statements are inaccurate.", "notes": "Holds, but fix two statements. (1) The Focused reproduction ends with 'XNA 4.0 would have updated twice', which contradicts the entry's own IL analysis and re-verification note: with a 1 s target XNA never updates (or draws). Say 'XNA 4.0 would not update either'. (2) The draw rate is not universally 'about twice a second': CNA draws at 1/(target - 500 ms), so twice a second only at a one-second target, and about 200 per second at 505 ms.", "correction": {"summary": "With IsFixedTimeStep true, Game::Tick clamps the accumulator to MaxElapsedTime (500 ms) before its update loop, so a TargetElapsedTime above 500 ms is never reached: Update does not run while Draw keeps running, at about 1 / (target - 500 ms) per second (twice a second for a one-second target). XNA 4.0's Tick has essentially the same ceiling (about 508 ms, because of its 1/64 snap), but neither updates nor draws above it, so the limit is inherited; the setter accepts such a value silently.", "evidence_note": "Checked by reading Game::Tick in Game.cpp at 009d40f5, and compared with the IL of XNA 4.0's Game.Tick and GameClock and with FNA's Game.Tick. The failure follows mechanically from the order 'clamp, then compare'. A pure-Python model of both Tick functions on a virtual clock (audit run, not the real code) agrees: at 505 and 507 ms XNA updates and CNA does not, and from about 508 ms XNA neither updates nor draws while CNA never updates and draws at 1 / (target - 500 ms). The Focused reproduction should end 'XNA 4.0 would not update either; at 009d40f5 game.updates == 0 and Draw runs about twice a second'."}}
{"id": "CNA-PLAT-009", "outcome": "HOLDS", "basis_after": "source-verified", "executed": [], "refutation_attempt": "Read Stop's bounded wait (runExitedCv_.wait_for with kNativeCallTimeout of 5 s, then detach and abandoned_.store(true)), abandoned_'s doc comment ('never reset'), Start's abandoned_ gate, Compass::Dispose nulling control_->owner before Stop, and the SensorOwnerControlBlock comment naming the concurrent-destruction window as a documented, accepted boundary. Looked for a reset of abandoned_ or a cancel path (none). Android-only code, not runnable here.", "notes": "Holds. The class rests on CNA's own comments: a thread stuck in a native NDK call cannot be cancelled from C++, and the code calls the abandoned state 'a deliberate fatal backend-health state, not a bug', so platform-limitation is the right class rather than bug.", "correction": null}
{"id": "CNA-BUG-168", "outcome": "HOLDS", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug168.cpp with TARGET modules/gamer-services/src/Internal/LocalGamerServicesStore.cpp, gamer-services PropertyDictionary.cpp and storage StorageDevice.cpp, StorageContainer.cpp, StorageDeviceNotConnectedException.cpp (g++ -O0; sharp-runtime from the unpinned sibling checkout), XDG_DATA_HOME pointed at a directory inside build-probe. Called SanitizeStoreFileNameComponent, SaveEarnedAchievementEXT and LoadEarnedAchievementsEXT.", "result": "\"Ann B\", \"Ann_B\" and \"Ann!B\" all sanitise to \"Ann_B\", and the two non-ASCII names \"\u00e9x\" and \"\u0161x\" both to \"__x\". After SaveEarnedAchievementEXT(\"Ann B\", \"first_steps\", ...) one file was written, achievements/Ann_B.json, and LoadEarnedAchievementsEXT(\"Ann_B\") and (\"Ann!B\") each returned 1 entry with key first_steps; \"Bob\" (a distinct sanitised name) returned 0."}], "refutation_attempt": "Looked for anything keeping gamers apart before the file name is chosen (SignedInGamer passes the raw gamertag to the store), for the gamertag being recorded in the file (it is not), for a test with colliding names (the isolation test uses tagA and tagB), and for a locale effect on isalnum (the C locale keeps ASCII only).", "notes": "Holds exactly as written. The four dispatcher-created gamers sanitise to distinct names, so only application-created gamers collide, as the entry says. The sanitiser depends on isalnum, which follows the C locale, so under a single-byte non-C locale the exact set of colliding names could differ; not needed for the entry.", "correction": null}
{"id": "CNA-BUG-183", "outcome": "HOLDS WITH NOTES", "basis_after": "reproduced", "executed": [{"probe": "build-probe/r17-bug183b.cpp: the real TARGET modules/platform/src/Sdl3/Sdl3SystemServices.cpp (g++ -O0) linked with the system SDL 3.4.0 in /usr/local/lib (NOT CNA's gitlinked SDL commit, which TARGET does not contain), calling Sdl3Dialogs::ShowOpenFileDialog with a callback that throws std::runtime_error; a virtual X display (Xvfb), zenity installed, so SDL's zenity driver opened a dialog; I ended the dialog by killing its zenity child. A second stand-in probe (r17-bug183.cpp plus a tiny C file) tested what an exception does when it crosses a C frame.", "result": "After zenity was terminated the callback ran (0 paths, so a cancel) through ResultTrampoline, threw, and the process died: 'terminate called after throwing an instance of std::runtime_error', exit 134 (SIGABRT). The stand-in C frame showed the other outcome: compiled with default x86-64 flags the exception unwound through the C frame and was caught in main; compiled with -fno-asynchronous-unwind-tables it terminated. The tray trampoline was not run (needs a system tray); the stray zenity processes were killed afterwards."}], "refutation_attempt": "Looked for a catch in the trampolines (none), in the devices-ext FileDialog and SystemTray wrappers (they forward the callback unchanged), and for the policy being documented as applying elsewhere: section 6 of devices-event-contract.md lists Accelerometer, Gyroscope, Compass and Motion only. Then ran the real dialog trampoline with a throwing handler.", "notes": "The claim reproduces for the file dialog and the entry's 'typically std::terminate' is what happened, because SDL's zenity driver delivers the result on its own worker thread, so the exception hits a thread entry. Update the Evidence text: the file-dialog half was run on the real ResultTrampoline against SDL 3.4.0; the tray half was only read. Where SDL delivers on the calling thread the outcome depends on unwind tables: x86-64 Linux defaults unwind through the C frames, others terminate.", "correction": {"evidence_note": "Checked by reading at 009d40f5. Reproduced for the file dialog by the audit: TARGET's Sdl3SystemServices.cpp was compiled with g++ -O0 and linked with a system SDL 3.4.0 (not CNA's pinned SDL commit); Sdl3Dialogs::ShowOpenFileDialog was called with a callback that throws, SDL's zenity driver delivered the (cancelled) result on its own thread through ResultTrampoline, and the process ended with 'terminate called after throwing an instance of std::runtime_error' (SIGABRT). The SystemTray entry-click trampoline was read but not run. Neither devices-ext wrapper adds a catch (FileDialog.cpp passes the callback straight to the platform and SystemTray.cpp forwards it). What SDL's C frames do with an exception delivered on the calling thread depends on the toolchain: with default x86-64 unwind tables it unwinds through them, without unwind tables it terminates."}}
