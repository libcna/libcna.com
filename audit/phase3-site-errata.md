# Phase-3 site errata — Phase-1/Phase-2 statements found wrong while absorbing the Bible

Collected from work-package reports; each item is re-verified against TARGET before the page is corrected (one pass, by the orchestrator).
Status: `open` until the page is fixed and the fix is recorded here.

| # | Page#anchor | Statement | TARGET fact and evidence | Reported by | Status |
|---|---|---|---|---|---|
| 1 | `docs/xna-compatibility.html#microsoftxnaframeworkgamerservices` | the 19-bone avatar rig is "verified by pixel-readback tests" | the pixel-readback tests use a synthetic one-bone quad, not the 19-bone rig | WP15 | fixed |
| 2 | `docs/tutorials/97-*.html` (SendDataOptions table) | gives XNA's delivery guarantees | in CNA `Reliable` is also ordered and `InOrder` can drop packets (see `deep-dives/services/network-sessions.html#guarantees`) | WP15 | fixed |
| 3 | `about.html` | FNA is "built on SDL2" | the FNA reference the Bible cites (`b3551247`) defaults to SDL3 — re-verify against the FNA revision CNA compares with before changing | WP01 | fixed — CNA compares with FNA 26.05 (`tools/fna-reference/README.md`); `FNAPlatform.cs` there defaults to SDL3, SDL2 via `FNA_PLATFORM_BACKEND` |
| 4 | `docs/faq.html` (sensors answer) | the whole device layer needs `CNA_DEVICES=ON` | `modules/CMakeLists.txt` always adds the `devices` module (verify: which parts `CNA_DEVICES` gates) | WP01 | fixed |
| 5 | `docs/tutorials/15-background-music.html` | "after the compiled .xnb and .cnb tiers it tries .mp3, .ogg…" | a loose `.cnj` step precedes the media-native extensions (CNA-BUG-008) | B3 | fixed |
| 6 | `development/repository/source-ownership.html#third-party` | "Licences are in THIRD_PARTY_NOTICES.md" | the file omits vendored cgltf, stb, ENet, dr_libs (CNA-BUG-045) | B3 | fixed |
| 7 | `development/debugging.html`, `development/internals/modules/devices.html`, `development/takeover/validation.html` | present the TimeSpan/TSan race as a current baseline | the cited race was fixed in sharp-runtime `9c2cb0ae` (2026-07-07, before TARGET) — re-verify which sharp-runtime revision TARGET consumes | B2 | fixed — race fixed in sharp-runtime `9c2cb0ae` (ancestor of every current branch and of the devices CI pin `bc8dbf41`); the three pages now call the preset text stale |
| 8 | `development/internals/graphics/resources.html#registry`, `development/internals/runtime/shutdown.html#tree` | copies of state objects become aliases | only copy *assignment* (and `VertexDeclaration`'s copy constructor) aliases; see CNA-BUG-029 | B2 | fixed |
| 9 | `development/internals/bindings/c-api-internals.html#destroy-order` | repeats the `cna_game_destroy` comment | the comment in CNA is wrong (new B2 finding) | B2 | fixed |
| 10 | `docs/math-types.html#bounding-types` | "XNA 4.0's own implementation is reported to have the same limitation"; the ray case is "the only functional gap" | XNA 4.0 IL fully implements `BoundingFrustum.Intersects(Ray)`; CNA has further defects (B1: CNA-BUG-002/003) | B1 | fixed |
| 11 | `docs/xna-compatibility.html` (Vector3 length/distance) | reproduces "XNA's 32-bit x87 accumulation" | `modules/math/src/Vector3.cpp` sums in `float`; also the "one named hole" narrowing (boundary origins only) needs re-checking | B1 | fixed |
| 12 | `development/internals/modules/math.html#curves` | Curve Step parity "not established" | established: CNA's Curve tangent/Step/loop code follows FNA and diverges from XNA 4.0 IL | B1 | fixed |
| 13 | `docs/tutorials/04-game-lifecycle.html#constructor` | the graphics device does not exist in the constructor | it does exist in the `Game` constructor path (WP03; verify exact statement) | WP03 | fixed |
| 14 | `docs/getting-started.html` | calls `Present()` inside `Draw` (double present each frame) and skips base `Update`/`Draw` | fix the sample to the real lifecycle | WP03 | fixed |
| 15 | `docs/tutorials/47-*.html` (FPS counter) | FPS counter | counts `Update` calls, so it reports the update rate, not the frame rate | WP03 | fixed |
| 16 | `docs/game-loop.html` | the first `Update` has zero elapsed time | true only for a fixed time step | WP03 | fixed |
| 17 | `docs/render-targets.html#rendertargetusage` | `PlatformContents` is "equivalent to `DiscardContents`" | `PlatformContents` preserves (`RenderTargetUsagePreservesContentsEXT`); the Discard bind clears to black, depth 1, stencil 0 (`GraphicsDevice::SetRenderTargets`) | WP09 | fixed |
| 18 | `docs/tutorials/61-*.html` (Vulkan occlusion queries) | Vulkan uses `VK_QUERY_RESULT_WITH_AVAILABILITY_BIT` | `IsComplete` uses the 64-bit flag and `VK_NOT_READY` | WP09 | fixed |
| 19 | `docs/effects.html#spriteeffect` | SpriteBatch uses `SpriteEffect` and it sets up the projection | SpriteBatch never creates it; `SpriteEffect::OnApply()` exits at its first statement (CNA-BUG-006) | B5 | fixed |
| 20 | `development/internals/modules/graphics-ext.html#frame-chain` | three tests "assert parts of this order" | they assert pass counts only | B5 | fixed |
| 21 | `development/internals/graphics/backends.html#opengl4-and-easygl` | the OPENGL4 + EasyGL pair is "permitted by configure rules rather than evidenced" | `plans/plan_opengl4_modern_graphics.md` (GL4-0006) records a build and run of `OPENGL4;OPENGLES3;OPENGL33` — re-verify and narrow | B5 | fixed — narrowed: evidence is the developer record GL4-0006 (Wayland build of `OPENGL4;OPENGLES3;OPENGL33`, same binaries run on each), not CI |
| 22 | `development/internals/graphics/device.html#resources` | WebGPU reports device loss and reset | it does so only through the debug simulation hooks; a real loss is only logged (new B5 finding) | B5 | fixed |
| 23 | `development/internals/modules/storage.html#boundary` | "The closure is enforced" | every CI job skips or filters out the module-link-closure gates (B6: verification gap NEW-B6-01) | B6 | fixed |
| 24 | `features.html`, `docs/sensors.html`, `development/testing/architecture.html#ci` | the devices workflow runs the CNA::Devices suites | it misses 7 suites; `CNA_DEVICES_GTEST_FILTER` still names renamed dialog suites (B6: CNA-BUG-035). `features.html` is protected: fix the sentence only, additively | B6 | fixed |
| 25 | `development/testing/index.html#recipes` | "deadlock … tracked separately" | diagnosed and fixed (VULKAN-154/157, pinned by `Sdl3XErrorHandlerTests`) | B6 | fixed |
| 26 | `development/internals/modules/media.html#scan` | the scan "indexes only the extensions the bundled mixer can actually play: … .opus" | `SDLMIXER_OPUS=OFF` in `cmake/ThirdPartySDL.cmake`; the ALSA mixer refuses Opus by name (`DecodeMixerAudio`) (B4: CNA-BUG-052) | B4 | fixed |
| 27 | `docs/tutorials/111-cnj-pipeline.html#sourcefile` | example `"sourceFile": "Textures/hero.png"` inside `Textures/hero.cnj` | `sourceFile` resolves from the `.cnj`'s own folder (`CnjSourceFile.hpp` `ResolveCnjSourceFileSafely`) | WP12 | fixed |
| 28 | `docs/content-manager.html#json-descriptor-formats` | Effect `vertex`/`fragment` are "relative to the descriptor" | they are content-root-relative (`ContentManager.cpp` `ReadCustomGlslEffect` → `ResolveRootRelativeSidecarPath`) | WP12 | fixed |
| 29 | `docs/tutorials/147-xnb-interop.html#container` | both decoders are "bounds-checked against the declared size" | LZX is compared with the declared size only at the end (`XnbDecompression.cpp`) | WP12 | fixed |
| 30 | `docs/tutorials/44-bounding-volumes.html` | `BoundingFrustum.Intersects(Ray)` is partial "exactly as in XNA" | XNA 4.0 implements it fully (XNA IL); CNA throws only for an origin exactly on a plane | WP04 | fixed |
| 31 | `docs/math-types.html#core-types` (Rectangle row) | `Contains(x, y)` has "integer and float overloads" | no float overload exists (`Rectangle.hpp`) | WP04 | fixed |
| 32 | `docs/math-types.html` (MathHelper) | "Mirrors … exactly"; `WithinEpsilon`, `MachineEpsilonFloat` … are "the remaining XNA constants" | those, plus `Clamp(int)` and `ClosestMSAAPower`, are FNA-internal helpers, not XNA API | WP04 | fixed |
| 33 | `docs/math-types.html` (Point) | lists `+ - * /` | those operators are not XNA API and carry no CNAEXT marker | WP04 | fixed |
| 34 | `docs/spritebatch.html#overview` | SpriteBatch "submits them to the GPU as a single batched draw" | `SpriteBatch.cpp` (`flushBatch`): one renderer call per sprite, then one native draw per texture run on GPU families; one call per sprite on SDL_RENDERER | WP05 | fixed |
| 35 | `docs/tutorials/21-spritebatch.html` (flush section) | describes GPU-renderer vertex batching as if every renderer did it | only the GPU families batch per texture run | WP05 | fixed |
| 36 | `development/repository/index.html#validators` | include reachability is a check that keeps the module map true | at TARGET the script uses an out-of-date module graph and nothing runs it (WP02) | WP02 | fixed |
| 37 | `docs/tutorials/109-metal-macos.html#ci` | the Metal workflow fires on pushes that touch the renderer's files | the path filter lists 3 files that no longer exist and omits the Metal CMake files | WP02 | fixed |
| 38 | `docs/input.html#touchpanel` | `TouchCollection.IsReadOnly` is a "known contract bug" | TARGET tests treat it as deliberate FNA parity (WP14) | WP14 | fixed — relabelled as deliberate FNA parity (pinned by `TouchCollectionTest.IsReadOnlyIsAdvisoryAndMutationStillSucceedsLikeFna`); XNA 4.0 IL throws `NotSupportedException` from the mutators, which the page now states |
| 39 | `docs/tutorials/120-xact.html#caveat-reverb` | band-pass filters are implemented | they cannot be reached from parsed XACT content (WP14) | WP14 | fixed |
| 40 | `docs/model-loading.html#gltf-to-cnj` | sidecars are `.vb.bin` / `.ib.bin` | the tool writes `<name>_meshN_verts.bin` and `_idx.bin` (`gltf_to_cnj.cpp`) | WP13 | fixed |
| 41 | `docs/model-loading.html#runtime-gltf` | "The alpha.1 importer validates extensionsRequired" (history framing for current behaviour) | reword as current behaviour | WP13 | fixed |
| 42 | `docs/model-loading.html` (skeletal animation table) | `SkinnedModelEXT` is what glTF, `.cnj` and `.cnb` deliver | they put `SkinningData` on `Model::Tag` | WP13 | fixed |
| 43 | `docs/3d-rendering.html#model-rendering` | feeds `CopyAbsoluteBoneTransformsTo` output to `SkinnedEffect::SetBoneTransforms` | that array is scene-node indexed with no inverse bind; the palette must come from `AnimationPlayer::GetSkinTransforms()` | WP13 | fixed |
| 44 | `docs/tutorials/111-cnj-pipeline.html#emitted` | clip `.cnj` files are written for skinned groups only | rigid clips are serialized too | WP13 | fixed |
| 45 | `docs/effects.html#effectparameter` | string `SetValue` throws `InvalidCastException` | only for compiled-effect parameters (`EffectParameter.cpp`) | WP07 | fixed |
| 46 | `docs/effects.html#stock-effects-at-a-glance` | the API is unchanged except shadow members | `BasicEffect` also gained XNA's parameter graph, technique name and `VertexColorEnabled` accessors | WP07 | fixed |
| 47 | `docs/rendering-backends.html#texture-formats` | DIRECTX9 has a "native D3DFMT mapping" | DIRECTX9 has no `ClassifySurfaceFormatEXT`; public textures are Color-only (`Texture2D.cpp`, `ValidateTexture2DFormatEXT`) | WP10 | fixed |
| 48 | `docs/tutorials/103-direct3d-windows.html#d3d12-split` | the routine suite reads render targets directly | at TARGET it runs forced-headless on an implicit back buffer, and 3 fixtures run through Proton | WP10 | fixed |
| 49 | `docs/graphics-state.html#depthstencilstate` | `DepthBufferFunction` default is `Less` | `DepthStencilState.hpp` has `LessEqual` | WP06 | fixed |
| 50 | `docs/3d-rendering.html#surface-formats` | the 7 EXT surface formats are Defer everywhere | `WebGPURenderer.cpp` classifies `Dxt5SrgbEXT`, `Bc7EXT`, `Bc7SrgbEXT` as Supported when BC is enabled | WP06 | fixed |
| 51 | `docs/c-api.html#status`, `docs/verification.html#coverage-reports` | release gate: "Not ready: 1 unmet criterion" / "9 of 10 criteria met" (from the committed `RELEASE_GATE.md`) | measured at TARGET by running the gate: 2 criteria unmet and `--check` fails (B9, executed in the read-only TARGET tree) | B9 | fixed — re-verified by running `check_release_gate.py --run/--check` (read-only TARGET tree): coverage-closed and limitations-matrix NOT MET, `--check` exits 1 |
| 52 | `development/internals/bindings/c-api-internals.html#evidence-gaps`, `development/handbook/update-the-c-api.html#traps` | child-first destruction is "the caller's obligation" | `graphics_device.h` allows destroying the device first (the device disposes its resources) | B9 | fixed |
| 53 | `development/internals/modules/devices-ext.html#test-seams` | a real platform's dialog callback does not fire inside the call | on native WIN32 the file dialogs block and call back before `Show*` returns (`Win32SystemServices.cpp`) — B8 NEW-B8-22 | B8 | fixed |
| 54 | `development/internals/modules/devices-ext.html#dialogs` | no test shows asynchronous timing | the X11 and Wayland portal tests do, at platform level | B8 | fixed |
| 55 | `development/internals/modules/devices-ext.html#change` | "runs the ten CNA::Devices suites" | only eight match the devices workflow filter (see CNA-BUG-035) | B8 | fixed |
| 56 | `deep-dives/build/third-party-dependencies.html#sharp-runtime` (our own page, WP02) | a `main`/`develop` sharp-runtime checkout fails later in the build | it fails inside sharp-runtime's own configure ("Unknown Sharp Runtime component"); the Windows `<poll.h>` reason was fixed upstream in `88c12f15` (WP16) | WP16 | fixed |
| 57 | `deep-dives/foundations/ecosystem.html#sharp-runtime` (WP01), `docs/tutorials/89-*.html` | "41 components" | the catalogue lists 44 modules / 47 components at sharp-runtime `next` @ `41b918c9` (WP16) | WP16 | fixed |
| 58 | `development/internals/modules/devices.html#tests`, `development/takeover/validation.html#devices` | the TimeSpan race as a current baseline | fixed in sharp-runtime on 2026-07-07 (also errata 7) | WP16 | fixed — same edits as row 7 |
| 59 | `docs/platforms.html#android` | cites a failing NDK build in sharp-runtime | the `clock_cast` failure is now guarded in sharp-runtime `next` @ `41b918c9` (from reading the code, not a build) — re-verify and reword | WP18 | fixed — reworded from reading sharp-runtime `next` @ `41b918c9` (`FileStream` field gone, `clock_cast` confined to MSVC STL); no NDK build run |
| 60 | `development/internals/graphics/backends.html#untoured-2d` | (GDI frame copy) | GDI copies the full frame by default; copying only damaged rows needs `CNA_GDI_DIRTY_PRESENTATION` (WP11) | WP11 | fixed — statement is inside expansion `wp10-backends-untoured-contracts`; source fragment edited and re-applied |
| 61 | `deep-dives/renderers/render-targets-clears-and-viewports.html#clear-families` (our page, WP08) | GDI has no stencil plane | GDI does have a stencil plane and can clear it (WP11) | WP11 | fixed |
| 62 | `deep-dives/renderers/presentation-and-backbuffer-readback.html#swap-interval` (our page, WP08) | DIRECT2D ignores the swap interval | DIRECT2D passes the interval to DXGI Present (WP11) | WP11 | fixed |
| 63 | `development/handbook/update-the-c-api.html#inventory` | nine release-gate criteria are "recorded and measured as met" | running the gate at TARGET measures two unmet and `--check` fails (WP21/B9) | WP21 | fixed |
| 64 | `deep-dives/practice/project-records-and-evidence.html#task-ids` (our page, WP20) | the gate "reads… one unmet criterion" | two unmet at TARGET (WP21/B9) | WP21 | fixed |
| 65 | `deep-dives/foundations/ecosystem.html#graph`, `#easy-gl` (our page, WP01) | easy-gl is C++20 with "two smoke-test targets" | its CMake requires `cxx_std_23` (PUBLIC); `tests/CMakeLists.txt` registers four executables (WP17, easy-gl @ deda7a42) | WP17 | fixed |
| 66 | `docs/faq.html` ("What does CNA stand for?") | gloss "C++ native approach to XNA" | that expansion does not appear anywhere in the CNA repository (WPB1) — verify and reword or hedge | WPB1 | hedged — no expansion of "CNA" exists in the TARGET tree; the FAQ now says the repository never spells it out and labels the gloss as the site's own |
| 67 | `architecture.html#cpp23-features` (protected; reword additively) | lists C++17/20 features as "Key C++23 features" | C++23 is only the compiler floor; the code uses no C++23-only feature (WPB1, `deep-dives/foundations/language-conventions.html#cpp-dialect`) | WPB1 | fixed |
| 68 | `deep-dives/graphics/effect-object-model.html#draw-packet` (our page, WP07) | nothing reads `specularEnabled` | at TARGET DIRECTX9 (`D3D9EffectDraw.cpp`) and FNA3D (`Fna3dDraw.cpp`) both read it (WPB2) | WPB2 | fixed |
| 69 | `deep-dives/renderers/easygl-state-and-resource-semantics.html#occlusion` (our page, WP08) | names `OcclusionQuery::PixelCountIsPreciseEXT()` | the public member is `isPixelCountPreciseEXT()` (WPB2) | WPB2 | fixed |
| 70 | `deep-dives/renderers/renderer-contract.html#draw-routes`, `deep-dives/renderers/easygl-gl-profiles.html#other-gl` (our pages, WP08) | EasyGL and OpenGL4 refuse a declaration that cannot feed the stock program | `RequireDeclarationMatchesStockProgram` refuses only formats it cannot convert; a missing input reads (0,0,0,1) (WPB3) | WPB3 | fixed |
| 71 | `deep-dives/graphics/vertex-streams-and-declarations.html#declaration-to-renderer` (our page, WP07) | may overstate Vulkan's refusal | a single-stream draw falls back to its stride route behind `RequireFaithfulDeclarationEXT` (WPB3, hedged) | WPB3 | fixed — verified: all three stock routes refuse an incomplete multi-stream declaration and send a single-stream one to the stride route behind `RequireFaithfulDeclarationEXT` |
| 72 | `features.html#xna-api` (protected; correct the number only) | "~145 Keys members" | `Keys.hpp` has 160 (WPB5) | WPB5 | fixed |
| 73 | `deep-dives/verification/oracles-and-engagement.html#goldens` (our page, WP19) | `ExpectPixel` "reads back one region" | it reads exactly one pixel: `GetBackBufferData` rejects any other element count (WPB6) | WPB6 | fixed |
| 74 | `docs/audio.html` (MIDI) | MIDI is switched off | the pinned SDL_mixer (3075d3ed) turns its Timidity MIDI decoder on by default and CNA's build does not turn it off (B11, read from CMake; nothing built) — re-verify | B11 | fixed — SDL_mixer `3075d3ed` CMake: `SDLMIXER_MIDI`/`SDLMIXER_MIDI_TIMIDITY` default ON, CNA sets only `SDLMIXER_MIDI_FLUIDSYNTH=OFF`; page says "probably compiled" (read, not built) |
| 75 | `deep-dives/renderers/direct3d11-and-12-resources.html#mrt-finalization` (our page, WP10) | no MRT+mipMap fixture was identified | the fixture exists: `bound_target_lifetime_test` leg L1, registered for DIRECTX11/12 (B10) | B10 | fixed |
| 76 | `deep-dives/renderers/windows-2d-renderers.html#d2d-formats` (our page, WP11) | advises setting `DepthFormat::None` for DIRECT2D | DIRECT2D never receives Depth24 on the Game path: `Reset` normalises it to None (B10) | B10 | fixed |

## Post-Phase-3 adversarial audit additions

Found by the independent adversarial audit (2026-09-25/26): statements the Phase-3 errata pass missed. Both are protected root pages, so only the sentence was changed.

| # | Page#anchor | Statement | TARGET fact and evidence | Reported by | Status |
|---|---|---|---|---|---|
| 77 | `roadmap.html` (Experimental C API card) | "Its release gate has one unmet criterion: 468 public C++ symbols are still unmapped" | `python3 tools/c-api/check_release_gate.py --run` in the read-only TARGET tree prints two `NOT MET` criteria (`coverage-closed`, `limitations-matrix`: the scope model does not classify `modules/design`, `modules/diagnostics`, `modules/inspector`) and eight `met`; "one unmet" is what the committed report records (the same correction as errata 51 and 63 on `docs/c-api.html`) | adversarial audit | fixed |
| 78 | `features.html` (C API status) | "its own release gate reads “Not ready” because of those 468" | same measurement: the gate stops earlier than the 468-row criterion; the 468 are the committed report's reason, not the measured one | adversarial audit | fixed |
| 79 | `docs/effects.html` (callout, at-a-glance table, `#spriteeffect`), `features.html`, `roadmap.html`, `docs/roadmap.html`, `docs/shader-effects.html`, `docs/tutorials/58-normal-mapping.html` | "all six XNA 4.0 stock effects … SpriteEffect"; "SpriteEffect is XNA's 2D sprite effect class"; the table row "Internal 2D effect used by SpriteBatch" | XNA 4.0 has five public stock effects (AlphaTestEffect, BasicEffect, DualTextureEffect, EnvironmentMapEffect, SkinnedEffect); in the decompiled `Microsoft.Xna.Framework.Graphics` assembly SpriteEffect is only `internal static class SpriteEffectCode` (SpriteBatch's bytecode) plus the `SpriteEffects` enum. CNA exposes a public `SpriteEffect` and its `SpriteBatch` never creates it (errata 19). The Phase-3 Deep Dive `shader-routes-four-answers.html` already says "the five public effects plus the internal SpriteEffect". Found by cross-checking the CNA-BUG-006 review's correction against the rest of the site | adversarial audit | fixed (`audit/data/adversarial/page-fixes/orchestrator-spriteeffect.json`) |
| 80 | `docs/tutorials/97-networking.html`, `docs/tutorials/15-background-music.html`, `docs/tutorials/45-content-manager.html`, `docs/tutorials/04-game-lifecycle.html`, `docs/tutorials/05-game-loop.html`, `docs/migration-from-monogame.html` and 40 more pages | The propagation of the Phase-3 corrections (errata 1–38 re-verified): the same stale claim survived on sibling pages — explicit `Present()` in `Draw`, "before the device exists" for the profile request, "the first Update is zero" for variable steps, shader paths "relative to the descriptor", `SendDataOptions` as non-flag values, Opus decoded by the SDL3 mixer | see `audit/data/adversarial/applied-page-fixes.json` (94 fixes with reasons) and `audit/phase3-adversarial-audit.md` §6.3 | adversarial audit | fixed |
| 81 | `docs/tutorials/26-tilemaps.html`, `development/workflows.html` | Code sample and a placeholder printed double-escaped entities (`std::vector&lt;int&gt;`, `modules/&lt;name&gt;/tests`) | rendering defect identical at `PHASE2_BASE`; entities corrected | adversarial audit | fixed |
| 82 | `development/internals/modules/math.html#decompose`, `docs/math-types.html`, `docs/tutorials/42-matrix-ops.html` | "a mirrored transform decomposes to positive scales with the reflection folded into the quaternion"; `Decompose` "returns false if not decomposable" | a reflection cannot be a quaternion: `CreateFromRotationMatrix` returns a non-unit quaternion, and XNA 4.0's IL negates the largest scale and returns false for a shear; CNA-BUG-259 | adversarial audit | fixed |
