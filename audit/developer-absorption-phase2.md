# Phase-2 absorption ledger — developer.libcna.com → libcna.com/development

Durable record of the additive absorption of every Developer knowledge unit into libcna.com. Generated by
`scripts/developer_ledger.py render` from `audit/data/developer-absorption-units.json` (page units are written by the
authors of each page; non-page units in `audit/data/developer-absorption-nonpage.json`). Nothing here is served as site content.

## 1. Source boundary

| Item | Value |
|---|---|
| `PHASE1_BASE` (libcna.com accepted Phase-1 HEAD, branch `docs/unified-v2`) | `94d758ae85d3209eee8f613d31fd9f7097c24dc6` — “docs: link the now-public Go and Ruby bindings on the homepage and the C API page” |
| CNA `TARGET` (= `cnahead`) | `009d40f5dd085c4e674d3479675fac84b12b3e0a` (branch `next`, 2026-09-24) |
| Developer repository HEAD | `9f07046d91ad01e835274b8027f8e30298193d01` (its **working tree** was read, not only HEAD) |
| Developer `cnahead` (the CNA commit it documents) | `d6e9ff050e32bcfe5e7fd640006619a90b92298c` — an ancestor of TARGET, 55 commits earlier (224 files changed between them, concentrated in OpenGL4, SDL_gpu, EasyGL, graphics tests/examples, `cmake/`) |
| Developer uncommitted work absorbed | `assets/search-index.js` (modified (uncommitted)); `assets/site.js` (modified (uncommitted)); `docs/COVERAGE.md` (modified (uncommitted)); `graphics/backends.html` (modified (uncommitted)); `graphics/headless.html` (untracked (uncommitted)); `graphics/stub.html` (untracked (uncommitted)); `takeover/validation.html` (modified (uncommitted)) |
| Developer status vocabulary | `NOT STARTED / INVESTIGATING / DRAFTED / SOURCE VERIFIED / COMPLETE` describe **Developer's own documentation audit**, never CNA product status; none appears on published pages |
| TARGET access | read-only `git archive` extraction at `/rv/tmp/libcna-v2/cna-target` (no CNA checkout, branch or working tree touched) |

## 2. Progress

```text
Developer units (files): 95   authored pages: 80   generated reference classes: 4
Audited: 95/95   Pending: 0
Actions: EXPAND 77, GENERATED REFERENCE 5, MERGE 5, OBSOLETE 1, SUPERSEDED 7
TARGET contradictions corrected: 214   claims dropped with reason: 37
Phase-1 errors found: 37   current bugs/gaps recorded for Phase 3: 222
```

## 3. Ledger (one row per Developer file)

Columns: Developer source · source type · Developer's own status (its audit state) · unique knowledge · TARGET verification needed? · libcna destination · action · done.

| Developer source | Type | Developer status | Unique knowledge | TARGET check | libcna destination | Action | Done |
|---|---|---|---|---|---|---|---|
| `.gitignore` | repository-metadata |  | Editor and Python-cache ignore patterns only; no documentation knowledge. | no | — | OBSOLETE | yes |
| `Makefile` | tooling |  | make generate / check / serve: regenerate references and search data, run link and source-link checks, preview locally. Replaced by scripts/generate_dev_reference.py, scripts/build_site_indexes.py and scripts/validate_all.sh, which the Maintenance page documents. | no | `scripts/validate_all.sh` | SUPERSEDED | yes |
| `README.md` | repository-metadata |  | States the discipline that the field manual is plain static files, that cnahead records the exact CNA revision documented, and that generators read Git objects directly and never change the CNA branch or working tree. The Git-objects / no-checkout discipline and the exact-revision marker carry over to libcna's generators and pin policy; the Developer site's own make targets do not. | no | `development/maintenance.html` | MERGE | yes |
| `architecture/audio-input.html` | authored-page | The input row in Developer's coverage document is SOURCE VERIFIED for input/events.html and the audio engine page covers audio (Developer's own audit state); this architecture page is a Phase 0 overview. | The audio stack from public API through the MixerEngine facade to the device layer with the fact that only the two mixer backends open the selected device (so SDL2 and NULL are compiled transports with no playback path), the corrected input flow in which platform services publish the keyboard, mouse and gamepad snapshots, and the per-subsystem threading rules with the sources that state them. | yes | `development/architecture/audio-input.html` | EXPAND | yes |
| `architecture/bindings.html` | authored-page | The bindings rows in Developer's coverage document mark the external binding pages SOURCE VERIFIED at their own pinned commits and note that live integration was blocked by the ABI mismatch (Developer's own audit state); this architecture page is a Phase 0 overview. | The C ABI boundary rules as implemented at ABI 0.29.0 (generation-checked handles, the exception barrier and result codes, thread affinity, count-then-copy strings, exact-version policy), the eight external binding repositories with the ABI generation each targets, and the gates a native API change must pass (ABI baseline, declared exports, coverage inventory, release gate). | yes | `development/architecture/bindings.html` | EXPAND | yes |
| `architecture/content.html` | authored-page | The content and content-pipeline module rows in Developer's coverage document are SOURCE VERIFIED for their own pages (Developer's own audit state); this architecture page is a Phase 0 overview. | Which build target owns which half of conversion (cna_content engine and writers, cna_content_pipeline build-time-only components, cna_content_compiler and the cna-content tool), the full ContentManager resolution ladder including the literal/.cnj/extension/bare order of the loose tier, the (type, normalized name) cache key with its tests and the SoundEffect exception, and a registration-and-test recipe with named tests. | yes | `development/architecture/content.html` | EXPAND | yes |
| `architecture/graphics.html` | authored-page | The graphics module row in Developer's coverage document is SOURCE VERIFIED for its device, draw-call and resources pages (Developer's own audit state); this architecture page is a Phase 0 overview. | The order in which a device, window and renderer actually come to exist (GraphicsDevice constructor, resolveRenderer, descriptor-driven window and services, then the manager's in-place Reset), the corrected frame path with the renderer-side CanBeginDrawEXT gate and two kinds of context lease, the disposal order, the device-loss plumbing with the six families that report real events, and the OPENGL4 versus EasyGL relationship. | yes | `development/architecture/graphics.html` | EXPAND | yes |
| `architecture/module-graph.html` | authored-page | SOURCE VERIFIED in Developer's coverage document: 'repository architecture and module dependency graph' (traces target creation, major direct edges, intentional cycles, umbrellas and optional modules; an exhaustive configured graph export remains) - Developer's own audit state, not CNA product status. | The complete declared target graph as a table with PUBLIC and PRIVATE edges, creation helper and gating for every module target, recomputed by parsing the CMake files; the two strongly connected groups of link cycles with the reason for each; the exact CNA umbrella part list; the source-partition exceptions; the optional-module and shared-runtime mechanics. | yes | `development/repository/module-graph.html` | EXPAND | yes |
| `architecture/overview.html` | authored-page | No dedicated row in Developer's coverage document; the Phase 0 architecture overview pages are, in its own words, not sufficient to make an area complete (Developer's own audit state, not CNA product status). | The one-page maintainer map that the user-level Architecture page does not give: which object owns what (Game, IPlatform, GraphicsDevice, ContentManager), the three backend selection axes with their configure-time refusal rules and the SDL availability gate, the five ownership facts with the tests that pin them, and the boundary rules that are enforced mechanically. The layer diagram is extended with the audio and media layers and the three declared static-archive cycles. | yes | `development/architecture/overview.html` | EXPAND | yes |
| `architecture/platform.html` | authored-page | The platform module row in Developer's coverage document is INVESTIGATING and cites this Phase 0 page (Developer's own audit state, not CNA product status). | The platform contract as a maintainer must hold it: factory and always-compiled implementations, subsystem acquisition without Initialize/Shutdown, the 32-flag capability rules, the batch event model, the RendererSurfaceInfo snapshot plus one narrow service handed to a renderer, the presenter-only-on-TERMINAL rule, the seven gates with what each enforces and where they run (including configure time), and the implementer's steps for a new backend. | yes | `development/architecture/platform.html` | EXPAND | yes |
| `architecture/runtime.html` | authored-page | The runtime module row in Developer's coverage document lists its runtime pages as SOURCE VERIFIED (Developer's own audit state); this architecture page is a Phase 0 overview. | An architecture-level trace of a game's control path that joins Game::Game, Run, Tick, PollEvents, Draw/Present and shutdown with the graphics manager and device, states which timing and event details are pinned compatibility behavior (with test names), and adds the browser, mobile and embedded-driver variants of the loop. Phase-1 pages give the user-level lifecycle only. | yes | `development/architecture/runtime.html` | EXPAND | yes |
| `assets/search-index.js` *(modified (uncommitted))* | presentation-asset |  | Generated data file. Replaced by libcna's single search-index.json, which lists every Development page. | no | `search-index.json` | SUPERSEDED | yes |
| `assets/site.css` | presentation-asset |  | Presentation only. libcna's shared stylesheet, extended additively for the Development area (local nav, source links, evidence box, related-pages block, pager), replaces it. | no | `css/style.css` | SUPERSEDED | yes |
| `assets/site.js` *(modified (uncommitted))* | presentation-asset |  | Developer's client-rendered navigation model (groups: Human Takeover, Architecture, Source tours, Making changes, Testing & debugging, Module/Graphics/Platform/Content/Input/Audio/Interop/Build internals, Reference) and revision constant. The grouping survives as the manifest-driven Development sidebar; navigation is server-generated static HTML, working without JavaScript. | no | `scripts/dev_manifest.py` | SUPERSEDED | yes |
| `audio/engine.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: 'SoundEffect/mixer/device/callback path traced; XACT, dynamic streams, capture and codec-specific paths remain'), not CNA product status | Maintainer-level trace of the audio stack that no Phase-1 page covers: the split between the four IAudioDevice transports and the two MixerEngine.hpp implementations, how SOUND_ENABLED and source filtering interact, the XNA-facade ownership and dispose/move rules, the fire-and-forget lock-free cleanup, the generation counter and reloadable audio in the SDL3 mixer, deferred track destruction in both mixers, per-transport callback barriers and timing, the ALSA engine's immortal-plus-atexit lifecycle, and which test files compile under which audio selection. Phase-1's Audio guide describes user-visible behaviour and the implementation matrix only. | yes | `development/internals/audio/engine.html` | EXPAND | yes |
| `bindings/c-api-internals.html` | authored-page | SOURCE VERIFIED (c-api: ABI/handle/error/game/child lifetime traced; specialized route families and callbacks remain) - Developer's own audit state, not CNA product status | The implementation view of the C ABI that the Phase-1 C API guide only states as contract: how HandleRegistry encodes and checks handles, the ordered exception-to-result arms of CallWithExceptionBarrier, the ownership shapes a handle can have (owned, parent-referencing child, borrowed game device, caller-created device, borrowed and callback-scoped content managers), CGame's callback latching and the exact cna_game_destroy sequence, and the SoundEffect parent/child counting that feeds the game-destroy gate. | yes | `development/internals/bindings/c-api-internals.html` | EXPAND | yes |
| `bindings/csharp.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md): bindings/csharp.html at external commit e223909986a705d0026908155f59388c100cbda3; its policy admits ABI 0.21.0, not pinned CNA 0.29.0. Native compatibility/validation remains blocked until binding is updated | The only maintainer-level description of CNA.NET's internals: the four-layer split, the resolver's selection order and ABI admission (reviewed point matrix plus symbol and shape canaries), the 64-bit handle and UTF-8 string-view marshalling, the owner-thread release queue that makes SafeHandle finalization legal against creation-thread-affine C handles, and the pinned callback-error buffer. Phase-1 docs/c-api.html only lists the binding and its ABI stance in one table row. | yes | `development/internals/bindings/csharp.html` | EXPAND | yes |
| `bindings/java.html` | authored-page | SOURCE VERIFIED: bindings/java.html at external commit 6661173c556bacd1ba3e636fa92d2e4ec330323d; compiled ABI 0.21.x excludes pinned CNA 0.29.0. JNI architecture, ownership and audio route traced; live integration remains blocked pending ABI migration (Developer docs/COVERAGE.md; Developer's audit state, not CNA product status) | The only source tour of the cna-java JNI boundary: three-part compatibility set (headers, JNI adapter, CNA library), exact-minor admission, header-typed dispatch table, generated vs handwritten routes, NativeHandle ownership modes, the Cleaner-to-owner-thread deferred release, the SoundEffect parent/child disposal order and the Java/JNI/C callback contract. No Phase-1 page goes below the one-row summary of cna-java in the C API guide. | yes | `development/internals/bindings/java.html` | EXPAND | yes |
| `bindings/python.html` | authored-page | SOURCE VERIFIED (Developer's own audit state): bindings/python.html at external commit f085bbb54c57bea553cff9b48898c11f3c49a77d; loader admits 0.21.x, not pinned CNA 0.29.0. ctypes/ownership/audio/runtime paths traced; 0.29 integration blocked until migration | The only maintainer-level description of how cna-python crosses into CNA: the exact loader resolution/admission rules and error classes, the PENDING_ROUTES escape hatch, the two-table game callback contract with keep-alive roots and original-exception re-raise, the NativeResource ownership state machine and Game.Dispose order, a complete SoundEffect route, and the evidence split between live-library tests and header/compiler gates. Expanded with which layout/manifest modules are generated versus hand-maintained, the TARGET status of the pending route, and what the two incompatible post-0.21 ABI steps concretely touch in this binding. | yes | `development/internals/bindings/python.html` | EXPAND | yes |
| `build/architecture.html` | authored-page | INVESTIGATING; SOURCE VERIFIED (Developer coverage state, not CNA product status) | The complete root configure order with the stages that decide behaviour, the three selectors and the SDL switch traced file by file, the module and umbrella mechanics (cna_add_module, cna_build_config contents, shared libcna.so via WHOLE_ARCHIVE, the declared static-archive cycles), a seven-step new-renderer checklist, a third-party table with pins and offline overrides, the persistent SDL prebuilt with its build manifest, stamp, patched staging, lock and the two carried SDL_gpu Vulkan patches, and the table of configure-time gates. Phase-1 docs/building.html and architecture.html describe options and layers but not the configure order, the manifest mechanism or the gates. | yes | `development/build/architecture.html` | EXPAND | yes |
| `build/index.html` | authored-page | not tracked in Developer's coverage ledger (Developer's own audit state, NOT CNA product status) | A maintainer's command entry point that Phase-1 docs/building.html does not have: what a configure needs and refuses without, every configure and build preset verified against CMakePresets.json (including which presets have no build preset), the refusals a hand-written selection can hit and the file that raises each, a rebuild table keyed by kind of change, a guide to the configure log lines, the build-speed controls and the two classes of generated artefact. | yes | `development/build/index.html` | EXPAND | yes |
| `change-map/index.html` | authored-page | Task-oriented navigation page; not a subsystem row in Developer's coverage ledger | A change-to-owner-to-tests-to-trap map whose every row now carries verified TARGET paths, symbols and registered test names, plus rows for the areas the older table omitted (component lifetime, shutdown order, resize, platform services, sprite batch, renderer identity, capability answers, the CNAEXT layer, storage, devices, net/gamer services, diagnostics/Inspector, video and public XNA behaviour). | yes | `development/handbook/change-map.html` | EXPAND | yes |
| `cnahead` | repository-metadata |  | Developer's cnahead names d6e9ff050e32bcfe5e7fd640006619a90b92298c, an ancestor of TARGET 009d40f5dd08 (55 commits earlier; 224 files changed, concentrated in OpenGL4, SDL_gpu, EasyGL, graphics tests/examples and cmake/). libcna's own cnahead (TARGET) is authoritative; the Developer pin and the delta are recorded in this ledger and drove the mandatory re-verification list. | no | `cnahead` | SUPERSEDED | yes |
| `content/pipeline.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md: canonical registry/CLI/texture and XNA bridge traced; all specialized importers and format fidelity remain) -- Developer's own audit state, not CNA product status | Maintainer-level trace of the build graph that no Phase-1 page has: why four targets share the pipeline and which edges enforce the runtime/build boundary, the frozen registry and its selection rules, the exact import/process/write validation contract with the manifest, the image-to-Texture2D route (importer types, processor order, profile and DXT rules, the two writers), the XNA ContentCompiler/bridge collision and sibling rules, the .contentproj task path, and the manifest/scheduler/publication/cleanup mechanics with their safety guards. Phase-1 docs/content-pipeline.html gives the user-facing routes, options, configuration and manifest behaviour; this page says where each happens and what a change must not bypass. | yes | `development/internals/content/pipeline.html` | EXPAND | yes |
| `content/runtime.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md: XNB/CNB/loose dispatch, cache, ownership and key tests mapped; deeper format-specific readers remain) -- Developer's own audit state, not CNA product status | Function-by-function trace of ContentManager::Load<T> that no Phase-1 page has: the exact tier order with the cache key (requested type plus normalized name), the header-template location of the ladder, path and packaged-asset services, the two-pass reader table, external-reference containment and its typed/untyped split, the three registries with their (absent) locking, the SoundEffect specialization, the renderer-thread context lease, and the maintainer change route with the tests that pin each rule. Phase-1 docs/content-manager.html and docs/content-pipeline-xnb.html give the user-visible ladder and the reader inventory; this page adds where each rule lives and what breaks when it moves. | yes | `development/internals/content/runtime.html` | EXPAND | yes |
| `conventions/index.html` | authored-page | Process page (evidence-weighted patterns); not a subsystem row in Developer's coverage ledger | Each convention matched to the mechanism that holds it (configure error, CTest, compile-time, review only) with the file, gate or test name; corrections of the C API language floor and the Doxygen enforcement scope; a naming quick reference; and the two unrelated meanings of CNAEXT. | yes | `development/handbook/conventions.html` | EXPAND | yes |
| `debugging/index.html` | authored-page | Task-oriented process page; not a subsystem row in Developer's coverage ledger | Ordered per-symptom probes with the exact TARGET functions, environment variables, presets, scripts, log levels and configure-message fragments that exist; a corrected two-path resize trace; the distinction between a missing dependency and an identity invalid on this host; an audio recipe and an instruments table. | yes | `development/debugging.html` | EXPAND | yes |
| `docs/COVERAGE.md` *(modified (uncommitted))* | process-document |  | Developer's own audit ledger: per-module, per-renderer-family, per-platform and per-binding review state with remaining-work lists, the human-takeover spine status, the meaning of its state vocabulary, and the completion gate for a deep page (purpose, types, ownership, runtime path, threading, failures, destruction, invariants, tests, realistic changes, reading order). Absorbed as: the honest 'covered in depth / not covered' section of the Development home and the generated module index (whose 'maintainer page' columns are the mechanical form of the ledger); the completion gate as 'How an internals page is organised' on the Internals hub; the reserved SDL12/EMSCRIPTEN note and the binding ABI pins on the pages that own them. Its states describe Developer's documentation audit, never CNA product status, and none is republished. | yes | `development/index.html` | MERGE | yes |
| `getting-started/index.html` | authored-page | Orientation page; not a subsystem row in Developer's coverage ledger | The verified fast display-free loop (unit preset, unit-core build preset, binary location, what each focused preset proves and does not), a reading order whose header paths exist at the snapshot, and the time-saving rules, extended with the clone-branch rules (next for cna and sharp-runtime), the snapshot comparison commands and CNA's build-hygiene conventions. | yes | `development/getting-started.html` | EXPAND | yes |
| `graphics/backends.html` *(modified (uncommitted))* | authored-page | Developer's own routing page (docs/COVERAGE.md lists it under 'renderers: INVESTIGATING'); the working tree has UNCOMMITTED changes adding the Headless and Stub rows to the source-toured table, which are absorbed here | A family-first map for maintainers: which physical family serves which of the 25 identities, what each family's descriptor asks of the platform, what gates its build, its declared maturity, and which families have an internals page. Adds the TARGET-correct relationship between OPENGL4 and EasyGL (separate family, shared stock-shader corpus and presentation transform, no easy-gl dependency) and a per-family validation-hook table. | yes | `development/internals/graphics/backends.html` | EXPAND | yes |
| `graphics/device.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md rows 'graphics' and 'GraphicsDevice creation': device, draw-call and resources pages source-verified; buffers, effects/shaders, formats and every renderer's implementation still need deep tours) | The ownership and lifetime model of GraphicsDevice: what it owns, borrows and merely tracks; the ordered constructor and Dispose sequences; why the resource registry is a disposal mechanism, not an allocator; why public state is observable state; and the threading and device-loss caveats a change to this layer must respect. Re-verified at TARGET and extended with the Present/viewport/Reset behaviour, the device-level capability derivation and the exact set of families that report device loss or hand out a context lease. | yes | `development/internals/graphics/device.html` | EXPAND | yes |
| `graphics/draw-call.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md row 'indexed draw call': traces neutral -> Vulkan deferred and Software immediate; other backends pending) | A function-level trace of one indexed draw from the neutral GraphicsDevice validation and element-versus-byte offset rules to two contrasting families: Vulkan's snapshot into Pending3DDraw and replay at SubmitFrame, and the Software renderer's immediate CPU rasterisation. Names the exact validation order, the stream table, the ownership of range validation and where each symptom class should be investigated. Re-verified at TARGET and extended with a where-to-look table for the other families, a historical Software case study and the named tests. | yes | `development/internals/graphics/draw-call.html` | EXPAND | yes |
| `graphics/easygl.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md row 'easygl': traces shared identities, context, draw, resources, FBO, context loss and tests; per-profile live validation and all optional shader paths remain) | Explains how one EasyGL target serves five public GL identities through a runtime GlProfile, the conservative pre-context adapter queries, the constructor's registration order, profile-pinned ES 2.0 limits, the base-vertex fallback, the weak-pointer bound-target record that makes target/renderer destruction order-safe, context-loss recovery and the thread lease. Re-verified at TARGET and extended with the shared GL stock-shader corpus and presentation transform now used by OPENGL4. | yes | `development/internals/graphics/easygl.html` | MERGE | yes |
| `graphics/headless.html` *(untracked (uncommitted))* | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md: traces no-window descriptor, modes, state/resource registry, draw/readback refusal and test semantics; all shared fixture branches and concurrent-mode safety remain unaudited) -- Developer's own audit state, not CNA product status | Maintainer-level account of the HEADLESS renderer that no Phase-1 page has: why it is a state/lifetime oracle and not an image oracle, the three meanings of 'headless' with the platform-versus-renderer distinction kept explicit, exactly which checks the strictness dial gates (and which refusals hold in every mode), the resource registry and statistics, the deliberate readback refusals and how the shared layer reacts, and what each registered Headless_* test does and does not prove. Phase-1 docs/tutorials/107-cpu-renderers.html#headless gives the user workflow (modes, statistics, trace diff); this page gives the source-level contract. | yes | `development/internals/graphics/headless.html` | EXPAND | yes |
| `graphics/resources.html` | authored-page | SOURCE VERIFIED for texture lifetime (Developer docs/COVERAGE.md row 'texture lifetime': per-backend native allocation/upload/destruction pending) and INVESTIGATING for render target and resize (binding/disposal traced; resolve and resize across backends pending) | Separates the public texture, the shareable renderer-side texture object and the render-target attachment policy, and traces construction, upload, binding and the two bound-target disposal paths, ending in why Vulkan defers freeing a MRT framebuffer. Re-verified at TARGET and extended with the CPU-shadow rules, shared renderer objects behind value copies, the RenderTarget2D format/size/MSAA validation and the exact test registrations. | yes | `development/internals/graphics/resources.html` | EXPAND | yes |
| `graphics/sdl-gpu.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md row 'sdl-gpu': traces device, shader routes, queued draw/segment replay, lifetime, submit/present and tests; driver-specific live validation and all stock shader variants remain) | The only maintainer-level account of SDL_GPU's deferred model: ConstructionResources rollback and failure points, per-draw snapshots and chronological replay, pass segments and load/store rules, deferred native release, retry-safe flush and the destructor's ordering. Re-derived at TARGET and extended with the SPIR-V remap layer, the custom-effect 3D and instanced/indirect routes, compute/storage resources with their ordering rule and teardown, shadow reception and the new tests. | yes | `development/internals/graphics/sdl-gpu.html` | EXPAND | yes |
| `graphics/selection.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md rows 'platform/renderer/audio selection' and 'GraphicsDevice creation': graphics/selection.html maps the renderer selector from configure time to device construction; deeper platform/audio build gates and per-family descriptor tests remain) | The maintainer trace of how a renderer is chosen: the configure-time include order, the identity-to-family macro and its host gates, why the registry is a generated explicit table rather than self-registration, the SelectionState precedence/refusal/fallback/latch rules, the candidate-by-candidate resolveRenderer loop, the descriptor fields, a seven-step worked change for adding an identity, and the tests that pin each rule. Re-verified against TARGET and extended with the descriptor gate, the variables a family CMakeLists may rely on, the latch-timing subtlety and the new CNA_OPENGL4_COMPILED_EFFECTS arm. | yes | `development/internals/graphics/selection.html` | EXPAND | yes |
| `graphics/software.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md row 'software': traces selection, CPU storage, indexed draw, targets, present and tests; full shader-opcode/format matrix and GDI 2D consumer need further audit) | The maintainer view of the CPU renderer: presenter-only display, the GDI source-membership exception and its 2D-only wrapper, the draw pipeline with its addressing rules, framebuffer storage and exact-format semantics, target binding/unbinding ownership, what CustomEffects false and compiled-effect interpreters mean, and which tests exist under which configuration. | yes | `development/internals/graphics/software.html` | MERGE | yes |
| `graphics/stub.html` *(untracked (uncommitted))* | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md: traces descriptor, minimal resource objects, no-op draw, smoke evidence and SDL fixture dependency; no native rendering or pixel evidence by design) -- Developer's own audit state, not CNA product status | Maintainer-level account of the STUB renderer that no Phase-1 page has: exactly which IGraphicsRenderer members STUB overrides and which shared defaults it inherits, how the public layer reacts to its null resources (render targets, 3D textures, cubes, readback), the exact scope of Stub_Smoke, and the build traps around it (unconditional SDL3 dependency, registration guard, preset build target). Phase-1 docs/tutorials/107-cpu-renderers.html#stub and docs/rendering-backends.html#cpu-renderers give the user-level summary; this page gives the source-level contract. | yes | `development/internals/graphics/stub.html` | EXPAND | yes |
| `graphics/vulkan.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md row 'vulkan': traces build, creation, draw, submit, swapchain and shutdown; modern compute, all shader families and exact CI matrix remain) | The maintainer model of the Vulkan renderer: constructor dependency order, draw-time records versus replay, the SubmitFrame fence/acquire ordering that avoids a hang, deferred-present readback, swapchain policy and recreation, frame-generation retirement, and the owner-disconnection invariant for resources that outlive the device. Re-verified at TARGET and extended with the shadow GLSL now shared with SDL_GPU, the validation output gate and a precise destructor walk. | yes | `development/internals/graphics/vulkan.html` | MERGE | yes |
| `index.html` | authored-page | home page; not tracked in Developer's coverage ledger | The shortest useful mental model diagram (Game -> GraphicsDevice/IPlatform -> host), the evidence policy (full-hash source links, labelled generated inventories, stated evidence gaps) and the entry points into the takeover path. Expanded into the Development hub: the four-altitude model (guide / architecture / internals / maintainer), path cards, the honest covered/not-covered section and the pin policy. | yes | `development/index.html` | EXPAND | yes |
| `input/events.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: 'event/snapshot split, SDL3/Win32 keyboard/mouse, touch, lazy controller and tests traced; deeper gamepad/haptics/sensors remain'), not CNA product status | Maintainer-level map of how platform events and platform snapshots reach public input: the two parallel paths, the sixteen-alternative event vocabulary with the bridge-versus-Game consumer table, a per-backend service-availability matrix, the exact PollEvents ordering, the legacy accumulators and the way the bridge tests shadow Keyboard and Mouse with them, the SDL3 versus Win32 wheel and keyboard derivations, and the touch and text paths with their gating. Phase-1 docs/input.html describes the public API only and none of these layers. | yes | `development/internals/input/events.html` | EXPAND | yes |
| `invariants/index.html` | authored-page | Task-oriented process page; not a subsystem row in Developer's coverage ledger | For each architectural invariant, the source evidence (function, member and test names) that holds it at the snapshot, the failure it prevents and, explicitly, what is not established; plus a table of how each rule is enforced (declaration order, configure gate, test, convention). Two further invariants (renderer selection latch, SDL native edges) were confirmed from source and added. | yes | `development/invariants.html` | EXPAND | yes |
| `maintenance/index.html` | authored-page | Process page about the standalone manual's own generator and checker; translated to libcna.com | The pin policy and the procedure for advancing the documented CNA revision translated to libcna.com's real tooling (cnahead, generate_dev_reference.py, site_dev.py, build_site_indexes.py, validate_all.sh and each validator's scope), including the list of places that embed the pin, the atomicity rule and what must stay manually reviewed. | yes | `development/maintenance.html` | EXPAND | yes |
| `modules/core.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: renderer selection, identity, paths, logger and build boundary traced; smaller helpers and a full cross-module call-site audit remained) | The maintainer-level contract of a small module with a wide blast radius: which modules link the header-only surface versus the archive, why the renderer identity enum is not a stable number, the exact selection state machine (precedence, call-time rejection, latch on success only, unsynchronised fields), the path-conversion and component-wise containment algorithm with its production callers, and the logger's locking limits. Phase-1 documents the user-facing selection precedence and path-containment outcome but none of these internals. | yes | `development/internals/modules/core.html` | EXPAND | yes |
| `modules/design.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: converter registration, boxed property edits, culture/descriptor behavior and tests traced; consuming-editor and cross-thread behavior remained unproved) | How the optional Framework.Design layer actually works and where it can surprise a maintainer: the header-triggered registration and what it needs from the linker, that only a converter (not property descriptors) is registered per type, the exact-type boxed descriptors and the no-op change hook, the shared culture parser and its two ArgumentException paths, the string-input asymmetries with the stale comment that contradicts the constructors, and a precise map of what the 25 tests prove. Phase-1 documents the converter table, usage and rules but none of the machinery. | yes | `development/internals/modules/design.html` | EXPAND | yes |
| `modules/devices-ext.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: capability/service translation, camera session-to-texture path, dialog callback failure and tray lifetime traced; real host service behavior and all platform implementations remain) | Per-wrapper failure contracts of the CNA::Devices services (inert camera, throwing tray, false-returning clipboard, silent or -1 message box, empty-callback file dialog with its moved-callback hole), the exact frame-validation order of Camera::TryAcquireFrame, which platforms actually supply each service and how the tests reach the platform through canned platforms. Phase-1 pages describe the layer for users (tutorial 117, CNAEXT guide) but not these seams. | yes | `development/internals/modules/devices-ext.html` | EXPAND | yes |
| `modules/devices.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: Accelerometer shared session, Compass/Motion owner generation, callback/disposal barriers and vibration teardown traced; Gyroscope/Motion parity and live Android/iOS hardware need deeper validation) | Maintainer-level lifetime contract of Microsoft::Devices: the shared per-class sensor session with ABA-safe registrations and a dispatch token, the two-stage disposal state, the per-instance control block and generation used by Compass/Motion, the Android bridge worker state machine with its five-second abandonment rule, and the VibrateController teardown order with its explicit and process-exit paths. Phase-1 pages give a user guide and a status table but no callback-lifetime, thread or teardown detail, and three of them mis-state which module the CNA_DEVICES option gates. | yes | `development/internals/modules/devices.html` | EXPAND | yes |
| `modules/diagnostics.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: compile/runtime modes, frame/counter/zone path, bounded producer/history storage, source/resource ownership and trace tests traced; every renderer's diagnostics source and UI consumer remained) | Source-level machinery behind the Phase-1 Diagnostics guide: why the level must be uniform (it changes GraphicsResource layout), the exact frame-completion algorithm and lock order, the SPSC per-thread ring and who drains it (including the Inspector's worker thread), EndZone's case table, recording and Chrome-export details, and the per-level test matrix with its CTest mechanics. Phase-1 documents the user-facing API, names and limits but none of the internals. | yes | `development/internals/modules/diagnostics.html` | EXPAND | yes |
| `modules/gamer-services.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: opt-in composition, stub sign-in ownership, local JSON achievements/leaderboards, Guide input/message and tests traced; avatar rendering, every Guide state path and live-host validation remain) | Ownership and lifecycle facts no Phase-1 page states: the four fixed stub gamers and their guest/PlayerIndex values, the non-owning GamerCollection rule with its three registries and the free-before-replace order in Initialize, who frees each Begin* result, the exact on-disk layout and failure model of the local JSON store, and the Guide keyboard/message-box request slots with their render, cancel and touch-suppression rules. Phase-1 documents the user-visible persistence and the no-op list but none of the internals. | yes | `development/internals/modules/gamer-services.html` | EXPAND | yes |
| `modules/graphics-ext.html` | authored-page | INVESTIGATING (Developer's own audit state: frame pipeline, chain, target pool, reset and pass order source-verified; shader-package/compute, shadows, clustered/PBR, transparency and individual effects still need separate deep traces) | Function-level maintainer trace of RenderPipeline/PostProcessChain/RenderTargetPool/ScopedRenderTarget with the exact fifteen-slot pass order, the scene-target decision, reset and exception edges, plus the shader-package authoring chain (manifest, generator, checked-in header, reproducibility ctest) and how ShaderPackageEXT::selectFor and the clustered forward package work at this snapshot, including the new desktop GLSL variant. Phase-1 gives the user-level layer reference and renderer matrix but none of the internals. | yes | `development/internals/modules/graphics-ext.html` | EXPAND | yes |
| `modules/inspector.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: Agent/provider/thread, versioned protocol, local bridge, security/rate/preview tests traced; live browser, renderer preview provider and remote deployment behavior remain) | Source-level mechanics behind the Phase-1 user guide: the Start validation order, Stop/join semantics and a Stop race window, authenticate-before-provider ordering and capability derivation from the compile-time build mode, the thread/lock coupling between the agent worker and the Game thread through diagnostics mutexes, the exact version-compatibility rule (1.0 at both ends; new message types are not compatible), which errors keep or drop connections, the preview validation order and ticket lifecycle, the bridge's HTTP parsing/token/Host behaviour and unreachable destructor, a layered debugging route, and precise test coverage including what is untested (WebBridge, Stop). | yes | `development/internals/modules/inspector.html` | EXPAND | yes |
| `modules/math.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: Matrix/Vector transform order, Color packing/rounding, geometry/curve boundaries and tests traced; deeper per-type ABI/oracles remained) | What a maintainer must know before touching XNA-compatible numerics: the row-vector convention and the actual view and projection terms, exactly which functions accumulate wider than float and the oracle corpora that pin them (with negative controls), the Color packing and truncation rules, the per-function failure map, and the behaviours of BoundingSphere, BoundingFrustum and Curve that tests do not cover. Phase-1 documents the type members and a summary of numerical behaviour but none of the mechanics. | yes | `development/internals/modules/math.html` | EXPAND | yes |
| `modules/media.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: library scan/owned graph, picture save, static music queue/mixer callback/dispatcher and video handoff traced; specialized tag formats, picture decoders and live audio/video hosts remain) | Maintainer-level contract of the three media lifecycles: the construction-time value-owning library graph (scan order, extension whitelist, tag readers, construction order, back-reference pass, saved-picture write path), the static MediaPlayer with its queue copies, audio-thread ended flag, dispatcher pump order and visualization tap, and the Video/VideoPlayer hand-off with its caller-thread-only decode loop. Phase-1 pages state that these features are real; none of them describes ownership, thread lanes, failure behaviour, the SOUND_ENABLED profile split or which tests are configuration-dependent. | yes | `development/internals/modules/media.html` | EXPAND | yes |
| `modules/net.html` | authored-page | INVESTIGATING (Developer's own audit state: session/ENet ownership, SystemLink pump, discovery, join and key tests source-verified; migration, packet protocol details, loss simulation and Emscripten behavior remain) | The internal contract behind Phase-1's user-level networking pages: one live session per process (static action/session pointers), who owns local versus remote gamers, the exact Update and PumpSession sequence, the two-step SendData path and its Local-session consequence, the bounded pre-handshake queue and drop counter, the handshake and forged-message rules, the discovery service's file-static state, teardown order and the per-suite test map with configure-time exclusions. | yes | `development/internals/modules/net.html` | EXPAND | yes |
| `modules/phone.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: lifecycle adapter, borrowed Game/event tokens, loopback listener/queue/sender and tests traced; native Windows notification path and hostile-peer shutdown remain unverified) | Maintainer-level contract of the opt-in Microsoft::Phone module: why it is outside the CNA umbrella and who links it; the exact Game-event-to-phone-event mapping including the first-activation rule, per-attach Launching and always-preserved Activated; the borrowed-Game lifetime hazard of the static Current; the loopback channel's Open/Listen/Dispatch/Close sequence, request-shape table, registry and concurrency limits; the synchronous sender's wire format and return-value semantics; and the Windows gap. Phase-1 pages only mention the module in one-line rows (features card, architecture module table, XNA compatibility row, C API scope). | yes | `development/internals/modules/phone.html` | EXPAND | yes |
| `modules/storage.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: fake-async Begin/End, process root, Sharp Runtime coupling, container authority, deletion, ownership and tests traced; native-host UTF-8 paths, concurrent root changes and race-resistant filesystem confinement remained unverified) | The maintainer view of a module that turns caller-supplied names into filesystem operations: what completes synchronously and where errors actually surface, how the process-static root and application name are held and coupled to Sharp Runtime, which shared helper guards each entry point and what it refuses, the one recursive delete and its blast radius, borrowed-pointer ownership, the C ABI adapter that repairs lifetimes, and a fixture that writes to the real per-user data root. Phase-1 documents the game-facing API, the root chain and the containment outcome but none of the machinery. | yes | `development/internals/modules/storage.html` | EXPAND | yes |
| `modules/video-ffmpeg.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: configure/link switch, decoder packet/audio/seek/ownership, media handoff and conditional tests traced; every codec/pixel format, real output host and non-FFmpeg platform path remain) | The configure-time contract of the optional FFmpeg backend (values of CNA_ENABLE_VIDEO, the excluded hosts, what the switch publishes and which presets and workflows select it), the decoder's NextFrame packet state machine, pixel-conversion table, strict error-recognition flags and the seek/track-switch transactions, a what-throws table, and an evidence-scoped reading of the conditional tests and their synthetic fixtures. Phase-1 pages describe FFmpeg setup and platform availability for users but not these maintainer invariants. | yes | `development/internals/modules/video-ffmpeg.html` | EXPAND | yes |
| `platforms/headless.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: in-memory window, injected events, capability refusals, time and conformance role traced; broader runtime integration remained) | Maintainer-level contract of the always-compiled Headless platform: which calls succeed (subsystem refcounts), which return null (15 optional services) and which throw (presenter, adoption, borderless fullscreen); the shared window-slot lifetime trick; the event-versus-snapshot consequence that injected key events never reach Keyboard::GetState; how tests actually script platforms (PlatformTestDecorator, golden transcript) and how Headless pairs with renderers at run time. Phase-1 pages only list Headless as 'one window, every capability false'. | yes | `development/internals/platforms/headless.html` | EXPAND | yes |
| `platforms/index.html` | authored-page | Platform backends table in Developer docs/COVERAGE.md lists all seven backends as SOURCE VERIFIED (Developer's own audit state) and states that SDL12/EMSCRIPTEN are reserved and that their refusal path still belongs in build documentation; the platform module row itself was INVESTIGATING. | The maintainer-level map of all seven backends in one place: what CMake offers where, what is always compiled, factory names, link inputs, the four refusal paths for reserved/unavailable names (including SDL12/EMSCRIPTEN and the host-conditional WIN32/TERMINAL reservations), the selection pipeline from CMake to Game's owned platform, per-backend comparison of event pump/scale/graphics services/adoption, the shared-source directory policy, the two-path input debugging chain, and each backend's CTest entries and CI cells. Phase-1 pages give the user-level matrix but not the source-level mechanics. | yes | `development/internals/platforms/index.html` | EXPAND | yes |
| `platforms/sdl2.html` | authored-page | SDL2 platform backend: SOURCE VERIFIED (Developer's own audit state; live display validation listed as remaining) | Maintainer trace of the deliberately narrow Sdl2Platform: the exact wired/null service boundary and six true capability flags, per-instance subsystem refcounts under a separate mutex, owning versus adopted Sdl2Window, drawable-size fallback and ratio-based display scale, event families mapped versus snapshot services absent (Mouse::GetState returns a default state), wheel-flip negation, the two function-key ranges, the GL-only graphics seam, and the SDL2-only configure guard. Phase-1 gives only the user-level rules and capability values. | yes | `development/internals/platforms/sdl2.html` | EXPAND | yes |
| `platforms/sdl3.html` | authored-page | SDL3 platform backend: SOURCE VERIFIED (Developer's own audit state; real-host and optional service validation listed as remaining) | Maintainer-level trace of Sdl3Platform: member-owned services and borrowed pointers, owning versus adopted Sdl3Window, per-instance subsystem refcounts without SDL_Quit, the dlsym-installed non-fatal Xlib error handler and the exit-under-lock destructor invariant, render-intent flags and the GL attribute reset, the value-event batch versus polled snapshots, lazy controller acquisition, logical/drawable/pixel-density separation, fullscreen fallback policy, and the GL/Vulkan/presenter seams. Phase-1 pages give only the user-level capability matrix and selection rules. | yes | `development/internals/platforms/sdl3.html` | EXPAND | yes |
| `platforms/terminal.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: 'traces POSIX session leases/restoration, event decoding, resize, CPU-frame presentation and tests; live emulator/TTY matrix remains'), not CNA product status | Maintainer-level trace of the POSIX terminal backend: lazy capability probe and its caching rule, the lease union and rebuild-not-mutate session design with signal-safe restoration, the Kitty versus timed-legacy keyboard decoder shared by event pump and snapshots, SIGWINCH coalescing into nominal 8x16 pixels, and the quantise/diff/budget presenter with its drop-safety invariant. Phase-1 pages (Tutorial 139, Platform Support) describe user-visible behaviour only; none covers session ownership, restoration order, decoder timing, presenter caching or the test/harness layout. | yes | `development/internals/platforms/terminal.html` | EXPAND | yes |
| `platforms/wayland.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md: traces connection/registry, xdg configure, event/service flow, scaling, EGL/Vulkan/shm and teardown; live compositor matrix remains) -- Developer's own audit state, not CNA product status | Source-level trace of the native Wayland backend that no Phase-1 page has: constructor order (services before connection, two bounded startup roundtrips, capabilities frozen once), the per-flag condition in ComputeCapabilities, the xdg role/configure state machine with stale-geometry filtering and the event set ApplyConfigure posts, scale-method decision, callback-to-event polling order and serial use, the three graphics seams with their ownership and frame pacing, the exact destruction order, every bounded wait, and the CTest split with prerequisites and isolation. Phase-1 docs/native-platforms.html covers dependencies, user-visible behaviour, build recipes and harness names; this page complements it at maintainer level. | yes | `development/internals/platforms/wayland.html` | EXPAND | yes |
| `platforms/win32.html` | authored-page | SOURCE VERIFIED (Developer's own audit state: 'traces HWND ownership, events, DPI, graphics seams and CI; real-host execution remains'), not CNA product status | Source-level map of the Win32 backend that no Phase-1 page has: the windows_/adopted_ registry split and why adoption must not share the owner's registry slot, the two-way GWLP_USERDATA/DetachHost detachment, the pure Win32EventMapper and its full message-to-event table, the event-vs-snapshot split (mapper events vs GetKeyboardState snapshot updated at the end of Game::PollEvents), the DPI resolution chain, the WGL bootstrap/pixel-format-once rules, the self-loaded Vulkan loader, and exactly what the win32-cross/win32-native CI jobs and standalone harness execute. It also records source-visible gaps (foreign-HWND adoption, exclusive-fullscreen refusal, WGL multisample/attributes) as scoped maintainer notes. | yes | `development/internals/platforms/win32.html` | EXPAND | yes |
| `platforms/x11.html` | authored-page | SOURCE VERIFIED (Developer docs/COVERAGE.md: traces connection/service lifetime, window visuals, event protocol, input, GLX/Vulkan/presenter and tests; live X server matrix remains) | Maintainer-level trace of the native Xlib backend: why the X connection opens in the constructor and capabilities are frozen, the exact service teardown order and error-policy lifetime, the visual/colormap/WM transaction in CreateWindow, the ordered PollEvents multiplexer (XIM filter, selections, TranslateEvent), how keyboard/mouse snapshots are built, the three graphics seams and the CTest split with host prerequisites. Phase-1 pages give dependency and capability tables and build recipes; none explains the code paths, ownership or failure modes. | yes | `development/internals/platforms/x11.html` | EXPAND | yes |
| `reference/cmake-options.html` | generated-reference |  | CMake option and cache-variable inventory with defaults, descriptions and declaring file. Regenerated from TARGET with a quote- and parenthesis-aware CMake scan (Developer's regex could attribute a CACHE clause to the wrong set() call) that also covers cmake_dependent_option; links to selection axes and CMake architecture. | yes | `development/reference/cmake-options.html` | GENERATED REFERENCE | yes |
| `reference/index.html` | authored-page | Hub page for generated references; not a subsystem row in Developer's coverage ledger | For each generated inventory: what it answers, exactly what it is derived from (the scan patterns and registries) and what it cannot tell you, plus the generation contract of scripts/generate_dev_reference.py (Git objects only, never a checkout) and how to read a generated page without over-reading it. | yes | `development/reference/index.html` | EXPAND | yes |
| `reference/modules.html` | generated-reference |  | Module inventory (headers, sources, test files per production module with entry points and the maintainer chapter). Now generated by scripts/generate_dev_reference.py from TARGET Git objects, extended with the renderer-directory table (implementation family vs shared helper) and links to the Development internals pages; counts recomputed (23 modules, 21 renderer families, 4,798 files under modules/). | yes | `development/reference/modules.html` | GENERATED REFERENCE | yes |
| `reference/public-headers.html` | generated-reference |  | Inventory of non-Internal headers under include/CNA and include/Microsoft grouped by module (808 at TARGET across 21 modules). Regenerated; states that this is tree shape, not supported-API status, and separates the experimental C ABI (0.29.0). | yes | `development/reference/public-headers.html` | GENERATED REFERENCE | yes |
| `reference/test-targets.html` | generated-reference |  | Test-related add_executable/add_test declarations with their CMake source. Regenerated from TARGET (239 declarations vs Developer's 237 at its pin); states that a declaration is a registration, not a result, and points to ctest -N for a configured build. | yes | `development/reference/test-targets.html` | GENERATED REFERENCE | yes |
| `repository/index.html` | authored-page | not tracked in Developer's coverage ledger (Developer's own audit state, NOT CNA product status) | A directory-by-directory map of the repository with tracked-file counts, the exact shape and naming rules of a module directory, a per-module table of targets, umbrella membership and direct link edges read from each CMakeLists.txt, the header classes (XNA-shaped, CNA-owned, Internal, module-private, C ABI, generated) with the two private src/ include exceptions, a table of generated versus checked-in generated files, the four vendor mechanisms with pins, and where the 904 C++ test sources sit by owner. Phase-1 architecture.html gives a user-level tree and module table but not link edges, header classes, generated files, vendor pins or test ownership. | yes | `development/repository/index.html` | EXPAND | yes |
| `runtime/frame.html` | authored-page | SOURCE VERIFIED (Developer's own audit state; open item: 'compare browser and native host tests'), not CNA product status | Function-level trace of one Game::Tick: the fixed-step wait and sleep-precision estimator, why events are polled after the wait, the exact PollEvents reaction table with the reserved F9/F10 keys, mobile suspend/resume arithmetic, the two measured XNA clock rules and their tests, BeginDraw/EndDraw/Present with the frame-long context lease, and a side-by-side table of the Emscripten frame body. Phase-1 states the clock contract and the Emscripten caveat for game authors; none traces the tick, the event ordering or the draw path. | yes | `development/internals/runtime/frame.html` | EXPAND | yes |
| `runtime/module.html` | authored-page | SOURCE VERIFIED (Developer's own audit state; open items: 'deeper entrypoint variants, platform-dependent validation'), not CNA product status | Maintainer-level map of the cna_runtime module: what its sources own, Game's real private declaration order (which is the lifetime contract), the window facade, service container and GraphicsDeviceManager as borrowed-in-both-directions objects, the component collection's ownership and the exact extent of componentListsMutex_, every state flag and the failure model, and a per-file account of the runtime tests. Phase-1 pages describe the Game API from the game author's side and do not cover module boundaries, member order, locking scope or the declared-but-undefined CNA::Runtime facade. | yes | `development/internals/runtime/module.html` | EXPAND | yes |
| `runtime/shutdown.html` | authored-page | SOURCE VERIFIED (Developer's own audit state; open item: 'audit backend-specific native teardown'), not CNA product status | Step-by-step teardown for the explicit-Dispose and destructor paths from the real member declaration order, exactly what UnloadContent's preconditions are, the five-step GraphicsDevice::Dispose with the weak lifetime token and the video-reference-as-bool repair, ambient platform stack and pin transfer, and a maintainer note listing manager-lifetime shapes the order does not protect. Phase-1 documents the user-level hooks; none traces destruction order or the ambient platform. | yes | `development/internals/runtime/shutdown.html` | EXPAND | yes |
| `runtime/startup.html` | authored-page | SOURCE VERIFIED (Developer's own audit state; open item: 'expand non-desktop entrypoints'), not CNA product status | Function-level trace from an executable's new Game to the first Tick: platform factory and ambient installation, the eager GraphicsDevice (adapter enumeration, renderer attempt loop, window creation or adoption, presenter decision, renderer arguments), DoInitialize with the manager's CreateDevice, and a failure table saying what each stage rolls back. Phase-1 pages describe the lifecycle for game authors and the fallback chain for users; none traces the constructor order, the installation guard or the point at which window and renderer first exist. | yes | `development/internals/runtime/startup.html` | EXPAND | yes |
| `scripts/check_site.py` | tooling |  | Static link/fragment checks plus verification that every source link is pinned to the recorded commit and the path exists in that Git tree. Ported as scripts/check_source_links.py (pinned-to-TARGET and path-exists rules for development/**; binding-repository pins) alongside scripts/validate_site.py. The Node/framework-artifact ban is specific to the Developer site and not carried over. | no | `scripts/check_source_links.py` | SUPERSEDED | yes |
| `scripts/generate_reference.py` | tooling |  | The derivation rules of the four generated inventories (module shape counts; option()/set(... CACHE ...) scan; add_executable/add_test scan filtered to test-related names; 'public header' = under include/CNA or include/Microsoft outside Internal), reading CNA only through git ls-tree/git show. Ported (with cmake_dependent_option support, renderer-family table and a fifth selection-axes inventory) and rendered in libcna chrome. | no | `scripts/generate_dev_reference.py` | GENERATED REFERENCE | yes |
| `scripts/generate_search.py` | tooling |  | Builds a client-side full-text index for the Developer site. libcna keeps a single search: scripts/build_site_indexes.py now also indexes every Development page (title, description, per-page keywords), so no second search system exists. | no | `scripts/build_site_indexes.py` | SUPERSEDED | yes |
| `takeover/blast-radius.html` | authored-page | DRAFTED (row 'blast radius and readiness' in Developer docs/COVERAGE.md; Developer's own audit state, not CNA product status) | A change-to-consumer matrix that tells a maintainer which other implementations, bindings and test configurations a one-line change can reach, with the readiness and self-review questions to answer first and the storage destructive-boundary warning. TARGET adds the CMake gates that widen reach by construction (descriptor gate, Vulkan and OpenGL4 output gates, skip convention) and the ABI-baseline checks; no Phase-1 page gives a maintainer this pre-edit map. | yes | `development/takeover/blast-radius.html` | EXPAND | yes |
| `takeover/case-study-components.html` | authored-page | SOURCE VERIFIED (row 'real historical bug-fix studies' in Developer docs/COVERAGE.md; Developer's own audit state, not CNA product status) | A worked reconstruction of a real, fixed CNA lifetime defect (commit 3faa1930, SAMPLE-065): the two separate hazards (concurrent list mutation vs raw-pointer snapshot outliving its referent), why the mutex is recursive and released before callbacks, which tests pin each mechanism, and the reviewer lessons. No Phase-1 page covers Game's component snapshot or its locking. | yes | `development/takeover/case-study-components.html` | EXPAND | yes |
| `takeover/case-study-storage.html` | authored-page | SOURCE VERIFIED (row 'real historical bug-fix studies' in Developer docs/COVERAGE.md; Developer's own audit state, not CNA product status) | A review-method case study on a destructive authority boundary: the operator/ pitfall behind StorageDevice::DeleteContainer, the outside-the-target sentinel oracle, why PathContainment returns lexical (not canonical) generic keys, the different authority roots of its callers, the destructive real-directory test fixture, and what the tests do not prove (TOCTOU, Windows, concurrent root change). Phase-1 docs/storage.html states the containment behaviour for users but not the review method, the helper family or the fixture hazard. | yes | `development/takeover/case-study-storage.html` | EXPAND | yes |
| `takeover/curriculum.html` | authored-page | DRAFTED (human takeover spine: first 10/50/100 hours; Developer notes a fresh-human dry run is still needed) | An ordered, staged self-study route through CNA with concrete files, symbols, commands, exercises and a completion outcome per stage (whiteboard startup/frame/exit; owner of an ordinary bug; an evidence-backed patch). No Phase-1 page offers a maintainer curriculum. | yes | `development/takeover/curriculum.html` | EXPAND | yes |
| `takeover/index.html` | authored-page | DRAFTED (row 'human takeover guide' in Developer docs/COVERAGE.md; Developer's own audit state, not CNA product status) | The entry point of the Human Takeover route: the ordered reading path with what each stop produces, the standard for independent maintenance (draw construction, call path, state, owner, failure, teardown, tests; name an independent backend or host), the investigation-record fields, and the snapshot-drift warning. Phase-1 pages teach CNA's use, not how to take over its source. | yes | `development/takeover/index.html` | EXPAND | yes |
| `takeover/inherited.html` | authored-page | SOURCE VERIFIED (human takeover spine: inherited engineering system) | A maintainer-altitude map of the whole repository: which engineering family owns what, the change boundary of each, why platform, audio and renderer are separate selections, which contracts carry the widest blast radius and which lifecycle facts (platform-first member, device-in-constructor, raw resource registry, dispose order, callback overlap, C handles) make CNA dangerous to edit casually. No Phase-1 page frames the layout by review scope and change boundary. | yes | `development/takeover/inherited.html` | EXPAND | yes |
| `takeover/investigation.html` | authored-page | SOURCE VERIFIED (human takeover spine: unknown-code method) | A mechanical twelve-check procedure for recovering call and ownership traces from unfamiliar CNA code, three worked traces across runtime/graphics, a platform backend and the C ABI, and a fill-in investigation record. No Phase-1 page teaches a maintainer how to investigate CNA source. | yes | `development/takeover/investigation.html` | EXPAND | yes |
| `takeover/mental-model.html` | authored-page | SOURCE VERIFIED (human takeover spine: canonical mental model) | The single cross-cutting model the Development area assumes: executable-owned Game, platform-first construction, device and renderer created inside the Game constructor, GraphicsDeviceManager as a settings applier rather than an owner, descriptor-driven renderer resolution, IPlatform as a service provider rather than a renderer superclass, and the declared static-archive cycles that make the dependency graph non-acyclic. Phase-1 pages describe each piece for users but not this joined maintainer model. | yes | `development/takeover/mental-model.html` | EXPAND | yes |
| `takeover/ownership.html` | authored-page | SOURCE VERIFIED (row 'ownership master map' in Developer docs/COVERAGE.md; Developer's own audit state, not CNA product status) | A single cross-subsystem map of owners vs borrowers: Game member order and ambient platform installation, GraphicsDevice disposal order and the weak device-lifetime token, content cache semantics, audio keep-alive/generation/deferred-userdata mechanisms, sensor session barriers and the late vibration singleton, C ABI handle semantics, and the explicit-vs-destructor teardown sequences. Phase-1 pages describe user-level lifecycle hooks only. | yes | `development/takeover/ownership.html` | EXPAND | yes |
| `takeover/threading.html` | authored-page | SOURCE VERIFIED (human takeover spine: thread and callback map; Developer's own audit state, not CNA product status) | The only cross-subsystem map of which CNA code runs on which thread: Game loop, platform pumps and signals, audio devices and mixers, loading threads, sensors, C ABI and foreign callbacks, with a table separating proven barriers from guarantees the source does not give. TARGET adds the renderer context lease protocol (frame and ContentReader leases on the GL renderers), the documented single-threaded input and SDL_gpu contracts, the SDL3 sensor-session barrier and the thread-creation inventory. No Phase-1 page describes any of this. | yes | `development/takeover/threading.html` | EXPAND | yes |
| `takeover/uncertainty.html` | authored-page | SOURCE VERIFIED (architectural history and legacy; uncertainty/history) | A maintainer-facing register of what CNA's source and tests do not establish, with a next human check for each area, a scoped devices-shutdown consequence, and architectural history reconstructed from Git with explicit fact / inference / unknown labels. Phase-1 verification pages list user-facing limits; none reconstructs history or states maintainer next checks. | yes | `development/takeover/uncertainty.html` | EXPAND | yes |
| `takeover/validation.html` *(modified (uncommitted))* | authored-page | SOURCE VERIFIED (human takeover spine: change-to-test matrix; the working-tree version with the graphics-evidence section was absorbed; Developer's own audit state, not CNA product status) | A change-driven evidence plan: which build preset, focused target, CTest entry or label gives fast feedback and which broader evidence a merge needs, per area, plus the rules for recording evidence honestly and choosing the graphics oracle (STUB, HEADLESS, SOFTWARE, native renderer) by the failure being guarded. TARGET adds how the test targets are assembled, the private-display and native-host requirements, the output gates and the C ABI baseline checks. Phase-1 building and verification pages give commands and CI scope, not a per-change plan. | yes | `development/takeover/validation.html` | EXPAND | yes |
| `takeover/worked-changes.html` | authored-page | DRAFTED (complete worked human changes; Developer notes representative patches still have to be performed in separate CNA work) | Five end-to-end change rehearsals across runtime lifetime, neutral graphics policy, one explicit-GPU backend, one native platform and the public-API-to-C-ABI-to-bindings path, each with symptom framing, invariant, tests, commands and self-review questions, plus one shared review memo. Phase-1 docs describe behaviour; none rehearses a maintainer change. | yes | `development/takeover/worked-changes.html` | EXPAND | yes |
| `testing/architecture.html` | authored-page | INVESTIGATING; SOURCE VERIFIED (Developer coverage state, not CNA product status) | How CnaTests is assembled from OBJECT groups with the full list of conditional source filters and helper-process wiring, a table of the 22 groups with focused targets, source counts and link closures, the two working-directory policies, the skip convention, the Vulkan and OpenGL4 output gates with their discovery hook, the X11 and Wayland CTest entries with what each needs, a table of script-mode and policy tests, and a per-workflow CI table with boundaries. Phase-1 docs/verification.html names the layers and CI counts but not the registration architecture. | yes | `development/testing/architecture.html` | EXPAND | yes |
| `testing/index.html` | authored-page | not tracked in Developer's coverage ledger (Developer's own audit state, NOT CNA product status) | A working test handbook that adds the exact focused-target list with its module mapping, a corrected full-suite recipe (full build, private display, label and name selectors), the meaning of exit 77 and the Ninja skip of link-closure gates, the private display wrapper's exact behaviour and --exec mode, the sanitizer presets and the Mesa suppression-file rule, and the concrete rules for adding a test (input filter tokens, glTF ladder partition, PRESENT guards). Phase-1 docs/verification.html states what the counts and CI mean but has no maintainer procedures. | yes | `development/testing/index.html` | EXPAND | yes |
| `workflows/index.html` | authored-page | Task-oriented process page; not a subsystem row in Developer's coverage ledger | The eight engineering loops tied to what CNA's repository actually contains: conformance suites per promise, the platform and renderer gates and scripts with their names, CNA's commit, task-record and report conventions from AGENTS.md and CLAUDE.md, and a table of which of the twenty CI workflows sees which kind of change. | yes | `development/workflows.html` | EXPAND | yes |

## 4. TARGET contradictions corrected (Developer text vs CNA `009d40f5`)

| Developer page | Developer claim | TARGET fact | Evidence | Resolution |
|---|---|---|---|---|
| `architecture/audio-input.html` | PlatformInputBridge::ProcessEvent (first) -> keyboard/mouse transitions, controller/joystick state, touch event map -> TouchPanel; per-frame public queries read Keyboard / Mouse / GamePad / TouchPanel. | The bridge feeds legacy InputManager accumulators (compatibility state only), text input and IME editing, mouse-click callbacks, device hot-plug notifications and the TouchPanel event map. Keyboard::GetState, Mouse::GetState and GamePad::GetState read the platform services' snapshots (IPlatformKeyboard/IPlatformMouse/IPlatformGamepad::GetSnapshot), which the services publish in Update() once per frame at the end of Game::PollEvents; there is no Game::UpdateInput. | modules/input/src/Internal/SdlInputBridge.cpp (ProcessEvent); modules/input/src/Xna/Keyboard.cpp, Mouse.cpp, GamePad.cpp; modules/input/include/CNA/Internal/Input/InputManager.hpp; modules/runtime/src/Game.cpp (PollEvents); docs/input-backend.md sections 1 and 3 | corrected on the page |
| `architecture/audio-input.html` | SDL2 and NULL do not claim the same mixer path. | Confirmed and sharpened: only Backend/Sdl3Mixer and Backend/CnaMixer call CreateSelectedAudioDevice(); without SOUND_ENABLED the XNA facade compiles without a mixer, SoundEffect::Play() returns false, SoundEffectInstance stays stopped, and SDL2 and NULL have no capture provider. | modules/audio/src/Backend/*/; modules/audio/src/Platform/AudioDeviceFactory.cpp; modules/audio/src/Xna/SoundEffect.cpp and SoundEffectInstance.cpp (#ifdef SOUND_ENABLED) | stated on the page |
| `architecture/audio-input.html` | Audio callback code must avoid allocation, blocking locks, logging and destruction that waits on the callback itself. | The IAudioBufferCallback contract says FillBuffer is noexcept and must not block, allocate or throw; Stop() is a callback barrier and Close() guarantees no further callback; lifecycle methods must not be called from the callback. Logging is the Developer's own extension of that rule. | modules/audio/include/CNA/Audio/Platform/IAudioDevice.hpp | kept with the contract stated first and the logging point marked as an extension |
| `architecture/bindings.html` | C#, Java, Python and Rust bindings live in separate repositories. | Eight public external bindings exist (C#, Java, TypeScript, Python, Rust, Swift, Go, Ruby); cna-go and cna-ruby became public on 2026-09-25 and are listed like the others. All target ABI 0.21.x (cna-ruby also admits 0.7.0) while TARGET exports 0.29.0. | data/current-facts.json (bindings_expected_abi); modules/c-api/include/CNA/C/abi.h | table extended to eight repositories with the exported ABI stated |
| `architecture/bindings.html` | Keep C headers valid C17. | The public headers are held to a C99 floor: every header is compiled alone and through the umbrella in C99, C11 and C17 and in C++11, C++14 and C++17 as required cells; C99 is the floor by design. | docs/c-api/COMPATIBILITY.md; tools/c-api/compatibility_matrix.json | corrected on the page |
| `architecture/bindings.html` | At investigation time, binding repositories referenced earlier CNA C ABI generations; do not claim all bindings support the pinned revision. | Stated as current fact: bindings target 0.21.x and this snapshot exports 0.29.0, with incompatible steps 0.28.0 and 0.29.0 after every binding's last commit; five refuse 0.29.0 by an exact-version rule and none has been qualified against it. | modules/c-api/include/CNA/C/abi.h; docs/c-api/ABI_VERSIONING.md; data/current-facts.json | restated without reference to the earlier pin |
| `architecture/content.html` | The content-pipeline module and cna-content executable own conversion. | The canonical importer/processor/writer engine, the CNB codecs and the XNB readers and writers live in cna_content (which ships in games); cna_content_pipeline holds only the build-time-only components and the XNA Content.Pipeline facade; the command-line coordinator is cna_content_compiler (cmake/ToolContentPipeline.cmake) with cna-content as a small main over it. | modules/content/src/{Pipeline,Cnb,Xnb}; modules/content-pipeline/CMakeLists.txt; cmake/ToolContentPipeline.cmake | corrected on the page (matches the Phase-1 architecture page) |
| `architecture/content.html` | Writers serialize CNB and XNB (supported paths). | Both output formats are selected per build (CNB default, XNB with its own container options); importer, processor and canonical value are shared and only the serializer differs; XNB writers are a separate format-keyed registry. | docs/content-pipeline.md; modules/content/src/Xnb/XnbTypeWriterRegistry.cpp; XnbOutputContentPipelineTest.WriterResolutionIsKeyedByFormatSoNeitherShadowsTheOther | stated precisely on the page |
| `architecture/content.html` | ContentManager resolves .xnb, then .cnb, then registered loose-file readers such as .cnj. | Confirmed, with detail: cache, .xnb, .cnb (including a literal Foo.cnb name), then per-T reader: literal path, .cnj, reader extensions, bare path. Load<SoundEffect> is an explicit specialization repeating the same tiers by hand and skipping the generic cache. | modules/content/include/Microsoft/Xna/Framework/Content/ContentManager.hpp (Load<T>, ResolveAssetPath); ContentManager.cpp (Load<SoundEffect>); CnbContentManagerTest.ResolutionOrderIsXnbThenCnbThenLiteralThenCnjThenNative | extended on the page |
| `architecture/graphics.html` | Device creation: GraphicsDeviceManager copies preferences into the Game-owned device; the device then resolves a GraphicsRendererDescriptor; the platform creates the window; the renderer is constructed. | The order is reversed: the Game-owned GraphicsDevice_ resolves the descriptor, creates or adopts the window and constructs the renderer in its own constructor (inside the base Game constructor, default adapter, Reach profile and default PresentationParameters); GraphicsDeviceManager::CreateDevice later applies the game's preferences to that existing device through SetGraphicsProfileEXT, SetPresentationMode and an in-place Reset. | modules/graphics/src/Xna/GraphicsDevice.cpp (GraphicsDevice constructor, resolveRenderer, createOrAttachWindow, createRenderer, Reset); modules/runtime/src/GraphicsDeviceManager.cpp (applyToExistingRenderer) | corrected on the page |
| `architecture/graphics.html` | Game::BeginDraw -> GraphicsDevice::CanBeginDrawEXT -> acquire renderer thread-context lease. | The symbol GraphicsDevice::CanBeginDrawEXT does not exist; GraphicsDeviceManager::BeginDraw calls IGraphicsRenderer::CanBeginDrawEXT() and then GraphicsDevice::AcquireRendererThreadContextLeaseForFrame(). GraphicsDevice::Present takes a second, short lease that restores the previous binding. | modules/runtime/src/GraphicsDeviceManager.cpp; modules/graphics/src/Xna/GraphicsDevice.cpp (Present, AcquireRendererThreadContextLease*) | corrected on the page |
| `architecture/graphics.html` | On disposal the device marks itself disposed, detaches the registry, disposes registered resources, and only afterward tears down renderer-native state and the window/context. | Confirmed; the exact order is isDisposed_, Disposing event, move the resource list out, dispose each resource while the renderer exists, then destroyNativeResources (renderer, presenter, shared input window tokens, window) and release of the video-subsystem reference. | modules/graphics/src/Xna/GraphicsDevice.cpp (Dispose(bool), destroyNativeResources) | confirmed and made exact |
| `architecture/module-graph.html` | modules/CMakeLists.txt creates one static target per physical production module, then an interface CNA umbrella. | cna_add_module creates the STATIC target for 18 of the 22 framework modules (inspector only when CNA_BUILD_INSPECTOR is ON); gamer-services (CNA_GamerServices), net (CNA_Net), video-ffmpeg (cna_video_ffmpeg) and c-api (cna_c_api, SHARED, STATIC under Emscripten) define their targets by hand; core adds a separate INTERFACE cna_core_headers; renderer families use cna_add_renderer()/cna_renderer_common_setup() in modules/renderers/CMakeLists.txt; the CNA umbrella is an INTERFACE library. | modules/CMakeLists.txt (cna_add_module, add_library(CNA INTERFACE)); every modules/*/CMakeLists.txt; modules/renderers/CMakeLists.txt | corrected on the page |
| `architecture/module-graph.html` | Another module's private src/ is not a shared include directory. | True for framework modules, but cna_renderer_common_setup adds modules/platform/src as a PRIVATE include root to every renderer target (comment: the four allowlisted SDL3-backed families resolve a window id at one SDL3 implementation edge). A few targets also compile tools/ sources (cna_content takes tools/gltf_to_cnj/gltf_to_cnj.cpp; cna_content_compiler lives in cmake/ToolContentPipeline.cmake). | modules/renderers/CMakeLists.txt (cna_renderer_common_setup); modules/content/CMakeLists.txt; cmake/ToolContentPipeline.cmake | exceptions listed on the page |
| `architecture/module-graph.html` | cna_content_pipeline ... compiler tools opt into it. | The only consumer is cna_content_compiler (alias CNA::ContentCompiler), a static library over tools/content/content.cpp defined in cmake/ToolContentPipeline.cmake; cna-content is a small main over it. | cmake/ToolContentPipeline.cmake | named exactly |
| `architecture/module-graph.html` | (No count is stated on the Developer page; a total of 23 module directories was expected.) | The Developer page text carries no count; the tree has 23 directories under modules/: 22 framework modules in _cna_framework_modules plus renderers/ (21 family directories and common/d3d and common/mojoshader). | modules/CMakeLists.txt (_cna_framework_modules, _cna_renderer_modules); directory listing | stated on the page |
| `architecture/overview.html` | Input snapshots are updated on the game-loop thread from platform events and selected services. | The keyboard and mouse snapshots are advanced by IPlatformKeyboard::Update() and IPlatformMouse::Update() at the end of Game::PollEvents (gamepad and joystick only once the gamepad subsystem is initialized); there is no Game::UpdateInput and the input bridge does not publish those snapshots. | modules/runtime/src/Game.cpp (Game::PollEvents); modules/input/src/Xna/Keyboard.cpp and Mouse.cpp (GetState read the platform snapshot); modules/input/include/CNA/Internal/Input/InputManager.hpp | corrected on the page (ownership fact 5) |
| `architecture/overview.html` | Game is the lifecycle coordinator, not a service locator for backend internals. | True for renderers and devices, but Game exposes the platform it owns through the CNAEXT accessor GetPlatformEXT() and has a protected constructor taking an explicit IPlatform for embedding hosts and tests. | modules/runtime/include/Microsoft/Xna/Framework/Game.hpp (GetPlatformEXT, Game(std::unique_ptr<IPlatform>)) | scoped on the page |
| `architecture/overview.html` | CNA_ENABLE_SDL=AUTO/ON/OFF controls availability. | AUTO and ON configure SDL identically; only OFF changes behavior and refuses selections that need SDL (SDL3/SDL2 platform and audio, SDL_RENDERER, SDL_GPU, FNA3D, FREEDIRECT). | cmake/SdlAvailability.cmake | made precise on the page |
| `architecture/platform.html` | The runtime sends each PlatformEvent through the input bridge first, which ensures a frame's input snapshot sees the native transition. | The input bridge feeds text input, touch, click callbacks, device notifications and legacy accumulators; the keyboard, mouse and gamepad snapshots that games read are published by the platform services' Update() at the end of Game::PollEvents. The reason for bridge-first ordering in the code is that Exit() must not stop the rest of the batch reaching input. | modules/runtime/src/Game.cpp (PollEvents comments); modules/input/src/Xna/Keyboard.cpp, Mouse.cpp, GamePad.cpp; InputManager.hpp; docs/input-backend.md sections 1 and 3 | corrected on the page |
| `architecture/platform.html` | Backend-native pointers should not escape through general platform APIs. | Toolkit types (SDL_Window etc.) never cross the contract, but a tagged, non-owning, trivially copyable NativeWindowHandle carrying HWND/Display*/wl_surface*/NSWindow*/ANativeWindow* is deliberately handed to renderers inside RendererSurfaceInfo; GL, Vulkan and presenter access go through narrow services. | modules/platform/include/CNA/Platform/NativeWindowHandle.hpp; modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp (RendererSurfaceInfo, GraphicsRendererCreateArgs) | made precise on the page |
| `architecture/platform.html` | Sync() ordering is part of the contract; consuming stale dimensions is a common route to an incorrectly sized swapchain. | IPlatformWindow::Sync() blocks until pending window state changes have been applied; SetSize followed immediately by GetClientBounds may report the old size. The swapchain-sizing remark is the Developer's own inference; the API states only the asynchrony and that renderers size swapchains from GetPixelSize(). | modules/platform/include/CNA/Platform/IPlatformWindow.hpp (Sync, GetPixelSize, GetDisplayScale) | kept with the documented rule stated first |
| `architecture/runtime.html` | Game::Run: DoInitialize ... Initialize -> BeginRun -> platform BeforeLoop; RunLoop; EndRun -> platform AfterLoop. | BeforeLoop and AfterLoop are private members of Game itself (BeforeLoop marks the game active and raises Activated; AfterLoop is empty), not platform hooks. Initialize runs inside DoInitialize before BeginRun, and OnExiting is raised at the end of RunLoop. | modules/runtime/src/Game.cpp (Game::Run, RunLoop, BeforeLoop, AfterLoop, DoInitialize); Game.hpp private section | corrected on the page |
| `architecture/runtime.html` | Game::BeginDraw ... asks the device whether drawing can begin (GraphicsDevice::CanBeginDrawEXT); the device acquires a renderer thread-context lease. | GraphicsDevice::CanBeginDrawEXT does not exist. GraphicsDeviceManager::BeginDraw calls IGraphicsRenderer::CanBeginDrawEXT() on the renderer (default true; overridden by DirectX 11, DirectX 12, WebGPU and EasyGL) and then takes the lease from GraphicsDevice::AcquireRendererThreadContextLeaseForFrame(). | modules/runtime/src/GraphicsDeviceManager.cpp (BeginDraw); modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp (CanBeginDrawEXT, AcquireThreadContextLeaseEXT); repository-wide search for CanBeginDrawEXT | corrected on the page, including the tracing-points table |
| `architecture/runtime.html` | Construction: bind Window and ContentManager, then Run: DoInitialize -> GraphicsDeviceManager::CreateDevice (if present). | Confirmed, and sharpened: the renderer and window are created inside the base Game constructor (GraphicsDevice_ value member, default profile and presentation parameters); CreateDevice only applies the game's preferences to that existing device through applyToExistingRenderer -> GraphicsDevice::Reset. | modules/runtime/src/Game.cpp; modules/runtime/src/GraphicsDeviceManager.cpp (CreateDevice, applyToExistingRenderer) | made explicit on the page |
| `audio/engine.html` | modules/CMakeLists.txt defines SOUND_ENABLED only for SDL3/ALSA, selecting exactly one implementation of MixerEngine.hpp. | Which MixerEngine.hpp implementation is compiled is decided by source filtering in modules/audio/CMakeLists.txt (Backend/Sdl3Mixer only for SDL3, Backend/CnaMixer only for ALSA, neither for SDL2/NULL). SOUND_ENABLED (defined on cna_build_config INTERFACE in modules/CMakeLists.txt) gates the facade's mixer branches and Microphone's recording-provider lookup; it does not select an implementation. | modules/audio/CMakeLists.txt (list(FILTER ... EXCLUDE REGEX)); modules/CMakeLists.txt (SOUND_ENABLED block and comment); SoundEffect.cpp / Microphone.cpp (#ifdef SOUND_ENABLED) | corrected and made precise on the page (boundary, sources-and-links, sound-enabled) |
| `audio/engine.html` | Playback diagram lists 'SDL3: MIX_Generate -> Sdl3AudioDevice::FeedStream -> SDL_PutAudioStreamData' and 'ALSA: CnaMixer::Render -> convert -> AlsaAudioDevice::Run/PcmWritei' as if the mixer called the device. | The device drives the mixer: SDL demand calls Sdl3AudioDevice::FeedStream, which calls IAudioBufferCallback::FillBuffer (MixerBufferCallback -> MIX_Generate) and then SDL_PutAudioStreamData; on ALSA AlsaAudioDevice::Run calls FillBuffer (DeviceCallback -> CnaMixer::Render), converts and calls snd_pcm_writei. | modules/audio/src/Platform/Sdl3/Sdl3AudioDevice.cpp (FeedStream); Backend/Sdl3Mixer/AudioMixer.cpp (MixerBufferCallback::FillBuffer); Platform/Alsa/AlsaAudioDevice.cpp (Run); Backend/CnaMixer/MixerEngine.cpp (DeviceCallback::FillBuffer) | diagram redrawn in call direction |
| `audio/engine.html` | SoundEffect constructors 'retain metadata/value semantics where possible' in SDL2/NULL builds (implying metadata is kept generally). | Only the raw-buffer constructors record frame count and rate; the path constructor creates an empty Impl and FromStream returns an empty effect (throwing only for an empty stream), so their duration is zero without a mixer. | modules/audio/src/Xna/SoundEffect.cpp (constructors, FromStream, getDurationProperty, #else branches) | made precise in the no-mixer subsection |
| `audio/engine.html` | First hardware initialization at XNA-facing SoundEffect entry points converts native exceptions to NoAudioHardwareException. | True for SoundEffect's constructors, FromStream, master-volume getter/setter and DynamicSoundEffectInstance::Play (each has its own EnsureMixerOrThrowXna). SoundEffectInstance::Play calls EnsureMixer directly (a raw std::runtime_error would escape) and the filter setters swallow the failure via TryEnsureMixer. | SoundEffect.cpp (EnsureMixerOrThrowXna); DynamicSoundEffectInstance.cpp (EnsureMixerOrThrowXna, Play); SoundEffectInstance.cpp (Play, TryGetMixer) | made precise in the failure table; noted under current_bugs_or_gaps |
| `bindings/c-api-internals.html` | The public headers under modules/c-api/include/CNA/C/ are C17-compatible | The public headers are held to a C99 consumer floor (compatibility matrix requires c99/c11/c17 and c++11/14/17; example built at C99); C17 is the level of CNA's own C targets such as AbiHeaderC.c | tools/c-api/compatibility_matrix.json; modules/c-api/examples/c/CMakeLists.txt; modules/c-api/CMakeLists.txt (cna_c_api_header_c) | corrected on the page |
| `bindings/c-api-internals.html` | Many handles are creation-thread-affine and return CNA_RESULT_THREAD on wrong-thread access | Every handle resolved through HandleRegistry::Get<T> or released through Release is creation-thread-affine; only the internal GetKind query skips the thread check | modules/c-api/src/CnaCApiDetail.hpp (HandleRegistry::Get), CnaCApiDetail.cpp (Release, GetUserTag, SetUserTag, GetKind) | corrected on the page |
| `bindings/c-api-internals.html` | CnaCApiAudio.cpp and AudioSoundEffectSmoke.c: compare one owned-parent/child route against its C-only test | The child-first refusal, wrong-thread refusal and double-destroy expectations are in tests/pure_c/AudioSmoke.c (CApi_AudioSmoke); AudioSoundEffectSmoke.c covers the extended creation routes, 3D-audio settings and play | modules/c-api/tests/pure_c/AudioSmoke.c (main), AudioSoundEffectSmoke.c | corrected on the page; both tests named in the reading order |
| `bindings/csharp.html` | After loading, it ... consults eng/cna-native-abi-policy.json | The resolver admits against a matrix compiled into CnaNativeAbiPolicy (ConsumerVersion 0.21.0); the JSON is the machine-readable record, kept equal to the code by CnaAbiTests.MachineReadablePolicy_MatchesExecutableMatrix, not read at run time | cna-cs@e223909 src/CNA.Interop/NativeLibraryResolver.cs (Resolve), src/CNA.Interop/CnaNativeAbiPolicy.cs, tests/CNA.Framework.Tests/CnaAbiTests.cs | corrected on the page |
| `bindings/csharp.html` | The XNA-compat facade uses that internal method to wrap the same handle in its derived public type | The compat SoundEffectInstance is not derived from CNA.Audio.SoundEffectInstance; it composes a new inner CNA.Audio.SoundEffectInstance around the same native handle and strongly roots its parent, while the compat parent keeps weak child references and disposes live children first | cna-cs@e223909 src/CNA.XnaCompat/Microsoft/Xna/Framework/Audio/SoundEffect.cs (CreateInstance, Dispose), SoundEffectInstance.cs (ctor) | corrected on the page (composition, not derivation); the one-handle/one-owner point is kept |
| `bindings/csharp.html` | against CNA at d6e9ff050e32bcfe5e7fd640006619a90b92298c ... this CNA revision declares 0.29.0 | Reframed to CNA snapshot 009d40f5, which also declares 0.29.0 (abi.h CNA_ABI_VERSION_MINOR 29) | modules/c-api/include/CNA/C/abi.h | corrected framing; version statement unchanged and TARGET-correct |
| `bindings/java.html` | NativeAudio.nativePlaySoundEffect is a special case that encodes played? as 0/1 and failure as a negative number | It is one instance of a general JNI convention: bool_result / int32_result / uint32_result return the value on success and the negated CNA_Result on failure, consumed by NativeBindings.booleanResult / longResult | cna-java@6661173 src/main/c/cna_java_jni.c (bool_result, nativePlaySoundEffect); NativeBindings.java (booleanResult, longResult) | corrected on the page (two result conventions listed) |
| `bindings/java.html` | build.gradle defaults to a sibling cnanext directory | Default is rootProject.file('../../cnanext') (two levels above the repository root); a missing cna.h is a GradleException with no fallback | cna-java@6661173 build.gradle (configuredCnaRoot) | corrected on the page |
| `bindings/java.html` | JNI side obtains bytes with GetByteArrayElements and pins the byte[] only for this call | GetByteArrayElements may pin or copy at the JVM's discretion; released with JNI_ABORT after the synchronous call | cna_java_jni.c (Java_org_openeggbert_cna_internal_NativeAudio_nativeCreateSoundEffect) | wording made precise on the page |
| `bindings/java.html` | Java stores returned 64-bit handle (implied for every handle including the game) | The Java game value is the address of a JNI-side JavaGame record that holds the CNA_Handle; resource values such as SoundEffect are raw CNA_Handle values | cna_java_jni.c (nativeCreateGame returns (jlong)(uintptr_t)wrapper; java_game()) | corrected/extended on the page |
| `bindings/java.html` | the creating thread drains its queue during framework dispatcher update | True, but the only main-source caller of NativeBindings.updateFrameworkDispatcher is the public FrameworkDispatcher.Update(); a failed deferred release is reported, not retried | NativeDeferredRelease.java (drain); NativeBindings.java (updateFrameworkDispatcher); FrameworkDispatcher.java | made precise on the page |
| `bindings/python.html` | The generated *_abi.py files define measured ctypes structures, callbacks and constants. | Only devices_abi.py, engine_abi.py, input_abi.py and online_abi.py are generated (tools/generate_family_abi.py); abi.py and cnb_abi.py are reviewed hand-maintained layouts; devices/input/online manifests are generated, engine and cnb manifests are hand-maintained; all prototypes are proven by tools/verify_prototypes.py. | cna-python@f085bbb src/_cna_native/*_abi.py and *_manifest.py module docstrings | corrected on the page |
| `bindings/python.html` | _active_native selects live Game and cna_c_api library | _active_native uses runtime_context.current_game(), the thread-local game set only while Game.Run/RunOneFrame is active on that thread, and requires the host to have a native handle; it does not fall back to live_game(). | cna-python@f085bbb src/Microsoft/Xna/Framework/Audio/_common.py (_active_native), src/_cna_native/runtime_context.py | corrected on the page |
| `bindings/python.html` | Audited source ... the handbook pins CNA d6e9ff05, ABI 0.29.0 | The CNA side is now the TARGET snapshot 009d40f5, which also declares ABI 0.29.0 (abi.h CNA_ABI_VERSION_MINOR 29). | modules/c-api/include/CNA/C/abi.h | restated against TARGET |
| `bindings/python.html` | The one documented PENDING_ROUTES exception is a header-declared but unexported network-session route | True for the artifacts the binding measured on 2026-09-02; at TARGET cna_network_session_replace_session_properties is declared, implemented (CnaCApiNetSessions.cpp), recorded in abi_baseline.json exports and used by NetSmoke.c (added by CNA commit 7712534d3, 2026-09-01). | modules/c-api/include/CNA/C/net_sessions.h; modules/c-api/src/CnaCApiNetSessions.cpp; tools/c-api/abi_baseline.json | kept as binding-revision fact, TARGET status added as a scoped note |
| `build/architecture.html` | Root order: identities, platform, audio, SDL availability, third-party + sharp-runtime, renderer selection, registry, enable_testing, modules, then 'root tools, harnesses, UnitTests, probes and final-link checks' | The real order adds BuildPerformance/ccache/options, TestDisplayPolicy, ApplePlatform, ThirdPartySDL2, Sdl2OnlyConfiguration, ThirdPartySDL, instrumentation, RendererRuntime, RendererDescriptorGate, PlatformRatchet and HotPathLint before modules, and after modules runs Harnesses, XnaPipelineParityGates, Tool*.cmake, UnitTests, AppleSmoke, ModuleProbes, the Apple bundle sweep and only then attaches private build policy; there is no 'final-link check' stage | CMakeLists.txt (include order) | diagram rewritten from the file |
| `build/architecture.html` | Cross-module internal contracts live in include/CNA/Internal, not a neighbouring module's src/ | True in general, with two private exceptions: every renderer target adds modules/platform/src PRIVATE (SDL-backed allow-listed families include Sdl3RendererInterop.hpp) and the test build adds modules/audio/src and other test-support roots | modules/renderers/CMakeLists.txt (cna_renderer_common_setup); cmake/UnitTests.cmake (cna_test_build_config) | exceptions stated |
| `build/architecture.html` | The generated reference's 237 syntactic test declarations | 239 declarations in the generated test target index at TARGET | development/reference/test-targets.html | number replaced |
| `build/index.html` | Renderer matrix work: multi-renderer preset plus private GPU wrapper | The multi-renderer preset compiles HEADLESS, SOFTWARE and STUB (no GPU) and has no build preset; the private display wrapper is for GPU or window tests in a hand-configured GPU tree | CMakePresets.json (multi-renderer, buildPresets) | corrected in #configs |
| `build/index.html` | Broad repository tests: tests preset (implying a complete build) | The tests build preset builds only CnaTests, while many CTest entries in that configuration are separate example executables; an unfiltered ctest after it reports missing binaries | CMakePresets.json (buildPresets.tests targets [CnaTests]); .github/workflows/general-tests-ci.yml (full default build first) | warning callout and corrected command |
| `build/index.html` | Manual selection HEADLESS platform + HEADLESS renderer + NULL audio presented as the display-free build | With the default CNA_ENABLE_SDL=AUTO the vendored SDL is still configured and built; only CNA_ENABLE_SDL=OFF is SDL-free | cmake/SdlAvailability.cmake (AUTO 'exactly as before') | note added |
| `change-map/index.html` | Keyboard / mouse: ... platform service → Game::TickInput tests | There is no Game::TickInput (or UpdateInput); the event bridge and snapshot updates run in Game::PollEvents, called by Tick | modules/runtime/src/Game.cpp (Game::PollEvents) | corrected on the page |
| `change-map/index.html` | Input public type → SdlInputBridge.cpp / platform service | SdlInputBridge.cpp defines PlatformInputBridge::ProcessEvent (the name is historical); the public Keyboard/Mouse types read snapshot services (IPlatformKeyboard/IPlatformMouse), not the bridge's accumulators | modules/input/src/Internal/SdlInputBridge.cpp; modules/input/src/Xna/Keyboard.cpp | both paths named and the test-shim trap stated |
| `change-map/index.html` | Buffers / geometry: bounds and type conversion; conformance | 32-bit index buffers are an explicit renderer opt-in (default factory throws) and multi-stream vertex input is capability-gated with NotSupportedException | modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp (CreateIndexBuffer32); modules/graphics/src/Xna/GraphicsDevice.cpp | extended in the trap column |
| `content/pipeline.html` | Treat a .contentproj's declared routes and the tool's format option as separate inputs: the same project can target a different output container. | A project build always writes XNB: Tasks::BuildContent runs the coordinator with --format xnb --config <generated> --xna-compatible --only-configured-assets, and cna_add_content refuses FORMAT, XNB_PLATFORM, XNB_PROFILE, XNB_COMPRESS, CONFIG_FILE and XNA_COMPATIBLE together with CONTENT_PROJECT. Platform, profile and compression can still be overridden from the command line; CNB requires a SOURCE_DIR build. | modules/content-pipeline/src/Xna/Tasks/BuildContent.cpp (argument vector); cmake/ToolContentPipeline.cmake (cna_add_content conflict checks); tools/content/content.cpp (RunContentProject) | corrected on the page (callout in #project) |
| `content/pipeline.html` | The processor's parameter validation checks that a requested compressed format has an encoder; the XNB path additionally imposes format/container restrictions, including DXT block dimensions. | The multiple-of-four DXT dimension rule, the Reach/HiDef size, aspect and mip limits are enforced inside TextureProcessor::Process, and only when ContentProcessorContext::OutputFormat() is Xnb; the XNB writer's own restriction is ConvertCnbTextureToXnb picking the first representation the container version can express. A CNB build asked for DXT keeps Rgba8 and warns. | modules/content/src/Pipeline/Texture2DContentPipeline.cpp (TextureProcessor::Process, ValidateParameters); modules/content/src/Pipeline/XnbOutputContentPipeline.cpp (ConvertCnbTextureToXnb) | refined on the page (#texture-process, #texture-write) |
| `content/runtime.html` | MonoGame LZ4 is explicitly rejected at this revision; its enum presence is not decoder support. | ContentManager::LoadXnbAsset<T> decodes an LZ4 payload through the bounded in-tree DecompressXnbLz4Payload. Only the untyped external-reference decoder LoadXnbAssetUntyped (used by ExternalReferenceReader) still refuses LZ4 with a 'not yet supported' ContentLoadException. | modules/content/include/Microsoft/Xna/Framework/Content/ContentManager.hpp (LoadXnbAsset, XnbCompression::Lz4 case); modules/content/src/Xna/ContentManager.cpp (LoadXnbAssetUntyped); modules/content/src/Xnb/XnbDecompression.cpp (DecompressXnbLz4Payload) | corrected on the page; both halves stated and kept as a maintainer note |
| `conventions/index.html` | C API headers must remain valid C17 | The C targets compile with C_STANDARD 17 (extensions off); the public headers' floor is C99 and every header is also compiled as C++11/14/17 by CApiHeaderCompatibility | modules/c-api/CMakeLists.txt; docs/c-api/COMPATIBILITY.md; cmake/Tests/ModuleProbes.cmake | corrected on the page |
| `conventions/index.html` | Exported public API declarations require Doxygen documentation (stated as enforced) | A repository rule held by review: the Doxyfile warns on undocumented members but WARN_AS_ERROR is NO and no default CTest fails on a missing comment; CNAEXT_DoxygenGroup covers the engine layer (skips without doxygen) and the C API coverage generator runs doxygen over the public C++ headers | Doxyfile; modules/graphics-ext/examples/CMakeLists.txt; tools/c-api/generate_coverage_inventory.py | corrected: enforcement scope stated |
| `conventions/index.html` | Platform/backend source selection is performed by CMake and audited scripts | Confirmed and made concrete: the SDL ratchet (budget zero, strict by default, X11 and Wayland counted), the hot-path lint and the descriptor gate run at configure time, and RuntimeRendererDiscipline forbids CNA_RENDERER_* in modules/graphics/src | cmake/PlatformRatchet.cmake; cmake/PlatformHotPathLint.cmake; scripts/check_runtime_renderer_discipline.py | extended with the mechanisms |
| `debugging/index.html` | Trace platform resize event → window Sync() → logical/drawable sizes → backend surface configuration | A resize event is handled in Game::PollEvents by refreshing GameWindow, calling GraphicsDevice::UpdateViewportFromWindow (which re-queries id, native handle, pixel size and scale and calls the renderer's OnSurfaceChanged) and OnSurfaceInvalidated, ignoring the payload; Sync() is called only on the requested-size path (applyPresentationParametersToWindow, GameWindow::EndScreenDeviceChange) | modules/runtime/src/Game.cpp (PollEvents); modules/graphics/src/Xna/GraphicsDevice.cpp (UpdateViewportFromWindow, applyPresentationParametersToWindow); modules/runtime/src/GameWindow.cpp | corrected: two resize paths described separately |
| `debugging/index.html` | Renderer initialization: confirm CMake selected the intended implementation rather than a fallback | Runtime fallback is opt-in (no chain means exactly one attempt) and configure never substitutes: an unknown, reserved or invalid identity is a configure error; the resolved renderer is logged at INFO at first device creation | modules/core/src/GraphicsRendererSelection.cpp; cmake/RendererIdentities.cmake; modules/graphics/src/Xna/GraphicsDevice.cpp | extended with GetSelected/GetActive/GetFallbackHistory and the failure text |
| `debugging/index.html` | Enable validation/debug layers before changing code (no switches named) | Per family: Vulkan layer on in non-NDEBUG builds; CNA_OPENGL4_DEBUG_OUTPUT; CNA_D3D11_DEBUG_LAYER; CNA_D3D12_DEBUG_LAYER and CNA_D3D12_GPU_VALIDATION; CNA_WEBGPU_VALIDATE_SHADERS; EasyGL installs no debug-message callback | modules/renderers/*/src (getenv sites); cmake/TestHelpers.cmake | extended: switches named |
| `getting-started/index.html` | Read modules/runtime/include/CNA/Game.hpp | The public header is modules/runtime/include/Microsoft/Xna/Framework/Game.hpp; the CNA/Game.hpp path never existed (absent at the Developer pin and at TARGET, no history) | modules/runtime/include/Microsoft/Xna/Framework/Game.hpp | corrected on the page, with a warning callout explaining the include-root rule |
| `getting-started/index.html` | Read modules/platform/include/CNA/Internal/Platform/IPlatform.hpp | IPlatform lives at modules/platform/include/CNA/Platform/IPlatform.hpp (PlatformFactory.hpp is beside it) | modules/platform/include/CNA/Platform/IPlatform.hpp | corrected on the page |
| `getting-started/index.html` | Read modules/graphics/include/CNA/Graphics/GraphicsDevice.hpp | GraphicsDevice.hpp is modules/graphics/include/Microsoft/Xna/Framework/Graphics/GraphicsDevice.hpp; its implementation is modules/graphics/src/Xna/GraphicsDevice.cpp | modules/graphics/include/Microsoft/Xna/Framework/Graphics/GraphicsDevice.hpp | corrected on the page |
| `getting-started/index.html` | Compare git rev-parse HEAD with cat ../developer.libcna.com/cnahead | Process content about the standalone manual's own pin file; on libcna.com the pin is the cnahead file of the site repository and the snapshot commit is stated on the page | cnahead (site repository) | translated into merge-base/rev-list comparison commands against the stated snapshot commit and a pointer to the maintenance page |
| `graphics/backends.html` | OpenGL4 and PortableGL (like Headless, Stub, GDI, FNA3D, ...) still require their own source-verified tours; nothing is said about how OPENGL4 relates to EasyGL. | OPENGL4 is a separate family (own directory, target, namespace, descriptor, loader, context request; no easy-gl/meta-gl dependency). What decides XNA semantics is shared with EasyGL through GlStockShaderSources.hpp (one GLSL ES 3.00 stock-program corpus moved out of EasyGLRenderer.cpp; OpenGL4 rewrites '#version 300 es' to '#version 410 core') and GlPresentationSurfaceState.hpp (EasyGLSurfaceState is now an alias); PlatformGlRendererState.hpp gained IsCurrent()/EnsureCurrent(). RendererCombinations has no rule against OPENGL4 beside an EasyGL identity, but only PORTABLEGL conflicts with real GL and no CI multi-renderer set contains that pair. | modules/renderers/opengl4/CMakeLists.txt; opengl4/src/OpenGL4Renderer.cpp (AdaptGlslEs300ForDesktopCore); modules/graphics/include/CNA/Internal/Renderers/Common/{GlStockShaderSources,GlPresentationSurfaceState,PlatformGlRendererState}.hpp; cmake/RendererCombinations.cmake; .github/workflows/* | added on the page (#opengl4-and-easygl); OpenGL4 internals page is a sibling |
| `graphics/backends.html` | Headless and Stub still require their own source tours (committed text); working tree moves both into the source-toured table. | Both families exist with descriptors (window kind None) and both are covered by sibling pages in the Development area. | modules/renderers/headless, modules/renderers/stub | absorbed: two rows in the source-toured table, removed from the uncovered list |
| `graphics/device.html` | Device loss is not one uniform cross-renderer recovery protocol: the deviceEventCallback reports real lost/reset events where a renderer calls it. | Confirmed and made concrete: six families call it (DirectX 9/11/12, Direct2D and WebGPU report Lost/Resetting/Reset; Vulkan reports Lost once and then throws). The comment on GraphicsDevice::deviceStatus_ ("every renderer except D3D9 never calls that") and the comment in createRenderer ("nine of the ten renderers never call this") are stale; the public DeviceLost doc says "never raised on desktop". | modules/renderers/{directx9,directx11,directx12,direct2d,webgpu,vulkan}/src (deviceEventCallback_ users); GraphicsDevice.hpp deviceStatus_ comment | corrected on the page (#resources, device-loss paragraph) |
| `graphics/device.html` | The frame path asks for an IRendererThreadContextLease before Draw. | GraphicsDeviceManager::BeginDraw returns false when CanBeginDrawEXT() is false, then stores AcquireRendererThreadContextLeaseForFrame() (ReleaseRendererBinding); Present() itself takes a RestorePreviousBinding lease. Only EasyGL and OpenGL4 return a non-null lease. | modules/runtime/src/GraphicsDeviceManager.cpp (BeginDraw/EndDraw); GraphicsDevice.cpp (AcquireRendererThreadContextLease*); renderers/{easygl,opengl4} AcquireThreadContextLeaseEXT | corrected and extended on the page (#thread) |
| `graphics/draw-call.html` | The method validates index/vertex ranges where the backend requests managed buffered validation (or no stream declaration exists), verifies stream capability, applies samplers and finally calls DrawIndexedPrimitivesEx. | Confirmed; made exact: applySamplerStatesToRenderer() at slot 0 runs validateDrawState() (XNA draw-time profile rules) and pushes rasterizer state before the sampler slots, and the range check is skipped for EasyGL, OpenGL4 and Software because they override RequiresManagedBufferedDrawRangeValidationEXT() to false. | modules/graphics/src/Xna/GraphicsDevice.cpp (DrawIndexedPrimitives, applySamplerStatesToRenderer); EasyGLRenderer.hpp, OpenGL4Renderer.hpp, SoftwareRenderer.hpp (RequiresManagedBufferedDrawRangeValidationEXT) | extended on the page (#public list and table) |
| `graphics/easygl.html` | EasyGLSurfaceState holds a platform-supplied RendererSurfaceInfo snapshot plus virtual resolution and presentation mode (an EasyGL class). | EasyGLSurfaceState is now a type alias of CNA::Internal::Renderers::GlPresentationSurfaceState in modules/graphics (moved verbatim by GL4-0010 so OPENGL4 shares the transform); behaviour unchanged. | modules/renderers/easygl/include/CNA/Internal/Renderers/EasyGL/EasyGLRenderer.hpp (using EasyGLSurfaceState = GlPresentationSurfaceState); modules/graphics/include/CNA/Internal/Renderers/Common/GlPresentationSurfaceState.hpp | corrected on the page |
| `graphics/easygl.html` | Stock programs and GLSL are EasyGLRenderer.cpp internals (implicit in 'shader headers' and 'selects a stock program'). | The stock-effect GLSL corpus moved verbatim to modules/graphics/include/CNA/Internal/Renderers/Common/GlStockShaderSources.hpp (GL4-0008) and is compiled by both EasyGL and OPENGL4; uniform/attribute names are a two-renderer contract. | GlStockShaderSources.hpp header comment; EasyGLRenderer.cpp includes it and calls GlStockShaders::*Source() | corrected on the page (new #shaders section) |
| `graphics/easygl.html` | Clear temporarily disables scissor and forces color writes because XNA Clear must cover the whole target. | Confirmed, and additionally EasyGLRenderer::Clear(r,g,b,a) clears colour only (REMED-GFX-142); combined clears have separate entry points. | EasyGLRenderer.cpp (EasyGLRenderer::Clear) | extended on the page |
| `graphics/resources.html` | "The pinned source contains a bound-target lifetime test referenced by the disposal code; its target/CI mapping remains to be audited." | bound_target_lifetime_test.cpp is registered as EasyGL_BoundTargetLifetime, Headless_BoundTargetLifetime, Software_BoundTargetLifetime, Vulkan_BoundTargetLifetime, WebGPU_BoundTargetLifetime, SdlGpu_BoundTargetLifetime (plus per-leg variants), OpenGL4_EasyGLParity_BoundTargetLifetime through the parity corpus, and Resource_BoundTargetLifetime in cmake/DirectXParityTests.cmake. | modules/renderers/{easygl,headless,software,vulkan,webgpu,sdl-gpu}/examples/CMakeLists.txt; modules/renderers/opengl4/examples/EasyGLParityCorpus.cmake; cmake/DirectXParityTests.cmake | corrected on the page (#dispose-target) |
| `graphics/resources.html` | RenderTarget2D::Dispose(bool) explicitly refuses to dispose a target that remains bound. | Confirmed, with the precise condition: the check runs only while the target is in the live device's currentRenderTargets_; the destructor path reaches Texture2D::Dispose which calls DetachDestroyedRenderTarget (noexcept, errors swallowed), clears currentRenderTargets_, sets boundRenderTargetDestroyed_ and deliberately leaves renderTargetBound_ set. | modules/graphics/src/Xna/RenderTarget2D.cpp (Dispose); Texture2D.cpp (Dispose); GraphicsDevice.cpp (DetachDestroyedRenderTarget) | extended on the page |
| `graphics/sdl-gpu.html` | Custom-effect instancing throws NotSupportedException in DrawInstancedPrimitivesEx. | DrawInstancedPrimitivesEx routes an instanced custom-effect draw to QueueCustomEffect3DDrawEXT with its instance count (SMG-0022); it throws NotSupportedException only when the effect is not a valid SdlGpuEffectRenderer. The custom 3D command binds only vertex slot 0, so per-instance vertex streams are not consumed; GetAdditionalLimitationsTextEXT still says ShaderEffect instancing is not implemented. | modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp (DrawInstancedPrimitivesEx, QueueCustomEffect3DDrawEXT, IssueCustomEffect3DDrawEXT, GetAdditionalLimitationsTextEXT) | corrected on the page; stale limitation text recorded as a gap |
| `graphics/sdl-gpu.html` | CNA's custom ShaderEffect path is only compiled when a target-native shaderc/SPIR-V driver path is found; on Windows/Apple it is explicitly disabled. | Only the GLSL route depends on CNA_SDL_GPU_SHADER_EFFECTS (libshaderc). CompileProgram detects SPIR-V payloads and uses CompileSpirvProgramEXT with RemapSpirvForSdlGpuEXT in every build (SMG-0006/0007). CustomEffects still reports the GLSL route only. | SdlGpuRenderer.cpp (SdlGpuEffectRenderer::CompileProgram, CompileSpirvProgramEXT); SdlGpuSpirvBindings.hpp | corrected on the page (new #shaders section) |
| `graphics/sdl-gpu.html` | The normal route calls DispatchStockDrawEXT, which selects a stock draw family and queues a typed command. | DispatchStockDrawEXT first sends a valid custom ShaderEffect to QueueCustomEffect3DDrawEXT (SMG-0019); only then does it select a stock shape; StrideDerived covers SkinnedEffect and PBR strides, anything else throws NotSupportedException. | SdlGpuRenderer.cpp (DispatchStockDrawEXT) | corrected on the page |
| `graphics/sdl-gpu.html` | examples/CMakeLists.txt registers SDL_gpu tests only when CNA_BUILD_TESTS and SDL_GPU is the build's default renderer. | The guard is CNA_GRAPHICS_RENDERER STREQUAL "SDL_GPU", and modules/renderers/CMakeLists.txt re-points CNA_GRAPHICS_RENDERER to each family's identity while entering it, so the block is entered whenever SDL_GPU is in CNA_GRAPHICS_RENDERERS; cna_register_renderer_test sets no renderer selection. | modules/renderers/sdl-gpu/examples/CMakeLists.txt; modules/renderers/CMakeLists.txt (family loop); cmake/TestHelpers.cmake (cna_register_renderer_test) | corrected on the page; recorded as a gap |
| `graphics/sdl-gpu.html` | Destructor: unregister window, drop queued data, destroy stock pipelines, compiled effects, drain releases, release depth/MSAA/proxy, unclaim, destroy device, release ShaderCross. | Still true but incomplete: the compute sampler is released first; storage-buffer draw bindings and the indirect keep-alive are dropped before stock resources; ReleaseStorageBuffersForRendererTeardownEXT detaches every surviving storage buffer before the device is destroyed (SMG-0040); compiled-effect teardown releases native effects via ReleaseCompiledEffectsForRendererTeardownEXT. | SdlGpuRenderer.cpp (~SdlGpuRenderer); SdlGpuModern.cpp (ReleaseStorageBuffersForRendererTeardownEXT) | corrected and extended on the page |
| `graphics/selection.html` | Latch(active) happens only after construction succeeds. | The latch closes at the end of GraphicsDevice::resolveRenderer(), i.e. once the renderer object exists, which is before the GraphicsDevice constructor has run UpdateViewportFromWindow() and pushed the default blend, depth-stencil and rasterizer state. If one of those later steps throws, the constructor's catch destroys the native resources but the selection stays latched. GraphicsRendererSelection.hpp's IsLatched() comment ("when the first GraphicsDevice begins construction") is also out of date. | modules/graphics/src/Xna/GraphicsDevice.cpp (GraphicsDevice::resolveRenderer, tail; GraphicsDevice constructor try-block); modules/core/include/CNA/GraphicsRendererSelection.hpp (IsLatched) | corrected on the page (#latch) |
| `graphics/selection.html` | Configure chain: RendererIdentities -> RendererIdentityDefault -> RendererSelection. | RendererIdentities.cmake is included by the top-level CMakeLists.txt before PlatformSelection.cmake; RendererIdentityDefault.cmake is first included from SdlAvailability.cmake (to decide whether SDL is needed) and only re-included by RendererSelection.cmake; RendererRuntime.cmake sits between RendererSelection and RendererRegistry; the descriptor gate is defined in RendererDescriptorGate.cmake and called from modules/renderers/CMakeLists.txt. | CMakeLists.txt (include order); cmake/SdlAvailability.cmake | corrected on the page (#configure diagram) |
| `graphics/selection.html` | (implicit) an identity's arm in RendererSelection.cmake adds only host gates and dependencies. | After the Developer pin the OPENGL4 arm gained the default-OFF option CNA_OPENGL4_COMPILED_EFFECTS (MojoShader, GLSL 1.20 route), shaped like CNA_EASYGL_COMPILED_EFFECTS. | cmake/RendererSelection.cmake (OPENGL4 arm) | extended on the page (#configure-variables) |
| `graphics/software.html` | examples/CMakeLists.txt registers many named CTest oracles when SOFTWARE is the default renderer and tests are on. | The guard is CNA_GRAPHICS_RENDERER STREQUAL "SOFTWARE" (after an SDL3-target early return); modules/renderers/CMakeLists.txt re-points CNA_GRAPHICS_RENDERER to each family's identity while entering it, so the block is entered when SOFTWARE is merely a member of CNA_GRAPHICS_RENDERERS (e.g. the multi-renderer preset HEADLESS;SOFTWARE;STUB); registrations set no renderer selection. | modules/renderers/software/examples/CMakeLists.txt; modules/renderers/CMakeLists.txt; CMakePresets.json (multi-renderer) | corrected on the page; recorded as a gap |
| `graphics/stub.html` | Textures remember width/height, not pixels; inherited default data methods discard the transfer. | Uploads (UpdatePixels, UpdatePixelsLevel) keep empty defaults and are discarded, but ITextureRenderer::GetData defaults to false ('this renderer read nothing back'), and the shared Texture2D layer raises NotSupportedException only where it has no CPU shadow; a plain Texture2D::GetData is answered from its own CPU-side pixels. The StubTextureRenderer header comment ('SetData()/GetData() accepted ... simply discarded') is stale for reads. | modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp (ITextureRenderer::GetData, REMED-GFX-127 comment); modules/graphics/src/Xna/Texture2D.cpp (GetData); modules/renderers/stub/include/CNA/Internal/Renderers/Stub/StubRenderer.hpp | corrected on the page (#behavior) |
| `graphics/vulkan.html` | The example suite is conditional on ... the default CNA_GRAPHICS_RENDERER=VULKAN; merely adding VULKAN to a multi-renderer set does not activate this block. | The guard is CNA_GRAPHICS_RENDERER STREQUAL "VULKAN", and modules/renderers/CMakeLists.txt re-points CNA_GRAPHICS_RENDERER to each family's identity while entering it, so the block IS entered when VULKAN is a non-default member of CNA_GRAPHICS_RENDERERS; cna_register_renderer_test sets no renderer selection. | modules/renderers/vulkan/examples/CMakeLists.txt (top guard); modules/renderers/CMakeLists.txt (family loop); cmake/TestHelpers.cmake | corrected on the page; recorded as a gap |
| `graphics/vulkan.html` | The destructor releases/disconnects compute shaders, timers, storage buffers, render targets, vertex/index buffers, textures, arrays, 3D/cube textures and cube targets. | Confirmed in substance; actual order is timers, compute shaders, storage buffers, render targets, vertex buffers, index buffers, own per-frame buffers, textures, 2D arrays, storage textures, Texture3D, TextureCube, RenderTargetCube; without a device only the instance is destroyed. | modules/renderers/vulkan/src/VulkanRenderer.cpp (~VulkanRenderer) | corrected on the page (ordered list) |
| `input/events.html` | PlatformEvent is a std::variant with discrete key, committed-text, editing, mouse, device, touch and window events. | PlatformEvent has sixteen alternatives: Quit, Window, Key, TextInput, TextEditing, TextEditingCandidates, MouseMotion, MouseButton, MouseWheel, Touch, Device, Sensor, ControllerAxis, ControllerButton, AppLifecycle and Drop. The bridge has branches for nine of them (Key, TextInput, TextEditing, TextEditingCandidates, MouseMotion, MouseButton, MouseWheel, Touch, Device) and Game::PollEvents for five (Quit, Window, Key, Drop, AppLifecycle); Sensor, ControllerAxis and ControllerButton have no branch in either. | modules/platform/include/CNA/Platform/PlatformEvent.hpp (PlatformEvent); modules/input/src/Internal/SdlInputBridge.cpp (PlatformInputBridge::ProcessEvent); modules/runtime/src/Game.cpp (PollEvents) | corrected on the page; consumer table added |
| `input/events.html` | TouchEvent reaches the bridge, which maps finger IDs, advances TouchPanel state and gesture detection. | The bridge only writes touch state (INTERNAL_setTouchState) and feeds the gesture recognizer (INTERNAL_onTouchEvent). Advancing previous state, retiring released touches, promoting Pressed to Moved and the gesture timing pass happen in TouchPanel::Update, reached from FrameworkDispatcher::Update once per Game::Update and only after a touch device is known to exist. | modules/input/src/Xna/Touch/TouchPanel.cpp (Update, advanceEventTouches); modules/audio/src/Xna/FrameworkDispatcher.cpp (Update); modules/runtime/src/Game.cpp (Update) | corrected on the page |
| `input/events.html` | DeviceEvent drives connected/disconnected extension callbacks. | Only mouse, keyboard and joystick DeviceEvents are forwarded (InputDevices::*ConnectedEXT/*DisconnectedEXT, Joysticks::ConnectedEXT/DisconnectedEXT with an announced-id set); gamepad, touch, haptic and sensor device events have no branch. | modules/input/src/Internal/SdlInputBridge.cpp (DeviceEvent branch) | made precise on the page |
| `input/events.html` | Wheel totals use XNA's cumulative 120-unit convention (presented as one convention for both backends). | SDL3 (Sdl3Mouse::ObserveEvent) and the bridge's accumulator cast each float to whole notches before multiplying by 120, discarding sub-notch motion; the Win32 mapper accumulates raw WHEEL_DELTA units without truncation. The snapshot contract is shared, the granularity differs. | modules/platform/src/Sdl3/Sdl3InputServices.cpp (AccumulateWheel); modules/platform/src/Win32/Win32EventMapper.cpp (TranslateMouseWheel, wheelVertical_); SdlInputBridgeMouseTests.cpp FractionalSubNotchIsTruncatedBeforeScaling | made precise on the page |
| `input/events.html` | Public static input APIs borrow the current platform and must not outlive platform teardown. | They call GetCurrentPlatform(), which returns the installed (newest live game's) platform but lazily creates and owns a default platform when none is installed, so an early or late call constructs one. | modules/platform/src/CurrentPlatform.cpp (ResolveCurrentPlatform, LazyDefault) | made precise on the page |
| `invariants/index.html` | Input sees every platform event before game handling. Inspect: Game::Tick | The bridge-before-handling loop is Game::PollEvents (called by Tick, WaitWhileSuspended and the Emscripten body); the keyboard/mouse (and gated gamepad/joystick) snapshot Update() calls are its tail; Game::UpdateInput does not exist | modules/runtime/src/Game.cpp (Game::PollEvents) | corrected on the page; sole production call site of PlatformInputBridge::ProcessEvent noted |
| `invariants/index.html` | Backend axes remain independent: platform, renderer and audio identities must not be collapsed into one implicit bundle | True, but the axes are constrained by explicit configure-time rules (TERMINAL only with CPU renderers, platform partitions of renderer identities, CNA_ENABLE_SDL=OFF refusals); independence is not absence of constraints | cmake/RendererSelection.cmake, cmake/RendererCombinations.cmake, cmake/SdlAvailability.cmake, .github/workflows/platform-ci.yml matrix | corrected: rule restated with its constraints and CI tuples |
| `invariants/index.html` | Claimed capabilities must be truthful (a backend cannot advertise a capability then silently no-op) | Enforced by convention plus false-by-default opt-in virtuals; the default IGraphicsRenderer::SupportsCapability still returns true for older entries and CustomEffects means accepts, not executes | modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp (SupportsCapability and the EXT opt-in queries); modules/graphics/src/Xna/GraphicsDevice.cpp (SupportsCapability) | corrected: mechanism, default-true trap and the accepts-versus-executes distinction stated |
| `invariants/index.html` | Content pipeline does not require runtime devices. Inspect: content-pipeline targets/tests | No pipeline source constructs a GraphicsDevice, platform or window; the mechanically enforced property is the link-closure boundary (CnaXnbDependencyBoundary); no test asserts device-freedom directly and load-back verification tests construct a device on the runtime side | cmake/UnitTests.cmake (CnaXnbDependencyBoundary); tools/xnb/dependency_boundary.py; docs/content-pipeline.md; content-pipeline-windows-ci.yml | corrected: enforcement and its limits stated |
| `invariants/index.html` | Platform outlives dependent members. Inspect: Game.hpp member order and Game.cpp construction/disposal | Confirmed; additionally Components_ is declared before GraphicsDevice_ (owned components outlive the device), an install guard covers failed construction, and no test asserts the declaration order | modules/runtime/include/Microsoft/Xna/Framework/Game.hpp; modules/runtime/src/Game.cpp | extended with the precise order, guard and test boundary |
| `invariants/index.html` | Graphics/window/input/audio teardown can still need platform services | Audio playback devices are independent of the window/input platform (IAudioDevice contract); the audio module touches the platform only for title-content file access; the dependents that reach the ambient platform are the graphics device, window wrapper, input services, content file system and devices sensors/haptics | modules/audio/include/CNA/Audio/Platform/IAudioDevice.hpp; modules/audio/src/Xna/TitleContentBytes.cpp; modules/content/src/Xna/ContentManager.cpp; modules/devices/src/Detail/PlatformVibrateBackend.cpp | corrected on the page: audio removed from the list of platform-dependent teardown, with the exception stated |
| `maintenance/index.html` | Update the revision constant in assets/site.js; run make generate then make check; scripts/check_site.py verifies the revision marker used by the site | libcna.com has no assets/site.js, Makefile or check_site.py: the pin is cnahead, generated pages come from scripts/generate_dev_reference.py, indexes from scripts/build_site_indexes.py and validation from scripts/validate_all.sh (validate_site.py, validate_presentation.py, compare_presentation.py, check_facts.py, check_source_links.py, site_dev.py check, developer_ledger.py check, check_retired_renderers.py) | cnahead; scripts/validate_all.sh; scripts/site_dev.py; scripts/generate_dev_reference.py; scripts/build_site_indexes.py | corrected: procedure rewritten against the real repository |
| `maintenance/index.html` | The checker rejects mismatched revision markers | Mismatch detection is split: check_source_links.py rejects Development-page links pinned to another commit or a moving ref, and validate_presentation.py requires cnahead to equal its embedded constant; nothing compares the footer text or prose | scripts/check_source_links.py; scripts/validate_presentation.py | corrected: the atomicity callout names the real checkers and lists the other places the hash is embedded |
| `modules/core.html` | On Emscripten, core carries an interface -lembind requirement because its browser preference reader uses emscripten::val. | modules/core/CMakeLists.txt still adds target_link_options(cna_core INTERFACE "-lembind") with a comment naming emscripten::val, but the reader (GraphicsRendererSelectionEmscripten.cpp) is written with EM_JS and its own comment records that embind was dropped deliberately; the link option is a leftover whose stated reason no longer holds. | modules/core/CMakeLists.txt (EMSCRIPTEN block); modules/core/src/GraphicsRendererSelectionEmscripten.cpp (CnaReadModulePreferredRenderer); GraphicsRendererSelection.cpp comment that names emscripten/val.h | corrected on the page (#boundary warning callout); removal is flagged as needing a real Emscripten bundle link, not executed |
| `modules/design.html` | The descriptors ... raise OnValueChanged after a setter. | CNA's descriptors do call OnValueChanged after a write, but they are final and do not override it; in the sibling Sharp Runtime checkout (next at 41b918c9, not pinned by TARGET) the base hook is a no-op and no handler list exists, so no change notification is delivered. | modules/design/include/CNA/Internal/Design/PropertyDescriptors.hpp (SetValue); Sharp Runtime modules/component-model PropertyDescriptor.hpp (OnValueChanged) read at 41b918c9 | refined on the page (#conversion): the call is described as a no-op at that Sharp Runtime revision |
| `modules/devices-ext.html` | FileDialog 'checks target policy (Web/iOS refused) and nativeFileDialog' before showing. | Target policy is applied only in FileDialog::getIsSupportedProperty. The three Show* functions gate on the nativeFileDialog capability alone (via a local Dialogs() helper), so policy affects the query, not whether a call reaches the service. Every call also passes a null parent window. | modules/devices-ext/src/FileDialog.cpp (getIsSupportedProperty, Dialogs, ShowOpenFile) | corrected on the page (#dialogs) |
| `modules/devices-ext.html` | 'The caller needs an installed current platform; these wrappers do not construct one themselves.' | GetCurrentPlatform() never returns null: on first use with nothing installed it creates and installs the build-time default platform (PlatformException only if that fails). The wrappers install none, but none must be installed first; the stated rule is that the platform must outlive every use of the returned reference. | modules/platform/include/CNA/Platform/CurrentPlatform.hpp; modules/platform/src/CurrentPlatform.cpp; modules/devices-ext/tests/CNA/Devices/FileDialogTests.cpp (closing comment) | corrected on the page (#boundary) |
| `modules/devices-ext.html` | Tests 'use injected fake services' for the dialogs. | The tests install CannedDialogPlatform / CannedCameraPlatform (PlatformTestDecorator subclasses) with ScopedCurrentPlatform; the per-class SetBackendForTesting hooks were removed (PLAT-90). The canned dialog service fires the file-dialog callback synchronously before returning, and no devices-ext test makes a service throw. | modules/platform/tests/CNA/Platform/CannedDialogs.hpp; modules/platform/tests/CNA/Platform/CannedCamera.hpp; modules/devices-ext/tests/CNA/Devices/*.cpp | refined on the page (#test-seams, #dialogs) |
| `modules/devices.html` | 'SDL3/other selected platforms implement those services' (IPlatformSensors, IPlatformHaptics). | Only SDL3 returns a sensor service; X11, Wayland, Win32, SDL2, Headless and Terminal return nullptr from GetSensors(). Haptics: SDL3 service; X11 and Wayland an evdev-backed service when built with evdev and controllers exist; Win32, SDL2, Headless and Terminal nullptr. The platform conformance suite requires each service to be null exactly when its capability is false. | modules/platform/src/*/…Platform.{hpp,cpp} (GetSensors, GetHaptics); modules/platform/tests/CNA/Platform/PlatformConformanceTests.cpp (EveryServiceIsNullExactlyWhenItsCapabilityIsFalse) | corrected on the page (#graph service table) |
| `modules/devices.html` | 'GetIsSupported' probes the actual selected platform (identifier as written). | The member is Accelerometer::getIsSupportedProperty (and Gyroscope's), delegating to PlatformSensorSubsystem::ProbeIsSupported; dev_pack flagged 'GetIsSupported' as absent from TARGET. | modules/devices/src/Sensors/Accelerometer.cpp; Gyroscope.cpp | corrected on the page (real identifiers used) |
| `modules/diagnostics.html` | At runtime ... toggling from Off invalidates open zone generations. | Every accepted SetRuntimeMode call that changes the mode increments modeGeneration (Full->Stats->Full included); an EndZone with a token from an older generation returns true and discards the zone silently, without a malformed count. | modules/diagnostics/src/Diagnostics.cpp (SetRuntimeMode, BeginZone, EndZone); DiagnosticsTests RuntimeModeTransitionInvalidatesOpenZones | corrected on the page (#build, #trace) |
| `modules/diagnostics.html` | EndZone requires the matching top zone on the same thread. | Only wrong-thread, empty-stack and unknown-correlation ends are refused outright. A token found deeper in the stack (or at the wrong depth) is accepted as malformed: the stack is unwound, inner zones are emitted as Zone events ending now, the token's own zone as EventKind::Malformed, malformedZones is incremented and false is returned. | modules/diagnostics/src/Diagnostics.cpp (EndZone) | corrected on the page (EndZone case table in #trace) |
| `modules/diagnostics.html` | The source contract is deliberately non-blocking: a renderer can publish already-available GPU statistics ... CPU frame completion may sample asynchronous GPU metrics from an earlier frame. | No production code registers an IDiagnosticsSource at TARGET (only tests do); the non-blocking rule is a contract for future sources, and no GPU metric exists. | grep of modules/ and tools/ for RegisterSource/IDiagnosticsSource at 009d40f5; modules/diagnostics/include/CNA/Diagnostics/Diagnostics.hpp (IDiagnosticsSource) | corrected on the page: contract kept, stated as having no production implementer |
| `modules/gamer-services.html` | Do not assume the phone module's unique_ptr fake-async convention applies here. | The phone module contains no IAsyncResult at all; the unique_ptr<IAsyncResult> Begin* convention belongs to StorageDevice (and the C API adapters). GamerServices Begin* returns raw, caller-owned IAsyncResult*. | grep of modules/phone for IAsyncResult (no hits); modules/storage/src/StorageDevice.cpp (BeginShowSelector returns std::unique_ptr<System::IAsyncResult>) | corrected on the page (#gamer-async): the comparison now names the storage module and links its fake-async section |
| `modules/gamer-services.html` | For a dispatcher lifetime fix, test two Initialize calls and borrowed-pointer invalidation (implying an in-process gamer-services test). | No test in modules/gamer-services calls GamerServicesDispatcher::Initialize (the flag cannot be reset); it is exercised only by four out-of-process cases in modules/net/tests (GamerServicesDispatcherHangRegressionTest) running tools/net/gamerservices_dispatcher_harness.cpp, including SecondInitializeDoesNotLeakThePreviousFourGamers and InitializePopulatesFourStubGamersCorrectly. Those cases belong to CnaNetTests, not CnaGamerServicesTests. | modules/gamer-services/tests/.../GamerServicesServiceTests.cpp (header comment); modules/net/tests/CNA/Internal/Net/GamerServicesDispatcherHangRegressionTest.cpp; cmake/Harnesses.cmake | corrected on the page (#tests, #build) |
| `modules/gamer-services.html` | Begin methods complete synchronously and invoke callbacks inline, despite their async-shaped names. | True for Gamer, SignedInGamer and LeaderboardReader actions. Guide::BeginShowKeyboardInput and BeginShowMessageBox do not: they complete when the user answers or a test simulates it (test BeginShowKeyboardInputDoesNotCompleteSynchronously). | modules/gamer-services/src/Xna/Guide.cpp (CompletePendingKeyboardInput, CompletePendingMessageBox) | scoped on the page: the ownership table in #gamer-async separates the two behaviours |
| `modules/graphics-ext.html` | begin ... chooses an HDR-capable format by querying the neutral device, falling back to Color if needed. | The scene format is Color unless HDR is enabled; only with setHDREnabled(true) does chooseSceneFormat try HdrBlendable, then Vector4, then Color. | modules/graphics-ext/src/RenderPipeline.cpp (chooseSceneFormat, wantsSceneTarget) | corrected on the page (#frame-begin) |
| `modules/graphics-ext.html` | many extension declarations and implementations are guarded by that macro. | Every production header and source of the module is required to carry the CNA_CNAEXT guard as its outermost construct; CNAEXT_GuardDiscipline (scripts/check_cnaext_guards.sh) enforces it with a text check that runs whenever tests are on. | scripts/check_cnaext_guards.sh; modules/graphics-ext/examples/CMakeLists.txt (add_test CNAEXT_GuardDiscipline) | tightened on the page (#build) |
| `modules/graphics-ext.html` | For shader changes, inspect the checked-in generated package headers and source GLSL variants and determine the regeneration step before editing generated bytes manually; the broader shader package/compute family remains to be documented separately. | The regeneration step is tools/shader_package/generate_shader_package.py <package.json> --output <header> (shaderc and naga-cli required, --check is write-free and exits 77 without the toolchain); 10 of the 11 src/shaders packages have a <Name>ShaderPackageReproducibility ctest in cmake/Tests/ModuleProbes.cmake, shadow_caster has none, and no workflow runs the check. | tools/shader_package/README.md; generate_shader_package.py; cmake/Tests/ModuleProbes.cmake; grep of .github/workflows | answered on the page (#shaders-chain, #change) |
| `modules/graphics-ext.html` | DeviceLifecycleTests are among the starting oracles for reset behaviour. | DeviceLossTest.EverySubsystemSurvivesAResetAndRendersAgain checks the pipeline's reallocation through getGpuMemoryEstimateBytes (chain pool plus scene target only) and only that standalone subsystems do not throw on isSupported-style calls; pass-owned pools in BloomPass, SsaoPass and VolumetricFogPass are neither reset by releaseDeviceResourcesEXT nor counted. | modules/graphics-ext/tests/CNA/Graphics/DeviceLifecycleTests.cpp; RenderPipeline.cpp (releaseDeviceResourcesEXT, getGpuMemoryEstimateBytes); BloomPass.hpp/SsaoPass.hpp/VolumetricFogPass.hpp (RenderTargetPool pool_) | scoped on the page (#targets): what the test proves and what it cannot observe |
| `modules/inspector.html` | Session metadata includes CNA version, target/platform/backend, current renderer and build configuration. | graphicsRenderer is CNA::getCurrentGraphicsRendererName(), a compile-time constant naming the default identity; in a multi-renderer build only the default identity's macros are defined project-wide, so the session does not report a runtime-selected or fallback renderer (GraphicsRendererSelection::GetActive). targetPlatform is the OS name from getCurrentPlatformName(), not the platform backend. | modules/inspector/src/Agent.cpp (HandleClient); modules/core/include/CNA/GraphicsRendererType.hpp (getCurrentGraphicsRendererName); cmake/RendererSelection.cmake (default identity defines); modules/core/include/CNA/TargetPlatform.hpp | corrected on the page (callout in #failure); recorded as a gap |
| `modules/inspector.html` | The bridge ... serves each on a detached thread and waits for all active connections in its destructor. | WebBridge::Impl::~Impl does wait on connectionIdle_, but WebBridge::Run is an endless accept loop and InspectorMain installs no signal handler; the shipped cna-inspector ends by process termination, so that destructor path is never reached there. | modules/inspector/src/WebBridge.cpp (Impl::~Impl, Impl::Run); modules/inspector/src/InspectorMain.cpp (comment 'Run() never returns') | corrected on the page (#bridge) |
| `modules/inspector.html` | For an agent change, run protocol and agent fixtures, including ... rate bounds and Stop/restart. | No test calls Agent::Stop directly, stops an agent with a client attached, or restarts an agent; Stop runs only through each test's agent destructor. | modules/inspector/tests/CNA/Inspector/InspectorTests.cpp (all 27 TESTs) | corrected on the page (#tests gap list and change table); recorded as a gap |
| `modules/inspector.html` | For HTTP changes, run frontend asset/security tests. | The only frontend test, InspectorFrontendTests.AssetsAreOfflineBoundedAndDemandDriven, performs string checks on the embedded assets; nothing exercises WebBridge (Host check, UI-token enforcement, connection cap, parsing, response headers). | modules/inspector/tests/CNA/Inspector/InspectorTests.cpp; modules/inspector/src/WebBridge.cpp | corrected on the page (#tests); recorded as a gap |
| `modules/inspector.html` | The agent first checks that the requested diagnostic resource ID exists and is a previewable texture/target, bounds pending previews and cooldown, validates returned size/bytes ... | Order is: capability/provider, request decode and bounds, pending-count bound, cooldown, then existence and kind via a fresh CaptureSnapshot, then the provider call and response validation. | modules/inspector/src/Agent.cpp (HandlePreviewRequest) | corrected on the page (#previews) |
| `modules/math.html` | Matrix::CreateLookAt constructs camera axes from camera-to-target and up vectors, places them in M11..M33 and negative dot products in M41..M43. | The backward axis is normalize(cameraPosition - cameraTarget), i.e. target-to-camera, not camera-to-target; right = normalize(cross(up, backward)), up = cross(backward, right). The axes fill the first three columns (M11,M21,M31 right; M12,M22,M32 up; M13,M23,M33 backward) and M41..M43 hold the negated dot products with the position. | modules/math/src/Matrix.cpp (CreateLookAt) | corrected on the page (#matrix-convention table) |
| `modules/media.html` | Play(Song*) clears its MediaQueue, which owns Song copies, so 'subsequent queue duration/play-count updates do not mutate a MediaLibrary's original Song'. | Play(Song*) hands the caller's own Song (not the queued copy) to PlaySong, so in a SOUND_ENABLED build the mixer-reported duration and the incremented play count are written to the caller's/library's Song. Only Play(SongCollection), MoveNext/MovePrevious and automatic advance call PlaySong on queue copies. Stop resets play counts on the copies only. The queue copy itself is still a distinct instance (test PlayEnqueuesADuplicateNotTheOriginalInstance). | modules/media/src/Xna/MediaPlayer.cpp (Play(Song*), Play(const SongCollection&, index), NextSong, PlaySong, Stop) | corrected on the page (music section, #queue) |
| `modules/net.html` | Ownership diagram: 'session owns Local/Remote NetworkGamer objects [unique_ptr registry]'. | NetworkSession::ownedGamers_ owns only the LocalNetworkGamer objects it creates; NetworkSession::AddRemoteGamer deliberately does not take ownership. Remote NetworkGamers are created by ENetBackend and owned by ENetBackend::SessionState::OwnedRemoteGamers, freed when TeardownSession erases the state. | modules/net/src/Xna/NetworkSession.cpp (constructor, AddRemoteGamer comment); modules/net/src/Internal/ENetBackend.cpp (SessionState::OwnedRemoteGamers, HandleClientHello/HandleServerWelcome/HandleGamerJoinBroadcast, TeardownSession); GamerCollection.hpp ownership contract | corrected on the page (#composition diagram, #shutdown) |
| `modules/net.html` | Before a client handshake has assigned wire IDs, a public LocalNetworkGamer::SendData can reach ENetBackend::SendAppData. | LocalNetworkGamer::SendData never calls the backend: every overload only appends a PacketSend NetworkEvent to the session queue, and NetworkSession::Update drains it and calls SendAppData (SystemLink only). The pre-handshake queue is therefore reached when a queued send is drained before its ends are wired (ConnectToHost without waiting, Emscripten where EndJoin does not wait, or a gamer introduced later), not by a send after a native Join that already waited for the welcome. | modules/net/src/Xna/LocalNetworkGamer.cpp (SendData overloads); NetworkSession.cpp (Update PacketSend branch, EndJoin wait loop); ENetBackend.cpp (SendAppData) | corrected on the page (#pump) |
| `modules/net.html` | TwoProcessLoopbackTest is excluded on unsuitable hosts and must be reported as skipped/unavailable, not counted as passing. | The test has no run-time skip. cmake/UnitTests.cmake filters its source (and GamerServicesDispatcherHangRegressionTest.cpp) out of CnaTests at configure time on WIN32, EMSCRIPTEN, ANDROID and CNA_APPLE_IOS, and cmake/Harnesses.cmake does not build cna_net_two_process_harness on WIN32/EMSCRIPTEN/ANDROID; on those hosts the cases do not exist. | cmake/UnitTests.cmake (FILTER EXCLUDE REGEX ... TwoProcessLoopbackTest); cmake/Harnesses.cmake; TwoProcessLoopbackTest.cpp (no GTEST_SKIP) | corrected on the page (#tests) |
| `platforms/headless.html` | InjectEvent lets a runtime test drive quit, resize, focus or keyboard event order without depending on desktop timing. | InjectEvent exists but is declared only on the concrete class in the private header src/Headless/HeadlessPlatform.hpp; the factory returns IPlatform and no test at TARGET calls InjectEvent. Tests script events by wrapping a real platform in PlatformTestDecorator (e.g. TranscriptPlatform::Queue in GameEventSemanticsGoldenTests). | modules/platform/src/Headless/HeadlessPlatform.hpp (InjectEvent); grep of modules/ and tools/ for InjectEvent; modules/platform/tests/CNA/Platform/PlatformTestDecorator.hpp; modules/runtime/tests/Microsoft/Xna/Framework/GameEventSemanticsGoldenTests.cpp | corrected on the page: capability kept, actual test technique documented |
| `platforms/headless.html` | Borderless fullscreen is explicitly refused. | Only HeadlessWindow::SetFullscreenMode refuses BorderlessFullscreen; the HeadlessWindow constructor copies WindowDescription::fullscreenMode unchecked, so creation with BorderlessFullscreen is accepted and reported back. | modules/platform/src/Headless/HeadlessPlatform.cpp (HeadlessWindow ctor, SetFullscreenMode) | corrected on the page as a scoped maintainer note; recorded as a gap |
| `platforms/headless.html` | Do not confuse it with ... SDL_gpu's HeadlessEXT mode. | HeadlessEXT is PresentationParameters::HeadlessEXT, a GraphicsDevice-level off-screen opt-in; its documentation names the D3D12 and SDL GPU renderers as supporting it and says D3D11 and EasyGL throw. | modules/graphics/include/Microsoft/Xna/Framework/Graphics/PresentationParameters.hpp (getHeadlessEXTProperty); modules/graphics/src/Xna/GraphicsDevice.cpp | corrected on the page (three-way comparison table) |
| `platforms/index.html` | For an input bug, prove each link: native message -> PlatformEvent -> input bridge -> frame snapshot -> public getter. | The bridge does not feed the snapshot. Game::PollEvents passes each event to PlatformInputBridge::ProcessEvent (InputManager accumulators, text input, touch, devices), applies Game handling, and only after the batch calls GetKeyboard()->Update()/GetMouse()->Update(); Keyboard::GetState/Mouse::GetState read those platform snapshots and return empty states when the service is null. There is no Game::UpdateInput. | modules/runtime/src/Game.cpp (Game::PollEvents); modules/input/src/Xna/Keyboard.cpp (Keyboard::GetState); modules/input/src/Xna/Mouse.cpp (Mouse::GetState); modules/input/src/Internal/SdlInputBridge.cpp (PlatformInputBridge::ProcessEvent) | corrected on the page: two-path diagram |
| `platforms/index.html` | SDL helper code is shared only by SDL-backed implementations. | No source is shared between src/Sdl3 and src/Sdl2; SDL2 includes only src/Common helpers. Sharing is organised as src/Common (any backend), src/Xkb, src/Freedesktop, src/Posix and src/Linux (X11 and Wayland only). | modules/platform/src/Sdl2/Sdl2Platform.hpp includes; modules/platform/CMakeLists.txt (Unix-desktop globs) | corrected on the page (shared-directory table) |
| `platforms/index.html` | WIN32 primary concerns include D3D/GDI integration. | The Win32 platform's graphics services are WGL (Win32GlContext), Vulkan (Win32VulkanSurface) and a GDI presenter (Win32SurfacePresenter); Direct3D renderers consume the HWND through the native handle snapshot, not a platform D3D service. | modules/platform/src/Win32/*.hpp (class list); modules/platform/include/CNA/Platform/NativeWindowHandle.hpp | reworded on the page |
| `platforms/sdl2.html` | Keyboard::GetState → Sdl2Platform::Keyboard::Update → SDL_GetKeyboardState + SDL_GetModState → KeyboardSnapshot | Keyboard::GetState only reads the snapshot; Sdl2Platform::Keyboard::Update is called by Game::PollEvents after the batch loop. | modules/input/src/Xna/Keyboard.cpp (Keyboard::GetState); modules/runtime/src/Game.cpp (Game::PollEvents) | corrected in the diagram and prose |
| `platforms/sdl2.html` | renderer selection must reject combinations requiring those capabilities rather than failing after construction | Nothing at configure or selection time rejects VULKAN on SDL2; GraphicsDevice::createRenderer passes the null Vulkan service and the Vulkan renderer's RequirePlatformVulkanSurface throws PlatformNotSupportedException(VulkanSurface) during construction, after the window exists. The SDL2-only guard message even lists VULKAN as a valid choice. | modules/graphics/src/Xna/GraphicsDevice.cpp (createRenderer); modules/graphics/include/CNA/Internal/Renderers/Common/PlatformVulkanRendererState.hpp; cmake/Sdl2OnlyConfiguration.cmake | the prescriptive sentence replaced by the actual refusal point on the page |
| `platforms/sdl3.html` | Game::UpdateInput → Sdl3Keyboard::Update / Sdl3Mouse::Update (event diagram) | There is no Game::UpdateInput. Keyboard and mouse snapshot Update() runs at the end of Game::PollEvents, after the batch loop; gamepad/joystick Update() runs there only once IsSubsystemInitialized(Gamepad). Two stale source comments still name Game::UpdateInput(). | modules/runtime/src/Game.cpp (Game::PollEvents); modules/platform/src/Sdl3/Sdl3Platform.cpp (comment above EnsureControllerSubsystem) | corrected on the page (diagram and prose) |
| `platforms/sdl3.html` | Game asks the selected platform to acquire video and create a WindowDescription | GraphicsDevice acquires PlatformSubsystem::Video when the renderer descriptor sets needsVideoSubsystem and then calls AdoptWindowHandle (non-zero device window handle) or CreateWindow with a WindowDescription built from the descriptor (resizable=false, wantsHighDpi, renderIntentFor(windowKind), GL framebuffer bits). | modules/graphics/src/Xna/GraphicsDevice.cpp (window creation path, renderIntentFor) | corrected on the page |
| `platforms/sdl3.html` | Unknown/web/headless systems return no native pointer | DetectSystem maps the emscripten driver to Web, and dummy/offscreen/unrecognised drivers to Headless (so GPU renderers refuse); Unknown only when SDL has no current video driver. None of these carries a pointer. | modules/platform/src/Sdl3/Sdl3Window.cpp (DetectSystem, GetNativeHandle) | corrected and tabulated on the page |
| `platforms/sdl3.html` | Sdl3Window implements title, resizing limits, focus, visibility, minimize/maximize, cursor-related window methods and fullscreen policy | Minimum/maximum size are applied only in Sdl3Platform::CreateWindow; cursor shape and visibility are Sdl3Mouse methods, not window methods. | modules/platform/src/Sdl3/Sdl3Platform.cpp (CreateWindow); modules/platform/src/Sdl3/Sdl3InputServices.cpp (Sdl3Mouse::SetCursor, SetCursorVisible) | corrected on the page |
| `platforms/sdl3.html` | Distinguishes logical WINDOW_RESIZED from physical WINDOW_PIXEL_SIZE_CHANGED; SDL's ordinary SetWindowSize is ineffective while fullscreen | The SDL3 spellings are SDL_EVENT_WINDOW_RESIZED, SDL_EVENT_WINDOW_PIXEL_SIZE_CHANGED (plus SDL_EVENT_WINDOW_DISPLAY_SCALE_CHANGED) and SDL_SetWindowSize. | modules/platform/src/Sdl3/Sdl3EventMapper.cpp; modules/platform/src/Sdl3/Sdl3Window.cpp (SetSize) | corrected on the page (tokens flagged by the verification pack) |
| `platforms/sdl3.html` | SDL3 is ... the reference against which the platform conformance tests compare native implementations | PlatformConformanceTests is parameterised over PlatformFactory::GetAvailable() and asserts the contract per implementation; it does not compare one implementation against another. Sdl3Platform.hpp calls SDL3 the reference implementation. | modules/platform/tests/CNA/Platform/PlatformConformanceTests.cpp (INSTANTIATE_TEST_SUITE_P EveryImplementation) | reworded on the page |
| `platforms/sdl3.html` | EasyGL needs SDL_WINDOW_OPENGL and later asks Sdl3GlContext to create a context (implying every OpenGL-intent renderer uses the platform GL service) | EasyGL identities and OPENGL4 set needsGlContext; FNA3D declares an OpenGL window but does not request the platform GL service. | modules/renderers/easygl/src/EasyGLRendererDescriptor.cpp; modules/renderers/opengl4/src/OpenGL4RendererDescriptor.cpp; modules/renderers/fna3d/src/Fna3dRendererDescriptor.cpp | made precise on the page |
| `platforms/terminal.html` | A window on an attached terminal installs TerminalResizeWatcher (implying window-scoped lifetime). | CreateWindow creates the watcher only when attached and no watcher is active (TerminalResizeWatcher::IsWatching); the platform owns it (resizeWatcher_), so it stays installed until the platform is destroyed, not the window. | modules/platform/src/Terminal/TerminalPlatform.cpp (CreateWindow); TerminalPlatform.hpp (resizeWatcher_) | corrected on the page |
| `platforms/terminal.html` | RendererSelection.cmake rejects a TERMINAL pairing ... before GPU dependency probing (single check). | The gate is at the top of macro cna_configure_renderer_identity, run once per identity, so every entry of CNA_GRAPHICS_RENDERERS is checked before that identity's own probes. The file's only change after the Developer pin is the OPENGL4 compiled-effects option; the TERMINAL gate itself is unchanged. | cmake/RendererSelection.cmake (cna_configure_renderer_identity, foreach over _cna_renderer_identities); git diff d6e9ff05..009d40f5 | corrected and made precise on the page |
| `platforms/terminal.html` | cmake/UnitTests.cmake includes these suites in the platform CTest selection. | All Terminal suites are compiled into CnaTests on non-Windows builds and discovered per case; the CnaPlatformTests aggregate filter matches all of them except TerminalPresenterThroughPlatformTest (filter token is TerminalPresenter.*). | cmake/UnitTests.cmake (CnaPlatformTests gtest_filter; Terminal exclusion only if WIN32); modules/platform/tests/CNA/Platform/TerminalPresenterTests.cpp | corrected on the page (one-suite exception stated) |
| `platforms/terminal.html` | CreateSurfacePresenter rejects a foreign window or non-TTY output (single refusal type implied by PlatformNotSupportedException). | A foreign window throws PlatformException; non-TTY output throws PlatformNotSupportedException(SurfacePresentation). | modules/platform/src/Terminal/TerminalPlatform.cpp (CreateSurfacePresenter) | made precise on the page |
| `platforms/wayland.html` | At startup the platform attaches EGL/Vulkan/presenter/dialog services where available. | EGL, Vulkan, dialogs and the idle inhibitor are created at startup; a wl_shm presenter is created per window on demand by CreateSurfacePresenter. Only the surfacePresentation flag is decided at startup, from wl_shm plus XRGB8888 seen during the second startup roundtrip. | modules/platform/src/Wayland/WaylandPlatform.cpp (constructor, ComputeCapabilities, CreateSurfacePresenter); WaylandConnection.cpp (OnShmFormat) | corrected on the page |
| `platforms/wayland.html` | RecordSerial stores recent input serials; activation, cursor shape, drag/selection and window menu routes consult them. | RecordSerial keeps only the latest serial and its seat. It is consumed by xdg-activation, clipboard/primary-selection ownership, the window menu and the built-in frame's interactive move/resize. Cursor changes (wl_pointer.set_cursor, cursor-shape-v1) use each pointer's own enter serial. | WaylandPlatform.cpp (RecordSerial, GetLatestInputSerial, RequestActivation); WaylandWindow.cpp (ShowWindowMenu, BeginMove, BeginResize); WaylandDataDevice.cpp (set_selection); WaylandMouse.cpp (enterSerial) | corrected on the page |
| `platforms/wayland.html` | If input vanishes after hide/show or a surface recreation, inspect surface-to-window mapping and seat attachment. | The wl_surface and its surface-to-window mapping live for the whole WaylandWindow; Hide/Show only destroys and recreates the xdg_surface/xdg_toplevel role. There is no surface recreation. | WaylandWindow.hpp (class comment 'Lifetime of the objects'); WaylandWindow.cpp (constructor MapSurface, DestroyRole, ReleaseNativeObjects) | corrected on the page (diagnosis now points at focus, seat attachment and configure path) |
| `platforms/wayland.html` | EGL, Vulkan and wl_shm are three independent integration seams serving different renderer families. | EGL serves the EasyGL identities and OPENGL4 (needsGlContext) and Vulkan serves VULKAN (needsVulkanSurface), but no stock renderer reaches the wl_shm presenter inside a Game on Wayland: GraphicsDevice gives a CPU family a window and presenter only when surfacePresentation is true and nativeWindowHandle is false (the terminal). The presenter is exercised by tests, the validation harness presenter scenario and direct IPlatform::CreateSurfacePresenter callers. | modules/graphics/src/Xna/GraphicsDevice.cpp (createOrAttachWindow presenterIsTheOnlyDisplay; needsGlContext/needsVulkanSurface wiring); modules/renderers/*/src/*Descriptor.cpp | corrected on the page |
| `platforms/wayland.html` | No signal handler, locale mutation, environment variable or alternate display server is installed by this backend. | No signal handler, no setlocale, no environment variable set, no compositor started -- but RequestActivation removes XDG_ACTIVATION_TOKEN from the environment after spending it on the first shown window (xdg-activation specification). Compose tables are looked up by LC_ALL/LC_CTYPE/LANG without setlocale. | WaylandPlatform.cpp (RequestActivation: unsetenv); WaylandPlatform.hpp (class comment); WaylandKeyboard.cpp (ComposeLocale) | corrected on the page |
| `platforms/win32.html` | WM_CLOSE becomes a CloseRequested event and suppresses default destruction; the application decides whether to exit. | True at the IPlatform level, but Game::PollEvents calls Exit() on WindowEventKind::CloseRequested (not filtered by window id), so in a Game the close button always exits. | modules/runtime/src/Game.cpp (Game::PollEvents, CloseRequested case) | corrected on the page: two layers (platform vs Game) stated separately |
| `platforms/win32.html` | CPU-style renderers can request a Win32SurfacePresenter; it maps SurfaceFrame to a GDI path. | The presenter exists and is tested, but GraphicsDevice creates a presenter only when the platform reports surfacePresentation && !nativeWindowHandle (TERMINAL on a TTY). Win32 reports a native handle, so SOFTWARE (the only needsSurfacePresenter family) stays off-screen on Win32. | modules/graphics/src/Xna/GraphicsDevice.cpp (presenterIsTheOnlyDisplay, createRenderer); modules/renderers/software/src/SoftwareRendererDescriptor.cpp | corrected on the page |
| `platforms/win32.html` | GraphicsDevice may adopt window by ID or native HWND. | GraphicsDevice adopts only through AdoptWindowHandle when PresentationParameters' device-window handle is non-zero; AdoptWindow(WindowId) is used by tests. Mouse::setWindowHandleProperty also adopts a temporary wrapper to resolve an id. | modules/graphics/src/Xna/GraphicsDevice.cpp (AdoptWindowHandle call); modules/input/src/Xna/Mouse.cpp | corrected on the page |
| `platforms/win32.html` | The native MSVC/Windows job tests real per-monitor DPI, shell/clipboard and D3D routes. | win32-native runs the standalone cna_platform_tests and cna_win32_directx_probe (exit 2 tolerated) on windows-latest; the DPI/dialog/clipboard additions are the workflow comment's description of what a real desktop adds, not separate steps. | .github/workflows/platform-ci.yml (win32-native) | restated precisely, attributed to the workflow comment |
| `platforms/win32.html` | Diagram: Game::PollEvents → PlatformInputBridge::ProcessEvent → input state queried through CNA Keyboard APIs (implying event-fed state). | Public Keyboard::GetState reads IPlatformKeyboard::GetSnapshot(); on Win32 the snapshot is refreshed by Win32Keyboard::Update from GetKeyboardState, called at the end of Game::PollEvents (there is no Game::UpdateInput). | modules/runtime/src/Game.cpp (Game::PollEvents); modules/platform/src/Win32/Win32InputServices.cpp (Win32Keyboard::Update); modules/input/src/Xna/Keyboard.cpp | diagram extended with the snapshot path |
| `platforms/x11.html` | Xlib calls are serialized by its connection mutex. | No mutex guards the X connection or any X11 service at TARGET; the only mutex in src/X11 guards X11ErrorPolicy's owned-display list. The X11Platform.hpp class comment claiming 'one per-instance mutex' is not implemented; correctness relies on single-threaded, once-per-frame use. | modules/platform/src/X11/*.cpp (grep: std::mutex only in X11Error.cpp); X11Platform.hpp class comment | corrected on the page; recorded as a gap |
| `platforms/x11.html` | X11SurfacePresenter is the CPU-frame route used by renderers that deliver pixels rather than GL/Vulkan commands. | GraphicsDevice creates a window and presenter for a needsSurfacePresenter renderer (SOFTWARE) only when the platform reports surfacePresentation without nativeWindowHandle (TERMINAL on a TTY); on X11 SOFTWARE stays off-screen, so no renderer reaches X11SurfacePresenter through GraphicsDevice. | modules/graphics/src/Xna/GraphicsDevice.cpp (presenterIsTheOnlyDisplay, createRenderer); modules/renderers/software/src/SoftwareRendererDescriptor.cpp | corrected on the page; presenter described as exercised by platform tests and direct IPlatform users |
| `platforms/x11.html` | This backend must not replace global locale ... permanently. | Opening the input method calls setlocale(LC_CTYPE, "") when LC_CTYPE is still "C"/"POSIX" and does not revert it; other categories are untouched. | modules/platform/src/X11/X11TextInput.cpp (PrepareLocaleForInputMethod) | corrected on the page with the exact scope |
| `platforms/x11.html` | Keyboard/mouse snapshots are read after the Game update path (implied Game::UpdateInput stage). | There is no Game::UpdateInput; keyboard/mouse Update() and gated gamepad/joystick Update() run at the end of Game::PollEvents. Game::PollEvents also calls Exit() on CloseRequested itself. | modules/runtime/src/Game.cpp (Game::PollEvents) | corrected on the page; stale comment in X11Platform.cpp noted |
| `platforms/x11.html` | X11Keyboard ... tracks held keys; public keyboard polling reads its snapshot. | X11Keyboard::Update builds the snapshot from XQueryKeymap and XkbGetState (the server's state), not from accumulated events; tracked held state only flags repeats. | modules/platform/src/X11/X11Keyboard.cpp (Update, TrackKeyState) | made precise on the page |
| `reference/index.html` | scripts/generate_reference.py reads cnahead, verifies the object in ../cna; make generate / make check | The generator is scripts/generate_dev_reference.py; it reads cnahead from the site repository, lists and reads the pinned commit with git ls-tree and git show in CNA_REPO (default ../cna), and writes five pages (selection axes was added); the follow-up is bash scripts/validate_all.sh, not make check | scripts/generate_dev_reference.py; scripts/validate_all.sh | corrected on the page |
| `reference/index.html` | Four technical indexes (modules, CMake options, test targets, public headers) | Five: the selection axes index derives the renderer identity/family, platform and audio tables from the CMake registries and refuses to write on disagreement with the canonical counts | scripts/generate_dev_reference.py (gen_axes) | extended: fifth index described |
| `repository/index.html` | Top-level include/: 'Compatibility/public include surface outside the physical module tree' | No top-level include/ (or src/) exists at TARGET or at the Developer pin, and a root src/ or include/ directory fails the configure ('legacy global tree reappeared'); docs/physical-modules.md agrees that the former global include/ root does not exist | modules/CMakeLists.txt (legacy trees FATAL_ERROR); Git tree of 009d40f5 | row dropped; replaced by an explicit warning callout |
| `repository/index.html` | Assets: build-time import/process/write (content-pipeline) is distinct from runtime load/cache (content) | The canonical pipeline API (CNA/Content/Pipeline), the XNB and CNB writers and the pipeline engine live in modules/content; modules/content-pipeline holds only the build-time-only components (FreeType route, FFmpeg importers, FBX/.x readers, effect-compiler service, XNA facade) and is linked by cna_content_compiler | modules/content/include/CNA/Content/Pipeline; modules/content-pipeline/CMakeLists.txt; cmake/ToolContentPipeline.cmake | corrected in #families and the module table |
| `repository/index.html` | Orchestration family = runtime, core, design | core is a foundation module (identities, logging, TargetPlatform, CNAEXT marker) and design is opt-in tooling outside the CNA umbrella; only runtime orchestrates | modules/CMakeLists.txt (_cna_runtime_parts); modules/design/CMakeLists.txt | regrouped into Foundations and Optional services and tooling |
| `repository/index.html` | tests/: cross-module, conformance and integration validation | Top-level tests/ is 2,340 files, mostly assets, glTF goldens and recorded XNA reference data, plus 15 module probes, an Apple smoke app and two C++ sources; module and conformance unit tests live in modules/<name>/tests and renderer example programs in each family's examples/ | Git tree of 009d40f5; cmake/UnitTests.cmake (globs) | corrected and a tests-ownership section added |
| `runtime/frame.html` | If less than a full span exists, the fixed-step Tick may draw without an Update. | On the desktop path the wait loops (Delay(1) while accumulator + worst sleep precision < target; then yield until accumulator >= target) return only once a full span exists, so a fixed-step tick performs at least one Update before its draw. The browser body never draws unless it updated. The one case with a draw but no update is TargetElapsedTime > MaxElapsedTime (500 ms), where the clamp leaves less than a span. | modules/runtime/src/Game.cpp (Tick: wait loops, clamp to MaxElapsedTime, while accumulated >= target; EmscriptenMainLoopCallback: 'updated &&') | corrected on the page (callout in #clock); the >500 ms case recorded as a gap; not covered by any test |
| `runtime/frame.html` | Focus, expose, minimize, maximize and restore also invalidate the surface 'as appropriate'. | FocusGained, Exposed, Minimized, Maximized, Restored and DisplayChanged call OnSurfaceInvalidated; FocusLost and Moved do not; Resized/PixelSizeChanged/DisplayScaleChanged refresh the window, viewport and invalidate. | modules/runtime/src/Game.cpp (PollEvents WindowEvent switch) | made precise (event table) |
| `runtime/module.html` | Game.hpp's own comment (and the Developer diagram: 'platform_ constructed first, destroyed last') implies platform_ is declared before every other member; the Developer object diagram also lists Components_ after the window. | The four public event handlers (Activated, Deactivated, Disposed, Exiting) and RunApplication are declared before platform_; Components_ is declared before GraphicsDevice_ (directly after eventBatch_), not after Window_. | modules/runtime/include/Microsoft/Xna/Framework/Game.hpp (public members, private section) | corrected on the page (diagram and 'Member order at this snapshot' callout); consequence for shared-owned components is carried to shutdown.html#order |
| `runtime/module.html` | Game.hpp forward-declares the platform contract instead of including it. | Only IPlatform is forward-declared; PlatformCapabilities.hpp (a value type) is included. | Game.hpp includes CNA/Platform/PlatformCapabilities.hpp | made precise on the page |
| `runtime/shutdown.html` | Ownership tree lists Components after Window_ (and before Services_), i.e. after the device in destruction order terms. | Components_ is declared before GraphicsDevice_ (after eventBatch_), so it is destroyed after the device, Content_, Window_ and Services_; shared-owned components therefore die after the device has gone. | modules/runtime/include/Microsoft/Xna/Framework/Game.hpp (private members) | corrected on the page (diagram and callout) and consequences added to #order |
| `takeover/blast-radius.html` | Existing pages for other families remain incomplete; the coverage ledger is honest about this. | The Development area has deep pages for EasyGL, Vulkan, OpenGL4, SDL_gpu, Software, HEADLESS and STUB plus a graphics backends map; the coverage ledger is Developer-site process content. | python3 scripts/dev_manifest.py --list (development/internals/graphics/*) | rewritten: deep pages are named, the remaining families point to the backends map, and a family without a deep page is stated to be a research assignment. |
| `takeover/blast-radius.html` | The existing storage fixture recursively removes a fixed per-user test directory during teardown. | The directory name is fixed (CnaTestsContent002StorageDevice) but its location is derived from XDG_DATA_HOME, LOCALAPPDATA, HOME or the working directory, so the caller controls what remove_all targets. | modules/storage/src/StorageDevice.cpp (EnsureStorageRoot, SetAppNameEXT); modules/storage/tests/Microsoft/Xna/Framework/Storage/StorageDeviceTests.cpp (SetUp, TearDown) | stated precisely with the mitigation (redirect the environment root to a disposable directory) and a link to the storage case study. |
| `takeover/case-study-storage.html` | The commit recorded other possible sites as out of scope; whether they remain open at today's pin requires a fresh source audit. | The recorded sites (Video/SongContentTypeReader ResolveRelativeFilePath, ContentManager manifest fields) use ResolveContainedPathRelativeToFile / ResolveContainedPath at TARGET (commit c04faaedb); StorageContainer joins, judged FNA-faithful and left by the commit, are contained since 1af12dac3. | modules/content/src/Xnb/SongContentTypeReader.cpp, VideoContentTypeReader.cpp, modules/content/src/Xna/ContentManager.cpp, modules/storage/src/StorageContainer.cpp | Fresh audit performed for those files and stated on the page; exhaustive audit of other joins explicitly not claimed. |
| `takeover/case-study-storage.html` | SetUp chooses the real per-user app root; TearDown calls remove_all(root_). | Confirmed, and additionally TearDown's SetAppNameEXT("") re-resolves the default root, creating <data root>/game and re-pointing Sharp Runtime's isolated-storage override there. | modules/storage/tests/Microsoft/Xna/Framework/Storage/StorageDeviceTests.cpp (TearDown); modules/storage/src/StorageDevice.cpp (SetAppNameEXT, EnsureStorageRoot) | Side effect added to the warning; XDG_DATA_HOME sandbox recipe given. |
| `takeover/curriculum.html` | Run a content-focused build with cmake --build --preset unit-content after unit configure; inspect actual CTest names rather than guessing a filter | unit-content builds only CnaContentTests, a focused executable that UnitTests.cmake explicitly does not register with CTest; its names are listed with --gtest_list_tests, not ctest | cmake/UnitTests.cmake (focused executables are developer iteration targets, not additional CTest registrations); CMakePresets.json buildPresets unit-content | corrected on the page |
| `takeover/curriculum.html` | Check GamePlatformOwnershipTests | The file is GamePlatformOwnershipTests.cpp but the GoogleTest suite (and CTest prefix) is GamePlatformOwnershipTest | modules/runtime/tests/Microsoft/Xna/Framework/GamePlatformOwnershipTests.cpp | corrected: file linked, CTest filter uses GamePlatformOwnershipTest\. |
| `takeover/index.html` | All source links in this site target CNA commit d6e9ff050e32bcfe5e7fd640006619a90b92298c on next; compare the target branch with this documentation's pin. | The Development area is pinned to snapshot 009d40f5dd085c4e674d3479675fac84b12b3e0a (branch next). | cnahead; scripts/site_dev.py TARGET; git cat-file -e 009d40f5:modules/runtime | rewritten to the TARGET commit with an example drift check and a link to the maintenance and pin policy. |
| `takeover/index.html` | The coverage ledger deliberately distinguishes verified chapters from incomplete ones; an absent chapter is a research assignment. | The coverage ledger is Developer-site process content; the Development area has no coverage statuses. | phase2-editorial-guide.md section 1 (Developer status labels are not published) | reference dropped; the research-assignment advice kept, worded for the Development area. |
| `takeover/inherited.html` | 23 production module directories | 23 directories under modules/ = 22 declared framework modules (_cna_framework_modules) plus the renderers/ tree; the count is right but was ambiguous | modules/CMakeLists.txt (_cna_framework_modules, _cna_renderer_modules) | corrected on the page with the exact composition |
| `takeover/inherited.html` | Directories not yet covered in depth (design, diagnostics, media, net, phone) ... coverage records where study is missing | Every one of those modules has a planned internals page in the Development area; Developer's coverage ledger is not carried over | scripts/dev_manifest.py (internals/modules/*) | rewritten: small modules are not presumed simple; their pages are source-read only |
| `takeover/inherited.html` | Terminal only supports CPU-frame presenter-compatible identities | Confirmed and made exact: SOFTWARE, PORTABLEGL, HEADLESS, STUB; the check precedes every renderer dependency probe | cmake/RendererSelection.cmake (_cna_terminal_renderers) | corrected to the exact list |
| `takeover/investigation.html` | For documentation pinned to d6e9ff05, use git show d6e9ff05:path | The Development area is pinned to 009d40f5 | cnahead; site_dev.py TARGET | rewritten to 009d40f5 with a link to the maintenance & pin policy |
| `takeover/investigation.html` | the maintained C#, Java and Python checkouts documented here do not establish compatibility | Still true, and all eight public bindings target ABI 0.21.x while TARGET exports 0.29.0 | modules/c-api/include/CNA/C/abi.h (0.29.0); cna-cs@e2239099 eng/cna-native-abi-policy.json (consumerAbi 0.21.0, exact matrix) | extended on the page |
| `takeover/mental-model.html` | CMake's CNA umbrella repeats archives to satisfy these cycles | The CNA umbrella is an INTERFACE composition; the repetition comes from cycles declared on both member targets (graphics-core<->input, graphics-core<->renderer, audio<->media) plus LINK_INTERFACE_MULTIPLICITY 3 on cna_graphics_core and cna_input | modules/CMakeLists.txt (add_library(CNA INTERFACE)); modules/graphics/CMakeLists.txt; modules/input/CMakeLists.txt; modules/audio/CMakeLists.txt | corrected on the page |
| `takeover/mental-model.html` | Interop: External C#/Java/Python layers call in-tree C ABI | Eight public external bindings (C#, Java, TypeScript, Python, Rust, Swift, Go, Ruby) target ABI 0.21.x; TARGET exports 0.29.0 | modules/c-api/include/CNA/C/abi.h; data/current-facts.json bindings_expected_abi | corrected on the page |
| `takeover/ownership.html` | Teardown order: explicit Game Dispose(true) -> manager releases frame lease -> GraphicsDevice Dispose drains registry -> ... | Game::Dispose(true) disposes IDisposable components, Content_ and the registered IGraphicsDeviceService (GraphicsDeviceManager, which never deletes/disposes a Game-owned device); GraphicsDevice_ is disposed only by ~GraphicsDevice during member destruction (or earlier by game code). | modules/runtime/src/Game.cpp (Game::Dispose(bool)); modules/runtime/src/GraphicsDeviceManager.cpp (Dispose(bool)); modules/graphics/src/Xna/GraphicsDevice.cpp (~GraphicsDevice) | corrected on the page; two explicit sequences shown. |
| `takeover/ownership.html` | An Accelerometer/Gyroscope instance owns a reference to the selected platform's Sensor subsystem. | The reference is acquired on the first successful Start() (subsystemHeld_/acquiredPlatform_) and released in Dispose(true), not at construction. | modules/devices/src/Sensors/Accelerometer.cpp (Start, Dispose) | corrected on the page. |
| `takeover/ownership.html` | SDL3's mixer generation ... its fire-and-forget cleanup defers freeing callback state; ALSA closes/joins its worker before freeing mixer state. | The deferred pan-state cleanup is backend-neutral SoundEffect.cpp code (both mixers); CNA's own mixer (ALSA pairing) is immortal, never torn down, always generation 1, and closes the device from an atexit handler; ALSA Stop/Close join the worker before releasing the callback. | modules/audio/src/Xna/SoundEffect.cpp (PendingPanStateCleanup); modules/audio/src/Backend/CnaMixer/MixerEngine.cpp (GetEngine, GetMixerEngineGeneration, CloseDeviceAtExit); modules/audio/src/Platform/Alsa/AlsaAudioDevice.cpp (StopLocked, CloseLocked) | corrected on the page. |
| `takeover/ownership.html` | Explicit DevicesShutdownCoordinator::Shutdown resets that backend while the platform lives. | True, but nothing in Game/runtime calls it; the header requires applications to call it before platform shutdown; an atexit fallback only marks shutdown. | modules/devices/include/Microsoft/Devices/Detail/DevicesShutdownCoordinator.hpp | stated precisely on the page. |
| `takeover/threading.html` | Emscripten has a callback-driven loop variant. | Game::RunLoop on Emscripten calls the frame body directly in a loop on the Run() stack and suspends between frames with an Asyncify requestAnimationFrame wait; no separately registered browser callback re-enters the Wasm instance. The wasm C API build is driven per frame from JavaScript with Asyncify disabled. | modules/runtime/src/Game.cpp (RunLoop, EmscriptenMainLoopCallback, CNA_WaitForAnimationFrame); docs/emscripten-mainloop-game-lifetime.md | corrected on the page (map row and game section). |
| `takeover/threading.html` | Vulkan and SDL_gpu have their own command/submission synchronization. | No CPU-side lock guards VulkanRenderer state (one static atomic for adapter sample counts); its synchronization is GPU-side fence-gated retirement of resource handles. SdlGpuModern.cpp states the SDL_gpu renderer is single-threaded and its registries take no lock; the only mutex there guards the optional shader-cross session count. | modules/renderers/vulkan/src/VulkanRenderer.cpp (ReleaseVulkanResources, sAdapterSampleCountsEXT); modules/renderers/sdl-gpu/src/SdlGpuModern.cpp (RegisterStorageBufferEXT comment SMG-0040); modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp (shaderCrossSessionMutex) | corrected on the page: scoped to GPU-side retirement for Vulkan and the documented single-threaded contract for SDL_gpu. |
| `takeover/threading.html` | OnComponentRemoved nulls entries in the in-flight snapshot so a removed-and-freed component is not called later in the frame (stated without qualification about the calling thread). | The snapshot loops in Update/Draw iterate without the lock, and GameComponentCollection has no lock, so the nulling protects removal by code on the loop thread only; removal from another thread while the loop iterates is not synchronized. | modules/runtime/src/Game.cpp (Update, Draw, OnComponentRemoved); modules/runtime/src/GameComponentCollection.cpp | stated precisely on the page (game-limits). |
| `takeover/threading.html` | ALSA's worker is joined by Close before mixer state is freed; its own mix path uses a recursive mutex shared with setters. | The join is in AlsaAudioDevice::StopLocked (called by Close); the recursive mutex belongs to the CNA mixer engine that is paired with ALSA (MixerEngine.cpp in Backend/CnaMixer), shared with every facade call and with the device callback, not to the device. | modules/audio/src/Platform/Alsa/AlsaAudioDevice.cpp (StopLocked, CloseLocked); modules/audio/src/Backend/CnaMixer/MixerEngine.cpp (Engine::mutex, DeviceCallback::FillBuffer, CloseDeviceAtExit) | attribution corrected on the page (device barrier and mixer-engine lock described separately). |
| `takeover/threading.html` | Compass/Motion's Android route captures a shared control block ... (implies a general route). | Compass::getIsSupportedProperty returns false off Android and a backend is created only under __ANDROID__ (or installed by a test); the control block protocol is therefore Android-only in production. | modules/devices/src/Sensors/Compass.cpp (getIsSupportedProperty, constructor); docs/devices-event-contract.md section 5 | made explicit on the page; real-Android behaviour beyond the fake-backend seam recorded as unverified by CNA's own document. |
| `takeover/uncertainty.html` | the recorded C#, Java and Python checkouts admit 0.21.x; Rust/TS/Swift remain uninvestigated in this handbook | Eight public bindings (C#, Java, TypeScript, Python, Rust, Swift, Go, Ruby) all target 0.21.x (Ruby also 0.7.0); five refuse 0.29.0 by exact-version rule; the Development area has internals pages only for C#, Java, Python | data/current-facts.json bindings_expected_abi; docs/c-api.html#bindings; scripts/dev_manifest.py | corrected on the page |
| `takeover/uncertainty.html` | Most families still lack source-verified deep pages and a maintained parity matrix | Seven of 21 families have Development internals pages; Phase-1 publishes a pixel-evidence-by-family table and CNA has 32 cross-renderer parity fixtures on four renderers | scripts/dev_manifest.py (internals/graphics/*); docs/verification.html#renderers | restated with current numbers |
| `takeover/uncertainty.html` | CI has Linux and some Windows/macOS/specialized cells | 20 workflow files, 28 jobs; Win32 cross under Wine on every push; native Windows lanes workflow_dispatch-only; no Android or Wayland workflow | .github/workflows/platform-ci.yml; docs/platforms.html#ci | made exact |
| `takeover/uncertainty.html` | Current CMake compiles Headless always and Terminal on POSIX | Confirmed; additionally the CMake file itself states Terminal's purpose (third implementation held to the conformance suite), so part of Developer's 'inference' is now a source-stated fact | modules/platform/CMakeLists.txt | relabelled on the page |
| `takeover/validation.html` | The 237 generated test declarations are a source inventory, not 237 tests guaranteed to run in any one build. | The generated Test target index reports a different current number of declarations; a hard-coded count is stale by construction. | development/reference/test-targets.html (generated by scripts/generate_dev_reference.py from the TARGET Git objects) | the number was removed and the generated index is linked; the inventory-versus-run distinction is kept. |
| `takeover/validation.html` | File-dialog no-service and platform-throw failures do not currently have the same callback guarantee. | Confirmed and made precise: with no dialog service FileDialog reports an empty result to the callback (tested); when the platform service throws PlatformException the callback has been moved from and is not reported. | modules/devices-ext/src/FileDialog.cpp (ReportUnavailable, ShowOpenFile catch); modules/devices-ext/tests/CNA/Devices/FileDialogTests.cpp (FileDialogWithoutAServiceTest) | stated precisely on the page. |
| `takeover/validation.html` | run_gpu_tests_private.sh runs inside private Weston/Xwayland, checks incompatible display cache settings and may skip unavailable devices. | Confirmed; additionally it refuses (exit 2) a tree whose cache forces CNA_TEST_DISPLAY, exits 77 without Xwayland or a compositor, warns on a display without DRI3, supports --exec for one command, and names failures that died on a graphics-profile refusal. | tools/platform/run_gpu_tests_private.sh; cmake/TestDisplayPolicy.cmake | corrected and extended in the display section. |
| `takeover/validation.html` | Run both Headless_Smoke and Stub_Smoke where configured. | Registration is conditional: HEADLESS fixtures only when HEADLESS is the default renderer identity; Stub_Smoke when the STUB family is in scope; Stub_Smoke links SDL3::SDL3 unconditionally, HEADLESS fixtures only when the target exists. | modules/renderers/headless/examples/CMakeLists.txt; modules/renderers/stub/examples/CMakeLists.txt | conditions stated on the page. |
| `takeover/worked-changes.html` | at this pin they admit 0.21.x rather than native 0.29.0 (C#, Java, Python) | True at TARGET and for all eight public bindings | modules/c-api/include/CNA/C/abi.h; data/current-facts.json bindings_expected_abi | restated for all bindings |
| `takeover/worked-changes.html` | GraphicsCapabilityFloatRenderTargetTests.cpp explicitly checks existing ordinals | It pins ordinals 0-16 only; the enum has 19 members; IndirectDraw == 18 is static_asserted only in modules/graphics/examples/modern_gpu_capability_probe.cpp | modules/graphics/tests/CNA/GraphicsCapabilityFloatRenderTargetTests.cpp (ExistingCapabilityOrdinalsAreUnchanged); modules/graphics/include/CNA/GraphicsCapability.hpp; modules/graphics/examples/modern_gpu_capability_probe.cpp | corrected on the page |
| `takeover/worked-changes.html` | confirm it is registered in a VULKAN-default build | Vulkan tests additionally require CNA_BUILD_EXAMPLES=ON and CNA_BUILD_TESTS=ON, not Windows, not Emscripten; Vulkan_RealWindowResize only with SDL3 | modules/renderers/vulkan/examples/CMakeLists.txt | made exact on the page |
| `testing/architecture.html` | It refuses a build tree whose CNA_TEST_DISPLAY cache value forces a different display | The wrapper refuses any non-empty CNA_TEST_DISPLAY in the cache, not only a different one | tools/platform/run_gpu_tests_private.sh (FORCED check) | corrected |
| `testing/index.html` | Use the nearest focused test target from the generated test target index | The generated index lists a single placeholder row for ${_cna_focused_test_target}; the 22 focused target names are defined by CNA_TEST_FOCUSED_TARGET_<group> in cmake/UnitTests.cmake | development/reference/test-targets.html; cmake/UnitTests.cmake | names and module mapping listed on the page |
| `testing/index.html` | cmake --build --preset tests --parallel then ctest --output-on-failure as the broader suite | The tests build preset builds only CnaTests; many CTest entries are separate example executables, so an unfiltered ctest reports missing binaries; window tests also open on the inherited DISPLAY | CMakePresets.json; .github/workflows/general-tests-ci.yml; cmake/TestDisplayPolicy.cmake | recipe rewritten with a full build and the private wrapper |
| `testing/index.html` | ctest --test-dir <build-dir> -N confirms what the configuration registered (after building a focused target) | Focused executables are not CTest registrations, and discovered CnaTests cases (DISCOVERY_MODE PRE_TEST) are known only once CnaTests is built (GoogleTest module behaviour, not observed) | cmake/UnitTests.cmake (gtest_discover_tests, focused targets EXCLUDE_FROM_ALL) | caveat stated and hedged |
| `workflows/index.html` | Run platform source gates, focused contract tests and native-host CI (unspecified) | The gates are tools/platform/{sdl_inventory,sdl_classify,renderer_sdl_audit,sdl_ratchet}.py --check and hot_path_lint.py (CLAUDE.md), with platform-ci.yml adding --strict, nonproduction_sdl_audit.py and check_contract.py; the configure step also runs the ratchet and lint by default | CLAUDE.md; .github/workflows/platform-ci.yml; cmake/PlatformRatchet.cmake; cmake/PlatformHotPathLint.cmake | extended: gates named |
| `workflows/index.html` | Repository engineering instructions: AGENTS.md | AGENTS.md and CLAUDE.md differ at this snapshot: CLAUDE.md carries the later rules (XNA-over-FNA tie-break of 2026-09-04, stable build directories and ccache, the platform boundary and its gates, the CNAEXT engine layer, the 25-identity curated renderer set); AGENTS.md carries the shorter set. CLAUDE.md also lists WIN32 among reserved platform identifiers, which cmake/PlatformSelection.cmake contradicts (WIN32 is implemented and offered when targeting Windows) | AGENTS.md; CLAUDE.md; cmake/PlatformSelection.cmake | page cites both files as the review standard and warns that they can lag the CMake and source |

## 5. Developer claims not carried over

| Developer page | Claim | Reason |
|---|---|---|
| `architecture/bindings.html` | The pinned CNA repository revision d6e9ff05 is the compatibility subject of the warning. | process content about the earlier pin; the warning is restated against the TARGET snapshot |
| `bindings/csharp.html` | I did not run these against the pinned 0.29 native revision | first-person voice; replaced by a neutral 'not executed' statement plus the stronger NativeFact-skip caveat |
| `bindings/java.html` | Audit framing against the older CNA commit (Developer pin) as the CNA side of the pairing | Development pages frame against TARGET 009d40f5; the ABI facts are unchanged (0.29.0 at both), so the pin reference was replaced, not the conclusion |
| `bindings/python.html` | The current site has not executed Python integration against 0.29 because the loader intentionally rejects it. | Rephrased in neutral voice ('No Python integration run against 0.29 is recorded here; the loader rejects that library by design'); reference to Developer's site removed. |
| `change-map/index.html` | Renderer family map link to the standalone manual's graphics/backends page | Retargeted to development/internals/graphics/backends.html (same content, this site) |
| `getting-started/index.html` | Never silently update cnahead (wording tied to the standalone manual's generator) | Kept in substance: the pin moves atomically with prose and generated references; the procedure lives on development/maintenance.html |
| `graphics/backends.html` | Links to Developer's own coverage ledger (docs/COVERAGE.md) and the 'SOURCE VERIFIED' label discussion. | Process content about Developer's own audit state, not CNA product status; the page now says where each uncovered family is described at user level. |
| `graphics/draw-call.html` | "Their target/host matrix still needs a dedicated testing audit" (Developer's own audit note). | Process content about Developer's coverage; replaced by the named test registrations and a pointer to the Test architecture page. |
| `graphics/easygl.html` | First-person 'I have verified the test wiring and source paths here, not executed a GL/display test on this host.' | Voice rule; restated as 'read for this page; no GL or display test was executed'. |
| `graphics/sdl-gpu.html` | First-person statement that registration and source were verified at the pinned commit and GPU tests were not executed on 'this host'. | Voice rule; replaced by the page's evidence box and an explicit 'none of these tests was executed' sentence. |
| `index.html` | Generated: 23 Sep 2026 / Runtime: none - static files (meta strip) | describes the Developer site's build, not CNA |
| `maintenance/index.html` | The checker rejects Node/framework manifests | Specific to the standalone manual's checker; libcna.com has no such check, and it is process content about that site |
| `maintenance/index.html` | Site README and cnahead links (../README.md, ../cnahead) | Links to the standalone manual's own files; replaced by prose describing cnahead and by the Generated references and Getting started links |
| `modules/graphics-ext.html` | Former ASCII renderer functionality now appears as an AsciiPostProcessEffect here, not a selectable physical renderer identity. | Kept in substance (AsciiPostProcessEffect is a CPU glyph quantiser migrated from an earlier renderer-level implementation) but the retired identity is not named, by site policy; names withheld. |
| `modules/inspector.html` | This is ... not ... the developer.libcna.com static website. | Self-reference to the Developer site; not a CNA fact and forbidden vocabulary. |
| `modules/inspector.html` | No live network or browser was exercised while writing this page. | Rephrased as 'checked by reading ... not executed' and moved into the evidence note; first-person-process wording not kept. |
| `platforms/index.html` | Other backends remain in the coverage ledger until equivalent investigation is done; see the Win32 chapter as the one source-verified example. | Process content about Developer's own audit; all seven backend pages now exist in the Development area. |
| `platforms/sdl2.html` | Verify CTest registration for the selected CNA_PLATFORM=SDL2 build in the test index | Replaced by the actual registration (cna_platform_sdl2_tests executable, CnaSdl2PlatformTests with SDL_VIDEODRIVER=dummy, CnaSdl2OnlyRendererGate); the index is still linked. |
| `platforms/sdl3.html` | The exact CTest target names and host requirements should be checked in the generated test index for the build configuration | Replaced by the actual registration read from cmake/UnitTests.cmake (CnaPlatformTests, CnaPlatformWindowTests, CnaPlatformXErrorHandlerTests, per-case discovery from CnaTests); the index is still linked. |
| `platforms/x11.html` | Developer's first-person verification sentence ('I verified source and registration locations ...'). | Voice rule; replaced by 'checked by reading ... at 009d40f5; no X server-dependent suite was executed'. |
| `platforms/x11.html` | 'window opens but nothing draws' diagnosis item 'a CPU presenter failing image creation'. | Not reachable through GraphicsDevice on X11 at TARGET (see contradiction); replaced by the 'no window by design with CPU renderers' case. |
| `reference/index.html` | Links to the standalone manual's cnahead file and its maintenance page | Process content about the standalone manual; replaced by the pin description in the lede and a link to development/maintenance.html |
| `repository/index.html` | Top-level include/ row | Does not exist at TARGET (see contradictions) |
| `runtime/shutdown.html` | 'The Phase 0 overview previously compressed these into one path; this source trace is the accurate distinction.' | Process content about Developer's own documentation history, not a statement about CNA. |
| `takeover/blast-radius.html` | Reference to Developer's docs/COVERAGE.md coverage ledger. | Process content about Developer's own site; not part of the published pages. |
| `takeover/case-study-components.html` | Developer's framing 'current pinned implementation' / 'this documentation project' | Developer-site voice; replaced with TARGET snapshot wording per the Phase-2 voice rules. |
| `takeover/case-study-storage.html` | References to 'the pinned revision' and 'the documentation pin' | Developer-site voice; rewritten as the TARGET snapshot. |
| `takeover/index.html` | Breadcrumb, 'Start here · maintainer manual' eyebrow and 'this manual' wording. | Developer-site chrome and vocabulary; replaced by 'these pages' and the generated header. |
| `takeover/index.html` | Reference to Developer's docs/COVERAGE.md ledger. | Process content about Developer's own site. |
| `takeover/index.html` | The Developer pin hash d6e9ff05... as the commit all source links target. | Replaced by the TARGET snapshot 009d40f5; Developer-pin vocabulary is not published. |
| `takeover/inherited.html` | link to docs/COVERAGE.md as the record of missing study | Developer-site process artefact; replaced by the Development area's own page set and the uncertainty register |
| `takeover/ownership.html` | 'System-level lifetime map · source verified' page badge | Developer-site status chrome; replaced by the generated evidence box. |
| `takeover/threading.html` | Developer-site chrome: breadcrumb, 'Proven paths, explicit limits' page subtitle and the 'pinned source' wording. | Process content about Developer's own site and pin; replaced by the generated evidence box and TARGET-pinned source links. |
| `takeover/uncertainty.html` | The full coverage ledger (docs/COVERAGE.md) is also an uncertainty register; NOT STARTED and INVESTIGATING areas need study | Developer-site process status; replaced by the statement that the Development area's own missing or source-only pages are the register |
| `takeover/uncertainty.html` | names implied by the commit message of fc1b6a537 (retired identities, reserved values by name) | withheld by site policy (Phase-1 guide section 4); only counts, values and mechanisms are published |
| `takeover/validation.html` | The numeral '237' and the 'pinned' wording in 'The pinned unit configure/build presets' and 'The pinned tree'. | Stale count (see contradictions) and Developer-pin vocabulary; replaced by snapshot wording. |
| `takeover/validation.html` | Developer-site chrome: breadcrumb and 'Change-driven evidence plan · pinned CMake source' subtitle. | Process content about Developer's own site. |

## 6. Phase-1 factual errors found during Phase 2

| Page | Issue | Evidence |
|---|---|---|
| `docs/audio.html` | The SoundEffect member table says Play() returns false 'for example the instance limit was reached'. SoundEffect::Play enforces no instance limit; it returns false for a disposed effect, no native audio (SDL2/NULL or empty), or a failed track create/bind/play. The wording mirrors a stale header comment. | modules/audio/src/Xna/SoundEffect.cpp (Play); SoundEffect.hpp doc comment |
| `docs/audio.html` | Minor: the implementation table's 'SoundEffect construction and Duration: Yes' for SDL2 and NULL holds for raw-buffer-built effects (which includes ContentManager-loaded XNB effects); an effect built with the path constructor or FromStream in a mixer-less build carries no audio and reports Duration zero. | modules/audio/src/Xna/SoundEffect.cpp (#else branches, getDurationProperty); SoundEffectDurationTests.cpp |
| `docs/building.html` | See build/index ledger: the .sdl-prebuilt key and reuse description omits the -wayland/-simulator/-min suffixes and the build-manifest condition | cmake/ThirdPartySDL.cmake; cmake/SdlPrebuiltFingerprint.cmake |
| `docs/building.html` | Step 3 says the SDL prebuilt lives in '.sdl-prebuilt-<os>-<arch>' and is reused; at TARGET the default key gains -wayland, -simulator and -min<version> suffixes and reuse is conditional on a build manifest (source hash, patch hashes, configure arguments), so a changed vendored source, patch or argument rebuilds it | cmake/ThirdPartySDL.cmake; cmake/SdlPrebuiltFingerprint.cmake |
| `docs/content-pipeline-xnb.html` | Container format support says an LZ4 file 'reached only as an external reference from another .xnb is refused'. Only the untyped external-reference decoder (ReadExternalReference() returning std::any, used by ExternalReferenceReader) refuses LZ4; the typed ReadExternalReference<Texture2D/TextureCube/shared_ptr<Effect>>() routes through ContentManager::Load<T> and its LZ4-capable LoadXnbAsset<T>. | modules/content/src/Xna/ContentReader.cpp (ReadExternalReference<T> calls Load<T>; the untyped overload calls LoadUntypedXnbReference); modules/content/src/Xna/ContentManager.cpp (LoadXnbAssetUntyped Lz4 case). Minor imprecision. |
| `docs/tutorials/107-cpu-renderers.html` | States that GetLastFrameStatistics() gives the counters 'diffed across the most recent Present()'. The code returns the counters accumulated since the most recent Present (the frame in progress), not the frame that ended at it. | modules/renderers/headless/src/HeadlessRenderer.cpp (GetLastFrameStatistics, Present); headless_coverage_gaps_test.cpp checks 0 at frame start and 2 after two draws. Minor wording imprecision. |
| `docs/effects.html` | The SDL_GPU row of the per-renderer table says 'ShaderEffect instancing is not implemented'. At TARGET an instanced custom-effect draw is queued with its instance count; only per-instance vertex streams are not bound. (docs/shader-effects.html words it as 'SDL_GPU documents that ...', which is literally true of the stale limitation text.) | modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp (DrawInstancedPrimitivesEx, QueueCustomEffect3DDrawEXT) |
| `docs/shader-effects.html` | The 'Other things a custom shader does not get' callout limits engine shader packages on SDL_GPU to builds with libshaderc, while the same page's renderer table (correctly) says SPIR-V words work in every build with a device; package SPIR-V variants take the SPIR-V intake, which needs no libshaderc. | SdlGpuRenderer.cpp (SdlGpuEffectRenderer::CompileProgram SPIR-V branch, SupportsShaderLanguageEXT) |
| `docs/input.html` | The #textinputext note says StartTextInput/StopTextInput 'have no visible effect' on desktop platforms. Text input is a mode: on Win32 the event mapper consumes every WM_CHAR without a TextInputEvent until the window's mode is enabled by StartTextInput, and the mapper's own comment says a game that never called it sees the same on every other backend. | modules/platform/src/Win32/Win32EventMapper.cpp (TranslateChar, SetTextInputActive); modules/platform/src/Win32/Win32InputServices.cpp (Win32TextInput::Start); modules/platform/src/Win32/Win32EventMapper.hpp (SetTextInputActive comment) |
| `docs/input.html` | The #mouse table describes MouseState X and Y as 'window pixels'. Mouse::GetState converts the window-client position to logical game coordinates through the window's renderer transform (letterboxing, virtual resolution); it equals window pixels only when no transform applies. | modules/input/src/Xna/Mouse.cpp (window_to_logical, GetState) |
| `docs/runtime-renderer-selection.html` | States that an invalid CNA_GRAPHICS_RENDERER value throws once on the first query and that the build default applies afterwards. In source the first query is made by PublishAvailable from the generated registry's namespace-scope initialiser, i.e. before main, so an invalid or not-compiled-in value (or page property) cannot be caught by game code and ends the process through std::terminate. Derived from source, not executed. | cmake/templates/CnaRendererRegistry.generated.cpp.in; modules/core/src/GraphicsRendererSelection.cpp (PublishAvailable -> RebuildAttemptOrder -> GetSelected -> ConsultEnvironmentOnce) |
| `docs/sensors.html` | The Build flag banner says the Microsoft::Devices layer is gated behind the CNA_DEVICES CMake option (default OFF) and must be configured with -DCNA_DEVICES=ON to be built and linked. At TARGET modules/CMakeLists.txt adds devices unconditionally, cna_devices is in the CNA umbrella, and CNA_DEVICES is tested only in devices-ext (CNA::Devices). | modules/CMakeLists.txt (add_subdirectory(devices), _cna_runtime_parts); no CNA_DEVICES use under modules/devices; CMakePresets.json devices-* descriptions (REMED-BUILD-011) |
| `docs/xna-compatibility.html` | A note callout states 'The Microsoft::Devices layer is built only when CNA_DEVICES=ON ... A stock build does not contain it.' Same source evidence as above; the gated layer is CNA::Devices (devices-ext). | modules/CMakeLists.txt; modules/devices/CMakeLists.txt; modules/devices-ext/CMakeLists.txt |
| `docs/input.html` | The Devices.Sensors row says 'Built only with CNA_DEVICES=ON.' Microsoft::Devices sensors are built in every configuration. | modules/CMakeLists.txt; general-tests-ci.yml notes CNA_DEVICES=OFF while running the Devices/Sensors suites in the unfiltered ctest |
| `docs/diagnostics.html` | #resources says the graphics layer registers metadata for textures, render targets and vertex/index buffers. In fact every GraphicsResource (state objects, effects, ...) registers a ResourceKind::Unknown, zero-byte record at construction, so Snapshot::resources also lists those; records are also removed on Dispose, not only when the object dies. An omission, not a wrong number. | modules/graphics/src/Xna/GraphicsResource.cpp (constructors, Dispose, ShareResourceIdentityWith); GraphicsDiagnosticsTests AssignedReferenceIdentityHasOneDiagnosticResource (BlendState adds records) |
| `docs/faq.html` | The persistence answer says the Begin*/End* pairs on both Storage and GamerServices are synchronous and the callback runs inline. That holds for achievements, profile and leaderboard pairs but not for Guide::BeginShowKeyboardInput/BeginShowMessageBox, which complete later (docs/tutorials/123-achievements.html #inert states this correctly). | modules/gamer-services/src/Xna/Guide.cpp; GamerServicesServiceTests BeginShowKeyboardInputDoesNotCompleteSynchronously |
| `docs/cnaext-engine.html` | The build section says the layer's embedded shaders are 11 packaged directories and 199 shader source files. Under modules/graphics-ext/src/shaders the tree holds 156 .glsl files (178 files including the 11 manifests and 11 generated headers) and the manifests declare 208 payload entries; the 199 figure could not be reproduced by any of these counts. | find modules/graphics-ext/src/shaders at 009d40f5; package.json payload counts (8+4+8+4+14+4+12+112+22+8+12) |
| `docs/math-types.html` | The Vector3 table row and the 'x87-width accumulation' row of the numerical-behaviour table say Vector3::Length, Distance and DistanceSquared reproduce XNA's wider-than-float accumulation. At TARGET those public functions sum their squares in float; the wide accumulation exists in Matrix::Multiply, Matrix::Invert, Vector3::Transform, Vector3::TransformNormal and in BoundingSphere.cpp's private helpers only. | modules/math/src/Vector3.cpp (Length, LengthSquared, Distance, DistanceSquared); modules/math/src/BoundingSphere.cpp (WideLength, WideDistance) |
| `docs/tutorials/97-networking.html` | The note under 'Session events' says one extra Update() is required after subscribing because local gamers are queued at construction and 'C++ has no equivalent hook' to XNA's replay. The NetworkSession constructor installs exactly that hook (EventHandler::SetReplayHook), so GamerJoined handlers are invoked immediately for every gamer already in the session (tests GamerJoinedReplaysImmediatelyOnSubscriptionForConstructionTimeGamers, GamerJoinedReplaysForALateSubscriber). | modules/net/src/Xna/NetworkSession.cpp (constructor); modules/net/tests/Microsoft/Xna/Framework/Net/NetworkSessionTests.cpp |
| `docs/tutorials/97-networking.html` | The status table says NetworkSessionType::Local is 'fully functional as a single-process session: ... the local packet queue all work'. Update()'s PacketSend handling is gated on ENetBackend::RealNetworkingEnabled (SystemLink only), so packets sent on a Local session are never delivered, not even to the sender; the corresponding test asserts IsDataAvailable stays false. docs/xna-compatibility.html ('the in-process Local type works too') and features.html carry the same overstatement for packets. | modules/net/src/Xna/NetworkSession.cpp (Update); LocalNetworkGamerTests.cpp SendDataThenReceiveDataRoundtrip; NetworkSessionTypePolicyTests.cpp SendDataStaysFullySyntheticForEverySyntheticType |
| `features.html` | The Windows Phone shell & push card lists 'PushNotificationSender and RawPushNotificationMessage' as the notification types. There is no PushNotificationSender type (it is only the header/source file name); the message classes are RawPushNotificationMessage and ToastPushNotificationMessage. | modules/phone/include/Microsoft/Phone/Notification/PushNotificationSender.hpp |
| `docs/storage.html` | States that UTF-8 roots and names are handled on Windows and that a root that cannot be created throws on the first access. Two omissions: three StorageDevice properties (free space, total space, connected) still build narrow paths from the UTF-8 root, and after that first failure the root is latched empty so later calls silently use the working directory. Source-level, not executed. | modules/storage/src/StorageDevice.cpp (EnsureStorageRoot, getFreeSpaceProperty, getTotalSpaceProperty, getIsConnectedProperty) |
| `docs/platforms.html` | The seven-implementation table describes HEADLESS as 'One window object, no services, every capability false'. It has two never-null services (a real StandardFileSystem rooted at cna-headless and StandardSystemInfo); 'no optional services' would be exact. | modules/platform/src/Headless/HeadlessPlatform.cpp (GetFileSystem, GetSystemInfo); PlatformConformanceTests ServicesEveryPlatformMustHaveAreNeverNull |
| `docs/native-platforms.html` | #limits says 'native Wayland and X11 are not offered on macOS, iOS, Android or the web'. X11 detection excludes only Windows, Emscripten, Android and iOS; on macOS X11 is offered whenever libX11, libXext and XKBlib.h are found. (docs/platforms.html#combination-rules states the exclusions correctly.) | cmake/PlatformX11.cmake (cna_detect_x11: if(WIN32 OR EMSCRIPTEN OR ANDROID OR CNA_APPLE_IOS)) |
| `docs/platforms.html` | #macos says 'SDL3 is the only platform implementation on macOS' and #web says 'The platform is always SDL3 here'; the #os-status rows for macOS, iOS, Android and WebAssembly list only SDL3 as available. At CMake level HEADLESS is always offered and compiled, SDL2 is always offered, TERMINAL is offered on every non-Windows target, and nothing forces SDL3 on Apple or Emscripten. SDL3 is the default and the only CI-configured choice there, which is probably what was meant. | cmake/PlatformSelection.cmake (_cna_platforms_available); modules/platform/CMakeLists.txt (Headless always, Terminal NOT WIN32) |
| `docs/tutorials/139-terminal-platform.html` | States that Ctrl+C/Z/S 'are signals or flow control unless the terminal's keyboard protocol reports them'. While a TerminalSession is active (the whole run of a SOFTWARE game on a TTY), raw mode clears ISIG and IXON, so these arrive as control-chord key events, not signals or flow control. | modules/platform/src/Terminal/TerminalSession.cpp (constructor raw-mode flags); TerminalKeyboard.cpp (DecodeLegacyByte bytes 1-26 -> Ctrl+letter) |
| `docs/tutorials/139-terminal-platform.html` | Says 'the back buffer is columns x 8 by rows x 16 pixels'. The window keeps the game's requested size until the first SIGWINCH-driven resize; only then does it become columns*8 by rows*16. | modules/platform/src/Terminal/TerminalPlatform.cpp (CreateWindow comment 'The requested size stands', PollEvents/ApplyTerminalSize) |
| `docs/tutorials/139-terminal-platform.html` | Minor: 'one colour pair plus one glyph per cell' - the writer emits a foreground colour only (and no colour escape for Monochrome), no background. | modules/platform/src/Terminal/TerminalAnsiWriter.cpp (AppendColour: 38;2 / 38;5 / 30-37,90-97) |
| `docs/platforms.html` | Capability matrix (Wayland column) says multipleDisplays is 'If wl_output'; the same wording is in docs/native-platforms.html#capabilities. | WaylandPlatform::ComputeCapabilities sets multipleDisplays = displays_ != nullptr, and displays_ is created for every live connection, so the flag is true whenever a compositor was reached, whether or not any wl_output was announced (enumeration then returns the described outputs). Minor imprecision. |
| `docs/native-platforms.html` | #win32 says the backend 'declares Windows 8.1 (0x0603) as its API floor'; 0x0603 is the header declaration level, and the source comment states the binary requires only the Windows 7 floor with newer DPI entry points resolved at run time (not executed). Low-severity imprecision. | modules/platform/src/Win32/Win32Common.hpp; modules/platform/src/Win32/Win32DpiSupport.cpp |
| `docs/native-platforms.html` | #win32 'Closing a window is a request … closing one of several windows does not quit the process' is true of the platform backend, but a Game calls Exit() on any CloseRequested (no window-id filter). Incomplete for Game users rather than wrong; low severity. | modules/runtime/src/Game.cpp (Game::PollEvents) |
| `docs/platforms.html` | Capability matrix, row vulkanSurface, X11 column says 'If Vulkan headers were present at build'. At TARGET X11 reports vulkanSurface true whenever an X connection exists; no X11 source reads CNA_X11_HAVE_VULKAN_HEADERS (the loader and xlib-surface entry point are resolved at run time). | modules/platform/src/X11/X11Platform.cpp (ComputeCapabilities, OpenConnection); X11GraphicsServices.cpp (X11VulkanSurface) |
| `docs/native-platforms.html` | Same X11 vulkanSurface condition in the capability table, and the X11 dependency table row 'Vulkan headers ... Without it: No vulkanSurface' is not what the code does. (The Wayland column/row is correct: WaylandPlatform.cpp does gate on CNA_WAYLAND_HAVE_VULKAN_HEADERS.) | as above; modules/platform/src/Wayland/WaylandPlatform.cpp |
| `docs/game-loop.html` | #drawgametime says 'With fixed timestep enabled, Draw may be skipped if the game is running behind'. In Game::Tick a lagging fixed-step frame runs extra Update calls (up to the 500 ms clamp) and still draws once; Draw is skipped only by SuppressDraw/Exit or a false BeginDraw(). | modules/runtime/src/Game.cpp (Tick) |
| `docs/game-loop.html` | #lifecycle-diagram and #unloadcontent present 'Dispose(bool)' as the teardown that follows OnExiting and say UnloadContent runs from the device-disposing event during Dispose(bool). Run() never calls Dispose; UnloadContent runs only when Dispose() is called explicitly (or the registered graphics device service is disposed) AND a graphics device service such as GraphicsDeviceManager is registered; it never runs from the destructor. | modules/runtime/src/Game.cpp (Run; Initialize subscription guarded by graphicsDeviceService_ != nullptr; Dispose(bool); ~Game) |
| `docs/game-loop.html` | #constructor states 'The graphics device is not yet created at this point, so no GPU calls are valid here.' In CNA the base Game constructor builds the GraphicsDevice (renderer and, for windowed renderers, the window) before a derived constructor body runs; only the manager's preferences (size, profile, presentation mode) are applied later, in DoInitialize. The advice not to rely on those preferences in the constructor stays sound. | modules/runtime/src/Game.cpp (Game(std::unique_ptr<IPlatform>) member initialisers: GraphicsDevice_()) |
| `docs/game-loop.html` | #gamewindow states 'the native window itself is created when Run() is called'. For any renderer whose descriptor needs a window it is created during Game construction (GraphicsDevice::createOrAttachWindow), and getClientBoundsProperty() already queries it from a derived constructor. | modules/graphics/src/Xna/GraphicsDevice.cpp (createOrAttachWindow, resolveRenderer); Game.cpp (Window_.setWindowInternal in the constructor body) |

## 7. Current bugs / gaps noted while absorbing (for the Phase-3 catalogue; not published as a catalogue)

| Source unit | Summary | Evidence |
|---|---|---|
| `architecture/module-graph.html` | modules/renderers/gdi/CMakeLists.txt prints 'GDI renderer archive = N shared CPU-2D sources + 3 GDI-owned units' with a hard-coded 3, but modules/renderers/gdi/src holds four .cpp files (Configuration, Presentation, Renderer, RendererDescriptor), so the message undercounts by one. | modules/renderers/gdi/CMakeLists.txt (CNA_GDI_ARCHIVE_TOTAL); modules/renderers/gdi/src/ |
| `architecture/module-graph.html` | modules/renderers/CMakeLists.txt enters common/d3d, the software directory for GDI, and the Metal family target from conditions that test the default identity (CNA_GRAPHICS_RENDERER) instead of list membership; a multi-renderer set whose non-default member needs one of them would not have that helper target defined by those lines. No CNA multi-renderer CI set or preset contains such a pair. | modules/renderers/CMakeLists.txt (pre-loop add_subdirectory conditions); modules/renderers/metal/CMakeLists.txt; modules/renderers/software/CMakeLists.txt |
| `architecture/module-graph.html` | cmake/SharedRuntimeLibrary.cmake comment says cna_graphics_ext and cna_devices_ext exist only with CNA_CNAEXT, while modules/CMakeLists.txt adds both directories unconditionally (their sources are guarded instead). | cmake/SharedRuntimeLibrary.cmake (cna_define_shared_runtime_deferred); modules/CMakeLists.txt |
| `architecture/runtime.html` | Game::PollEvents calls the renderer's DebugSimulateContextLoss()/DebugRestoreContext() on any non-repeating F9/F10 key press with no build-type or option guard; renderers that override the hooks (Direct2D, DirectX 9/11/12, EasyGL, WebGPU) will simulate a loss in a shipped game. | modules/runtime/src/Game.cpp (Game::PollEvents, KeyEvent branch); IGraphicsRenderer.hpp default no-ops |
| `audio/engine.html` | AudioMixer::DestroyMixer (SDL3 mixer) has no production caller: the generation counter and reload machinery protect a teardown that occurs only in tests and tools/audio harnesses. As read, at exit the device is closed by the static g_audioDevice's destructor and the native mixer pointer is never destroyed, unlike the immortal ALSA engine with its atexit close. | modules/audio/include/CNA/Internal/Audio/AudioMixer.hpp (comment on DestroyMixer); modules/audio/src/Backend/Sdl3Mixer/AudioMixer.cpp; tree-wide search |
| `audio/engine.html` | SoundEffectInstance::Play calls CNA::Internal::Audio::EnsureMixer() directly, so a mixer-creation failure at that point would surface as std::runtime_error rather than NoAudioHardwareException (the effect constructor normally creates the mixer first, so this is reachable only if creation fails later, e.g. after a mixer teardown). | modules/audio/src/Xna/SoundEffectInstance.cpp (Play); SoundEffect.cpp (EnsureMixerOrThrowXna) |
| `audio/engine.html` | Under CNA_AUDIO_PLATFORM=ALSA the SoundEffect, SoundEffectInstance, DynamicSoundEffectInstance, Cue, SoundBank, WaveBank and offline-renderer suites are filtered out of CnaTests (they read SDL3_mixer track handles); XNA-facade coverage on ALSA rests on CnaMixerXnaTests, CnaMixerTests, AudioCategoryTests and the microphone suites. | cmake/UnitTests.cmake (_cna_sdl3_mixer_tests filter) |
| `audio/engine.html` | Stale comments: SoundEffect.cpp says FireAndForgetPanState is freed in OnFireAndForgetStopped (code defers it to the next Play/exit); SoundEffect.hpp documents SoundEffect::Play as returning false 'if the instance limit was reached' although the implementation checks no limit; SoundEffectInstance.hpp says is3D_ never resets although Pan before the first Play resets it; the content SoundEffectContentTypeReader.hpp describes a synthetic-WAV FromStream route while the .cpp constructs via the raw-buffer constructor. | modules/audio/src/Xna/SoundEffect.cpp; modules/audio/include/Microsoft/Xna/Framework/Audio/SoundEffect.hpp and SoundEffectInstance.hpp; modules/content/include/CNA/Internal/Xnb/SoundEffectContentTypeReader.hpp vs src/Xnb/SoundEffectContentTypeReader.cpp |
| `bindings/c-api-internals.html` | cna_graphics_device_destroy (caller-created device) disposes and releases without a child-count check in its route body, while resources on that device hold a raw GraphicsDevice pointer in their BorrowedGraphicsDevice view; child-first order is the caller's obligation. Whether a later resource call is refused was not traced. | modules/c-api/src/CnaCApiDisplay.cpp (cna_graphics_device_destroy); modules/c-api/src/CnaCApiRuntimeDetail.hpp (OwnedGraphicsDevice, BorrowedGraphicsDevice) |
| `bindings/c-api-internals.html` | The C library is not built by any CI workflow and CNA_BUILD_C_API defaults to OFF; the release gate reads Not ready (468 unmapped symbols). | CMakeLists.txt (CNA_BUILD_C_API); .github/workflows/c-api-*.yml; docs/c-api/RELEASE_GATE.md |
| `bindings/csharp.html` | cna-cs@e223909 admits only C ABI 0.21.0; a library built from CNA snapshot 009d40f5 (0.29.0) is rejected by the resolver, so no binding-level evidence exists for the snapshot | cna-cs eng/cna-native-abi-policy.json; src/CNA.Interop/CnaNativeAbiPolicy.cs; CNA modules/c-api/include/CNA/C/abi.h |
| `bindings/csharp.html` | cna-cs README states ABI 0.20.0 and 861 imports while code and policy say 0.21.0 and 1,002; CnaAbi.cs doc comment still says 0.6.0 | cna-cs@e223909 README.md; src/CNA.Framework/CnaAbi.cs |
| `bindings/csharp.html` | Finalizer-queued native releases are drained only at Game construction, Run entry/exit, direct frame calls and Dispose, not inside per-frame callbacks; during a long Run() loop abandoned wrappers stay pending | cna-cs@e223909 src/CNA.Framework/Game.cs; src/CNA.Framework/NativeResourceHandle.cs |
| `bindings/java.html` | cna-java@6661173 admits only ABI 0.21.x; TARGET exports 0.29.0, so the binding refuses the TARGET library (UnsatisfiedLinkError) and nativeAbiCheck against 0.29 headers reports MANIFEST_ABI_DRIFT | cna-java@6661173 NativeBindings.java (COMPILED_ABI_VERSION, requireAvailable), tools/native-abi/bindings.json, tools/native-abi/verify.py; TARGET modules/c-api/include/CNA/C/abi.h |
| `bindings/java.html` | cna-java README still names ABI 0.20.0 while code and manifest say 0.21.0 | cna-java@6661173 README.md vs NativeBindings.java / bindings.json |
| `bindings/java.html` | NativeDeferredRelease only drains when the owning thread calls FrameworkDispatcher.Update(); failed deferred releases are not retried | cna-java@6661173 NativeDeferredRelease.java, NativeBindings.updateFrameworkDispatcher, FrameworkDispatcher.java |
| `bindings/java.html` | Binding-recorded upstream defect JAVA-UPSTREAM-014: on EasyGL-served identities, process exit with a live vertex buffer after its creating thread ended aborts in a static destructor | cna-java@6661173 NativeShutdownSubprocessTests.java, tools/native-abi/probes/exit_with_live_graph.c |
| `bindings/python.html` | cna-python@f085bbb admits only ABI 0.21.x; a TARGET (0.29.0) library is refused with NativeAbiMismatchError, so no binding/TARGET integration evidence exists | cna-python@f085bbb src/_cna_native/loader.py (SUPPORTED_ABI_MINOR = 21); TARGET modules/c-api/include/CNA/C/abi.h |
| `bindings/python.html` | cna.extensions.graphics.Renderer at the pin carries 14 renderer identity values that ABI 0.28.0 retired; TARGET routes refuse them with CNA_RESULT_INVALID_ARGUMENT | cna-python@f085bbb src/cna/extensions/graphics.py; TARGET modules/c-api/include/CNA/C/graphics.h |
| `bindings/python.html` | PENDING_ROUTES still lists cna_network_session_replace_session_properties, which TARGET declares, implements and records as exported; audit_cna_abi.py would report it stale against an artifact that exports it | cna-python@f085bbb src/_cna_native/loader.py; TARGET modules/c-api/src/CnaCApiNetSessions.cpp, tools/c-api/abi_baseline.json |
| `build/architecture.html` | cmake/Tests/ModuleProbes.cmake's header comment still says the probes pin '42 public renderer identities' (25 at TARGET) | cmake/Tests/ModuleProbes.cmake (header comment); cmake/RendererIdentities.cmake |
| `build/architecture.html` | common/d3d is entered only when the default identity is DIRECTX11 or DIRECTX12, so a Windows multi-renderer build listing one of them as a non-default member would reach a family file that links cna_renderer_d3dcommon without that directory entered (by code reading; not run) | modules/renderers/CMakeLists.txt; already noted on development/internals/graphics/selection.html |
| `build/index.html` | cmake/ThirdPartyENet.cmake's missing-ENet message recommends 'git submodule update --init third_party/enet' although ENet is vendored in the tree, not a submodule | cmake/ThirdPartyENet.cmake; .gitmodules |
| `content/pipeline.html` | Doc comments on ContentCompiler.hpp (RunContentCompiler) say the XNB output writers are registered 'when the command line selects --format xnb'; tools/content/content.cpp registers them unconditionally (its comment says a project may select xnb per asset). | modules/content/include/CNA/Content/Pipeline/ContentCompiler.hpp; tools/content/content.cpp (Run) |
| `content/runtime.html` | ContentManager::LoadXnbAssetUntyped (the external-reference path) does not repeat LoadXnbAsset<T>'s 'totalLength < 14 for a compressed file' guard before building a 4-byte MemoryStream at offset 10 to read the decompressed size; a compressed-flag file of 10-13 bytes reaches an unguarded read. No test located for the message. | modules/content/src/Xna/ContentManager.cpp (LoadXnbAssetUntyped) vs modules/content/include/Microsoft/Xna/Framework/Content/ContentManager.hpp (LoadXnbAsset) |
| `content/runtime.html` | The OpenStream header comment says every .xnb load goes through OpenStream, but Load<T> reads through TryReadAssetBytes; only the protected ReadAsset<T> honours an override, so ResourceContentManager's public Load<T> still searches files. | modules/content/include/Microsoft/Xna/Framework/Content/ContentManager.hpp (OpenStream, Load, ReadAsset); modules/content/src/Xna/ResourceContentManager.cpp |
| `content/runtime.html` | Asking for the wrong C++ type of an .xnb asset can surface std::bad_any_cast instead of a ContentLoadException (the CNB tier and the loose tier wrap it). | modules/content/include/Microsoft/Xna/Framework/Content/ContentReader.hpp (InnerReadObject<T>) |
| `content/runtime.html` | Load<T> throws std::runtime_error on a disposed manager while ReadAsset<T> throws System::ObjectDisposedException; SoundEffect's loose tier does not wrap std::exception in ContentLoadException while the generic template does. | modules/content/include/Microsoft/Xna/Framework/Content/ContentManager.hpp; modules/content/src/Xna/ContentManager.cpp (Load<SoundEffect>) |
| `conventions/index.html` | CHECKLIST.md still says FNA wins on semantics in a per-file checklist line, contradicting the owner decision of 2026-09-04 recorded in CLAUDE.md that CNA follows measured XNA where XNA and FNA disagree | CHECKLIST.md (per-file checklist); CLAUDE.md (Where FNA and XNA disagree, XNA wins) |
| `debugging/index.html` | A documentation comment in the C API header content_readers.h refers to cna_get_last_error_message, which is not declared anywhere; the declared accessors are cna_error_get_last_info, cna_error_get_last_message_size and cna_error_copy_last_message | modules/c-api/include/CNA/C/content_readers.h (cna_object_dictionary_ext_copy_value comment); modules/c-api/include/CNA/C/core.h |
| `debugging/index.html` | The devices-tsan preset description records one known unrelated race in sharp-runtime's TimeSpan copy constructor as of the task it cites; not re-checked | CMakePresets.json (devices-tsan description) |
| `graphics/backends.html` | OPENGL4 beside an EasyGL identity in one multi-renderer binary is permitted by the configure rules but is not in any CI multi-renderer set. | cmake/RendererCombinations.cmake; .github/workflows/multi-renderer-ci.yml, platform-ci.yml, emscripten-multi-renderer-ci.yml; CMakePresets.json |
| `graphics/device.html` | Stale comments about device loss: GraphicsDevice.hpp says every renderer except D3D9 never calls the device-event callback, and GraphicsDevice.cpp says nine of ten renderers never call it; at TARGET six families do. The public DeviceLost documentation says the event is never raised on desktop. | modules/graphics/include/Microsoft/Xna/Framework/Graphics/GraphicsDevice.hpp (DeviceLost, deviceStatus_); modules/graphics/src/Xna/GraphicsDevice.cpp (createRenderer) |
| `graphics/device.html` | GraphicsDevice mutates public state, the resource registry and bindings without any lock; game-thread use is the only supported default. | modules/graphics/include/Microsoft/Xna/Framework/Graphics/GraphicsDevice.hpp |
| `graphics/easygl.html` | EasyGLRendererDescriptor.cpp's file header still says making the GL profile a runtime choice is future work (plan P11), while the same file implements per-identity runtime profiles. | modules/renderers/easygl/src/EasyGLRendererDescriptor.cpp (header comment vs DescriptorFor<Identity>) |
| `graphics/headless.html` | HeadlessRenderer::GetLastFrameStatistics is documented in the header as the statistics 'for the frame ending at the most recent Present()', but the code subtracts the snapshot taken at the most recent Present from the running totals, i.e. it reports the frame in progress; Headless_CoverageGaps pins the code behaviour. | modules/renderers/headless/include/CNA/Internal/Renderers/Headless/HeadlessRenderer.hpp (GetLastFrameStatistics); modules/renderers/headless/src/HeadlessRenderer.cpp; modules/renderers/headless/examples/headless_coverage_gaps_test.cpp |
| `graphics/headless.html` | Headless_XnaPixelCenter is registered with SDL_VIDEODRIVER=x11 and DISPLAY=${CNA_TEST_DISPLAY} although the file header states these tests deliberately set no SDL_VIDEODRIVER/DISPLAY environment; no reason is given. | modules/renderers/headless/examples/CMakeLists.txt |
| `graphics/headless.html` | The second HeadlessTextureRenderer constructor (width, height, typeNameOverride) has no caller in the tree at TARGET, and unlike the first it neither increments texturesCreated nor traces. | modules/renderers/headless/src/HeadlessRenderer.cpp; modules/renderers/headless/include/CNA/Internal/Renderers/Headless/HeadlessRenderer.hpp (text search for callers) |
| `graphics/sdl-gpu.html` | SdlGpuRenderer::GetAdditionalLimitationsTextEXT still says CNAEXT ShaderEffect instancing is not implemented, while DrawInstancedPrimitivesEx queues instanced custom-effect draws (SMG-0022); the custom-effect command binds only vertex slot 0, so per-instance vertex streams are not consumed. | modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp (GetAdditionalLimitationsTextEXT, DrawInstancedPrimitivesEx, IssueCustomEffect3DDrawEXT) |
| `graphics/sdl-gpu.html` | SPIR-V ShaderEffect intake and compute pipelines pass SPIR-V directly to SDL (no SDL_shadercross), but SupportsComputeShadersEXT and SupportsShaderLanguageEXT(SpirV, ...) answer true for any device; on a D3D12/Metal driver the refusal would come at shader/pipeline creation (source reading, not executed). | SdlGpuRenderer.cpp (SupportsComputeShadersEXT, SupportsShaderLanguageEXT, CreateShaderFromRemappedSpirvEXT); SdlGpuModern.cpp (SdlGpuComputeShaderRenderer::CompileProgram) |
| `graphics/sdl-gpu.html` | The SDL_GPU example block is entered for a non-default SDL_GPU member of a multi-renderer build; registrations set no renderer selection, so executables may run the build default (the EasyGL/HEADLESS blocks guard on _cna_default_renderer_identity for this reason). | modules/renderers/sdl-gpu/examples/CMakeLists.txt; modules/renderers/CMakeLists.txt; modules/renderers/headless/examples/CMakeLists.txt (VKPAR-0003 comment) |
| `graphics/sdl-gpu.html` | On non-Windows hosts SdlGpu_HeadlessGraphicsDevice/SdlGpu_2D_HeadlessDriver reuse CNA_SDLGPU_TEST_ENVIRONMENT (SDL_VIDEODRIVER=x11 by default) while the constructor refuses CNA_SDLGPU_TEST_FORCE_HEADLESS=1 unless the video driver is dummy/offscreen; SdlGpu_2D_HeadlessDriver therefore depends on CNA_SDLGPU_TEST_VIDEO_DRIVER (reading only; not executed). | modules/renderers/sdl-gpu/examples/CMakeLists.txt (CNA_SDLGPU_HEADLESS_TEST_ENVIRONMENT); SdlGpuRenderer.cpp (constructor, IsIsolatedVideoDriver) |
| `graphics/selection.html` | GraphicsRendererSelection::IsLatched() documentation says the selection latches when the first GraphicsDevice begins construction; the code latches at the end of resolveRenderer(). | modules/core/include/CNA/GraphicsRendererSelection.hpp; modules/graphics/src/Xna/GraphicsDevice.cpp |
| `graphics/selection.html` | By code reading, if UpdateViewportFromWindow() or one of the default-state pushes in the GraphicsDevice constructor throws after resolveRenderer() succeeded, the constructor destroys the renderer but the selection stays latched to it; no test found covers that edge. | modules/graphics/src/Xna/GraphicsDevice.cpp (constructor try-block) |
| `graphics/selection.html` | modules/renderers/CMakeLists.txt enters common/d3d only when the DEFAULT identity is DIRECTX11 or DIRECTX12 (the guard reads CNA_GRAPHICS_RENDERER before the per-family loop), while directx11/directx12 CMakeLists link cna_renderer_d3dcommon; by code reading a Windows multi-renderer build listing one of them as a non-default member would reference an undefined target. | modules/renderers/CMakeLists.txt (common/d3d guard); modules/renderers/directx11/CMakeLists.txt; modules/renderers/directx12/CMakeLists.txt |
| `graphics/software.html` | The Software example block is entered for a non-default SOFTWARE member of a multi-renderer build (for example the multi-renderer preset, default HEADLESS); registrations set no renderer selection, so executables may run the build default. | modules/renderers/software/examples/CMakeLists.txt; modules/renderers/CMakeLists.txt; CMakePresets.json |
| `graphics/stub.html` | Stub_Smoke's registration guard (CNA_BUILD_TESTS AND CNA_GRAPHICS_RENDERER STREQUAL STUB) has no comparison with _cna_default_renderer_identity; in a multi-renderer build where STUB is a non-default member the block is entered and the fixture would run under the default renderer (it only static_casts the renderer, so it would still pass). | modules/renderers/stub/examples/CMakeLists.txt vs modules/renderers/headless/examples/CMakeLists.txt; modules/renderers/CMakeLists.txt (family loop re-points CNA_GRAPHICS_RENDERER) |
| `graphics/stub.html` | Stub_Smoke cannot be built without an SDL3 target: the test source includes <SDL3/SDL.h> unconditionally and the CMake macro links SDL3::SDL3 without an if(TARGET) guard. | modules/renderers/stub/examples/stub_smoke_test.cpp; modules/renderers/stub/examples/CMakeLists.txt |
| `graphics/stub.html` | StubTextureRenderer's class comment says SetData()/GetData() are accepted as no-op defaults and discarded; the interface's GetData default is false. | modules/renderers/stub/include/CNA/Internal/Renderers/Stub/StubRenderer.hpp; modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp |
| `graphics/vulkan.html` | The Vulkan example block is entered for a non-default VULKAN member of a multi-renderer build; registrations set no renderer selection, so executables may run the build default (the EasyGL and HEADLESS blocks guard on _cna_default_renderer_identity, VKPAR-0003/0016). | modules/renderers/vulkan/examples/CMakeLists.txt; modules/renderers/CMakeLists.txt |
| `graphics/vulkan.html` | Partial-construction cleanup inside VulkanRenderer (raw Vk handles created before a throwing constructor step) is unaudited. | modules/renderers/vulkan/src/VulkanRenderer.cpp (VulkanRenderer::VulkanRenderer) |
| `input/events.html` | FNA_KEYBOARD_USE_SCANCODES is honoured only by the legacy bridge and Keyboard::GetKeyFromScancodeEXT; no platform keyboard service consults it, so Keyboard::GetState is not layout-independent under it, while docs/platform-input-notes.md still describes scancode mode as layout-independent polling. | modules/input/src/Internal/SdlInputBridge.cpp (use_scancode_mode); modules/platform/src/Sdl3/Sdl3InputServices.cpp (Sdl3Keyboard::Update); docs/platform-input-notes.md |
| `input/events.html` | Comments in Sdl3Platform.cpp and X11Platform.cpp (and plans/plan_platform.md) refer to Game::UpdateInput(), which does not exist; the pump is the tail of Game::PollEvents. | modules/platform/src/Sdl3/Sdl3Platform.cpp; modules/platform/src/X11/X11Platform.cpp; modules/runtime/src/Game.cpp |
| `input/events.html` | Game::PollEvents' F9/F10 renderer debug hooks have no build guard, so every game build reserves those keys for the renderer's context-loss seam (a no-op except in renderer families that override it). | modules/runtime/src/Game.cpp (PollEvents, KeyEvent branch); modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp (DebugSimulateContextLoss) |
| `input/events.html` | Wheel granularity differs between backends: SDL3 snapshots discard sub-notch motion, the Win32 snapshot accumulates raw WHEEL_DELTA units. | modules/platform/src/Sdl3/Sdl3InputServices.cpp (AccumulateWheel); modules/platform/src/Win32/Win32EventMapper.cpp (TranslateMouseWheel) |
| `input/events.html` | InputManager::SetMouseRelativeMode has no non-test caller, so the legacy accumulator's relative-delta path never accumulates in production. | modules/input/src/Internal/InputManager.cpp; call-site search of modules excluding tests |
| `input/events.html` | docs/input-backend.md says SdlInputBridge retains a raw ProcessEvent overload for legacy SDL-shaped tests and cites src/Input/Internal/InputManager.cpp; at this snapshot SdlInputBridge.hpp declares no ProcessEvent and the file is src/Internal/InputManager.cpp. | docs/input-backend.md section 1; modules/input/include/CNA/Internal/Input/SdlInputBridge.hpp |
| `invariants/index.html` | Reordering Game's data members is not guarded by any assertion; the platform-outlives-dependents rule is held by declaration order and comments only | modules/runtime/include/Microsoft/Xna/Framework/Game.hpp; modules/runtime/tests/Microsoft/Xna/Framework/GamePlatformOwnershipTests.cpp |
| `invariants/index.html` | The header comment of GraphicsRendererSelection says the latch closes when the first device begins construction; the code latches at the end of a successful resolveRenderer | modules/core/include/CNA/GraphicsRendererSelection.hpp; modules/graphics/src/Xna/GraphicsDevice.cpp (resolveRenderer) |
| `invariants/index.html` | A newly added GraphicsCapability enumerator is answered true by the default IGraphicsRenderer::SupportsCapability unless the device routes it to an opt-in query; nothing enforces an explicit answer per renderer | modules/graphics/include/CNA/Internal/Renderers/Common/IGraphicsRenderer.hpp |
| `modules/core.html` | modules/core/CMakeLists.txt still adds -lembind for Emscripten although the browser preference reader no longer uses embind. | modules/core/CMakeLists.txt; modules/core/src/GraphicsRendererSelectionEmscripten.cpp |
| `modules/core.html` | An invalid or not-compiled-in CNA_GRAPHICS_RENDERER value (or Module.cnaPreferredRenderer) is read during static initialisation through PublishAvailable, so it terminates the process instead of throwing into game code. No test sets an invalid value. | cmake/templates/CnaRendererRegistry.generated.cpp.in (cnaRendererRegistryPublished); modules/core/src/GraphicsRendererSelection.cpp (PublishAvailable, RebuildAttemptOrder, ConsultEnvironmentOnce) |
| `modules/core.html` | Logger::minimumLevel_ is a plain static read without the lock, and the user sink is called under a non-recursive mutex, so concurrent level changes race and a re-entrant sink deadlocks; neither is tested. | modules/core/src/Logger.cpp (Log, SetMinimumLevel, SetSink); modules/core/tests/CNA/LoggerTests.cpp |
| `modules/core.html` | Renderer selection state is unsynchronised apart from the latch flag; ThrowLatched reads the active identity non-atomically. | modules/core/src/GraphicsRendererSelection.cpp (SelectionState, ThrowLatched) |
| `modules/core.html` | The IsLatched() header comment says the latch happens when the first device begins construction; the implementation latches on successful resolution. A probe_core comment in ModuleProbes.cmake still describes SDL3 in Logger.cpp. | modules/core/include/CNA/GraphicsRendererSelection.hpp; modules/graphics/src/Xna/GraphicsDevice.cpp (resolveRenderer); cmake/Tests/ModuleProbes.cmake |
| `modules/design.html` | The comment on supportStringConvert in MathTypeConverter.hpp says only BoundingBox, BoundingSphere and Ray clear the flag and that the matrix and rectangle converters leave it set; the constructors clear it for Rectangle, Matrix, BoundingBox, BoundingSphere, Plane and Ray. | modules/design/include/Microsoft/Xna/Framework/Design/MathTypeConverter.hpp; modules/design/src/Converters.cpp; FrameworkDesignTests.cpp (expectStringInputDisabled) |
| `modules/design.html` | Registration passes only a TypeConverterAttribute per type, so TypeDescriptor::GetProperties(Type) is empty for these types and property descriptors are reachable only through the converter; CNA's docs/framework-design.md describes registration as also carrying ordered descriptor collections. | modules/design/src/Registration.cpp (registerConverter); Sharp Runtime TypeDescriptor.cpp (GetProperties) read at 41b918c9; docs/framework-design.md |
| `modules/design.html` | The descriptors' OnValueChanged call delivers no notification at the read Sharp Runtime revision (protected no-op hook, no handler list). | modules/design/include/CNA/Internal/Design/PropertyDescriptors.hpp; Sharp Runtime PropertyDescriptor.hpp at 41b918c9 |
| `modules/design.html` | Registration depends on a header include plus a link; nothing orders it against other static initialisers and no test covers a lookup from another translation unit's static initialiser. | modules/design/include/CNA/Internal/Design/Registration.hpp |
| `modules/design.html` | No automated oracle, no consuming-editor test, no cross-thread test; no workflow names CnaDesignTests (the unfiltered CnaTests run would include the cases). | modules/design/tests; .github/workflows/general-tests-ci.yml |
| `modules/devices-ext.html` | If the platform dialog service throws PlatformException after the callback was moved into it, FileDialog::Show* swallows the exception and the callback is never invoked, so a caller can wait forever for a dialog that never opened; MessageBox::Show reports the same failure as -1, indistinguishable from a dismissal. No test makes the service throw. | modules/devices-ext/src/FileDialog.cpp; modules/devices-ext/src/MessageBox.cpp; modules/devices-ext/tests/CNA/Devices |
| `modules/devices-ext.html` | A FileDialog call with an empty callback reaches the platform, which throws PlatformException (SDL3 and canned services), and the wrapper swallows it silently. | modules/platform/src/Sdl3/Sdl3SystemServices.cpp (RequireCallback); modules/devices-ext/src/FileDialog.cpp |
| `modules/devices-ext.html` | The three FileDialog::Show* functions do not apply the Web/iOS target policy that getIsSupportedProperty applies, and always pass a null parent window. | modules/devices-ext/src/FileDialog.cpp |
| `modules/devices-ext.html` | Camera.hpp documents that TryAcquireFrame returns false when the state is not Ready, but the wrapper never tests the state; behaviour depends on the platform session. CameraState::Closed is declared and never produced. | modules/devices-ext/include/CNA/Devices/Camera.hpp; modules/devices-ext/src/Camera.cpp; CameraState.hpp |
| `modules/devices-ext.html` | The canned dialog service invokes the file-dialog callback before returning, unlike a real platform whose callback fires after the Show* call on the event-pumping thread, so no test shows the asynchronous timing. | modules/platform/tests/CNA/Platform/CannedDialogs.hpp; modules/platform/include/CNA/Platform/IPlatformSystemServices.hpp |
| `modules/devices-ext.html` | No test drives the SDL3 camera provider, dialogs or tray implementation itself; SystemTray callback thread and lifetime rely on the backend. | modules/platform/src/Sdl3/Sdl3Camera.cpp; Sdl3Tray.cpp; modules/devices-ext/tests |
| `modules/devices.html` | Documented, accepted gap: Dispose reads started_ under its lock, releases it, then calls Stop; a concurrent Start on the same instance in that gap can leave a disposed instance whose backend is still logically started. Not a supported usage pattern. | docs/devices-thread-safety.md; modules/devices/src/Sensors/Accelerometer.cpp (Dispose) |
| `modules/devices.html` | SensorOwnerControlBlock's stated remaining boundary: another thread destroying a Compass/Motion owner while a callback that already passed its owner check runs inside it is unsupported. | modules/devices/include/Microsoft/Devices/Sensors/Detail/SensorOwnerControlBlock.hpp |
| `modules/devices.html` | AndroidSensorBridge abandons a worker that does not exit within 5 s and can never Start again; the abandoned native call cannot be cancelled. By design, unverified on hardware. | modules/devices/src/Sensors/Detail/AndroidSensorBridge.cpp (Stop, abandoned_) |
| `modules/devices.html` | Production code never calls DevicesShutdownCoordinator::Shutdown; correctness at process exit relies on the atexit fallback and the platform-identity guard, and the explicit path is exercised only by tests and a harness that shows no crash outside sanitizer builds. | modules/devices/src/Detail/DevicesShutdownCoordinator.cpp; tools/devices/shutdown_ordering_harness.cpp |
| `modules/devices.html` | PlatformVibrateBackend::EnsureService drops its cached service without releasing the old platform's Haptic subsystem reference when the selected platform changes (the identity check in ReleaseService fails by design). | modules/devices/src/Detail/PlatformVibrateBackend.cpp (EnsureService, ReleaseService) |
| `modules/devices.html` | In release (NDEBUG) builds a swallowed handler exception is recorded only in NativeDiagnosticSink's counter and last-record hooks; no log line is written. | modules/devices/src/Sensors/Detail/NativeDiagnostic.cpp |
| `modules/devices.html` | docs/devices-event-contract.md section 6 still describes the Android bridge as swallowing exceptions silently; source routes them through NativeDiagnosticSink. docs/devices-thread-safety.md's instanceCountMutex_ statement is true only for Compass and Motion. | docs/devices-event-contract.md; docs/devices-thread-safety.md; AndroidSensorBridge.cpp (Run); Accelerometer.cpp |
| `modules/devices.html` | devices-tests.yml's exact-name filter omits seven suites present in modules/devices/tests and runs only the devices-ubsan preset (no TSan or ASan lane). | .github/workflows/devices-tests.yml; modules/devices/tests |
| `modules/diagnostics.html` | Gauges maintained with GAUGE_ADD deltas (Audio/AllocatedVoices) drift after a runtime Off window: AddMetric/SetMetric discard updates while Off and nothing resynchronises; only mixer-engine shutdown resets the gauge to 0. The mode-transition test covers a counter only. | modules/diagnostics/src/Diagnostics.cpp (AddMetric, SetMetric); modules/audio/src/Backend/CnaMixer/MixerEngine.cpp and Sdl3Mixer/MixerEngine.cpp (GAUGE_ADD +/-1, EndMixerEngineShutdown); DiagnosticsTests OffStatsAndFullTransitionsPreserveMetricSemantics |
| `modules/diagnostics.html` | The frame is process-wide: an application BeginFrame inside a running Game is counted malformed, and its EndFrame completes Game's frame early, after which Game's FrameScope ends silently. No test covers the interaction. | modules/diagnostics/src/Diagnostics.cpp (TryBeginFrame, FinishFrame reportMalformed); modules/runtime/src/Game.cpp (Tick) |
| `modules/diagnostics.html` | On Emscripten the published frame covers only the main-loop callback (CNA_WaitForAnimationFrame is outside it), so FrameSample.framesPerSecond is a work rate, not the display rate; CNA's docs/diagnostics.md does not say so. | modules/runtime/src/Game.cpp (RunLoop, EmscriptenMainLoopCallback); docs/diagnostics.md |
| `modules/diagnostics.html` | No CI workflow or configure preset sets CNA_DIAGNOSTICS, so 38 of 40 DiagnosticsTests and all 7 graphics/audio diagnostics tests are never compiled in CI. | .github/workflows (no CNA_DIAGNOSTICS); CMakePresets.json; DiagnosticsTests.cpp level gates |
| `modules/diagnostics.html` | No production IDiagnosticsSource exists and no producer uses ResourceKind::AudioVoice; both are extension seams only. | repository grep at 009d40f5 (only tests and inspector mapping reference them) |
| `modules/gamer-services.html` | LeaderboardReader::Read, PageDown and PageUp (synchronous wrappers) never delete the LeaderboardAction their Begin call allocates, unlike Gamer::GetProfile and SignedInGamer::GetAchievements; one small heap object per call appears to be left behind. No test can observe it (private type, no counter). | modules/gamer-services/src/Xna/LeaderboardReader.cpp (Read, PageDown, PageUp, CompleteReadEXT, CompletePageEXT) |
| `modules/gamer-services.html` | GamerServicesDispatcher::Initialize deletes every SignedInGamer* in whatever collection is currently installed, so gamers an application published via Gamer::setSignedInGamersProperty would be deleted by a later Initialize. | modules/gamer-services/src/Xna/GamerServicesDispatcher.cpp (Initialize loop), Gamer.cpp (setSignedInGamersProperty) |
| `modules/gamer-services.html` | LocalGamerServicesStore write path ignores stream and directory errors and stores int64/tick values as doubles; DateTime ticks for present-day dates exceed 2^53 and are rounded on write. No test asserts an exact 64-bit round trip. | modules/gamer-services/src/Internal/LocalGamerServicesStore.cpp (WriteJsonFile, SaveEarnedAchievementEXT); modules/content/include/CNA/Internal/Json.hpp (WriteJsonValue) |
| `modules/gamer-services.html` | SignedIn is raised only inside Initialize and SignedOut is never raised by production code; a late subscriber sees no sign-in events. | GamerServicesDispatcher.cpp; SignedInGamer.cpp (OnSignOut has no caller outside SignedInGamerTestAccess) |
| `modules/gamer-services.html` | Sanitised file names are many-to-one, so gamertags or leaderboard keys differing only in punctuation share a file. | LocalGamerServicesStore.cpp (SanitizeStoreFileNameComponent) |
| `modules/graphics-ext.html` | RenderPipeline::releaseDeviceResourcesEXT (the DeviceReset handler) resets the scene target and the chain's pool but not the private RenderTargetPools of BloomPass, SsaoPass and VolumetricFogPass (resize resets Bloom and SSAO only); getGpuMemoryEstimateBytes counts neither, so DeviceLossTest cannot observe them. Whether the pooled targets stay valid after a context loss is renderer-dependent and was not established. | modules/graphics-ext/src/RenderPipeline.cpp; BloomPass.cpp, SsaoPass.cpp (resetTargets); include/CNA/Graphics/VolumetricFogPass.hpp (pool_); tests/CNA/Graphics/DeviceLifecycleTests.cpp |
| `modules/graphics-ext.html` | A throwing transparent-phase or shadow-caster callback is not recovered: end() has already lowered frameOpen_ (scene target left bound, depth/blend state left altered), and begin() has already raised it (shadow map begin/end unpaired). | RenderPipeline.cpp (begin, end, drawTransparentPhase) |
| `modules/graphics-ext.html` | The shadow_caster shader package has no ShaderPackageReproducibility ctest, and no CI workflow runs the generator check for any graphics-ext package. | cmake/Tests/ModuleProbes.cmake (ten source-package tests, none for shadow_caster); .github/workflows (no generate_shader_package invocation) |
| `modules/graphics-ext.html` | ClusteredForwardEffectTests pin the packaged ES fragment against five helper GLSL texts but nothing pins the desktop fragment; no test names the desktop variant. | modules/graphics-ext/tests/CNA/Graphics/ClusteredForwardEffectTests.cpp (PackagedEsSourceRetainsEveryPublicGlslFragment) |
| `modules/graphics-ext.html` | No single test asserts the whole fifteen-slot pass order; RenderPipelineTests assert the SSAO/SSR/DOF/bloom/tonemap/FXAA relations only. | RenderPipelineTests.cpp |
| `modules/inspector.html` | ServerHello.graphicsRenderer reports the compile-time default renderer identity; in a multi-renderer build the Session view does not show the runtime-selected or fallback renderer. | modules/inspector/src/Agent.cpp (HandleClient); modules/core/include/CNA/GraphicsRendererType.hpp; cmake/RendererSelection.cmake |
| `modules/inspector.html` | Diagnostics accepts metric/event names up to 1 MiB and does not bound resource labels, while the Inspector protocol caps strings at 4096 bytes; one oversized name or label makes every affected snapshot/events response fail with LimitExceeded instead of truncating. | modules/diagnostics/src/Diagnostics.cpp (MaximumTraceNameBytes, RegisterMetric, CopyResource); modules/inspector/src/Protocol.cpp (Writer::String, MaximumStringBytes); modules/inspector/src/Agent.cpp (HandleRequest) |
| `modules/inspector.html` | No automated test exercises WebBridge (Host check, UI-token enforcement, 32-connection cap, request parsing, response headers). | modules/inspector/tests/CNA/Inspector/InspectorTests.cpp; modules/inspector/src/WebBridge.cpp |
| `modules/inspector.html` | No test calls Agent::Stop explicitly, stops with a client attached, or restarts an agent. | modules/inspector/tests/CNA/Inspector/InspectorTests.cpp |
| `modules/inspector.html` | Possible Stop latency race: a connection accepted just before Stop publishes it as activeClient_ is not shut down, so join waits for that connection's handshake reads (3 s each for header and payload) to finish or time out. | modules/inspector/src/Agent.cpp (Impl::Stop, Impl::Run, HandleClient) |
| `modules/inspector.html` | Pending preview tickets are dropped on client disconnect with no cancel call to IResourcePreviewProvider; a provider must bound its own outstanding readbacks. | modules/inspector/src/Agent.cpp (Run: pendingPreviews_.clear()); modules/inspector/include/CNA/Inspector/Agent.hpp |
| `modules/inspector.html` | No renderer implements IResourcePreviewProvider; preview capability is never negotiated in real sessions. | repository grep for IResourcePreviewProvider (only modules/inspector and its benchmark) |
| `modules/math.html` | Vector3::Length, LengthSquared, Distance and DistanceSquared accumulate in float, although the oracle-test comments record that XNA accumulates x*x+y*y+z*z wider than float; only BoundingSphere.cpp's private helpers reproduce that. No oracle covers the public functions. | modules/math/src/Vector3.cpp (Length, LengthSquared, Distance, DistanceSquared); modules/math/src/BoundingSphere.cpp (SquaredLength, WideLength, WideDistance); modules/math/tests/Microsoft/Xna/Framework/BoundingSphereOracleTests.cpp header comment |
| `modules/math.html` | Curve step continuity returns next.Value when position >= 1.0f (an absolute constant) and prev.Value otherwise; the only Step test evaluates at 0.5 in a 0..1 segment. XNA parity of the >= 1 branch is not established. | modules/math/src/Curve.cpp (GetCurvePosition); CurveTest.StepContinuityReturnsCurrentSegmentValue |
| `modules/math.html` | Curve::ComputeTangent uses MathHelper::WithinEpsilon for the smooth in tangent and /pn/ < denorm_min for the smooth out tangent; the only smooth-tangent test uses equal values. | modules/math/src/Curve.cpp (ComputeTangent); CurveTest.ComputeTangentSmoothMiddleKey |
| `modules/math.html` | Cyclic loop modes divide by last.Position - first.Position without a guard (infinity cast to int for a zero span) and Evaluate divides by the segment width even when duplicate key positions were accepted by CurveKeyCollection::Add. | modules/math/src/Curve.cpp (GetNumberOfCycle, GetCurvePosition); modules/math/src/CurveKeyCollection.cpp (Add) |
| `modules/math.html` | Matrix::Decompose uses a file-local epsilon (FLT_EPSILON, <=) while MathHelper::WithinEpsilon uses MachineEpsilonFloat (<); a comment calls them typically identical. | modules/math/src/Matrix.cpp (WithinEpsilon, Decompose); modules/math/src/MathHelper.cpp |
| `modules/math.html` | CheckForNaNs is inconsistent (Vector2/3/4 always throw, Matrix and Quaternion only without NDEBUG) and is never called in the snapshot. | modules/math/src/Vector2.cpp, Vector3.cpp, Vector4.cpp, Matrix.cpp, Quaternion.cpp |
| `modules/math.html` | CreateLookAt and the perspective/orthographic builders have no oracle and no value-level unit test (only M44, thrown cases and M34 are asserted); their terms are exercised only through consumer tests in other modules. | modules/math/tests/Microsoft/Xna/Framework/MatrixTests.cpp |
| `modules/math.html` | BoundingFrustum::Intersects(Ray) never computes an entry distance and throws NotImplementedException for an origin on a plane; Contains(point) stops at the first plane the point lies exactly on. | modules/math/src/BoundingFrustum.cpp; BoundingFrustumTest.IntersectsRay* |
| `modules/math.html` | Exception families differ across near-identical functions (BoundingBox::CreateFromPoints throws System::ArgumentException, BoundingSphere::CreateFromPoints std::invalid_argument; GetCorners uses ArgumentOutOfRange for the box and std::out_of_range for the frustum). | modules/math/src/BoundingBox.cpp, BoundingSphere.cpp, BoundingFrustum.cpp |
| `modules/media.html` | MediaPlayer::Play(Song*) clears the queue and then reads the given Song. If the caller passes a Song owned by the queue (for example the active song), Clear() has already destroyed it, so LoadSong reads a destroyed object. Source-read; not executed; no test found. | modules/media/src/Xna/MediaPlayer.cpp (Play(Song*), LoadSong); modules/media/src/Xna/MediaQueue.cpp (Clear) |
| `modules/media.html` | Play(Song*) compares the old queue's first song (a copy) with the caller's pointer to decide whether to flag ActiveSongChanged, so for library or standalone songs the flag is raised on every call, including replaying the same song. | modules/media/src/Xna/MediaPlayer.cpp (Play(Song*)) |
| `modules/media.html` | Play(const SongCollection&, index) does not set the ActiveSongChanged dispatcher flag (only the state-change flag, via PlaySong), whereas Play(Song*), NextSong and the end-of-queue path do. | modules/media/src/Xna/MediaPlayer.cpp (Play(const SongCollection&, index), Play(Song*), NextSong, Update) |
| `modules/media.html` | Play(Song*) passes the caller's Song to PlaySong, so the mixer duration and play count land on the library's Song, and Stop() only resets play counts on queue copies; comments in MediaLibrary.cpp still say MediaPlayer::Play operates on a duplicate and never backfills library songs. | modules/media/src/Xna/MediaPlayer.cpp; modules/media/src/Xna/MediaLibrary.cpp (BuildFromRoots comment) |
| `modules/media.html` | A failed PlaySong (load, track creation, attach or start failure) returns before the timer and state are touched. If the player was already Playing, the old track has been destroyed but the state stays Playing with no track and no ended flag, so Update never advances. Source-read; not executed. | modules/media/src/Xna/MediaPlayer.cpp (PlaySong, Update) |
| `modules/media.html` | MediaPlayer::ProgramExit is documented as called at application exit but has no runtime call site; within modules only the C API route cna_media_player_program_exit_ext calls it. | modules/media/include/Microsoft/Xna/Framework/Media/MediaPlayer.hpp; modules/c-api/src/CnaCApiMediaPlayer.cpp |
| `modules/media.html` | A Song copy that fails to construct (file removed after the scan) throws FileNotFoundException out of Play after the queue was already cleared and possibly partly filled. | modules/media/src/Xna/Song.cpp (constructor); modules/media/src/Xna/MediaPlayer.cpp (Play, LoadSong) |
| `modules/media.html` | Without a mixer (SDL2 or NULL audio) queue copies never receive a duration and DetectSongEndedByElapsedTime ignores zero durations, so songs never auto-advance in that profile. | modules/media/src/Xna/MediaPlayer.cpp (PlaySong under SOUND_ENABLED, Update, DetectSongEndedByElapsedTime) |
| `modules/media.html` | VideoPlayer keeps a raw Video* and Video keeps a raw VideoPlayer* parent; CloseDecoder writes through the stored Video*, so destroying a Video before its player is stopped, disposed or destroyed would write through a dangling pointer. The header states no lifetime rule. Source-read; not executed. | modules/media/src/Xna/Video/VideoPlayer.cpp (CloseDecoder, Play); modules/media/include/Microsoft/Xna/Framework/Media/Video/Video.hpp |
| `modules/media.html` | VideoPlayer::GetTexture does not catch exceptions from the decoder (std::runtime_error for decode, resample and I/O errors), and the header documents only ObjectDisposedException for it. | modules/media/src/Xna/Video/VideoPlayer.cpp (GetTexture); modules/media/include/Microsoft/Xna/Framework/Media/Video/VideoPlayer.hpp |
| `modules/media.html` | MediaLibraryIndex.hpp's class comment lists four scanned extensions while the implementation also indexes .flac and .opus. | modules/media/include/CNA/Internal/Media/MediaLibraryIndex.hpp; modules/media/src/Internal/MediaLibraryIndex.cpp |
| `modules/media.html` | No test found for: the natural song-end path under SDL3_mixer, the no-mixer or failed-install visualization branch, MediaLibrary::Dispose semantics, or a throwing path in SavePicture beyond the sanitizer cases. | modules/media/tests (MediaPlayerTests.cpp, MediaLibraryTests.cpp) |
| `modules/net.html` | Packets sent through a Local (or LocalWithLeaderboards) session are never delivered, even to the sender's own local gamers, because the PacketSend branch of Update is gated on RealNetworkingEnabled; the test named SendDataThenReceiveDataRoundtrip asserts non-delivery. | modules/net/src/Xna/NetworkSession.cpp (Update); modules/net/tests/Microsoft/Xna/Framework/Net/LocalNetworkGamerTests.cpp |
| `modules/net.html` | Possible double delivery of GamerJoined to a handler subscribed after AddRemoteGamer added a gamer but before the next Update (replay hook plus queued event). Derived from reading; no test covers the window. | NetworkSession.cpp (constructor SetReplayHook, AddRemoteGamer, Update) |
| `modules/net.html` | Every SystemLink session, including one created by Join, starts an accepting ENet host and registers as a discovery host; a joiner therefore also answers discovery queries with its own port. | NetworkSession.cpp constructor; ENetBackend.cpp StartHosting; ENetDiscoveryService.cpp ReplyToQuery |
| `modules/net.html` | ENetHostHandle::Send drops a packet silently on allocation or enet_peer_send failure; ENetLibrary has no deinitialise call; ENetDiscoveryService keeps one registered host pointer. | ENetHostHandle.cpp; ENetLibrary.cpp; ENetDiscoveryService.cpp |
| `modules/net.html` | Emscripten: EndJoin does not wait for the welcome, discovery is stubbed, hosting uses a fixed port and ConnectToHost rebuilds the state as a client-only host; none of this is covered by a test named here. | NetworkSession.cpp (#ifndef __EMSCRIPTEN__ block); ENetBackend.cpp; ENetDiscoveryService.cpp |
| `modules/phone.html` | PhoneApplicationService::getCurrentProperty() is a function-static; attached to a Game that is destroyed first (e.g. a Game on main's stack), its destructor runs DetachEXT against a dangling Game* at static destruction. Nothing detaches automatically; no test covers this order. | modules/phone/src/PhoneApplicationService.cpp (getCurrentProperty, ~PhoneApplicationService, DetachEXT) |
| `modules/phone.html` | ReadRequestBody's comment says unsupported requests are refused rather than delivered truncated, but a request without Content-Length (including chunked) gets 200 and is dropped, and a body shorter than Content-Length gets 200 and is queued truncated; body size is unbounded. | modules/phone/src/HttpNotificationChannel.cpp (ReadRequestBody, Listen) |
| `modules/phone.html` | No receive timeout on accepted connections; a stalled peer parks the single listener thread and Close()/the destructor join on it. Source-level; runtime shutdown latency unverified. | modules/phone/src/HttpNotificationChannel.cpp (Listen, Close) |
| `modules/phone.html` | RawPushNotificationMessage/ToastPushNotificationMessage::SendAsync are documented to return true 'when the receiver accepted it' but return true whenever the request bytes were written; an HTTP 400 still returns true. | modules/phone/include/Microsoft/Phone/Notification/PushNotificationSender.hpp vs modules/phone/src/PushNotificationSender.cpp (Post) |
| `modules/phone.html` | Toast body concatenates Title/SubTitle into XML without escaping. | modules/phone/src/PushNotificationSender.cpp (ToastPushNotificationMessage::SendAsync) |
| `modules/phone.html` | Duplicate channel names: Open overwrites the registry entry, and the later channel's Close erases the name while the earlier channel is still open, so Find returns null for a live channel. | modules/phone/src/HttpNotificationChannel.cpp (Open, Close, Find) |
| `modules/phone.html` | A handler exception during DispatchPendingNotificationsEXT propagates and discards the rest of the already-swapped batch. | modules/phone/src/HttpNotificationChannel.cpp (DispatchPendingNotificationsEXT) |
| `modules/phone.html` | Windows notification path: no WSAStartup in modules/phone; 'listener < 0' / 'connection < 0' never detect INVALID_SOCKET (unsigned SOCKET); HttpNotificationChannelTests.cpp (with PushNotificationSenderTest) excluded under WIN32, so the Winsock path has no test. | modules/phone/src/HttpNotificationChannel.cpp; modules/phone/src/PushNotificationSender.cpp; cmake/UnitTests.cmake (WINNATIVE-0014 exclusion) |
| `modules/phone.html` | Channel and sender sockets send with flags 0 and set no SO_NOSIGPIPE, unlike the Inspector socket layer; SIGPIPE exposure against a resetting peer is unverified. | modules/phone/src/HttpNotificationChannel.cpp (Listen); modules/phone/src/PushNotificationSender.cpp (Post); modules/inspector/src/InternalSocket.cpp |
| `modules/phone.html` | Concurrency: listenSocket_ is a plain int read by the worker and reset by Close (formal data race); Open's listening_ check is not a compare-exchange; a persistent accept() error busy-loops without backoff. | modules/phone/include/Microsoft/Phone/Notification/HttpNotificationChannel.hpp; modules/phone/src/HttpNotificationChannel.cpp (Open, Listen, Close) |
| `modules/phone.html` | CNA's docs/physical-modules.md lists phone's Sharp Runtime components as Core.Base, Collections.Core; modules/phone/CMakeLists.txt also links Uri. | docs/physical-modules.md; modules/phone/CMakeLists.txt |
| `modules/storage.html` | A failed root resolution latches an empty root: EnsureStorageRoot sets storageRootInitialized_ before resolving, so after one failure every later call returns the empty string; core helpers treat an empty root as '.', so containers open and titles delete under the working directory while getIsConnectedProperty reports false. Only another SetAppNameEXT resets it. | modules/storage/src/StorageDevice.cpp (EnsureStorageRoot, SetAppNameEXT); modules/core/include/CNA/Internal/PathContainment.hpp; PathContainmentTest.EmptyBaseDirTreatsCurrentDirectoryAsBase |
| `modules/storage.html` | getFreeSpaceProperty, getTotalSpaceProperty and getIsConnectedProperty pass the UTF-8 root through the narrow std::filesystem::path constructor, so with a non-ASCII root on Windows they can query another path. | modules/storage/src/StorageDevice.cpp (the three properties); modules/core/include/CNA/Internal/PathUtf8.hpp |
| `modules/storage.html` | DeleteFile and DeleteDirectory do not check the entry type; GlobMatch treats '?' as one byte. | modules/storage/src/StorageContainer.cpp |
| `modules/storage.html` | A save container titled GamerServices shares a directory with the gamer-services local store, so DeleteContainer("GamerServices") deletes local achievements and leaderboards. | modules/gamer-services/src/Internal/LocalGamerServicesStore.cpp (StoreRoot); modules/storage/src/StorageDevice.cpp (DeleteContainer) |
| `modules/storage.html` | Container operations keep working after Dispose, streams outlive container and device, and FileShare is ignored (also through the C route). | modules/storage/src/StorageContainer.cpp (Dispose, OpenFile); modules/c-api/src/CnaCApiStorage.cpp |
| `modules/storage.html` | StorageDeviceTests writes to and removes the real per-user data root of a fixed application name; the CnaTests registration sets no storage environment. | modules/storage/tests/Microsoft/Xna/Framework/Storage/StorageDeviceTests.cpp (SetUp, TearDown); cmake/UnitTests.cmake |
| `modules/storage.html` | Several comments still say StorageDevice reaches the platform or SDL preference path, which no longer holds; GetPreferencesPath has no production caller at TARGET. | modules/platform/include/CNA/Platform/CurrentPlatform.hpp; IPlatformSystemServices.hpp (IPlatformFileSystem); modules/runtime/include/Microsoft/Xna/Framework/Game.hpp |
| `modules/video-ffmpeg.html` | A genuine switch to a different video stream does not seek the demuxer or flush, so the new codec context starts at the current position; no fixture has a second video stream and only the same-stream no-op and out-of-range cases are tested. | modules/video-ffmpeg/src/VideoDecoder.cpp (OpenVideoStreamByIndex, SetVideoStream); modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp (comment on SetVideoStreamReselectingTheSameStreamPreservesDimensions) |
| `modules/video-ffmpeg.html` | The pixel-conversion path applies one BT.601 integer matrix without range expansion or a BT.709 branch to all planar formats, including YUVJ; colour checks are +/-20 tolerance on one frame of synthetic bars. | modules/video-ffmpeg/src/VideoDecoder.cpp (yuv_planar8_to_rgba, yuv_planar16_to_rgba, ConvertFrameToRGBA); modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp |
| `modules/video-ffmpeg.html` | The FFmpeg probe states no minimum version while the sources rely on the FFmpeg 5.1-era channel-layout API, so an older pkg-config-visible FFmpeg would likely fail at compile time instead of configure time (not tried). | modules/CMakeLists.txt (pkg_check_modules); modules/video-ffmpeg/src/VideoDecoder.cpp (swr_alloc_set_opts2, ch_layout) |
| `modules/video-ffmpeg.html` | Trailing truncation of a Matroska file is reported as a clean end of stream rather than an error, so a truncated file plays to the point of truncation without any exception. | modules/media/tests/CNA/Internal/Media/VideoDecoderTests.cpp (TruncatedFileEndsAtCleanEOFRatherThanThrowing) |
| `platforms/headless.html` | HeadlessWindow accepts WindowDescription::fullscreenMode = BorderlessFullscreen at creation although the platform reports borderlessFullscreen=false and SetFullscreenMode refuses it; the conformance case only covers the setter. | modules/platform/src/Headless/HeadlessPlatform.cpp (HeadlessWindow constructor vs SetFullscreenMode); modules/platform/tests/CNA/Platform/PlatformConformanceTests.cpp (BorderlessFullscreenFollowsItsCapability) |
| `platforms/headless.html` | The CnaPlatformTests gtest filter lists a HeadlessPlatform* token that matches no test suite at TARGET (dead filter token; Headless coverage comes from the parameterised conformance instances). | cmake/UnitTests.cmake (CnaPlatformTests registration); no TEST*(HeadlessPlatform…) in modules/ |
| `platforms/headless.html` | HeadlessPlatform::InjectEvent has no caller anywhere in the TARGET tree. | modules/platform/src/Headless/HeadlessPlatform.hpp; repository grep |
| `platforms/index.html` | No CMake or CTest test exercises the reserved-identifier refusal path (SDL12, EMSCRIPTEN, WIN32 off Windows, TERMINAL on Windows). | cmake/PlatformSelection.cmake; cmake/Tests/ (only WaylandPlatformSelection.cmake covers selection, for Wayland) |
| `platforms/index.html` | CNA's own docs/platform-abstraction.md 'Implementations' table lists five backends and omits X11 and WAYLAND. | docs/platform-abstraction.md (Implementations section) vs cmake/PlatformSelection.cmake |
| `platforms/index.html` | The selection files do not exclude HEADLESS, TERMINAL or SDL2 on macOS/Emscripten, nor X11 on macOS, so unvalidated combinations are offered by CMake. | cmake/PlatformSelection.cmake (_cna_platforms_available); cmake/PlatformX11.cmake (cna_detect_x11 exclusion list) |
| `platforms/sdl2.html` | The SDL2-only configure guard lists VULKAN among recommended SDL-independent renderers, but the SDL2 platform has no Vulkan surface service, so a VULKAN build on SDL2 can only fail at renderer construction. | cmake/Sdl2OnlyConfiguration.cmake (error text); modules/platform/src/Sdl2/Sdl2Platform.cpp (GetVulkanSurface); modules/graphics/include/CNA/Internal/Renderers/Common/PlatformVulkanRendererState.hpp |
| `platforms/sdl2.html` | Sdl2OnlyConfiguration.cmake checks only CNA_GRAPHICS_RENDERER, not the CNA_GRAPHICS_RENDERERS multi-renderer list; no other gate for SDL2 plus an SDL3-linked family in that list was found in cmake/Renderer*.cmake. | cmake/Sdl2OnlyConfiguration.cmake; cmake/RendererCombinations.cmake |
| `platforms/sdl2.html` | SDL2 negates flipped wheel deltas while SDL3 passes SDL's adjusted values through, so the two backends can report opposite wheel signs under natural scrolling. | modules/platform/src/Sdl2/Sdl2Platform.cpp (PollEvents); modules/platform/src/Sdl3/Sdl3EventMapper.cpp (SDL_EVENT_MOUSE_WHEEL) |
| `platforms/sdl3.html` | Source comments in Sdl3Platform.cpp and X11Platform.cpp refer to Game::UpdateInput(), which does not exist; the controller-pump gate is in Game::PollEvents. | modules/platform/src/Sdl3/Sdl3Platform.cpp; modules/platform/src/X11/X11Platform.cpp; modules/runtime/src/Game.cpp |
| `platforms/sdl3.html` | Reading SDL3 capabilities starts SDL_INIT_CAMERA for the process lifetime (outside ownedRefCounts_) wherever SDL has a camera driver; deliberate per the source, but it means a capability query has side effects. | modules/platform/src/Sdl3/Sdl3Camera.cpp (EnsureCameraSubsystem); modules/platform/src/Sdl3/Sdl3Platform.cpp (GetCapabilities) |
| `platforms/terminal.html` | Game's IsMouseVisible setter forwards to IPlatformMouse::SetCursorVisible when a platform window exists; TerminalMouse::SetCursorVisible always throws PlatformException, and the Game comment still calls the terminal mouse null. A game changing IsMouseVisible on a queryable terminal would get an exception (by reading; not executed). | modules/runtime/src/Game.cpp (IsMouseVisible setter); modules/platform/src/Terminal/TerminalMouse.cpp (SetCursorVisible); TerminalMouseTests.cpp UnsupportedPointerControlsRefuseDeterministically |
| `platforms/terminal.html` | Window title is stored but never emitted: SetTitle's comment defers OSC emission to the session, but BuildSessionPrologue has no title sequence. | modules/platform/src/Terminal/TerminalPlatform.cpp (TerminalWindow::SetTitle); TerminalSession.cpp (BuildSessionPrologue) |
| `platforms/terminal.html` | TerminalPresenterThroughPlatformTest is not matched by the CnaPlatformTests aggregate gtest filter (token TerminalPresenter.*); it runs only through per-case discovery of CnaTests. | cmake/UnitTests.cmake (CnaPlatformTests filter); TerminalPresenterTests.cpp |
| `platforms/terminal.html` | SIGTSTP/SIGCONT are not in TerminalSession's restoring signal set; an externally stopped process leaves the terminal in session state until continued. | modules/platform/src/Terminal/TerminalSession.cpp (kRestoringSignals) |
| `platforms/terminal.html` | TerminalPlatform::queued_ is appended to every PollEvents but nothing ever fills it (dead state). | modules/platform/src/Terminal/TerminalPlatform.cpp (PollEvents), TerminalPlatform.hpp |
| `platforms/wayland.html` | No GitHub workflow selects CNA_PLATFORM=WAYLAND; the backend's suites are launched only locally through tools/platform/wayland_test_server.sh. | .github/workflows/platform-ci.yml; cmake/UnitTests.cmake |
| `platforms/wayland.html` | WaylandKeyboard::SetComposeSuppressed is declared but never called, so composeSuppressed_ is always false (an unused hook, not an observed behaviour defect). | modules/platform/src/Wayland/WaylandKeyboard.hpp (SetComposeSuppressed); no caller in modules/ or tools/ |
| `platforms/win32.html` | The CnaPlatformWindow.Owned window property is written and removed but never read; its comment claims AdoptWindowHandle refuses an owned HWND, while the code returns a borrowed wrapper sharing the owner's id (and a test asserts that). | modules/platform/src/Win32/Win32Window.cpp (kOwnedWindowProperty); modules/platform/src/Win32/Win32Platform.cpp (AdoptWindowHandle); Win32WindowTest.LegacyTokenRoundTripsThroughAdoption |
| `platforms/win32.html` | A foreign HWND adopted by handle is not subclassed (no CNA events) and is absent from windows_, so WGL CreateContext, Vulkan CreateSurface, relative-mouse targeting and Win32TextInput::Start refuse its id; no test covers foreign-HWND adoption with these services. | modules/platform/src/Win32/Win32Platform.cpp (FindWindow, AdoptWindowHandle); modules/platform/src/Win32/Win32GraphicsServices.cpp |
| `platforms/win32.html` | EnterExclusiveFullscreen returns silently when monitor info, EnumDisplaySettingsW or ChangeDisplaySettingsExW fails although its comment says a refusal is reported; GetFullscreenMode then reports ExclusiveFullscreen. The mode change requests the monitor's current size. | modules/platform/src/Win32/Win32Window.cpp (EnterExclusiveFullscreen, SetFullscreenMode) |
| `platforms/win32.html` | Win32GlContext ignores GlContextDescription multisample fields and per-channel sizes and WindowDescription::renderIntent/openGlFramebuffer; when wglCreateContextAttribsARB is absent or fails the legacy bootstrap context is returned while GetContextAttributes reports the requested description. | modules/platform/src/Win32/Win32GraphicsServices.cpp (DescribeFormat, CreateContext) |
| `platforms/win32.html` | Minor comment drift: Win32DirectXIntegrationTests.cpp cites spikes/win32-directx-spike/ (absent; spikes/win32-spike exists); Win32Common.hpp's comment says 'Windows 10 1703' while defining 0x0603. | modules/platform/tests/CNA/Platform/Win32DirectXIntegrationTests.cpp; modules/platform/src/Win32/Win32Common.hpp |
| `platforms/x11.html` | X11Platform.hpp documents that every Xlib call goes through one per-instance mutex, but no such mutex exists; X11Display.hpp likewise says the connection opens on first Video acquisition and closes on last release, while the code opens it in the constructor and closes it in the destructor. Header comments contradict the implementation. | modules/platform/src/X11/X11Platform.hpp, modules/platform/src/X11/X11Display.hpp, modules/platform/src/X11/X11Platform.cpp |
| `platforms/x11.html` | Several X11Platform.hpp accessor comments say services are 'null before Video', but services are created with the constructor's connection and are non-null whenever the connection succeeded; AcquireSubsystem's @throws PlatformNotSupportedException is never thrown. | modules/platform/src/X11/X11Platform.hpp vs X11Platform.cpp (OpenConnection, AcquireSubsystem) |
| `platforms/x11.html` | Stale comments in X11Platform.cpp (and Sdl3Platform.cpp) refer to Game::UpdateInput(), which does not exist; snapshot updates happen at the end of Game::PollEvents. | modules/platform/src/X11/X11Platform.cpp (EnsureControllerSubsystem comment); modules/runtime/src/Game.cpp |
| `platforms/x11.html` | CNA_X11_HAVE_VULKAN_HEADERS is defined by cmake/PlatformX11.cmake but consumed by no source; the dependency summary therefore implies a Vulkan-header gate that does not exist. | cmake/PlatformX11.cmake; modules/platform/src/X11/X11GraphicsServices.cpp |
| `repository/index.html` | docs/physical-modules.md says top-level examples/ holds exactly one thing (examples/golden), but the tree also has examples/xvfb_screenshot_demo.cpp, which no CMake file references; its dependency table also omits edges of cna_runtime (input, diagnostics, platform) | docs/physical-modules.md; Git tree of 009d40f5; modules/runtime/CMakeLists.txt |
| `repository/index.html` | cmake/Tests/ and cmake/tests/ are two directories that differ only in letter case (script-mode gates versus WebGPU checksum tests and a linker probe), a hazard on case-insensitive checkouts | Git tree of 009d40f5 (cmake/Tests/*, cmake/tests/*) |
| `repository/index.html` | root main.cpp is a hello-world stub referenced by no CMake file | main.cpp; CMakeLists.txt |
| `runtime/frame.html` | With IsFixedTimeStep true and TargetElapsedTime greater than MaxElapsedTime (500 ms) the accumulator is clamped below one step every tick, so Update never runs while Draw does. | modules/runtime/src/Game.cpp (Tick: clamp to MaxElapsedTime before while accumulated >= TargetElapsedTime) |
| `runtime/frame.html` | The Emscripten frame body ignores IsFixedTimeStep=false, suppressDraw_ and isSuspended_, advances TotalGameTime before Update, gives the first update a full step, always reports IsRunningSlowly false, and swallows a frame exception (logs it, then Run() returns normally without OnExiting). | modules/runtime/src/Game.cpp (EmscriptenMainLoopCallback, RunLoop) |
| `runtime/frame.html` | F9 and F10 are consumed by every Game (renderer DebugSimulateContextLoss / DebugRestoreContext) with no opt-out. | modules/runtime/src/Game.cpp (PollEvents KeyEvent branch) |
| `runtime/frame.html` | If the platform performance counter never advances, the fixed-step wait loops spin forever; nothing guards it. | modules/runtime/src/Game.cpp (Tick wait loops) |
| `runtime/module.html` | Game::InactiveSleepTime is stored, validated and returned by the C API getter/setter but nothing in the loop reads it: the desktop Tick does not sleep while IsActive is false. | modules/runtime/src/Game.cpp (setInactiveSleepTimeProperty; no reader in Tick/RunLoop); modules/c-api/src/CnaCApiGameProperties.cpp |
| `runtime/module.html` | CNA::Runtime and CNA::RuntimeOptions are declared in CNA/Misc.hpp but never defined; a call would fail to link. A CMake check keeps the C API coverage record honest. | modules/runtime/include/CNA/Misc.hpp; modules/c-api/cmake/CheckUnimplementedRuntimeFacade.cmake |
| `runtime/module.html` | GameComponentCollection's own items_/owned_ have no lock; concurrent mutation, or mutation during Game::Initialize/Dispose(true)/DoInitialize iteration, is unprotected. A component added from LoadContent is categorized but its Initialize() is not called by the base path. | modules/runtime/src/GameComponentCollection.cpp; Game.cpp (DoInitialize, Initialize, OnComponentAdded) |
| `runtime/module.html` | GraphicsDeviceManager lifetime is only safe when nested inside its game: its ClientSizeChanged subscription is never removed, Game caches raw manager/service pointers that unregisterServices does not clear, and ~GraphicsDeviceManager dereferences its Game. No test covers the unsupported shapes. | modules/runtime/src/GraphicsDeviceManager.cpp (registerServices, unregisterServices, Dispose); Game.cpp (graphicsDeviceService_, graphicsDeviceManager_) |
| `runtime/module.html` | Game::IsMouseVisible forwards to IPlatformMouse::SetCursorVisible whenever a window and a mouse service exist; TerminalMouse::SetCursorVisible always throws PlatformException (same item as the Terminal ledger). | modules/runtime/src/Game.cpp; modules/platform/src/Terminal/TerminalMouse.cpp |
| `runtime/shutdown.html` | UnloadContent is reached only through explicit disposal with a registered graphics device service, after Content_.Dispose(); it never runs from ~Game or Run() returning. | modules/runtime/src/Game.cpp (Initialize, Dispose(bool), ~Game); GameTests.cpp DisposingDeviceInvokesUnloadContent |
| `runtime/shutdown.html` | After the last game uninstalls its platform, any static API that calls GetCurrentPlatform() during member teardown would lazily create a new default platform (immortal holder) instead of failing. | modules/platform/src/CurrentPlatform.cpp (ResolveCurrentPlatform, LazyDefault); modules/runtime/src/Game.cpp (~Game order) |
| `runtime/shutdown.html` | Manager-lifetime shapes not protected: manager destroyed while the game runs, or manager outliving an undisposed game (see module ledger). | modules/runtime/src/GraphicsDeviceManager.cpp; Game.cpp |
| `runtime/shutdown.html` | Shared-owned components are destroyed after the device, content manager, window and services; a component destructor that calls back through its Game reference would touch destroyed members. | Game.hpp (declaration order); GameComponentCollection.hpp (defaulted destructor, owned_) |
| `runtime/startup.html` | A game without a GraphicsDeviceManager never gets UnloadContent from disposal (Game::Initialize subscribes only to a registered IGraphicsDeviceService); demo_2d is such a game. | modules/runtime/src/Game.cpp (Initialize); modules/graphics/examples/demo_2d/src/Game1.cpp |
| `takeover/blast-radius.html` | A comment in StorageDeviceTests.cpp still describes the storage root as an SDL pref-path directory; StorageDevice no longer uses SDL (root from XDG_DATA_HOME, LOCALAPPDATA, HOME, then the working directory). | modules/storage/tests/Microsoft/Xna/Framework/Storage/StorageDeviceTests.cpp (EscapingTitleNameThrowsAndDeletesNothing comment); modules/storage/src/StorageDevice.cpp (EnsureStorageRoot) |
| `takeover/case-study-components.html` | Game's frame snapshot nulling is not ordered against a removal performed on another thread while the game thread iterates; GameComponentCollection itself has no lock, so concurrent Add (loader) and Remove (game thread) race on items_. | modules/runtime/src/Game.cpp (Update, Draw, OnComponentRemoved); modules/runtime/src/GameComponentCollection.cpp (InsertItem, RemoveItem) |
| `takeover/case-study-storage.html` | StorageDeviceDeleteContainerTest recursively deletes a fixed per-user directory resolved from the real environment (XDG_DATA_HOME/LOCALAPPDATA/HOME) and its TearDown creates <data root>/game as a side effect. | modules/storage/tests/Microsoft/Xna/Framework/Storage/StorageDeviceTests.cpp (SetUp, TearDown) |
| `takeover/case-study-storage.html` | StorageDevice's process-static root (storageRoot_, appName_, storageRootInitialized_) is unsynchronized; concurrent SetAppNameEXT and storage access is not covered. | modules/storage/src/StorageDevice.cpp (EnsureStorageRoot, SetAppNameEXT) |
| `takeover/ownership.html` | ContentManager::Unload() only clears loadedAssets_; disposables are recorded only through an explicit callback (ContentReader::RecordDisposable's manager fallback is marked deferred), while ContentManager::ReadAsset's doc comment says 'when empty, this manager records them itself, as Load<T>() does'. | modules/content/src/Xna/ContentManager.cpp (Unload, Dispose); modules/content/include/Microsoft/Xna/Framework/Content/ContentReader.hpp (RecordDisposable); modules/content/include/Microsoft/Xna/Framework/Content/ContentManager.hpp (ReadAsset doc) |
| `takeover/ownership.html` | cna_game_destroy's comment says Shutdown has already disposed the canonical graphics device, but Game::Dispose(true) disposes only the registered IGraphicsDeviceService, which does not dispose a Game-owned device. | modules/c-api/src/CnaCApiRuntime.cpp (cna_game_destroy, CGame::Shutdown); modules/runtime/src/Game.cpp (Dispose(bool)) |
| `takeover/ownership.html` | DevicesShutdownCoordinator::Shutdown() must be called by the application before platform shutdown when VibrateController may have been used; neither Game nor the runtime calls it. | modules/devices/include/Microsoft/Devices/Detail/DevicesShutdownCoordinator.hpp |
| `takeover/threading.html` | The X11Platform class comment says every Xlib call goes through one per-instance mutex; no such mutex exists in modules/platform/src/X11 (only the process-wide error-policy mutex). | modules/platform/src/X11/X11Platform.hpp (Xlib threading comment); modules/platform/src/X11/X11Error.cpp (PolicyMutex) |
| `takeover/threading.html` | Renderer context leases (EasyGL, OpenGL4) keep nesting state in a thread_local map; a release on a thread other than the acquiring one logs an error and returns without unlocking the recursive mutex. | modules/renderers/easygl/src/EasyGLRenderer.cpp (ReleaseThreadContextLease); modules/renderers/opengl4/src/OpenGL4Renderer.cpp (AcquireThreadContextLeaseEXT releaseLease) |
| `takeover/threading.html` | GameComponentCollection storage and Game::Exit are unsynchronized while Game guards its derived component lists for loading-thread adds. | modules/runtime/src/GameComponentCollection.cpp; modules/runtime/src/Game.cpp (Exit) |
| `takeover/threading.html` | XNB ContentTypeReaderManager registries and the ContentManager asset cache have no lock while the CNB loader registry uses a shared_mutex. | modules/content/src/Xna/ContentTypeReaderManager.cpp; modules/content/include/CNA/Content/Cnb/CnbLoaderRegistry.hpp |
| `takeover/threading.html` | docs/c-api/LIMITATIONS.md still says 'Four of the 49 renderers'; the snapshot has 25 renderer identities. | docs/c-api/LIMITATIONS.md |
| `takeover/uncertainty.html` | No production code calls Microsoft::Devices::Detail::DevicesShutdownCoordinator::Shutdown(); applications rely on the atexit fallback and PlatformVibrateBackend::ReleaseService's current-platform guard; the ordering harness covers SDL3/POSIX only | modules/devices/src/Detail/DevicesShutdownCoordinator.cpp; modules/devices/src/Detail/PlatformVibrateBackend.cpp; tools/devices/shutdown_ordering_harness.cpp; cmake/UnitTests.cmake |
| `takeover/uncertainty.html` | No CI workflow builds the C API library (CNA_BUILD_C_API=ON); build at TARGET unverified | .github/workflows/c-api-*.yml |
| `takeover/validation.html` | FileDialog::ShowOpenFile/ShowSaveFile/ShowOpenFolder swallow PlatformException after moving the callback, so a rejected request produces no callback at all (unlike the no-service path, which reports an empty result). | modules/devices-ext/src/FileDialog.cpp (catch blocks) |
| `takeover/validation.html` | StorageDeviceDeleteContainerTest teardown calls remove_all on a storage root derived from the caller's environment (XDG_DATA_HOME, LOCALAPPDATA, HOME, working directory). | modules/storage/tests/Microsoft/Xna/Framework/Storage/StorageDeviceTests.cpp (SetUp, TearDown); modules/storage/src/StorageDevice.cpp (EnsureStorageRoot) |
| `takeover/validation.html` | Stub_Smoke links SDL3::SDL3 unconditionally, whereas the HEADLESS fixtures link it only when the target exists. | modules/renderers/stub/examples/CMakeLists.txt (cna_stub_test) |
| `takeover/validation.html` | The tests preset description says ctest spuriously reports unbuilt display-dependent graphics smoke executables as failed and races tests sharing hard-coded fixture paths; direct CnaTests invocation is the recommended route. | CMakePresets.json (tests preset description) |
| `testing/architecture.html` | metal-macos-ci.yml's push path filter lists cmake/BackendLibraries.cmake, cmake/Examples.cmake and cmake/Tests/MetalTests.cmake, none of which exists at TARGET, so edits to the current test registration (cmake/UnitTests.cmake, module CMake) do not trigger it | .github/workflows/metal-macos-ci.yml; Git tree of 009d40f5 |
| `testing/architecture.html` | SDL_gpu example registrations replace FAIL_REGULAR_EXPRESSION after registering and, unlike the Vulkan and OpenGL4 example directories, do not re-apply the output gates, so in a tree where VULKAN or OPENGL4 is also compiled in the gate may not reach them (by code reading; not run) | modules/renderers/sdl-gpu/examples/CMakeLists.txt; cmake/TestHelpers.cmake |
| `testing/index.html` | CnaInputTests once stalled indefinitely (single thread in futex_wait) twice while another copy of the suite ran from another checkout; a 1,200 s timeout bounds it and the CMake comment says the deadlock itself is tracked separately | cmake/UnitTests.cmake (CnaInputTests registration comment) |
| `testing/index.html` | ModuleLinkClosure_* gates read CMakeFiles/<target>.dir/link.txt, which only Makefile generators write, so they are skipped (exit 77) in Ninja trees, including every preset that inherits base-ninja and CI | scripts/check_module_link_closure.py; cmake/Tests/ModuleProbes.cmake |
| `workflows/index.html` | CLAUDE.md's Platform Boundary paragraph lists SDL12, WIN32 and EMSCRIPTEN as reserved platform identifiers, but cmake/PlatformSelection.cmake implements WIN32 (offered when the target is Windows) and reserves TERMINAL on Windows instead | CLAUDE.md; cmake/PlatformSelection.cmake |
| `workflows/index.html` | CLAUDE.md describes WEBGPU twice with different capability statements (past a 2D baseline with 3D and Emscripten support, versus a baseline of clear/present, Texture2D, buffer uploads and WGSL SpriteBatch) | CLAUDE.md (renderer selection paragraph and 'WebGPU Is Active' section) |

## 8. Pages with no single Developer counterpart (new material written from TARGET, drawing on the listed Developer units)

| Page | Kind | Built | Draws on (Developer units) | Purpose |
|---|---|---|---|---|
| `development/handbook/index.html` | hub | yes | `change-map/index.html`, `workflows/index.html` | Task-oriented recipes for changing CNA safely: fix a renderer bug, change public XNA behaviour, modify a platform backend, add a regression test, update the C API, make a release. |
| `development/handbook/fix-a-renderer-bug.html` | new | yes | `takeover/worked-changes.html`, `takeover/validation.html`, `change-map/index.html`, `testing/architecture.html`, `graphics/selection.html`, `graphics/backends.html`, `graphics/device.html`, `graphics/draw-call.html`, `graphics/headless.html`, `graphics/software.html`, `graphics/stub.html`, `graphics/easygl.html`, `graphics/vulkan.html`, `graphics/sdl-gpu.html` | A recipe for fixing renderer defects: identify identity, family and host, separate shared neutral logic from one translation layer with a second backend, use the right harnesses and gates, and prove the fix. |
| `development/handbook/change-public-xna-behavior.html` | new | yes | `change-map/index.html`, `takeover/worked-changes.html`, `takeover/validation.html`, `conventions/index.html`, `invariants/index.html` | A recipe for changing the XNA-shaped surface: where it lives, how to establish what real XNA does when references disagree, the CNAEXT marker, recording a deliberate difference, and the C ABI, content and test ripple. |
| `development/handbook/modify-a-platform-backend.html` | new | yes | `platforms/index.html`, `platforms/sdl3.html`, `platforms/x11.html`, `platforms/wayland.html`, `platforms/win32.html`, `platforms/headless.html`, `platforms/terminal.html`, `architecture/platform.html`, `input/events.html`, `takeover/worked-changes.html`, `takeover/validation.html`, `testing/architecture.html` | A recipe for changing a platform backend or the IPlatform contract: ownership of window, event, input and present, the conformance suite, sibling comparison, boundary gates and per-backend test registration. |
| `development/handbook/add-a-regression-test.html` | new | yes | `testing/architecture.html`, `testing/index.html`, `takeover/validation.html`, `change-map/index.html` | A procedure for adding a CNA test: choose the layer, place the file so CMake globs and filters pick it up, write a narrow failing case with negatives, run and confirm registration, plus worked math and content examples. |
| `development/handbook/modify-contentmanager.html` | new | yes | `content/runtime.html`, `change-map/index.html`, `takeover/validation.html` | A recipe for changing ContentManager: the xnb, cnb and loose load ladder, cache key and unload semantics, reader registries, the pipeline-to-runtime contract, error paths, tests and blast radius. |
| `development/handbook/modify-the-content-pipeline.html` | new | yes | `content/pipeline.html`, `change-map/index.html`, `takeover/validation.html` | A recipe for changing the content pipeline: importer, processor and writer routes, the cna-content tool and cna_add_content, deterministic output, the XNA facade, the no-device rule, golden round trips and gates. |
| `development/handbook/update-the-c-api.html` | new | yes | `change-map/index.html`, `bindings/c-api-internals.html` | A maintainer recipe for changing the experimental C ABI: handle and error contracts, version discipline, the checked-in coverage inventory and release gate, pure-C tests, and what a C++ change costs the bindings. |
| `development/handbook/debug-shutdown-and-lifetime.html` | new | yes | `debugging/index.html`, `takeover/ownership.html` | A maintainer recipe for shutdown and lifetime bugs: which lifetime ended, the destruction order, borrowed pointers, callback barriers, a catalogue of real failure shapes, sanitizers and exit-status harnesses, and a triage. |
| `development/handbook/investigate-a-sample-failure.html` | new | yes | `debugging/index.html` | A triage flow for a failing XNA sample: the sample ecosystem and its pins, reproducing with the sample's CNA revision, profile, renderer, content, storage and audio axes, and separating a CNA defect from a sample or host fault. |
| `development/handbook/change-build-configuration.html` | new | yes | `build/architecture.html`, `workflows/index.html` | A maintainer recipe for changing CNA's CMake configuration: option rules, the three selectors and their configure-time refusals and tests, renderer combination rules, presets, CI cells, third-party pins and build-performance policy. |
| `development/handbook/make-a-release.html` | new | yes | `workflows/index.html`, `maintenance/index.html` | A maintainer checklist for cutting a CNA release from the source's own procedure: version sources, generated Version.hpp, changelog and pins, the independent C ABI version and gate, tagging, and what is not established. |
| `development/handbook/update-libcna-com.html` | new | yes | `maintenance/index.html` | The maintenance procedure for this site: choose the new CNA commit, compute the delta, map it to pages, recompute the canonical facts, re-read the code, move the pin atomically, regenerate indexes and run every validator. |
| `development/takeover/own-a-subsystem.html` | new | yes | `takeover/blast-radius.html`, `takeover/curriculum.html`, `takeover/index.html`, `takeover/inherited.html` | A procedure for picking one CNA subsystem and owning it: selection criteria from blast radius, size and tests, verified first candidates, a dossier, a seven-rung ladder and checks that replace reliance on AI. |
| `development/takeover/review-ai-generated-code.html` | new | yes | `takeover/inherited.html`, `takeover/uncertainty.html`, `takeover/validation.html`, `testing/architecture.html` | How to read CNA's AI-era paper trail (task IDs, plans, audits, notes, comments) as leads, what its mechanical guards really enforce, and a review method worked on the snapshot's head merge. |
| `development/takeover/xna-oracle.html` | new | yes | `takeover/uncertainty.html`, `takeover/validation.html`, `testing/architecture.html` | What CNA's real-XNA scene corpus is, how a diff is run and judged, what CNA's records claim per renderer and what each gate enforces, when to use it, how to add a scene, and where its evidence stops. |
| `development/repository/source-ownership.html` | new | yes | `repository/index.html`, `change-map/index.html`, `takeover/inherited.html` | Which CNA module, family or backend owns a behaviour: how the tree encodes ownership, which files are generated, third-party and sibling boundaries, and a 63-row lookup from behaviour to header, source and tests. |
| `development/internals/index.html` | hub | yes | `docs/COVERAGE.md` | Source tours of CNA's runtime, graphics, platforms, audio, content, input, modules and C API bindings, pinned to one exact snapshot with ownership, lifetime, failure and test detail. |
| `development/internals/graphics/opengl4.html` | new | yes | `graphics/easygl.html`, `graphics/sdl-gpu.html`, `graphics/backends.html` | How the OPENGL4 family works: 4.1 core context creation per platform, per-device context isolation, resource lifetime, draw and shader routes, compute and modern additions, capability answers and tests. |
| `development/reference/selection-axes.html` | generated | yes | — |  |

## 10. Method, evidence and process notes

**Enumeration.** The units are the *files of the Developer working tree* (95: 80 authored pages, 4 generated-reference pages, 3 presentation assets, 1 process
document, 4 tooling files, 3 repository-metadata files), enumerated mechanically by `scripts/developer_ledger.py enumerate`; the seven files that are modified or
untracked in the Developer working tree (uncommitted work in `graphics/backends.html`, `graphics/headless.html`, `graphics/stub.html`, `takeover/validation.html`,
`docs/COVERAGE.md`, `assets/site.js`, `assets/search-index.js`) were read from the working tree, not from Git HEAD.

**Verification, not transcription.** Developer documents CNA `d6e9ff05`, 55 commits before TARGET; 224 files differ between the two (OpenGL4, SDL_gpu, EasyGL,
graphics tests and examples, `cmake/`). For every page `scripts/dev_pack.py` listed each cited source path (present at TARGET? changed since the pin? renamed?), every code
token unknown to a source-wide identifier index built from TARGET, cited commits and external pins; authors then re-read the source for every load-bearing claim (the
"unchanged since the pin" paths were spot-checked too: Developer contained wrong header paths, a non-existent `Game::UpdateInput`, mis-ordered device creation, and other errors
that byte-identical files could not have caught). The ledger's `target_verification.contradictions` records each correction.

**Conservation checks** (`scripts/developer_ledger.py check`, part of `scripts/validate_all.sh`): every Developer `<h2>/<h3>` is mapped to a destination section whose anchor exists
(or explicitly dropped with a reason); every CNA source path cited by a Developer page is cited by its destination or explained; the destination is not shorter than 85% of its source in
words; at most 35% of the source's `<code>` tokens may be absent from the destination. The remaining review flags (Developer-pin hashes, Developer-site tooling such as `make check`, ellipsis
placeholders) are intentional drops.

**Other mechanical gates.** `scripts/check_dev_page.py` (strict HTML5, fragments, pinned source paths exist at TARGET, no retired renderer identity, no first-person voice, no leftover Developer
vocabulary), `scripts/check_dev_claims.py` (repository paths, CMake presets, options and identifiers against TARGET), `scripts/check_source_links.py` (every CNA link on a Development page is pinned to TARGET and
exists there; binding-repository links exist at their pinned commit), `scripts/check_facts.py` (whole-registry counts on every page equal the canonical facts), `scripts/compare_presentation.py --phase2` (Phase-1 presentation
preserved, 10% shrink guard) and the retired-renderer scan.

**Evidence semantics.** Every Development page states its evidence basis. Nothing was built or run to write these pages; where a page reports a test result or a measurement it attributes it to CNA's own records.
Developer's coverage vocabulary (NOT STARTED / INVESTIGATING / DRAFTED / SOURCE VERIFIED / COMPLETE) describes Developer's own documentation audit and is never republished as CNA product status; the Development home
states which subsystems have a dedicated internals page and which do not, without implying anything about their maturity.

**Process note (model availability).** The first wave of page authors ran on Opus and was cut short when the account's weekly Opus limit was reached (resets 2026-09-27 20:00 Europe/Prague). The pages that had been
written and built by then survived on disk. All remaining work, and the audit and ledger of every page whose author had been interrupted, was done by Sonnet-class agents against TARGET with the same guide and the
same mechanical gates; the orchestrator additionally re-verified sampled claims (Wayland activation token handling, `PlatformEvent` alternatives, `Game` member order, `SetClockForTesting`, the CMake option scan) and fixed the
defects the claim checker and the authors' hand-backs exposed in sibling pages (a mis-named net test hook, three repository-map statements, the Vulkan example-block guard, the "reference implementation" wording).

**Deliberate non-actions.** Bible, the abandoned first unified-documentation attempt, the CNA repository and the Developer repository were not modified, and nothing was pushed. Bug-like findings (222 across the ledgers) are recorded in
section 7 for the later catalogue phase; they are not published as a catalogue.

## 11. Changes to protected Phase-1 pages (all additive or corrective; 0 unexplained presentation losses)

| Change | Pages | Why |
|---|---|---|
| One header entry, "Development", after "Documentation" (`scripts/patch_global_nav.py`, idempotent) | every page (204 + the new ones) | the only global-navigation change; the area's own pages are reached through hubs and a local sidebar |
| Header cap 1360→1460 px, link size/padding 0.9375/0.5 rem → 0.925/0.4 rem, two-row breakpoint 1240→1265 px (`css/style.css`) | all pages | keeps the full menu on one row where it fitted before the extra link |
| New section "Development, internals and the maintainer handbook" (six cards) | `documentation.html` | discoverability; no existing card touched |
| "Go deeper" callout linking Architecture maps, module graph, internals, selection axes | `architecture.html` | additive |
| Third secondary button "Development & Maintainer Guide" beside the two existing buttons of "Get running from source" | `index.html` | additive; no primary CTA changed |
| "Working on CNA itself?" callout | `contribute.html` | additive |
| Factual corrections found while verifying Developer's claims | see `audit/phase2-phase1-errata.md` (58 decisions on 33 pages) | Phase-1 errors, corrected in place |
| Stylesheet and script additions for the Development area (local nav, source links, evidence box, related pages, pager; mobile collapse of the local nav) | `css/style.css`, `js/main.js` | new area only |

