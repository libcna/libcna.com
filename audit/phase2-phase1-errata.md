# Phase-2 errata: factual errors found in the accepted Phase-1 pages

While absorbing developer.libcna.com, the authors of the Development pages re-derived many claims from CNA source at TARGET (`009d40f5`) and reported
places where an accepted Phase-1 user page disagreed with the source. Each report was verified again by an editor against TARGET and, where confirmed,
corrected with a minimal edit inside the existing sentence, cell or callout (no section, card, CTA, image or video was removed; every edited page passes
`scripts/compare_presentation.py` and `--phase2` with 0 unexplained losses). The raw decision logs are `audit/data/phase2-phase1-errata-E1.json` and `-E2.json`.

58 decisions: CONFIRMED 51, PARTLY 7.

| Page | Reported issue | Decision | New wording | Evidence |
|---|---|---|---|---|
| `docs/audio.html` | SoundEffect table: Play() returns false 'for example the instance limit was reached' (no instance limit exists) | CONFIRMED | returns false if the sound could not start (the effect is disposed, there is no native audio as under SDL2 and NULL, or the mixer track could not be created, bound or played); Play() itself enforces no instance limit | modules/audio/src/Xna/SoundEffect.cpp (SoundEffect::Play(float,float,float): isDisposed_, getNativeAudioHandle null, CreateMixerTrack/SetMixerTrackAudio/PlayMixerTrack failures, #else returns false; n |
| `docs/audio.html` | Implementation table 'SoundEffect construction and Duration: Yes' for SDL2/NULL holds only for raw-buffer effects | PARTLY | SDL2 cell: 'Yes for effects built from a raw PCM buffer (including .xnb and .cnb content); an effect built from a path or FromStream carries no audio and reports a zero Duration'; NULL cell: 'Same as SDL2'. Duration member row given the same qualifier. | modules/audio/src/Xna/SoundEffect.cpp (raw-buffer ctor sets impl_->frames/frameRate outside SOUND_ENABLED; path ctor and FromStream #else branches carry an empty Impl; getDurationProperty); modules/co |
| `docs/cnaext-engine.html` | '11 packaged shader directories, 199 shader source files' is not reproducible | CONFIRMED | (11 packaged shader directories holding 156 GLSL source files, plus 11 package manifests and 11 generated headers; the manifests declare 208 shader payload entries) in the build bullet; table cell '11 (156 GLSL source files; 208 manifest payload entries)' | modules/graphics-ext/src/shaders/*/ (find: 178 files = 156 .glsl + 11 package.json + 11 *.generated.hpp); package.json 'payloads' arrays 8+4+8+4+14+4+12+112+22+8+12 = 208 |
| `docs/diagnostics.html` | #resources lists only textures/RTs/buffers; every GraphicsResource registers a zero-byte Unknown record; records also removed on Dispose | CONFIRMED | unregisters it when the object is disposed or dies. Every other GraphicsResource (a state object, an effect and so on) also registers a zero-byte ResourceKind::Unknown record when it is constructed, so Snapshot::resources lists those too. | modules/graphics/src/Xna/GraphicsResource.cpp (constructors: ResourceHandle(ResourceDescriptor{}) under CNA_DIAGNOSTICS_LEVEL>=1; Dispose: diagnosticResource_.Reset()); modules/diagnostics/include/CNA |
| `docs/effects.html` | SDL_GPU row: 'ShaderEffect instancing is not implemented' - at TARGET an instanced custom-effect draw is queued with its instance count | PARTLY | an instanced ShaderEffect draw is queued with its instance count, but per-instance vertex streams are not bound to a custom effect. | modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp (DrawInstancedPrimitivesEx: customEffectRenderer -> QueueCustomEffect3DDrawEXT(..., instanceCount); command.instanceCount; attributes all buffer_slot 0 |
| `docs/shader-effects.html` | 'Other things a custom shader does not get' callout: SDL_GPU instancing 'not implemented' and package shaders need libshaderc on SDL_GPU | CONFIRMED | SDL_GPU's own limitation text still says 'ShaderEffect instancing is not implemented', but at this snapshot an instanced custom-effect draw is queued with its instance count; only per-instance vertex streams are not bound to a custom effect. ... (EasyGL, OPENGL4, VULKAN, WEBGPU; SDL_GPU through the SPIR-V variant, which needs no libshaderc) | modules/renderers/sdl-gpu/src/SdlGpuRenderer.cpp (SdlGpuEffectRenderer::CompileProgram SPIR-V branch 'available in every build, including the ones that deliberately have no libshaderc'; SupportsShader |
| `docs/faq.html` | Persistence answer: Begin*/End* on Storage and GamerServices are all synchronous; Guide keyboard/message-box complete later | CONFIRMED | Begin*/End* pairs on Storage, and the GamerServices achievement, profile and leaderboard pairs, are synchronous ... The exceptions are Guide::BeginShowKeyboardInput and Guide::BeginShowMessageBox, which complete later, when the player responds. | modules/gamer-services/src/Xna/Guide.cpp (BeginShowMessageBox returns the pending action; GuideKeyboardInputAction completes on TextInputEXT); GamerServicesServiceTests BeginShowKeyboardInputDoesNotCo |
| `docs/game-loop.html` | #drawgametime: with fixed timestep 'Draw may be skipped if the game is running behind' | CONFIRMED | a frame that is running behind runs extra Update calls to catch up (the accumulated time is clamped to 500 ms) and still draws once; Draw is skipped only when SuppressDraw() or Exit() was called or BeginDraw() returns false, and it is never called more than once between two Update calls. | modules/runtime/src/Game.cpp (Game::Tick: while(accumulated >= TargetElapsedTime) Update; MaxElapsedTime clamp; suppressDraw_ / BeginDraw gate; Game::Exit sets suppressDraw_) |
| `docs/game-loop.html` | #lifecycle-diagram / #unloadcontent: Dispose(bool) follows OnExiting and UnloadContent runs from device-disposing during Dispose(bool) | CONFIRMED | Lifecycle item now says teardown happens only when the game calls Dispose() (Run() does not; the destructor's Dispose(false) never reaches UnloadContent) and only when a graphics device service such as GraphicsDeviceManager is registered; #unloadcontent rewritten the same way, with 'call game.Dispose() after Run() if you rely on it'; 'when it runs at all' added to the consequences paragraph. Ids/anchors kept. | modules/runtime/src/Game.cpp (Run: BeginRun/RunLoop/EndRun/AfterLoop, no Dispose; RunLoop ends with OnExiting; Initialize subscribes UnloadContent only if graphicsDeviceService_ != nullptr; Dispose(bo |
| `docs/game-loop.html` | #constructor: 'The graphics device is not yet created at this point' | CONFIRMED | In CNA the base Game constructor has already built the GraphicsDevice (the renderer and, for a windowed renderer, the window) before your constructor body runs, but the manager's preferences (size, profile, presentation mode) are only applied later, when the game initialises just before Initialize(), so do not rely on them being in effect in the constructor. | modules/runtime/src/Game.cpp (Game(std::unique_ptr<IPlatform>): GraphicsDevice_() member, Window_.setWindowInternal(GraphicsDevice_.GetPlatformWindowInternal()); DoInitialize -> graphicsDeviceManager_ |
| `docs/game-loop.html` | #gamewindow: 'the native window itself is created when Run() is called' | CONFIRMED | for any renderer that needs a window, the native window is already created during Game construction, so getClientBoundsProperty() can be queried from a derived constructor, although the manager's preferred size is only applied when the game initialises, just before Initialize() runs. | modules/graphics/src/Xna/GraphicsDevice.cpp (createOrAttachWindow via resolveRenderer in the constructor); modules/runtime/src/Game.cpp (Window_.setWindowInternal in constructor); modules/runtime/src/ |
| `docs/input.html` | #textinputext note: StartTextInput/StopTextInput 'have no visible effect' on desktop | CONFIRMED | Text input is a mode, not a no-op on desktop: on Win32, for example, the event mapper consumes every WM_CHAR without raising a TextInput event until the window's mode has been enabled by StartTextInput(), and the mapper's own source comment says a game that never called it sees the same on every other backend. Call StartTextInput() before you expect TextInput events. | modules/platform/src/Win32/Win32EventMapper.cpp (TranslateChar: if(!textInputActive_) return true; SetTextInputActive); Win32EventMapper.hpp (SetTextInputActive comment); Win32InputServices.cpp (Win32 |
| `docs/input.html` | #mouse table: MouseState X/Y described as 'window pixels' | CONFIRMED | X: 'Cursor X position in logical game coordinates: the window-client position mapped through the window's renderer transform (letterboxing, virtual resolution). It equals window pixels only when no transform applies.' Y: 'in logical game coordinates, mapped the same way as X.' | modules/input/src/Xna/Mouse.cpp (window_to_logical via renderer->TransformWindowToLogical; Mouse::GetState) |
| `docs/input.html` | Devices.Sensors row: 'Built only with CNA_DEVICES=ON' | CONFIRMED | Built in every configuration: CNA_DEVICES gates only the CNA-specific device extensions (CNA::Devices), not the Microsoft::Devices sensors. | modules/CMakeLists.txt (add_subdirectory(devices) unconditional; cna_devices in _cna_runtime_parts); no CNA_DEVICES use under modules/devices; CNA_DEVICES guards only modules/devices-ext (#ifdef CNA_D |
| `docs/math-types.html` | Vector3 Length/Distance/DistanceSquared reproduce x87 wider-than-float accumulation (Vector3 table row and numerical-behaviour row) | CONFIRMED | Vector3 row: 'sum their squares in float; the wider-than-float accumulation ... is used by Vector3::Transform / TransformNormal, matrix products and BoundingSphere's internal helpers instead'. Behaviour row now lists Vector3::Transform, TransformNormal, Matrix::Multiply, Matrix::Invert and BoundingSphere::CreateFromPoints (private helpers) as wide, and states Length/LengthSquared/Distance/DistanceSquared sum in float | modules/math/src/Vector3.cpp (Length, LengthSquared, Distance, DistanceSquared all float; Transform/TransformNormal use double); modules/math/src/Matrix.cpp (Multiply ~1105, Invert ~999 wide); modules |
| `docs/runtime-renderer-selection.html` | Invalid CNA_GRAPHICS_RENDERER 'throws once on the first query; build default applies afterwards' | CONFIRMED | The variable is read once, on the first query, and in our reading of the code that first query is made before main() (registry namespace-scope initialiser -> PublishAvailable -> RebuildAttemptOrder -> GetSelected -> ConsultEnvironmentOnce). A bad value (or one naming a renderer that is not compiled in), like an invalid Module.cnaPreferredRenderer page property, therefore cannot be caught by game code and ends the pro | cmake/templates/CnaRendererRegistry.generated.cpp.in (cnaRendererRegistryPublished namespace-scope lambda); modules/core/src/GraphicsRendererSelection.cpp (PublishAvailable sets published=true then Re |
| `docs/sensors.html` | Build flag banner: Microsoft::Devices layer gated by CNA_DEVICES (default OFF), needs -DCNA_DEVICES=ON | CONFIRMED | the Microsoft::Devices sensors documented here are built in every configuration: the devices module is added unconditionally and is part of the CNA umbrella target. The CNA_DEVICES option, default OFF, gates only the separate CNA-specific device extensions (CNA::Devices in devices-ext: clipboard, dialogs, camera and so on); configure with -DCNA_DEVICES=ON to compile those. (workflow sentence kept) | modules/CMakeLists.txt (add_subdirectory(devices), _cna_runtime_parts); modules/devices/CMakeLists.txt; modules/devices-ext/CMakeLists.txt + #ifdef CNA_DEVICES in devices-ext sources; CMakePresets.jso |
| `docs/storage.html` | UTF-8 roots handled on Windows / root that cannot be created throws on first access: two omissions | CONFIRMED | UTF-8 roots and names are handled on Windows by the root resolution and the container paths, but getIsConnectedProperty(), getFreeSpaceProperty() and getTotalSpaceProperty() still build narrow paths from the UTF-8 root ...; if the root cannot be created, the first access throws StorageDeviceNotConnectedException; that failure is not repeated, because the root is then latched empty and later calls silently resolve aga | modules/storage/src/StorageDevice.cpp (EnsureStorageRoot sets storageRootInitialized_ before create_directories and throws without setting storageRoot_; getFreeSpaceProperty/getTotalSpaceProperty/getI |
| `docs/tutorials/97-networking.html` | Session-events note: one extra Update() required; 'C++ has no equivalent hook' to XNA's replay | CONFIRMED | Callout lead-in 'Calling Update() once immediately after subscribing is optional.' (kept close to the original title so the callout keeps its identity) + text: the NetworkSession constructor installs a replay hook so += on GamerJoined invokes the new handler immediately for every gamer already present; no extra Update() needed and a later Update() does not deliver them twice. Code-sample comment changed from 'require | modules/net/src/Xna/NetworkSession.cpp (constructor: GamerJoined.SetReplayHook); modules/net/tests/.../NetworkSessionTests.cpp GamerJoinedReplaysImmediatelyOnSubscriptionForConstructionTimeGamers, Gam |
| `docs/tutorials/97-networking.html` | Status table: NetworkSessionType::Local 'fully functional ... the local packet queue all work' | CONFIRMED | Local row: 'Works as a single-process session: gamers, events and state transitions all work, with no sockets involved. Packets are not delivered: Update() handles PacketSend only for SystemLink, so data sent on a Local session never arrives, not even for the sender (getIsDataAvailableProperty() stays false).' Summary sentence: 'prototype single-process session logic (lobby, readiness, state transitions) on Local'. | modules/net/src/Xna/NetworkSession.cpp (Update: PacketSend inside if(ENetBackend::RealNetworkingEnabled)); LocalNetworkGamerTests.cpp SendDataThenReceiveDataRoundtrip (EXPECT_FALSE IsDataAvailable); N |
| `docs/xna-compatibility.html` | Note callout: 'Microsoft::Devices layer is built only when CNA_DEVICES=ON ... A stock build does not contain it' | CONFIRMED | callout: 'The Microsoft::Devices sensor layer is built in every configuration, a stock build included. CNA_DEVICES=ON, which is not the default, gates only the separate CNA-specific device extensions (CNA::Devices: clipboard, dialogs, camera and so on).' Same error repaired in the Devices row ('The sensor layer is built in every configuration; CNA_DEVICES=ON ... gates only ... CNA::Devices'), in the CNAEXT/opt-in cal | modules/CMakeLists.txt; modules/devices/CMakeLists.txt; modules/devices-ext/CMakeLists.txt (as for sensors.html) |
| `docs/xna-compatibility.html` | (from the tutorial-97 report) 'the in-process Local type works too' overstates packets | CONFIRMED | Net row: 'the in-process Local type works for gamers, events and state transitions, but no packets are delivered on it (packet handling in Update() is SystemLink only)'; Net bullet: '(plus the in-process Local type, which has gamers, events and state transitions but delivers no packets)'; limits bullet: '(plus in-process Local, which delivers no packets)'. | modules/net/src/Xna/NetworkSession.cpp (Update PacketSend gate) |
| `features.html` | Windows Phone shell & push card lists 'PushNotificationSender' as a type | CONFIRMED | '... HttpNotificationChannel, which is its own local HTTP listener thread, RawPushNotificationMessage and ToastPushNotificationMessage)' | modules/phone/include/Microsoft/Phone/Notification/PushNotificationSender.hpp (declares only MessageSendPriority, RawPushNotificationMessage, ToastPushNotificationMessage) |
| `features.html` | (from the tutorial-97 report) Net card 'in-process Local session type' carries the packet overstatement | PARTLY | 'an in-process Local session type (gamers, events and state transitions; it delivers no packets)' - the card did not claim packet delivery, so only a precise qualifier was added. | modules/net/src/Xna/NetworkSession.cpp (Update) |
| `features.html` | (from the devices report) Devices & Sensors card: 'CNA_DEVICES defaults to OFF' reads as gating the sensors | PARTLY | 'the CNA_DEVICES CMake option defaults to OFF and gates only the CNA-specific CNA::Devices extensions (the Microsoft::Devices sensors are always built).' (qualifier only) | modules/CMakeLists.txt; modules/devices-ext #ifdef CNA_DEVICES |
| `contribute.html` | 'a default configure reports it on 24 of 25 identities' (compiled effects) - should say unsupported on 24 of 25 | CONFIRMED | so a default configure reports CompiledEffects as unsupported on 24 of 25 identities (it is true on FNA3D only). | modules/renderers/fna3d/include/.../Fna3dRenderer.hpp:1394 (SupportsCompiledEffects override returns true unconditionally); the other renderers' overrides are guarded by CNA_<FAMILY>_COMPILED_EFFECTS  |
| `docs/building.html` | Step 3 / CNA_SDL_PREBUILT_ROOT row: .sdl-prebuilt-<os>-<arch> key and reuse omit -wayland/-simulator/-min suffixes and the build-manifest condition (reported twice) | CONFIRMED | Step 3 now names the -wayland (native Linux with SDL Wayland prerequisites), -simulator (iOS Simulator) and -min<version> (Apple deployment target) suffixes and says reuse lasts only while the recorded build manifest (vendored-source hash, patch hashes, configure arguments) matches; a note that installing Wayland prerequisites later selects the separate -wayland directory; the CNA_SDL_PREBUILT_ROOT row gets the same  | cmake/ThirdPartySDL.cmake (_cna_sdl_key, -simulator, -min, -wayland, cna_sdl_prebuilt_is_current use); cmake/SdlPrebuiltFingerprint.cmake (cna_sdl_build_manifest, cna_sdl_prebuilt_is_current) |
| `docs/building.html` | (consistency, not in report) 'Windows 8.1 (0x0603)' floor row for native WIN32 | PARTLY | Cell now says the level the backend's headers declare, not enforced by CMake; a source comment says entry points above a Windows 7 floor are resolved at run time (read, not executed). | modules/platform/src/Win32/Win32Common.hpp (_WIN32_WINNT/WINVER comment); Win32DpiSupport.cpp (GetProcAddress) |
| `docs/content-pipeline-xnb.html` | Container-format text says an LZ4 file 'reached only as an external reference from another .xnb is refused' | CONFIRMED | Now: an LZ4 file reached as an untyped external reference (the std::any-returning ReadExternalReference() used by ExternalReferenceReader) is refused; the typed external references (Texture2D, TextureCube, compiled effects) load through ContentManager::Load<T>, whose loader decodes LZ4. | modules/content/src/Xna/ContentReader.cpp (ReadExternalReference<T> -> Load<T>; untyped overload -> LoadUntypedXnbReference); modules/content/src/Xna/ContentManager.cpp (LoadXnbAssetUntyped Lz4 case t |
| `docs/native-platforms.html` | #limits: 'native Wayland and X11 are not offered on macOS, iOS, Android or the web' | CONFIRMED | Wayland is not offered on macOS/iOS/Android/web; X11 is not offered on iOS/Android/web, and on macOS is offered whenever libX11, libXext and XKBlib.h are found (not exercised). | cmake/PlatformX11.cmake (cna_detect_x11: if(WIN32 OR EMSCRIPTEN OR ANDROID OR CNA_APPLE_IOS)); cmake/PlatformWayland.cmake:52 (WIN32 OR EMSCRIPTEN OR ANDROID OR APPLE OR CNA_APPLE_IOS) |
| `docs/native-platforms.html` | #win32: 'declares Windows 8.1 (0x0603) as its API floor' | PARTLY | Now: the level its headers target; a source comment says every entry point above a Windows 7 floor is resolved at run time, so the binary is not meant to require more (read from source, not executed on Windows 7). | modules/platform/src/Win32/Win32Common.hpp (comment above _WIN32_WINNT 0x0603); Win32DpiSupport.cpp |
| `docs/native-platforms.html` | #win32 'closing one of several windows does not quit the process' incomplete for Game users | PARTLY | Added: '...at the platform level. A Game, however, calls Exit() on any CloseRequested without checking which window sent it.' | modules/runtime/src/Game.cpp (PollEvents, WindowEventKind::CloseRequested -> Exit(), 'Deliberately do not filter by window id') |
| `docs/native-platforms.html` | #capabilities vulkanSurface X11 column 'If Vulkan headers were present at build' and X11 dependency-table row 'Vulkan headers ... Without it: No vulkanSurface' | CONFIRMED | X11 capability cell: 'Yes, with an X connection (loader and xlib-surface entry point resolved at run time)'. Dependency row: vulkanSurface unaffected; CMake defines CNA_X11_HAVE_VULKAN_HEADERS but no X11 source reads it. Wayland row 'As under X11' rewritten so the Wayland Vulkan-header gate is stated on its own (unlike X11). Wayland column unchanged (correct). | modules/platform/src/X11/X11Platform.cpp (OpenConnection: vulkanSurface_ = make_unique<X11VulkanSurface> unconditionally; ComputeCapabilities: vulkanSurface = vulkanSurface_ != nullptr); X11GraphicsSe |
| `docs/native-platforms.html` | #capabilities multipleDisplays Wayland column 'If wl_output' | CONFIRMED | 'Yes, whenever the compositor connection succeeded (outputs are enumerated from wl_output)'. | modules/platform/src/Wayland/WaylandPlatform.cpp (constructor: displays_ = make_unique<WaylandDisplays> for every live connection; ComputeCapabilities: multipleDisplays = displays_ != nullptr) |
| `docs/native-platforms.html` | (same conflation as platforms.html #macos, found on this page) 'SDL3 is still the default and the only platform on macOS, iOS, Android and the web' and 'It is the only platform on macOS, iOS, Android and Emscripten' | CONFIRMED | Both now say SDL3 is the default and the only one CI configures on those targets. | cmake/PlatformSelection.cmake (_cna_platforms_available: SDL3 SDL2 HEADLESS always, TERMINAL if NOT WIN32); .github/workflows (CNA_PLATFORM set only in platform-ci.yml Linux matrix and content-pipelin |
| `docs/platforms.html` | Seven-implementation table: HEADLESS 'One window object, no services, every capability false' | CONFIRMED | 'One window object, no optional services (only the always-present file-system and system-information services), every capability false.' | modules/platform/src/Headless/HeadlessPlatform.cpp (ctor fileSystem_/systemInfo_; GetFileSystem, GetSystemInfo; all other accessors nullptr); PlatformConformanceTests.cpp ServicesEveryPlatformMustHave |
| `docs/platforms.html` | #macos 'SDL3 is the only platform implementation on macOS'; #web 'The platform is always SDL3 here'; #os-status rows for macOS/iOS/Android/WebAssembly list only SDL3; SDL3-row 'only one on macOS...' | CONFIRMED | #macos: SDL3 is the default and the only implementation any CI job configures; CMake also offers SDL2, HEADLESS, TERMINAL, and native X11 when libs found. #web: SDL3 by default and in every browser workflow, CMake would also list SDL2/HEADLESS/TERMINAL. #os-status cells for macOS/iOS/Android/WebAssembly and the SDL3 row of the implementation table qualified the same way (features.html SDL3 row too). | cmake/PlatformSelection.cmake (_cna_platforms_available, _cna_platforms_reserved); cmake/PlatformX11.cmake; cmake/PlatformWayland.cmake:52; modules/platform/CMakeLists.txt (Headless always, Terminal N |
| `docs/platforms.html` | Capability matrix Wayland multipleDisplays 'If wl_output' | CONFIRMED | 'Yes, whenever the compositor connection succeeded (outputs are enumerated from wl_output)'. | modules/platform/src/Wayland/WaylandPlatform.cpp (displays_ created for every connection; ComputeCapabilities) |
| `docs/platforms.html` | Capability matrix vulkanSurface X11 column 'If Vulkan headers were present at build' | CONFIRMED | X11 cell: 'Yes, with an X connection (loader and xlib-surface entry point resolved at run time, not at build time)'; Wayland cell unchanged. features.html X11 row 'Vulkan if headers were present' corrected the same way. | modules/platform/src/X11/X11Platform.cpp (OpenConnection, ComputeCapabilities); X11GraphicsServices.cpp; cmake/PlatformX11.cmake:137 |
| `docs/tutorials/139-terminal-platform.html` | 'Ctrl+C/Z/S are signals or flow control unless the terminal's keyboard protocol reports them' | CONFIRMED | Now: while a terminal session is active (whole run of a SOFTWARE game on a TTY) raw mode clears ISIG and IXON, so Ctrl+C/Z/S arrive as ordinary control-chord key events; Ctrl+C does not interrupt the game (read from source, not executed). | modules/platform/src/Terminal/TerminalSession.cpp (raw.c_lflag &= ~(ICANON/ECHO/ISIG); raw.c_iflag &= ~(IXON/ICRNL); rawMode = keyboard//mouse//presenter in TerminalSessionController.cpp:125); Termina |
| `docs/tutorials/139-terminal-platform.html` | 'the back buffer is columns x 8 by rows x 16 pixels' | CONFIRMED | Now: the window keeps the size the game asked for until the first terminal resize (SIGWINCH); from then on it is columns x 8 by rows x 16 pixels. | modules/platform/src/Terminal/TerminalPlatform.cpp (CreateWindow comment 'The requested size stands'; PollEvents -> CellGridToPixelSize -> ApplyTerminalSize) |
| `docs/tutorials/139-terminal-platform.html` | 'one colour pair plus one glyph per cell' | CONFIRMED | Now: one glyph per cell drawn in one foreground colour (no background colour written, no colour at all in monochrome). | modules/platform/src/Terminal/TerminalAnsiWriter.cpp (AppendColour: 38;2 / 38;5 / 30-37,90-97; Monochrome returns); TerminalFrameGrid.hpp (TerminalCell: glyph + rgb) |
| `docs/tutorials/107-cpu-renderers.html` | GetLastFrameStatistics() 'diffed across the most recent Present()' | CONFIRMED | Now: figures accumulated since the most recent Present() (the frame in progress, so it reads zero straight after a Present()). | modules/renderers/headless/src/HeadlessRenderer.cpp (GetLastFrameStatistics = stats - statsAtLastPresent; Present copies stats into statsAtLastPresent) |
| `docs/verification.html` | Software_XnaLineCoverage described as 'Exact gate on two line scenes only' (renderer table), 'Not established: byte-exactness beyond the two line scenes', and 'except for two line scenes on EasyGL and SOFTWARE' in the warning callout | CONFIRMED | Software row: only two line scenes covered by a CTest; renders and diffs at tolerance 0 but the script exits non-zero only if a scene fails to render or has no reference; a difference is printed as DIFF, not failed; CNA records both scenes byte-exact (2026-09-11). Evidence row: 'Any CTest-enforced byte-exactness (the two-scene CTest fails only on a render failure); byte-exactness beyond the two line scenes'. Callout: | scripts/run-oracle-corpus-diff-software.sh (exit 1 only when RENDER_FAILED != 0, i.e. render failure or NO-REFERENCE; a diff prints 'DIFF' and increments DIFFERENT only; header: 'a measurement, not an |
| `docs/tutorials/161-xna-oracle-diff.html` | 'SOFTWARE and EasyGL are exact-gated on two line scenes only' (What it can tell you), heading 5 'Run the exact gate CNA registers' and its paragraph | CONFIRMED | Intro callout: EasyGL fails on any difference; SOFTWARE renders/diffs at tolerance 0 but fails only if a scene does not render. Heading 5 (id=gate kept) now 'Run the two-scene test CNA registers...'. Paragraph rewritten: script exits 1 only on render failure / missing reference PNG; DIFF is printed not failed; registration comment calls it an exact regression and CNA records both scenes byte-exact (2026-09-11); exit  | scripts/run-oracle-corpus-diff-software.sh; modules/renderers/software/examples/CMakeLists.txt:1145-1157 (comment 'promotes ... to an exact Software regression' vs script behaviour); plans/plan_softwa |
| `docs/tutorials/161-xna-oracle-diff.html` | 'No committed per-renderer totals' for run-oracle-corpus-multi.sh (RTR-P9-24 / P11-11 record OPENGLES3 leg 9 passed / 30 failed) | CONFIRMED | Now: plan records verifying the script on OPENGLES3;OPENGL33 (2026-08-15): the OPENGLES3 leg reproduced the single-renderer EasyGL script at 9 passed and 30 failed of 39, and the plan gives no OPENGL33 total. 'Not re-measured since' qualified with the 9-of-39 leg (also pre-fix). Same qualifier applied to the EasyGL cell in verification.html, tutorial 125 and rendering-backends.html. | plans/plan_runtimerenderer.md RTR-P9-24 (line 825) and RTR-P11-11 (line 891); commit b7a091518 (2026-08-15) 'run the XNA oracle corpus against several renderers from one binary'; scripts/run-oracle-co |
| `docs/tutorials/125-pixel-testing.html` | 'Software_XnaLineCoverage is an exact gate' family: table cell 'Two line scenes' and 'which CNA holds exact' | CONFIRMED | SOFTWARE cell: 'Two line scenes, diffed at tolerance 0; only a failure to render fails the test'; EasyGL cell: 'The same two line scenes; any difference fails the test' plus 9-of-39 qualifier; explanatory paragraph rewritten (EasyGL any difference fails the CTest; SOFTWARE CTest fails only if a scene does not render; difference printed; CNA records both byte-exact). | scripts/run-oracle-corpus-diff-software.sh; scripts/run-oracle-corpus-diff-easygl.sh |
| `docs/tutorials/107-cpu-renderers.html` | 'Software_XnaLineCoverage ... requires two line scenes to match exactly' | CONFIRMED | Now: the only oracle test registered as a CTest is Software_XnaLineCoverage: it renders two line scenes and diffs them at tolerance 0, but its script fails the test only if a scene does not render or has no reference image, and a pixel difference is printed, not failed (read from the script, not executed). | scripts/run-oracle-corpus-diff-software.sh; modules/renderers/software/examples/CMakeLists.txt:1154 |
| `docs/faq.html` | oracle claim: 'EasyGL and SOFTWARE renderers are gated in CTest on two line scenes only' | CONFIRMED | Now: EasyGL and SOFTWARE have a CTest on two line scenes only (the EasyGL test fails on any pixel difference, the SOFTWARE test only if a scene does not render; SOFTWARE measured 18 of 39 byte-exact). Only that sentence changed (the sibling agent's GamerServices item on faq.html not touched). | scripts/run-oracle-corpus-diff-software.sh; scripts/run-oracle-corpus-diff-easygl.sh |
| `features.html` | Software card 'only two line scenes are an exact CTest gate'; oracle paragraph 'EasyGL and Software are gated on two line scenes' | CONFIRMED | Card: 'only two line scenes have a CTest, which fails on a render failure rather than on a pixel difference.' Oracle paragraph: 'have a CTest on two line scenes (the EasyGL test fails on any pixel difference; the Software test only if a scene does not render)'. Also the X11 Vulkan cell and the SDL3-row 'only implementation on macOS...' wording (same claims as platforms.html). | scripts/run-oracle-corpus-diff-software.sh; X11Platform.cpp; cmake/PlatformSelection.cmake |
| `roadmap.html` | (same Software gate claim repeated; not named in the report list) 'EasyGL and Software are exact-gated on two line scenes only' | CONFIRMED | Same short qualifier added: the EasyGL test fails on any pixel difference; the Software test only if a scene does not render. Phrase 'exact-gated' dropped for Software where it appeared. | scripts/run-oracle-corpus-diff-software.sh; scripts/run-oracle-corpus-diff-easygl.sh |
| `showcase.html` | (same Software gate claim repeated; not named in the report list) 'EasyGL and Software are exact-gated on two line scenes only' | CONFIRMED | Same short qualifier added: the EasyGL test fails on any pixel difference; the Software test only if a scene does not render. Phrase 'exact-gated' dropped for Software where it appeared. | scripts/run-oracle-corpus-diff-software.sh; scripts/run-oracle-corpus-diff-easygl.sh |
| `about.html` | (same Software gate claim repeated; not named in the report list) 'EasyGL and Software are exact-gated on two line scenes only' | CONFIRMED | Same short qualifier added: the EasyGL test fails on any pixel difference; the Software test only if a scene does not render. Phrase 'exact-gated' dropped for Software where it appeared. | scripts/run-oracle-corpus-diff-software.sh; scripts/run-oracle-corpus-diff-easygl.sh |
| `docs/roadmap.html` | (same Software gate claim repeated; not named in the report list) 'EasyGL and Software are gated on two line scenes' | CONFIRMED | Same short qualifier added: the EasyGL test fails on any pixel difference; the Software test only if a scene does not render. Phrase 'exact-gated' dropped for Software where it appeared. | scripts/run-oracle-corpus-diff-software.sh; scripts/run-oracle-corpus-diff-easygl.sh |
| `docs/tutorials/72-backend-selection.html` | (same Software gate claim repeated; not named in the report list) 'EasyGL and SOFTWARE are gated on two line scenes only' | CONFIRMED | Same short qualifier added: the EasyGL test fails on any pixel difference; the Software test only if a scene does not render. Phrase 'exact-gated' dropped for Software where it appeared. | scripts/run-oracle-corpus-diff-software.sh; scripts/run-oracle-corpus-diff-easygl.sh |
| `docs/tutorials/84-migrate-xna.html` | (same Software gate claim repeated; not named in the report list) 'EasyGL and SOFTWARE on two line scenes only' | CONFIRMED | Same short qualifier added: the EasyGL test fails on any pixel difference; the Software test only if a scene does not render. Phrase 'exact-gated' dropped for Software where it appeared. | scripts/run-oracle-corpus-diff-software.sh; scripts/run-oracle-corpus-diff-easygl.sh |
| `docs/rendering-backends.html` | #verification table: SOFTWARE 'Two line scenes'; EasyGL 'no newer whole-corpus count is committed' | CONFIRMED | SOFTWARE: 'Two line scenes (Software_XnaLineCoverage), diffed at tolerance 0; only a failure to render fails the test'. EasyGL: only later count is 9 of 39 for the OPENGLES3 leg of a 2026-08-15 multi-renderer run (also pre-fix); 'any difference fails the test'. Line 324 ('EasyGL is gated on two line scenes at tolerance 0') is true and left alone. | scripts/run-oracle-corpus-diff-*.sh; plans/plan_runtimerenderer.md RTR-P9-24 |
| `docs/rendering-backends.html#modern-features` | Row 'Compute and storage buffers' says the SDL_GPU and OpenGL4 stress soaks are not run by CTest | CONFIRMED | Now: the SDL_GPU stress soak is not run by CTest; the OpenGL4 one is registered as OpenGL4_ModernStress, 900 s timeout, in an OPENGL4 test build with CNA_CNAEXT on. | modules/renderers/opengl4/examples/CMakeLists.txt:291-295 (cna_register_renderer_test NAME OpenGL4_ModernStress TIMEOUT 900, inside if(CNA_CNAEXT) inside if(CNA_BUILD_TESTS AND CNA_GRAPHICS_RENDERER S |
