# GitHub Pages (Jekyll) configuration.
#
# The repository root is the published site. These paths are repository content -- audit records,
# maintenance scripts and notes -- not site pages, so they are kept out of the published output.
exclude:
  - audit
  - scripts
  - plan.md
